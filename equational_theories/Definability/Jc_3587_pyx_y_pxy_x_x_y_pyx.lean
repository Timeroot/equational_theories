import equational_theories.Definability.AutBox2
import equational_theories.Definability.Kc_02

/-!
# Structurality from `Equation3587` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3587` is `x ◇ y = z ◇ ((x ◇ y) ◇ y)`, and the companion is
`x □ y = if (M.op b a) = b then a else if (M.op a b) = a then b else (M.op b a)`.  Its automorphism half is `GuardAut2_pyx_y_pxy_x_x_y_pyx_Equation3587`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches none of these on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation419` from `Equation3587`: `x ◇ y = z ◇ ((x ◇ y) ◇ y)` ⊢ `x = x ◇ (x ◇ (y ◇ (y ◇ x)))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op a b) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation419_StructuralFromFin_Equation3587 : Law419.StructuralFromFin Law3587 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pxy_x_x_y_pyx_Equation3587 (fun {G} M hM ↦ ?_)
  have hm := (@Law3587.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op a b) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law419 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law419.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X0 X1) X1)) := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op x (N.op x (N.op y (N.op y x)))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 : G, (M.op X2 (M.op (M.op X0 X1) X1)) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X1) X1) X3
       have i₂ := eq6 X0 X1 (M.op X2 (M.op (M.op X0 X1) X1))
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq13 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ X2 ∨ (M.op (M.op X0 X1) X1) = (N.op (M.op (M.op X0 X1) X1) X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op (M.op X0 X1) X1) X2
       have i₂ := eq6 X0 X1 X2
       grind)
    | exact superpose eq6 eq7
    | (have j0 := eq7 (M.op (M.op X0 X1) X1) X2
       grind)
    | (have r₁ := eq7 (M.op (M.op X0 X1) X1) (M.op X0 X1)
       have r₂ := eq6 X0 X1 (M.op X0 X1)
       grind)
    | exact resolve eq7 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X3 : G, (M.op X0 X1) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X3
    first
    | (have i₁ := eq12 X0 X1 x X3
       have i₂ := eq6 X0 X1 x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq19 : ∀ X0 X1 : G, (M.op X0 X1) = (M.op (M.op X0 X1) X1) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 X1 x
       have i₂ := eq14 (M.op X0 X1) X1 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq21 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ X2 ∨ (M.op X0 X1) = (N.op (M.op X0 X1) X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op X0 X1) X2
       have i₂ := eq14 X0 X1 X2
       grind)
    | exact superpose eq14 eq7
    | (have j0 := eq7 (M.op X0 X1) X2
       grind)
    | (have r₁ := eq7 (M.op X0 X1) (M.op X0 X1)
       have r₂ := eq14 X0 X1 (M.op X0 X1)
       grind)
    | exact resolve eq7 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq33 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (N.op X1 (M.op X0 X1)) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 (M.op X0 X1)
       have i₂ := eq19 X0 X1
       grind)
    | exact superpose eq19 eq7
    | (have j0 := eq7 X1 (M.op X0 X1)
       grind)
    | (have r₁ := eq7 X1 (M.op X0 X1)
       have r₂ := eq19 X0 X1
       grind)
    | exact resolve eq7 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq36 : ∀ X0 X1 : G, (N.op X1 (M.op X0 X1)) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq33 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33
  have eq45 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op (M.op X0 X1) (M.op X0 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq36 x (M.op X0 X1)
       have i₂ := eq14 X0 X1 x
       grind)
    | exact superpose eq14 eq36
    | exact resolve eq36 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq49 : x ≠ (N.op x (N.op x (N.op y (M.op x y)))) ∨ y = (M.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq50 : x ≠ (N.op x (N.op x (M.op (N.op y x) y))) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y (N.op y x)
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y (N.op y x)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq51 : x ≠ (N.op x (M.op (N.op y (N.op y x)) x)) ∨ x = (M.op x (N.op y (N.op y x))) ∨ (N.op y (N.op y x)) = (M.op (N.op y (N.op y x)) x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x (N.op y (N.op y x))
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x (N.op y (N.op y x))
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq54 : (N.op y (N.op y x)) = (M.op (N.op y (N.op y x)) x) ∨ x = (M.op x (N.op y (N.op y x))) := by
    first
    | (have r₁ := eq51
       have r₂ := eq36 (N.op y (N.op y x)) x
       grind)
    | exact resolve eq51 eq36
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq55 : x ≠ (N.op x (N.op x y)) ∨ y = (M.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq49
       have i₂ := eq36 x y
       grind)
    | exact superpose eq36 eq49
    | exact resolve eq49 eq36
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq49
  have eq63 : x ≠ (N.op x (M.op y x)) ∨ y = (M.op y x) ∨ x = (M.op x y) ∨ x = (M.op x y) ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq55
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq55
    | (have j1 := eq9 y x
       grind)
    | exact resolve eq55 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq55
  have eq65 : x ≠ (N.op x (M.op y x)) ∨ y = (M.op y x) ∨ x = (M.op x y) := by grind
  clear eq63
  have eq66 : y = (M.op y x) ∨ x = (M.op x y) := by
    first
    | (have r₁ := eq65
       have r₂ := eq36 y x
       grind)
    | exact resolve eq65 eq36
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq65
  have eq69 : y ≠ y ∨ x = (M.op x y) ∨ x = (N.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq8 y x
       have i₂ := eq66
       grind)
    | exact superpose eq66 eq8
    | (have j0 := eq8 y x
       grind)
    | (have r₁ := eq8 y x
       have r₂ := eq66
       grind)
    | (have r₁ := eq8 x y
       have r₂ := eq66
       grind)
    | exact resolve eq8 eq66
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq70 : ∀ X0 : G, y = (M.op X0 y) ∨ x = (M.op x y) := by
    intro X0
    first
    | (have i₁ := eq14 y x x
       have i₂ := eq66
       grind)
    | exact superpose eq66 eq14
    | exact resolve eq14 eq66
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq74 : y ≠ y ∨ x = (M.op x y) ∨ x = (N.op y x) := by grind
  clear eq69
  have eq75 : x = (N.op y x) ∨ x = (M.op x y) := by grind
  clear eq74
  have eq105 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq45 x y
       have i₂ := eq70 X0
       grind)
    | exact superpose eq70 eq45
    | (have j1 := eq70 X0
       grind)
    | exact resolve eq45 eq70
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq106 : x ≠ y ∨ x = (M.op x y) := by grind
  clear eq70
  have eq112 : (M.op (N.op y x) y) = (M.op (M.op (N.op y x) y) x) ∨ x = (M.op x (M.op (N.op y x) y)) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq54
       have i₂ := eq9 y (N.op y x)
       grind)
    | exact superpose eq9 eq54
    | (have j1 := eq9 (M.op (N.op y x) y) x
       grind)
    | exact resolve eq54 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq116 : ∀ X0 : G, (N.op y (N.op y x)) ≠ X0 ∨ (M.op (N.op y (N.op y x)) x) = (N.op (M.op (N.op y (N.op y x)) x) X0) ∨ x = (M.op x (N.op y (N.op y x))) := by
    intro X0
    first
    | (have i₁ := eq13 (N.op y (N.op y x)) x x
       have i₂ := eq54
       grind)
    | exact superpose eq54 eq13
    | (have r₁ := eq13 (N.op y (N.op y x)) x (N.op y (N.op y x))
       have r₂ := eq54
       grind)
    | (have r₁ := eq13 x (N.op y (N.op y x)) x
       have r₂ := eq54
       grind)
    | exact resolve eq13 eq54
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq117 : ∀ X0 : G, (N.op y (N.op y x)) = (M.op X0 (N.op y (N.op y x))) ∨ x = (M.op x (N.op y (N.op y x))) := by
    intro X0
    first
    | (have i₁ := eq14 (N.op y (N.op y x)) x x
       have i₂ := eq54
       grind)
    | exact superpose eq54 eq14
    | exact resolve eq14 eq54
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq54
  have eq125 : (M.op (N.op y x) y) = (M.op (M.op (N.op y x) y) x) ∨ x = (M.op (N.op y x) y) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq112
       have i₂ := eq14 (N.op y x) y x
       grind)
    | exact superpose eq14 eq112
    | exact resolve eq112 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq112
  have eq258 : x ≠ (N.op y (N.op y x)) ∨ x = (M.op x (N.op y (N.op y x))) := by grind
  clear eq117
  have eq335 : (M.op (N.op y (N.op y x)) x) = (N.op (M.op (N.op y (N.op y x)) x) (N.op y (N.op y x))) ∨ x = (M.op x (N.op y (N.op y x))) := by
    first
    | (have j0 := eq116 (N.op y (N.op y x))
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq116
  have eq407 : x ≠ (N.op y x) ∨ x = (M.op x (N.op y x)) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq258
       have i₂ := eq75
       grind)
    | exact superpose eq75 eq258
    | exact resolve eq258 eq75
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq258
  have eq411 : x = (M.op x (N.op y x)) ∨ x = (M.op x y) := by
    first
    | (have r₁ := eq407
       have r₂ := eq75
       grind)
    | exact resolve eq407 eq75
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq75 eq407
  have eq563 : ∀ X0 : G, x = (M.op X0 x) ∨ x = (M.op x y) := by
    intro X0
    first
    | (have i₁ := eq14 x (N.op y x) x
       have i₂ := eq411
       grind)
    | exact superpose eq411 eq14
    | exact resolve eq14 eq411
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq411
  have eq590 : x = y ∨ x = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq66
       have i₂ := eq563 y
       grind)
    | exact superpose eq563 eq66
    | exact resolve eq66 eq563
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq66 eq563
  have eq617 : x = y ∨ x = (M.op x y) := by grind
  clear eq590
  have eq627 : x = (M.op x y) := by
    first
    | (have r₁ := eq617
       have r₂ := eq106
       grind)
    | exact resolve eq617 eq106
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq106 eq617
  have eq726 : ∀ X0 : G, x = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 x y x
       have i₂ := eq627
       grind)
    | exact superpose eq627 eq14
    | exact resolve eq14 eq627
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq728 : ∀ X0 : G, x ≠ X0 ∨ x = (N.op x X0) := by
    intro X0
    first
    | (have i₁ := eq21 x y x
       have i₂ := eq627
       grind)
    | exact superpose eq627 eq21
    | (have r₁ := eq21 x y x
       have r₂ := eq627
       grind)
    | exact resolve eq21 eq627
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq729 : y = (N.op y x) := by
    first
    | (have i₁ := eq36 x y
       have i₂ := eq627
       grind)
    | exact superpose eq627 eq36
    | exact resolve eq36 eq627
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36
  have eq732 : x = (N.op x x) := by
    first
    | (have i₁ := eq45 x y
       have i₂ := eq627
       grind)
    | exact superpose eq627 eq45
    | exact resolve eq45 eq627
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45
  have eq743 : x = (M.op (N.op y x) y) ∨ x = (M.op (N.op y x) y) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq125
       have i₂ := eq726 (M.op (N.op y x) y)
       grind)
    | exact superpose eq726 eq125
    | exact resolve eq125 eq726
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq125
  have eq764 : x = (M.op (N.op y x) y) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) := by grind
  clear eq743
  have eq772 : x = (M.op y y) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq764
       have i₂ := eq729
       grind)
    | exact superpose eq729 eq764
    | exact resolve eq764 eq729
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq764
  have eq779 : y = (M.op y y) ∨ x = (M.op y y) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq772
       have i₂ := eq729
       grind)
    | exact superpose eq729 eq772
    | exact resolve eq772 eq729
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq772
  have eq783 : y = (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq779
       have i₂ := eq729
       grind)
    | exact superpose eq729 eq779
    | exact resolve eq779 eq729
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq779
  have eq784 : y = (M.op y y) ∨ x = (M.op y y) := by grind
  clear eq783
  have eq795 : x ≠ (N.op x (N.op x (N.op y y))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq729
       grind)
    | exact superpose eq729 eq10
    | exact resolve eq10 eq729
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq796 : x ≠ (N.op x (N.op x (M.op y y))) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq50
       have i₂ := eq729
       grind)
    | exact superpose eq729 eq50
    | exact resolve eq50 eq729
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq50
  have eq816 : x ≠ (N.op x (N.op x (M.op y y))) ∨ y = (M.op y y) := by grind
  clear eq796
  have eq831 : (M.op (M.op (N.op y x) y) x) = (N.op (M.op (M.op (N.op y x) y) x) (M.op (N.op y x) y)) ∨ x = (M.op x (M.op (N.op y x) y)) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq335
       have i₂ := eq9 y (N.op y x)
       grind)
    | exact superpose eq9 eq335
    | (have j1 := eq9 y (N.op y x)
       grind)
    | exact resolve eq335 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq335
  have eq835 : (M.op (M.op y y) x) = (N.op (M.op (M.op y y) x) (M.op y y)) ∨ x = (M.op x (M.op (N.op y x) y)) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq831
       have i₂ := eq729
       grind)
    | exact superpose eq729 eq831
    | exact resolve eq831 eq729
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq831
  have eq836 : x = (N.op x (M.op y y)) ∨ x = (M.op x (M.op (N.op y x) y)) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq835
       have i₂ := eq726 (M.op y y)
       grind)
    | exact superpose eq726 eq835
    | exact resolve eq835 eq726
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq726 eq835
  have eq837 : x = (M.op (N.op y x) y) ∨ x = (N.op x (M.op y y)) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq836
       have i₂ := eq14 (N.op y x) y x
       grind)
    | exact superpose eq14 eq836
    | exact resolve eq836 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq836
  have eq838 : x = (M.op y y) ∨ x = (N.op x (M.op y y)) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq837
       have i₂ := eq729
       grind)
    | exact superpose eq729 eq837
    | exact resolve eq837 eq729
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq837
  have eq839 : x = (N.op x (M.op y y)) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have j1 := eq728 (M.op y y)
       grind)
    | (have r₁ := eq838
       have r₂ := eq728 (M.op y y)
       grind)
    | (have r₁ := eq838
       have r₂ := eq728 (N.op x (M.op y y))
       grind)
    | exact resolve eq838 eq728
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq728 eq838
  have eq840 : y = (M.op y y) ∨ x = (N.op x (M.op y y)) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq839
       have i₂ := eq729
       grind)
    | exact superpose eq729 eq839
    | exact resolve eq839 eq729
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq839
  have eq841 : y = (M.op y y) ∨ y = (M.op y y) ∨ x = (N.op x (M.op y y)) := by
    first
    | (have i₁ := eq840
       have i₂ := eq729
       grind)
    | exact superpose eq729 eq840
    | exact resolve eq840 eq729
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq729 eq840
  have eq842 : x = (N.op x (M.op y y)) ∨ y = (M.op y y) := by grind
  clear eq841
  have eq1252 : ∀ X0 : G, y = (M.op X0 y) ∨ x = (M.op y y) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq784
       grind)
    | exact superpose eq784 eq14
    | exact resolve eq14 eq784
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14 eq784
  have eq1308 : ∀ X0 X1 : G, (M.op X0 y) = (M.op X1 y) ∨ x = (M.op y y) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 y X1
       have i₂ := eq1252 (M.op X0 y)
       grind)
    | exact superpose eq1252 eq6
    | exact resolve eq6 eq1252
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1252
  have eq2532 : ∀ X0 : G, x = (M.op X0 y) ∨ x = (M.op y y) := by
    intro X0
    first
    | (have i₁ := eq627
       have i₂ := eq1308 x X0
       grind)
    | (have i₁ := eq627
       have i₂ := eq1308 X0 x
       grind)
    | exact superpose eq1308 eq627
    | exact resolve eq627 eq1308
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq627
  have eq2577 : ∀ X0 : G, x ≠ (M.op X0 y) ∨ x = (M.op y y) := by
    intro X0
    grind
  clear eq1308
  have eq2595 : x = (M.op y y) := by
    first
    | (have j0 := eq2532 y
       grind)
    | (have r₁ := eq2532 x
       have r₂ := eq2577 x
       grind)
    | (have r₁ := eq2532 x
       have r₂ := eq2577 y
       grind)
    | exact resolve eq2532 eq2577
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2532 eq2577
  have eq3922 : x ≠ (N.op x x) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq816
       have i₂ := eq842
       grind)
    | exact superpose eq842 eq816
    | exact resolve eq816 eq842
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq816 eq842
  have eq3925 : x ≠ (N.op x x) ∨ y = (M.op y y) := by grind
  clear eq3922
  have eq3928 : y = (M.op y y) := by
    first
    | (have j1 := eq105 y
       grind)
    | (have r₁ := eq3925
       have r₂ := eq105 x
       grind)
    | exact resolve eq3925 eq105
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq105 eq3925
  have eq3934 : x = y := by
    first
    | (have i₁ := eq3928
       have i₂ := eq2595
       grind)
    | exact superpose eq2595 eq3928
    | exact resolve eq3928 eq2595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2595 eq3928
  have eq4234 : x ≠ (N.op x (N.op x (N.op x x))) := by
    first
    | (have i₁ := eq795
       have i₂ := eq3934
       grind)
    | exact superpose eq3934 eq795
    | exact resolve eq795 eq3934
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq795 eq3934
  have eq4264 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq4234
       have i₂ := eq732
       grind)
    | exact superpose eq732 eq4234
    | exact resolve eq4234 eq732
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4234
  have eq4300 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq4264
       have i₂ := eq732
       grind)
    | exact superpose eq732 eq4264
    | exact resolve eq4264 eq732
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4264
  have eq4333 : False := by grind
  exact eq4333

/-- `Equation620` from `Equation3587`: `x ◇ y = z ◇ ((x ◇ y) ◇ y)` ⊢ `x = x ◇ (x ◇ ((y ◇ x) ◇ y))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op a b) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation620_StructuralFromFin_Equation3587 : Law620.StructuralFromFin Law3587 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pxy_x_x_y_pyx_Equation3587 (fun {G} M hM ↦ ?_)
  have hm := (@Law3587.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op a b) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law620 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law620.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X0 X1) X1)) := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op x (N.op x (N.op (N.op y x) y))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 : G, (M.op X2 (M.op (M.op X0 X1) X1)) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X1) X1) X3
       have i₂ := eq6 X0 X1 (M.op X2 (M.op (M.op X0 X1) X1))
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq13 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ X2 ∨ (M.op (M.op X0 X1) X1) = (N.op (M.op (M.op X0 X1) X1) X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op (M.op X0 X1) X1) X2
       have i₂ := eq6 X0 X1 X2
       grind)
    | exact superpose eq6 eq7
    | (have j0 := eq7 (M.op (M.op X0 X1) X1) X2
       grind)
    | (have r₁ := eq7 (M.op (M.op X0 X1) X1) (M.op X0 X1)
       have r₂ := eq6 X0 X1 (M.op X0 X1)
       grind)
    | exact resolve eq7 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X3 : G, (M.op X0 X1) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X3
    first
    | (have i₁ := eq12 X0 X1 x X3
       have i₂ := eq6 X0 X1 x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq19 : ∀ X0 X1 : G, (M.op X0 X1) = (M.op (M.op X0 X1) X1) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 X1 x
       have i₂ := eq14 (M.op X0 X1) X1 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq33 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (N.op X1 (M.op X0 X1)) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 (M.op X0 X1)
       have i₂ := eq19 X0 X1
       grind)
    | exact superpose eq19 eq7
    | (have j0 := eq7 X1 (M.op X0 X1)
       grind)
    | (have r₁ := eq7 X1 (M.op X0 X1)
       have r₂ := eq19 X0 X1
       grind)
    | exact resolve eq7 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq36 : ∀ X0 X1 : G, (N.op X1 (M.op X0 X1)) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq33 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33
  have eq39 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X1 (M.op X0 X1)) = X1 ∨ (N.op (M.op X0 X1) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq8 (M.op X0 X1) X1
       have i₂ := eq19 X0 X1
       grind)
    | exact superpose eq19 eq8
    | (have j0 := eq8 (M.op X0 X1) X1
       grind)
    | (have r₁ := eq8 (M.op X0 X1) X1
       have r₂ := eq19 X0 X1
       grind)
    | exact resolve eq8 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq40 : ∀ X0 X1 : G, (M.op X1 (M.op X0 X1)) = X1 ∨ (N.op (M.op X0 X1) X1) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq39 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq41 : ∀ X0 X1 : G, (N.op (M.op X0 X1) X1) = X1 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq40 X0 X1
       have i₂ := eq14 X0 X1 X1
       grind)
    | exact superpose eq14 eq40
    | (have j0 := eq40 X0 X1
       grind)
    | exact resolve eq40 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq40
  have eq45 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op (M.op X0 X1) (M.op X0 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq36 x (M.op X0 X1)
       have i₂ := eq14 X0 X1 x
       grind)
    | exact superpose eq14 eq36
    | exact resolve eq36 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq50 : x ≠ (N.op x (N.op x (M.op y (N.op y x)))) ∨ (N.op y x) = (M.op (N.op y x) y) ∨ y = (M.op y (N.op y x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 (N.op y x) y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 (N.op y x) y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq51 : x ≠ (N.op x (M.op (N.op (N.op y x) y) x)) ∨ x = (M.op x (N.op (N.op y x) y)) ∨ (N.op (N.op y x) y) = (M.op (N.op (N.op y x) y) x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x (N.op (N.op y x) y)
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x (N.op (N.op y x) y)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq53 : x ≠ (N.op x (N.op x (N.op (M.op x y) y))) ∨ y = (M.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq54 : (N.op (N.op y x) y) = (M.op (N.op (N.op y x) y) x) ∨ x = (M.op x (N.op (N.op y x) y)) := by
    first
    | (have r₁ := eq51
       have r₂ := eq36 (N.op (N.op y x) y) x
       grind)
    | exact resolve eq51 eq36
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq77 : x ≠ (N.op x (M.op (N.op (M.op x y) y) x)) ∨ y = (M.op y x) ∨ x = (M.op x y) ∨ x = (M.op x (N.op (M.op x y) y)) ∨ (N.op (M.op x y) y) = (M.op (N.op (M.op x y) y) x) := by
    first
    | (have i₁ := eq53
       have i₂ := eq9 x (N.op (M.op x y) y)
       grind)
    | exact superpose eq9 eq53
    | (have j1 := eq9 y x
       grind)
    | exact resolve eq53 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq53
  have eq79 : (N.op (M.op x y) y) = (M.op (N.op (M.op x y) y) x) ∨ x = (M.op x y) ∨ x = (M.op x (N.op (M.op x y) y)) ∨ y = (M.op y x) := by
    first
    | (have r₁ := eq77
       have r₂ := eq36 (N.op (M.op x y) y) x
       grind)
    | exact resolve eq77 eq36
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq77
  have eq88 : (N.op (N.op y x) y) ≠ (N.op (N.op y x) y) ∨ x = (M.op x (N.op (N.op y x) y)) ∨ x = (N.op (N.op (N.op y x) y) x) ∨ x = (M.op x (N.op (N.op y x) y)) := by
    first
    | (have i₁ := eq8 (N.op (N.op y x) y) x
       have i₂ := eq54
       grind)
    | exact superpose eq54 eq8
    | (have j0 := eq8 (N.op (N.op y x) y) x
       grind)
    | (have r₁ := eq8 (N.op (N.op y x) y) x
       have r₂ := eq54
       grind)
    | (have r₁ := eq8 x (N.op (N.op y x) y)
       have r₂ := eq54
       grind)
    | exact resolve eq8 eq54
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq90 : ∀ X0 : G, (N.op (N.op y x) y) = (M.op X0 (N.op (N.op y x) y)) ∨ x = (M.op x (N.op (N.op y x) y)) := by
    intro X0
    first
    | (have i₁ := eq14 (N.op (N.op y x) y) x x
       have i₂ := eq54
       grind)
    | exact superpose eq54 eq14
    | exact resolve eq14 eq54
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq96 : (N.op (N.op y x) y) ≠ (N.op (N.op y x) y) ∨ x = (M.op x (N.op (N.op y x) y)) ∨ x = (N.op (N.op (N.op y x) y) x) := by grind
  clear eq88
  have eq97 : x = (N.op (N.op (N.op y x) y) x) ∨ x = (M.op x (N.op (N.op y x) y)) := by grind
  clear eq96
  have eq101 : x = (N.op (N.op (M.op x y) y) x) ∨ x = (M.op x (N.op (M.op x y) y)) ∨ y = (M.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq97
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq97
    | (have j1 := eq9 y x
       grind)
    | exact resolve eq97 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq97
  have eq155 : x ≠ (N.op (N.op y x) y) ∨ x = (M.op x (N.op (N.op y x) y)) := by grind
  clear eq90
  have eq206 : y = (M.op y x) ∨ x = (M.op x y) ∨ x = (M.op x y) ∨ y = (M.op y x) ∨ y = (M.op x y) := by
    first
    | (have i₁ := eq79
       have i₂ := eq41 x y
       grind)
    | exact superpose eq41 eq79
    | (have j1 := eq41 x y
       grind)
    | exact resolve eq79 eq41
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq41 eq79
  have eq224 : y = (M.op y x) ∨ x = (M.op x y) ∨ y = (M.op x y) := by grind
  clear eq206
  have eq232 : ∀ X0 : G, y = (M.op x y) ∨ y = (M.op X0 y) ∨ x = (M.op x y) := by
    intro X0
    first
    | (have i₁ := eq14 y x x
       have i₂ := eq224
       grind)
    | exact superpose eq224 eq14
    | exact resolve eq14 eq224
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq224
  have eq274 : y ≠ y ∨ y = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have j0 := eq232 x
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq232
  have eq276 : y = (M.op x y) ∨ x = (M.op x y) := by grind
  clear eq274
  have eq307 : ∀ X0 : G, y ≠ X0 ∨ (M.op y y) = (N.op (M.op y y) X0) ∨ x = (M.op x y) := by
    intro X0
    first
    | (have i₁ := eq13 x y x
       have i₂ := eq276
       grind)
    | exact superpose eq276 eq13
    | (have r₁ := eq13 x y y
       have r₂ := eq276
       grind)
    | (have r₁ := eq13 x y x
       have r₂ := eq276
       grind)
    | exact resolve eq13 eq276
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq308 : ∀ X0 : G, y = (M.op X0 y) ∨ x = (M.op x y) := by
    intro X0
    first
    | (have i₁ := eq14 x y x
       have i₂ := eq276
       grind)
    | exact superpose eq276 eq14
    | exact resolve eq14 eq276
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq314 : x ≠ y ∨ x = (M.op x y) := by grind
  clear eq276
  have eq349 : ∀ X0 X1 : G, (M.op X0 y) = (M.op X1 y) ∨ x = (M.op x y) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 y X1
       have i₂ := eq308 (M.op X0 y)
       grind)
    | exact superpose eq308 eq6
    | exact resolve eq6 eq308
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq354 : ∀ X0 X1 : G, x = (M.op X0 x) ∨ y = (M.op X1 y) := by
    intro X0 X1
    first
    | (have i₁ := eq14 x y x
       have i₂ := eq308 X0
       grind)
    | exact superpose eq308 eq14
    | (have j1 := eq308 X1
       grind)
    | exact resolve eq14 eq308
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq357 : ∀ X0 : G, y = (N.op y x) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq36 x y
       have i₂ := eq308 X0
       grind)
    | exact superpose eq308 eq36
    | (have j1 := eq308 X0
       grind)
    | exact resolve eq36 eq308
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq471 : ∀ X0 : G, x = (N.op (N.op y x) y) ∨ x = (M.op x (N.op (N.op y x) y)) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq54
       have i₂ := eq354 (N.op (N.op y x) y) X0
       grind)
    | exact superpose eq354 eq54
    | (have j1 := eq354 X0 X0
       grind)
    | exact resolve eq54 eq354
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq54 eq354
  have eq512 : ∀ X0 : G, x = (M.op x (N.op (N.op y x) y)) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq471 X0
       grind)
    | (have r₁ := eq471 X0
       have r₂ := eq155
       grind)
    | exact resolve eq471 eq155
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq155 eq471
  have eq2036 : ∀ X0 X1 : G, x = (M.op x (N.op y y)) ∨ y = (M.op X0 y) ∨ y = (M.op X1 y) := by
    intro X0 X1
    first
    | (have i₁ := eq512 X0
       have i₂ := eq357 X1
       grind)
    | exact superpose eq357 eq512
    | (have j0 := eq512 X0
       have j1 := eq357 X0
       grind)
    | exact resolve eq512 eq357
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq357 eq512
  have eq4807 : (M.op y y) = (N.op (M.op y y) y) ∨ x = (M.op x y) := by
    first
    | (have j0 := eq307 y
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq307
  have eq4840 : ∀ X0 : G, (M.op X0 y) = (N.op (M.op X0 y) y) ∨ x = (M.op x y) ∨ x = (M.op x y) := by
    intro X0
    first
    | (have i₁ := eq4807
       have i₂ := eq349 y x
       grind)
    | (have i₁ := eq4807
       have i₂ := eq349 X0 y
       grind)
    | exact superpose eq349 eq4807
    | exact resolve eq4807 eq349
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq349 eq4807
  have eq4861 : ∀ X0 : G, (M.op X0 y) = (N.op (M.op X0 y) y) ∨ x = (M.op x y) := by
    intro X0
    first
    | (have j0 := eq4840 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4840
  have eq4917 : x = (N.op (M.op x y) x) ∨ x = (M.op x (M.op x y)) ∨ y = (M.op y x) ∨ x = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq101
       have i₂ := eq4861 x
       grind)
    | exact superpose eq4861 eq101
    | exact resolve eq101 eq4861
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq101 eq4861
  have eq4951 : x = (N.op (M.op x y) x) ∨ x = (M.op x (M.op x y)) ∨ y = (M.op y x) ∨ x = (M.op x y) := by grind
  clear eq4917
  have eq4977 : x = (M.op x y) ∨ x = (N.op (M.op x y) x) ∨ y = (M.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq4951
       have i₂ := eq14 x y x
       grind)
    | exact superpose eq14 eq4951
    | exact resolve eq4951 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4951
  have eq4978 : x = (N.op (M.op x y) x) ∨ x = (M.op x y) ∨ y = (M.op y x) := by grind
  clear eq4977
  have eq8049 : ∀ X0 : G, y ≠ y ∨ x = (M.op x (N.op y y)) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq2036 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2036
  have eq8050 : ∀ X0 : G, x = (M.op x (N.op y y)) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq8049 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8049
  have eq8224 : x = (M.op x (N.op y y)) ∨ y = (N.op y y) := by
    first
    | (have i₁ := eq45 x y
       have i₂ := eq8050 x
       grind)
    | exact superpose eq8050 eq45
    | exact resolve eq45 eq8050
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8050
  have eq8297 : x = (M.op x (M.op y y)) ∨ y = (M.op y y) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq8224
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq8224
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq8224 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8224
  have eq8311 : x = (M.op x (M.op y y)) ∨ y = (M.op y y) := by grind
  clear eq8297
  have eq8313 : y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq8311
       have i₂ := eq14 y y x
       grind)
    | exact superpose eq14 eq8311
    | exact resolve eq8311 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8311
  have eq8398 : ∀ X0 : G, y = (M.op X0 y) ∨ x = (M.op y y) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq8313
       grind)
    | exact superpose eq8313 eq14
    | exact resolve eq14 eq8313
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14 eq8313
  have eq8790 : ∀ X0 X1 : G, (M.op X0 y) = (M.op X1 y) ∨ x = (M.op y y) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 y X1
       have i₂ := eq8398 (M.op X0 y)
       grind)
    | exact superpose eq8398 eq6
    | exact resolve eq6 eq8398
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8398
  have eq9527 : ∀ X0 : G, x ≠ (M.op X0 y) ∨ x = (M.op y y) := by
    intro X0
    grind
  clear eq8790
  have eq12801 : x = (N.op y x) ∨ x = y ∨ y = (M.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq4978
       have i₂ := eq308 x
       grind)
    | exact superpose eq308 eq4978
    | exact resolve eq4978 eq308
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq308 eq4978
  have eq12852 : x = (N.op y x) ∨ x = y ∨ x = (M.op x y) := by
    first
    | (have j1 := eq8 y x
       grind)
    | (have r₁ := eq12801
       have r₂ := eq8 y x
       grind)
    | (have r₁ := eq12801
       have r₂ := eq8 x y
       grind)
    | exact resolve eq12801 eq8
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12801
  have eq12857 : x = (N.op y x) ∨ x = (M.op x y) := by
    first
    | (have r₁ := eq12852
       have r₂ := eq314
       grind)
    | exact resolve eq12852 eq314
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq314 eq12852
  have eq12915 : x ≠ (N.op x (N.op x (N.op x y))) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq12857
       grind)
    | exact superpose eq12857 eq10
    | exact resolve eq10 eq12857
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq13005 : x = (M.op x y) ∨ y = (M.op y x) ∨ x = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq9 y x
       have i₂ := eq12857
       grind)
    | exact superpose eq12857 eq9
    | (have j0 := eq9 x y
       grind)
    | exact resolve eq9 eq12857
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12857
  have eq13006 : y = (M.op y x) ∨ x = (M.op x y) := by grind
  clear eq13005
  have eq13173 : x = (N.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq36 y x
       have i₂ := eq13006
       grind)
    | exact superpose eq13006 eq36
    | exact resolve eq36 eq13006
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13006
  have eq13264 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq12915
       have i₂ := eq13173
       grind)
    | exact superpose eq13173 eq12915
    | exact resolve eq12915 eq13173
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12915 eq13173
  have eq13275 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op x y) := by grind
  clear eq13264
  have eq13408 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq13275
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq13275
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq13275 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq13410 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x y) ∨ x = (M.op x x) := by grind
  clear eq13408
  have eq13411 : x = (M.op x y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq13410
       have r₂ := eq36 x x
       grind)
    | exact resolve eq13410 eq36
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13410
  have eq13499 : x = (N.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq45 x y
       have i₂ := eq13411
       grind)
    | exact superpose eq13411 eq45
    | exact resolve eq45 eq13411
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45 eq13411
  have eq13503 : x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq13499
       have r₂ := eq7 x x
       grind)
    | exact resolve eq13499 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13499
  have eq13535 : x ≠ (N.op x x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq13275
       have i₂ := eq13503
       grind)
    | exact superpose eq13503 eq13275
    | exact resolve eq13275 eq13503
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13275
  have eq13541 : x = (M.op x y) := by
    first
    | (have r₁ := eq13535
       have r₂ := eq13503
       grind)
    | exact resolve eq13535 eq13503
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13535
  have eq13694 : x ≠ x ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq9527 x
       have i₂ := eq13541
       grind)
    | exact superpose eq13541 eq9527
    | (have r₁ := eq9527 x
       have r₂ := eq13541
       grind)
    | exact resolve eq9527 eq13541
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9527
  have eq13704 : y = (N.op y x) := by
    first
    | (have i₁ := eq36 x y
       have i₂ := eq13541
       grind)
    | exact superpose eq13541 eq36
    | exact resolve eq36 eq13541
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36 eq13541
  have eq13710 : x = (M.op y y) := by grind
  clear eq13694
  have eq14419 : x ≠ (N.op x (N.op x (M.op y y))) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq50
       have i₂ := eq13704
       grind)
    | exact superpose eq13704 eq50
    | exact resolve eq50 eq13704
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq50 eq13704
  have eq14529 : x ≠ (N.op x (N.op x (M.op y y))) ∨ y = (M.op y y) := by grind
  clear eq14419
  have eq14554 : x ≠ (N.op x (N.op x x)) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq14529
       have i₂ := eq13710
       grind)
    | exact superpose eq13710 eq14529
    | exact resolve eq14529 eq13710
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14529
  have eq14558 : x ≠ (N.op x x) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq14554
       have i₂ := eq13503
       grind)
    | exact superpose eq13503 eq14554
    | exact resolve eq14554 eq13503
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14554
  have eq14560 : y = (M.op y y) := by
    first
    | (have r₁ := eq14558
       have r₂ := eq13503
       grind)
    | exact resolve eq14558 eq13503
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14558
  have eq14562 : x = y := by
    first
    | (have i₁ := eq14560
       have i₂ := eq13710
       grind)
    | exact superpose eq13710 eq14560
    | exact resolve eq14560 eq13710
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13710 eq14560
  have eq14564 : x ≠ (N.op x (N.op x (N.op (N.op x x) x))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq14562
       grind)
    | exact superpose eq14562 eq10
    | exact resolve eq10 eq14562
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14562
  have eq14993 : x ≠ (N.op x (N.op x (N.op x x))) := by
    first
    | (have i₁ := eq14564
       have i₂ := eq13503
       grind)
    | exact superpose eq13503 eq14564
    | exact resolve eq14564 eq13503
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14564
  have eq15071 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq14993
       have i₂ := eq13503
       grind)
    | exact superpose eq13503 eq14993
    | exact resolve eq14993 eq13503
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14993
  have eq15117 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq15071
       have i₂ := eq13503
       grind)
    | exact superpose eq13503 eq15071
    | exact resolve eq15071 eq13503
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15071
  have eq15139 : False := by grind
  exact eq15139

/-- `Equation622` from `Equation3587`: `x ◇ y = z ◇ ((x ◇ y) ◇ y)` ⊢ `x = x ◇ (x ◇ ((y ◇ y) ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op a b) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation622_StructuralFromFin_Equation3587 : Law622.StructuralFromFin Law3587 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pxy_x_x_y_pyx_Equation3587 (fun {G} M hM ↦ ?_)
  have hm := (@Law3587.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op a b) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law622 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law622.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X0 X1) X1)) := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op x (N.op x (N.op (N.op y y) x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 : G, (M.op X2 (M.op (M.op X0 X1) X1)) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X1) X1) X3
       have i₂ := eq6 X0 X1 (M.op X2 (M.op (M.op X0 X1) X1))
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X3 : G, (M.op X0 X1) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X3
    first
    | (have i₁ := eq12 X0 X1 x X3
       have i₂ := eq6 X0 X1 x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq19 : ∀ X0 X1 : G, (M.op X0 X1) = (M.op (M.op X0 X1) X1) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 X1 x
       have i₂ := eq14 (M.op X0 X1) X1 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq33 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (N.op X1 (M.op X0 X1)) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 (M.op X0 X1)
       have i₂ := eq19 X0 X1
       grind)
    | exact superpose eq19 eq7
    | (have j0 := eq7 X1 (M.op X0 X1)
       grind)
    | (have r₁ := eq7 X1 (M.op X0 X1)
       have r₂ := eq19 X0 X1
       grind)
    | exact resolve eq7 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq36 : ∀ X0 X1 : G, (N.op X1 (M.op X0 X1)) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq33 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33
  have eq39 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X1 (M.op X0 X1)) = X1 ∨ (N.op (M.op X0 X1) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq8 (M.op X0 X1) X1
       have i₂ := eq19 X0 X1
       grind)
    | exact superpose eq19 eq8
    | (have j0 := eq8 (M.op X0 X1) X1
       grind)
    | (have r₁ := eq8 (M.op X0 X1) X1
       have r₂ := eq19 X0 X1
       grind)
    | exact resolve eq8 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq40 : ∀ X0 X1 : G, (M.op X1 (M.op X0 X1)) = X1 ∨ (N.op (M.op X0 X1) X1) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq39 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq41 : ∀ X0 X1 : G, (N.op (M.op X0 X1) X1) = X1 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq40 X0 X1
       have i₂ := eq14 X0 X1 X1
       grind)
    | exact superpose eq14 eq40
    | (have j0 := eq40 X0 X1
       grind)
    | exact resolve eq40 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq40
  have eq45 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op (M.op X0 X1) (M.op X0 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq36 x (M.op X0 X1)
       have i₂ := eq14 X0 X1 x
       grind)
    | exact superpose eq14 eq36
    | exact resolve eq36 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq51 : x ≠ (N.op x (M.op (N.op (N.op y y) x) x)) ∨ x = (M.op x (N.op (N.op y y) x)) ∨ (N.op (N.op y y) x) = (M.op (N.op (N.op y y) x) x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x (N.op (N.op y y) x)
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x (N.op (N.op y y) x)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq53 : x ≠ (N.op x (N.op x (N.op (M.op y y) x))) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq54 : x ≠ (N.op x (N.op x (N.op (M.op y y) x))) ∨ y = (M.op y y) := by grind
  clear eq53
  have eq55 : (N.op (N.op y y) x) = (M.op (N.op (N.op y y) x) x) ∨ x = (M.op x (N.op (N.op y y) x)) := by
    first
    | (have r₁ := eq51
       have r₂ := eq36 (N.op (N.op y y) x) x
       grind)
    | exact resolve eq51 eq36
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq64 : x ≠ (N.op x (M.op (N.op (M.op y y) x) x)) ∨ y = (M.op y y) ∨ x = (M.op x (N.op (M.op y y) x)) ∨ (N.op (M.op y y) x) = (M.op (N.op (M.op y y) x) x) := by
    first
    | (have i₁ := eq54
       have i₂ := eq9 x (N.op (M.op y y) x)
       grind)
    | exact superpose eq9 eq54
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq54 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq66 : (N.op (M.op y y) x) = (M.op (N.op (M.op y y) x) x) ∨ x = (M.op x (N.op (M.op y y) x)) ∨ y = (M.op y y) := by
    first
    | (have r₁ := eq64
       have r₂ := eq36 (N.op (M.op y y) x) x
       grind)
    | exact resolve eq64 eq36
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36 eq64
  have eq90 : (N.op (N.op y y) x) ≠ (N.op (N.op y y) x) ∨ x = (M.op x (N.op (N.op y y) x)) ∨ x = (N.op (N.op (N.op y y) x) x) ∨ x = (M.op x (N.op (N.op y y) x)) := by
    first
    | (have i₁ := eq8 (N.op (N.op y y) x) x
       have i₂ := eq55
       grind)
    | exact superpose eq55 eq8
    | (have j0 := eq8 (N.op (N.op y y) x) x
       grind)
    | (have r₁ := eq8 (N.op (N.op y y) x) x
       have r₂ := eq55
       grind)
    | (have r₁ := eq8 x (N.op (N.op y y) x)
       have r₂ := eq55
       grind)
    | exact resolve eq8 eq55
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq92 : ∀ X0 : G, (N.op (N.op y y) x) = (M.op X0 (N.op (N.op y y) x)) ∨ x = (M.op x (N.op (N.op y y) x)) := by
    intro X0
    first
    | (have i₁ := eq14 (N.op (N.op y y) x) x x
       have i₂ := eq55
       grind)
    | exact superpose eq55 eq14
    | exact resolve eq14 eq55
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq55
  have eq98 : (N.op (N.op y y) x) ≠ (N.op (N.op y y) x) ∨ x = (M.op x (N.op (N.op y y) x)) ∨ x = (N.op (N.op (N.op y y) x) x) := by grind
  clear eq90
  have eq99 : x = (N.op (N.op (N.op y y) x) x) ∨ x = (M.op x (N.op (N.op y y) x)) := by grind
  clear eq98
  have eq106 : x = (N.op (M.op x (N.op y y)) x) ∨ x = (M.op x (M.op x (N.op y y))) ∨ (N.op y y) = (M.op (N.op y y) x) ∨ x = (M.op x (N.op y y)) := by
    first
    | (have i₁ := eq99
       have i₂ := eq9 (N.op y y) x
       grind)
    | exact superpose eq9 eq99
    | (have j1 := eq9 (N.op y y) x
       grind)
    | exact resolve eq99 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq99
  have eq112 : x = (M.op x (N.op y y)) ∨ x = (N.op (M.op x (N.op y y)) x) ∨ (N.op y y) = (M.op (N.op y y) x) ∨ x = (M.op x (N.op y y)) := by
    first
    | (have i₁ := eq106
       have i₂ := eq14 x (N.op y y) x
       grind)
    | exact superpose eq14 eq106
    | exact resolve eq106 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq106
  have eq113 : x = (N.op (M.op x (N.op y y)) x) ∨ x = (M.op x (N.op y y)) ∨ (N.op y y) = (M.op (N.op y y) x) := by grind
  clear eq112
  have eq165 : x ≠ (N.op (N.op y y) x) ∨ x = (M.op x (N.op (N.op y y) x)) := by grind
  clear eq92
  have eq219 : (M.op x (M.op y y)) = (M.op (M.op x (M.op y y)) x) ∨ x = (M.op x (M.op x (M.op y y))) ∨ y = (M.op y y) ∨ (M.op y y) = (M.op (M.op y y) x) ∨ x = (M.op x (M.op y y)) := by
    first
    | (have i₁ := eq66
       have i₂ := eq9 (M.op y y) x
       grind)
    | exact superpose eq9 eq66
    | (have j1 := eq9 (M.op x (M.op y y)) x
       grind)
    | exact resolve eq66 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq239 : (M.op y y) = (M.op (M.op y y) x) ∨ x = (M.op x (M.op x (M.op y y))) ∨ y = (M.op y y) ∨ (M.op y y) = (M.op (M.op y y) x) ∨ x = (M.op x (M.op y y)) := by
    first
    | (have i₁ := eq219
       have i₂ := eq14 y y x
       grind)
    | exact superpose eq14 eq219
    | exact resolve eq219 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq219
  have eq240 : (M.op y y) = (M.op (M.op y y) x) ∨ x = (M.op x (M.op x (M.op y y))) ∨ y = (M.op y y) ∨ x = (M.op x (M.op y y)) := by grind
  clear eq239
  have eq241 : x = (M.op x (M.op y y)) ∨ (M.op y y) = (M.op (M.op y y) x) ∨ y = (M.op y y) ∨ x = (M.op x (M.op y y)) := by
    first
    | (have i₁ := eq240
       have i₂ := eq14 x (M.op y y) x
       grind)
    | exact superpose eq14 eq240
    | exact resolve eq240 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq240
  have eq242 : x = (M.op x (M.op y y)) ∨ (M.op y y) = (M.op (M.op y y) x) ∨ y = (M.op y y) := by grind
  clear eq241
  have eq243 : (M.op y y) = (M.op (M.op y y) x) ∨ x = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq242
       have i₂ := eq14 y y x
       grind)
    | exact superpose eq14 eq242
    | exact resolve eq242 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq242
  have eq275 : x = (N.op (M.op y y) x) ∨ x = (M.op y y) ∨ x = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq41 (M.op y y) x
       have i₂ := eq243
       grind)
    | exact superpose eq243 eq41
    | (have j0 := eq41 y y
       grind)
    | exact resolve eq41 eq243
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq41
  have eq277 : x = (N.op (M.op y y) x) ∨ x = (M.op y y) ∨ y = (M.op y y) := by grind
  clear eq275
  have eq314 : x = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq66
       have i₂ := eq277
       grind)
    | exact superpose eq277 eq66
    | exact resolve eq66 eq277
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq66 eq277
  have eq319 : y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op y y) := by grind
  clear eq314
  have eq334 : ∀ X0 : G, y = (M.op X0 y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq319
       grind)
    | exact superpose eq319 eq14
    | exact resolve eq14 eq319
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq319
  have eq379 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq45 y y
       have i₂ := eq334 X0
       grind)
    | exact superpose eq334 eq45
    | (have j1 := eq334 X0
       grind)
    | exact resolve eq45 eq334
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq334
  have eq383 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq379 X0
       have j1 := eq7 x x
       grind)
    | (have r₁ := eq379 y
       have r₂ := eq7 y y
       grind)
    | (have r₁ := eq379 X0
       have r₂ := eq7 x x
       grind)
    | exact resolve eq379 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq379
  have eq403 : ∀ X0 : G, x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq9 x x
       have i₂ := eq383 X0
       grind)
    | exact superpose eq383 eq9
    | (have j0 := eq9 x x
       have j1 := eq383 X0
       grind)
    | exact resolve eq9 eq383
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq383
  have eq416 : ∀ X0 : G, x = (M.op x x) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq403 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq403
  have eq545 : ∀ X0 X1 : G, x = (M.op X0 x) ∨ y = (M.op X1 y) := by
    intro X0 X1
    first
    | (have i₁ := eq14 x x x
       have i₂ := eq416 X0
       grind)
    | exact superpose eq416 eq14
    | (have j1 := eq416 X1
       grind)
    | exact resolve eq14 eq416
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq416
  have eq606 : ∀ X1 : G, y = (N.op y y) ∨ x = (M.op X1 x) := by
    intro X1
    first
    | (have i₁ := eq45 X1 y
       have i₂ := eq545 x X1
       grind)
    | exact superpose eq545 eq45
    | (have j1 := eq545 X1 X1
       grind)
    | exact resolve eq45 eq545
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq545
  have eq712 : x = (M.op y y) ∨ x = (M.op y y) ∨ y = (M.op y y) ∨ y = (N.op y y) := by
    first
    | (have i₁ := eq243
       have i₂ := eq606 (M.op y y)
       grind)
    | exact superpose eq606 eq243
    | exact resolve eq243 eq606
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq243 eq606
  have eq735 : x = (M.op y y) ∨ y = (M.op y y) ∨ y = (N.op y y) := by grind
  clear eq712
  have eq744 : y = (N.op y y) ∨ x = (M.op y y) := by
    first
    | (have j1 := eq7 y y
       grind)
    | (have r₁ := eq735
       have r₂ := eq7 y y
       grind)
    | exact resolve eq735 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq735
  have eq785 : y = (M.op y y) ∨ y = (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq9 y y
       have i₂ := eq744
       grind)
    | exact superpose eq744 eq9
    | (have j0 := eq9 y y
       grind)
    | exact resolve eq9 eq744
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq744
  have eq786 : y = (M.op y y) ∨ x = (M.op y y) := by grind
  clear eq785
  have eq793 : ∀ X0 : G, y = (M.op X0 y) ∨ x = (M.op y y) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq786
       grind)
    | exact superpose eq786 eq14
    | exact resolve eq14 eq786
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq786
  have eq836 : ∀ X0 X1 : G, (M.op X0 y) = (M.op X1 y) ∨ x = (M.op y y) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 y X1
       have i₂ := eq793 (M.op X0 y)
       grind)
    | exact superpose eq793 eq6
    | exact resolve eq6 eq793
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq852 : ∀ X0 : G, x = (M.op x y) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq19 y y
       have i₂ := eq793 X0
       grind)
    | exact superpose eq793 eq19
    | (have j1 := eq793 X0
       grind)
    | exact resolve eq19 eq793
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19 eq793
  have eq933 : y = (N.op y y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq45 x y
       have i₂ := eq852 x
       grind)
    | exact superpose eq852 eq45
    | exact resolve eq45 eq852
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq936 : x ≠ y ∨ x = (M.op x y) := by grind
  have eq1000 : x = (N.op (M.op x y) x) ∨ x = (M.op x y) ∨ y = (M.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq113
       have i₂ := eq933
       grind)
    | exact superpose eq933 eq113
    | exact resolve eq113 eq933
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq113
  have eq1008 : x ≠ (N.op y x) ∨ x = (M.op x (N.op y x)) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq165
       have i₂ := eq933
       grind)
    | exact superpose eq933 eq165
    | exact resolve eq165 eq933
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq165 eq933
  have eq1017 : x = (N.op (M.op x y) x) ∨ x = (M.op x y) ∨ y = (M.op y x) := by grind
  clear eq1000
  have eq2290 : ∀ X0 : G, x ≠ (M.op X0 y) ∨ x = (M.op y y) := by
    intro X0
    grind
  clear eq836
  have eq15642 : x = (N.op y x) ∨ x = y ∨ y = (M.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq1017
       have i₂ := eq852 x
       grind)
    | exact superpose eq852 eq1017
    | exact resolve eq1017 eq852
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq852 eq1017
  have eq15677 : x = (N.op y x) ∨ x = y ∨ x = (M.op x y) := by
    first
    | (have j1 := eq8 y x
       grind)
    | (have r₁ := eq15642
       have r₂ := eq8 y x
       grind)
    | (have r₁ := eq15642
       have r₂ := eq8 x y
       grind)
    | exact resolve eq15642 eq8
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15642
  have eq15683 : x = (N.op y x) ∨ x = (M.op x y) := by
    first
    | (have r₁ := eq15677
       have r₂ := eq936
       grind)
    | exact resolve eq15677 eq936
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15677
  have eq15738 : x ≠ x ∨ x = (M.op x x) ∨ x = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq1008
       have i₂ := eq15683
       grind)
    | exact superpose eq15683 eq1008
    | (have r₁ := eq1008
       have r₂ := eq15683
       grind)
    | exact resolve eq1008 eq15683
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1008
  have eq15743 : x = (M.op x y) ∨ y = (M.op y x) ∨ x = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq9 y x
       have i₂ := eq15683
       grind)
    | exact superpose eq15683 eq9
    | (have j0 := eq9 x y
       grind)
    | exact resolve eq9 eq15683
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15683
  have eq15744 : y = (M.op y x) ∨ x = (M.op x y) := by grind
  clear eq15743
  have eq15746 : x ≠ x ∨ x = (M.op x x) ∨ x = (M.op x y) := by grind
  clear eq15738
  have eq15747 : x = (M.op x y) ∨ x = (M.op x x) := by grind
  clear eq15746
  have eq16242 : x = (N.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq45 x y
       have i₂ := eq15747
       grind)
    | exact superpose eq15747 eq45
    | exact resolve eq45 eq15747
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45 eq15747
  have eq16261 : x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq16242
       have r₂ := eq7 x x
       grind)
    | exact resolve eq16242 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq16242
  have eq16438 : x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq9 x x
       have i₂ := eq16261
       grind)
    | exact superpose eq16261 eq9
    | (have j0 := eq9 x x
       grind)
    | exact resolve eq9 eq16261
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq16439 : x = (M.op x x) := by grind
  clear eq16438
  have eq16501 : ∀ X0 : G, x = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 x x x
       have i₂ := eq16439
       grind)
    | exact superpose eq16439 eq14
    | exact resolve eq14 eq16439
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14 eq16439
  have eq16582 : x = y ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq15744
       have i₂ := eq16501 y
       grind)
    | exact superpose eq16501 eq15744
    | exact resolve eq15744 eq16501
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15744 eq16501
  have eq16606 : x = (M.op x y) := by
    first
    | (have r₁ := eq16582
       have r₂ := eq936
       grind)
    | exact resolve eq16582 eq936
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq936 eq16582
  have eq16706 : x ≠ x ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq2290 x
       have i₂ := eq16606
       grind)
    | exact superpose eq16606 eq2290
    | (have r₁ := eq2290 x
       have r₂ := eq16606
       grind)
    | exact resolve eq2290 eq16606
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2290 eq16606
  have eq16732 : x = (M.op y y) := by grind
  clear eq16706
  have eq16841 : x ≠ (N.op x (N.op x (N.op x x))) ∨ x = y := by
    first
    | (have i₁ := eq54
       have i₂ := eq16732
       grind)
    | exact superpose eq16732 eq54
    | exact resolve eq54 eq16732
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq54 eq16732
  have eq17003 : x ≠ (N.op x (N.op x x)) ∨ x = y := by
    first
    | (have i₁ := eq16841
       have i₂ := eq16261
       grind)
    | exact superpose eq16261 eq16841
    | exact resolve eq16841 eq16261
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq16841
  have eq17032 : x ≠ (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq17003
       have i₂ := eq16261
       grind)
    | exact superpose eq16261 eq17003
    | exact resolve eq17003 eq16261
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq17003
  have eq17039 : x = y := by
    first
    | (have r₁ := eq17032
       have r₂ := eq16261
       grind)
    | exact resolve eq17032 eq16261
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq17032
  have eq17345 : x ≠ (N.op x (N.op x (N.op (N.op x x) x))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq17039
       grind)
    | exact superpose eq17039 eq10
    | exact resolve eq10 eq17039
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq17039
  have eq17773 : x ≠ (N.op x (N.op x (N.op x x))) := by
    first
    | (have i₁ := eq17345
       have i₂ := eq16261
       grind)
    | exact superpose eq16261 eq17345
    | exact resolve eq17345 eq16261
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq17345
  have eq17865 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq17773
       have i₂ := eq16261
       grind)
    | exact superpose eq16261 eq17773
    | exact resolve eq17773 eq16261
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq17773
  have eq17920 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq17865
       have i₂ := eq16261
       grind)
    | exact superpose eq16261 eq17865
    | exact resolve eq17865 eq16261
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq17865
  have eq17950 : False := by grind
  exact eq17950

/-- `Equation825` from `Equation3587`: `x ◇ y = z ◇ ((x ◇ y) ◇ y)` ⊢ `x = x ◇ ((x ◇ y) ◇ (y ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op a b) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation825_StructuralFromFin_Equation3587 : Law825.StructuralFromFin Law3587 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pxy_x_x_y_pyx_Equation3587 (fun {G} M hM ↦ ?_)
  have hm := (@Law3587.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op a b) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law825 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law825.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X0 X1) X1)) := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op x (N.op (N.op x y) (N.op y x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 : G, (M.op X2 (M.op (M.op X0 X1) X1)) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X1) X1) X3
       have i₂ := eq6 X0 X1 (M.op X2 (M.op (M.op X0 X1) X1))
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X3 : G, (M.op X0 X1) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X3
    first
    | (have i₁ := eq12 X0 X1 x X3
       have i₂ := eq6 X0 X1 x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq19 : ∀ X0 X1 : G, (M.op X0 X1) = (M.op (M.op X0 X1) X1) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 X1 x
       have i₂ := eq14 (M.op X0 X1) X1 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq33 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (N.op X1 (M.op X0 X1)) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 (M.op X0 X1)
       have i₂ := eq19 X0 X1
       grind)
    | exact superpose eq19 eq7
    | (have j0 := eq7 X1 (M.op X0 X1)
       grind)
    | (have r₁ := eq7 X1 (M.op X0 X1)
       have r₂ := eq19 X0 X1
       grind)
    | exact resolve eq7 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq36 : ∀ X0 X1 : G, (N.op X1 (M.op X0 X1)) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq33 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33
  have eq39 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X1 (M.op X0 X1)) = X1 ∨ (N.op (M.op X0 X1) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq8 (M.op X0 X1) X1
       have i₂ := eq19 X0 X1
       grind)
    | exact superpose eq19 eq8
    | (have j0 := eq8 (M.op X0 X1) X1
       grind)
    | (have r₁ := eq8 (M.op X0 X1) X1
       have r₂ := eq19 X0 X1
       grind)
    | exact resolve eq8 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq40 : ∀ X0 X1 : G, (M.op X1 (M.op X0 X1)) = X1 ∨ (N.op (M.op X0 X1) X1) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq39 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq41 : ∀ X0 X1 : G, (N.op (M.op X0 X1) X1) = X1 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq40 X0 X1
       have i₂ := eq14 X0 X1 X1
       grind)
    | exact superpose eq14 eq40
    | (have j0 := eq40 X0 X1
       grind)
    | exact resolve eq40 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq40
  have eq45 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op (M.op X0 X1) (M.op X0 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq36 x (M.op X0 X1)
       have i₂ := eq14 X0 X1 x
       grind)
    | exact superpose eq14 eq36
    | exact resolve eq36 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq50 : x ≠ (N.op x (M.op (N.op y x) (N.op x y))) ∨ (N.op x y) = (M.op (N.op x y) (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) (N.op x y)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 (N.op x y) (N.op y x)
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 (N.op x y) (N.op y x)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq51 : x ≠ (N.op x (N.op (M.op y x) (N.op y x))) ∨ x = (M.op x y) ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq74 : x ≠ (N.op x (N.op (M.op y x) (M.op x y))) ∨ x = (M.op x y) ∨ y = (M.op y x) ∨ y = (M.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq51
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq51
    | (have j1 := eq9 x y
       grind)
    | exact resolve eq51 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq77 : x ≠ (N.op x (N.op (M.op y x) (M.op x y))) ∨ x = (M.op x y) ∨ y = (M.op y x) := by grind
  clear eq74
  have eq90 : x ≠ (N.op x (M.op (M.op x y) (M.op y x))) ∨ x = (M.op x y) ∨ y = (M.op y x) ∨ (M.op y x) = (M.op (M.op y x) (M.op x y)) ∨ (M.op x y) = (M.op (M.op x y) (M.op y x)) := by
    first
    | (have i₁ := eq77
       have i₂ := eq9 (M.op y x) (M.op x y)
       grind)
    | exact superpose eq9 eq77
    | (have j1 := eq9 x y
       grind)
    | exact resolve eq77 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq92 : x ≠ (N.op x (M.op y x)) ∨ x = (M.op x y) ∨ y = (M.op y x) ∨ (M.op y x) = (M.op (M.op y x) (M.op x y)) ∨ (M.op x y) = (M.op (M.op x y) (M.op y x)) := by
    first
    | (have i₁ := eq90
       have i₂ := eq14 y x (M.op x y)
       grind)
    | exact superpose eq14 eq90
    | exact resolve eq90 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq90
  have eq93 : x = (M.op x y) ∨ y = (M.op y x) ∨ (M.op y x) = (M.op (M.op y x) (M.op x y)) ∨ (M.op x y) = (M.op (M.op x y) (M.op y x)) := by
    first
    | (have r₁ := eq92
       have r₂ := eq36 y x
       grind)
    | exact resolve eq92 eq36
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq92
  have eq94 : (M.op y x) = (M.op x y) ∨ x = (M.op x y) ∨ y = (M.op y x) ∨ (M.op x y) = (M.op (M.op x y) (M.op y x)) := by
    first
    | (have i₁ := eq93
       have i₂ := eq14 x y (M.op y x)
       grind)
    | exact superpose eq14 eq93
    | exact resolve eq93 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq93
  have eq95 : (M.op y x) = (M.op x y) ∨ (M.op y x) = (M.op x y) ∨ x = (M.op x y) ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq94
       have i₂ := eq14 y x (M.op x y)
       grind)
    | exact superpose eq14 eq94
    | exact resolve eq94 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq94
  have eq96 : (M.op y x) = (M.op x y) ∨ x = (M.op x y) ∨ y = (M.op y x) := by grind
  clear eq95
  have eq99 : x ≠ (M.op (M.op (N.op y x) (N.op x y)) x) ∨ (N.op x y) = (M.op (N.op x y) (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) (N.op x y)) ∨ x = (M.op x (M.op (N.op y x) (N.op x y))) ∨ (M.op (N.op y x) (N.op x y)) = (M.op (M.op (N.op y x) (N.op x y)) x) := by
    first
    | (have i₁ := eq50
       have i₂ := eq9 x (M.op (N.op y x) (N.op x y))
       grind)
    | exact superpose eq9 eq50
    | (have j1 := eq9 (N.op x y) (N.op y x)
       grind)
    | exact resolve eq50 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq100 : x ≠ (M.op (M.op (N.op y x) (N.op x y)) x) ∨ x = (M.op (N.op y x) (N.op x y)) ∨ (N.op x y) = (M.op (N.op x y) (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) (N.op x y)) ∨ (M.op (N.op y x) (N.op x y)) = (M.op (M.op (N.op y x) (N.op x y)) x) := by
    first
    | (have i₁ := eq99
       have i₂ := eq14 (N.op y x) (N.op x y) x
       grind)
    | exact superpose eq14 eq99
    | exact resolve eq99 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq99
  have eq105 : x ≠ (N.op x (N.op (M.op x y) (M.op x y))) ∨ x = (M.op x y) ∨ y = (M.op x y) ∨ x = (M.op x y) ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq77
       have i₂ := eq96
       grind)
    | exact superpose eq96 eq77
    | exact resolve eq77 eq96
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq77
  have eq114 : x = (N.op x (M.op x y)) ∨ x = (M.op x y) ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq36 y x
       have i₂ := eq96
       grind)
    | exact superpose eq96 eq36
    | exact resolve eq36 eq96
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq117 : y ≠ (M.op x y) ∨ x = (M.op x y) ∨ y = (M.op y x) := by grind
  clear eq96
  have eq121 : x ≠ (N.op x (N.op (M.op x y) (M.op x y))) ∨ x = (M.op x y) ∨ y = (M.op x y) ∨ y = (M.op y x) := by grind
  clear eq105
  have eq124 : x ≠ (N.op x (N.op (M.op x y) (M.op x y))) ∨ x = (M.op x y) ∨ y = (M.op y x) := by
    first
    | (have r₁ := eq121
       have r₂ := eq117
       grind)
    | exact resolve eq121 eq117
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq117 eq121
  have eq125 : x ≠ (N.op x (M.op x y)) ∨ x = (M.op x y) ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq124
       have i₂ := eq45 x y
       grind)
    | exact superpose eq45 eq124
    | exact resolve eq124 eq45
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq124
  have eq126 : y = (M.op y x) ∨ x = (M.op x y) := by
    first
    | (have r₁ := eq125
       have r₂ := eq114
       grind)
    | exact resolve eq125 eq114
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq114 eq125
  have eq134 : y ≠ y ∨ x = (M.op x y) ∨ x = (N.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq8 y x
       have i₂ := eq126
       grind)
    | exact superpose eq126 eq8
    | (have j0 := eq8 y x
       grind)
    | (have r₁ := eq8 y x
       have r₂ := eq126
       grind)
    | (have r₁ := eq8 x y
       have r₂ := eq126
       grind)
    | exact resolve eq8 eq126
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq139 : x = (N.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq36 y x
       have i₂ := eq126
       grind)
    | exact superpose eq126 eq36
    | exact resolve eq36 eq126
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq126
  have eq142 : y ≠ y ∨ x = (M.op x y) ∨ x = (N.op y x) := by grind
  clear eq134
  have eq143 : x = (N.op y x) ∨ x = (M.op x y) := by grind
  clear eq142
  have eq213 : x ≠ (N.op x (N.op x (N.op y x))) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq139
       grind)
    | exact superpose eq139 eq10
    | exact resolve eq10 eq139
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq139
  have eq1261 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq213
       have i₂ := eq143
       grind)
    | exact superpose eq143 eq213
    | exact resolve eq213 eq143
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq143 eq213
  have eq1266 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op x y) := by grind
  clear eq1261
  have eq1270 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1266
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq1266
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq1266 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1272 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x y) ∨ x = (M.op x x) := by grind
  clear eq1270
  have eq1273 : x = (M.op x y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1272
       have r₂ := eq36 x x
       grind)
    | exact resolve eq1272 eq36
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1272
  have eq1310 : x = (N.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq45 x y
       have i₂ := eq1273
       grind)
    | exact superpose eq1273 eq45
    | exact resolve eq45 eq1273
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45 eq1273
  have eq1313 : x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq1310
       have r₂ := eq7 x x
       grind)
    | exact resolve eq1310 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1310
  have eq1336 : x ≠ (N.op x x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq1266
       have i₂ := eq1313
       grind)
    | exact superpose eq1313 eq1266
    | exact resolve eq1266 eq1313
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1266
  have eq1337 : x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq9 x x
       have i₂ := eq1313
       grind)
    | exact superpose eq1313 eq9
    | (have j0 := eq9 x x
       grind)
    | exact resolve eq9 eq1313
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1338 : x = (M.op x x) := by grind
  clear eq1337
  have eq1340 : x = (M.op x y) := by
    first
    | (have r₁ := eq1336
       have r₂ := eq1313
       grind)
    | exact resolve eq1336 eq1313
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1336
  have eq1405 : ∀ X0 : G, x = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 x x x
       have i₂ := eq1338
       grind)
    | exact superpose eq1338 eq14
    | exact resolve eq14 eq1338
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1338
  have eq1441 : y = (N.op y x) := by
    first
    | (have i₁ := eq36 x y
       have i₂ := eq1340
       grind)
    | exact superpose eq1340 eq36
    | exact resolve eq36 eq1340
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36
  have eq1443 : y = (N.op x y) ∨ x = y := by
    first
    | (have i₁ := eq41 x y
       have i₂ := eq1340
       grind)
    | exact superpose eq1340 eq41
    | exact resolve eq41 eq1340
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq41
  have eq1515 : x ≠ (N.op x (N.op (N.op x y) y)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1441
       grind)
    | exact superpose eq1441 eq10
    | exact resolve eq10 eq1441
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1584 : x ≠ (N.op x (M.op (N.op y x) y)) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) ∨ x = y := by
    first
    | (have i₁ := eq50
       have i₂ := eq1443
       grind)
    | exact superpose eq1443 eq50
    | exact resolve eq50 eq1443
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq50
  have eq1589 : x ≠ (M.op (M.op (N.op y x) y) x) ∨ x = (M.op (N.op y x) y) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) ∨ (M.op (N.op y x) y) = (M.op (M.op (N.op y x) y) x) ∨ x = y := by
    first
    | (have i₁ := eq100
       have i₂ := eq1443
       grind)
    | exact superpose eq1443 eq100
    | exact resolve eq100 eq1443
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq100 eq1443
  have eq1597 : x = (M.op (N.op y x) y) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) ∨ (M.op (N.op y x) y) = (M.op (M.op (N.op y x) y) x) ∨ x = y := by
    first
    | (have r₁ := eq1589
       have r₂ := eq1405 (M.op (N.op y x) y)
       grind)
    | exact resolve eq1589 eq1405
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1589
  have eq1600 : x ≠ (N.op x (M.op y y)) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) ∨ x = y := by
    first
    | (have i₁ := eq1584
       have i₂ := eq1441
       grind)
    | exact superpose eq1441 eq1584
    | exact resolve eq1584 eq1441
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1584
  have eq1602 : x = (M.op y y) ∨ y = (M.op y (N.op y x)) ∨ (N.op y x) = (M.op (N.op y x) y) ∨ (M.op (N.op y x) y) = (M.op (M.op (N.op y x) y) x) ∨ x = y := by
    first
    | (have i₁ := eq1597
       have i₂ := eq1441
       grind)
    | exact superpose eq1441 eq1597
    | exact resolve eq1597 eq1441
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1597
  have eq1604 : y = (M.op y y) ∨ x ≠ (N.op x (M.op y y)) ∨ (N.op y x) = (M.op (N.op y x) y) ∨ x = y := by
    first
    | (have i₁ := eq1600
       have i₂ := eq1441
       grind)
    | exact superpose eq1441 eq1600
    | exact resolve eq1600 eq1441
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1600
  have eq1605 : y = (M.op y y) ∨ x = (M.op y y) ∨ (N.op y x) = (M.op (N.op y x) y) ∨ (M.op (N.op y x) y) = (M.op (M.op (N.op y x) y) x) ∨ x = y := by
    first
    | (have i₁ := eq1602
       have i₂ := eq1441
       grind)
    | exact superpose eq1441 eq1602
    | exact resolve eq1602 eq1441
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1602
  have eq1607 : y = (M.op y y) ∨ y = (M.op y y) ∨ x ≠ (N.op x (M.op y y)) ∨ x = y := by
    first
    | (have i₁ := eq1604
       have i₂ := eq1441
       grind)
    | exact superpose eq1441 eq1604
    | exact resolve eq1604 eq1441
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1604
  have eq1608 : x ≠ (N.op x (M.op y y)) ∨ y = (M.op y y) ∨ x = y := by grind
  clear eq1607
  have eq1609 : y = (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ (M.op (N.op y x) y) = (M.op (M.op (N.op y x) y) x) ∨ x = y := by
    first
    | (have i₁ := eq1605
       have i₂ := eq1441
       grind)
    | exact superpose eq1441 eq1605
    | exact resolve eq1605 eq1441
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1605
  have eq1610 : y = (M.op y y) ∨ x = (M.op y y) ∨ (M.op (N.op y x) y) = (M.op (M.op (N.op y x) y) x) ∨ x = y := by grind
  clear eq1609
  have eq1612 : x = (M.op (N.op y x) y) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = y := by
    first
    | (have i₁ := eq1610
       have i₂ := eq1405 (M.op (N.op y x) y)
       grind)
    | exact superpose eq1405 eq1610
    | exact resolve eq1610 eq1405
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1405 eq1610
  have eq1613 : x = (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = y := by
    first
    | (have i₁ := eq1612
       have i₂ := eq1441
       grind)
    | exact superpose eq1441 eq1612
    | exact resolve eq1612 eq1441
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1441 eq1612
  have eq1614 : y = (M.op y y) ∨ x = (M.op y y) ∨ x = y := by grind
  clear eq1613
  have eq2198 : ∀ X0 : G, y = (M.op X0 y) ∨ x = (M.op y y) ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq1614
       grind)
    | exact superpose eq1614 eq14
    | exact resolve eq14 eq1614
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14 eq1614
  have eq4143 : ∀ X0 : G, x ≠ (N.op x x) ∨ x = y ∨ x = y ∨ y = (M.op X0 y) ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq1608
       have i₂ := eq2198 X0
       grind)
    | exact superpose eq2198 eq1608
    | (have j1 := eq2198 X0
       grind)
    | exact resolve eq1608 eq2198
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1608 eq2198
  have eq4163 : ∀ X0 : G, x ≠ (N.op x x) ∨ x = y ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq4143 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4143
  have eq4165 : ∀ X0 : G, y = (M.op X0 y) ∨ x = y := by
    intro X0
    first
    | (have j0 := eq4163 X0
       grind)
    | (have r₁ := eq4163 X0
       have r₂ := eq1313
       grind)
    | exact resolve eq4163 eq1313
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4163
  have eq4230 : x = y ∨ x = y := by
    first
    | (have i₁ := eq1340
       have i₂ := eq4165 x
       grind)
    | exact superpose eq4165 eq1340
    | exact resolve eq1340 eq4165
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1340 eq4165
  have eq4273 : x = y := by grind
  clear eq4230
  have eq4409 : x ≠ (N.op x (N.op (N.op x x) x)) := by
    first
    | (have i₁ := eq1515
       have i₂ := eq4273
       grind)
    | exact superpose eq4273 eq1515
    | exact resolve eq1515 eq4273
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1515 eq4273
  have eq4433 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq4409
       have i₂ := eq1313
       grind)
    | exact superpose eq1313 eq4409
    | exact resolve eq4409 eq1313
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4409
  have eq4441 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq4433
       have i₂ := eq1313
       grind)
    | exact superpose eq1313 eq4433
    | exact resolve eq4433 eq1313
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4433
  have eq4446 : False := by grind
  exact eq4446
