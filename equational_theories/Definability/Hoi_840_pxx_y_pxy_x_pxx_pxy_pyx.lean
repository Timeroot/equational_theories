import equational_theories.Definability.AutBox2
import equational_theories.Definability.Hof_03

/-!
# Structurality from `Equation840` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation840` is `x = x ◇ ((y ◇ x) ◇ (z ◇ z))`, and the companion is
`x □ y = if (M.op a a) = b then (M.op a a) else if (M.op a b) = a then (M.op a b) else (M.op b a)`.  Its automorphism half is `GuardAut2_pxx_y_pxy_x_pxx_pxy_pyx_Equation840`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches it on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation3254` from `Equation840`: `x = x ◇ ((y ◇ x) ◇ (z ◇ z))` ⊢ `x ◇ x = x ◇ (x ◇ (x ◇ y))`, through the
companion `x □ y = if (M.op a a) = b then (M.op a a) else if (M.op a b) = a then (M.op a b) else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3254_StructuralFromFin_Equation840 : Law3254.StructuralFromFin Law840 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) ((Lf 0) ⋆ (Lf 1)) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pxx_y_pxy_x_pxx_pxy_pyx_Equation840 (fun {G} M hM ↦ ?_)
  have hm := (@Law840.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op a a) ≠ b ∨ N.op a b = (M.op a a)) →
      (∀ a b : G, (M.op a a) = b ∨ (M.op a b) ≠ a ∨ N.op a b = (M.op a b)) →
      (∀ a b : G, (M.op a a) = b ∨ (M.op a b) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3254 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) ((Lf 0) ⋆ (Lf 1)) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) ((Lf 0) ⋆ (Lf 1)) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) ((Lf 0) ⋆ (Lf 1)) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3254.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op X0 (M.op (M.op X1 X0) (M.op X2 X2))) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (M.op X0 X0) = (N.op X0 X1) := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (N.op X0 X1) = (M.op X0 X1) ∨ (M.op X0 X1) ≠ X0 ∨ (M.op X0 X0) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ (M.op X0 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op x (N.op x y))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 : G, (M.op X1 (M.op X0 X1)) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 X0 (M.op (M.op X0 X1) (M.op X0 X1))
       have i₂ := eq6 (M.op X0 X1) (M.op X0 X1) (M.op X0 X1)
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq90 : x ≠ (M.op x y) ∨ (N.op x x) ≠ (N.op x (N.op x (M.op x y))) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq8 x y
       grind)
    | exact superpose eq8 eq10
    | (have j1 := eq8 x y
       grind)
    | exact resolve eq10 eq8
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq95 : (N.op x x) ≠ (N.op x (N.op x (M.op y x))) ∨ x = (M.op x y) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq506 : (N.op x x) ≠ (N.op x (M.op x (M.op y x))) ∨ x = (M.op x y) ∨ y = (M.op x x) ∨ x ≠ (M.op x (M.op y x)) ∨ (M.op x x) = (M.op y x) := by
    first
    | (have i₁ := eq95
       have i₂ := eq8 x (M.op y x)
       grind)
    | exact superpose eq8 eq95
    | (have j1 := eq8 x (M.op y x)
       grind)
    | (have r₁ := eq95
       have r₂ := eq8 x y
       grind)
    | exact resolve eq95 eq8
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq509 : (N.op x x) ≠ (N.op x (M.op x (M.op y x))) ∨ x = (M.op x y) ∨ y = (M.op x x) ∨ (M.op x x) = (M.op y x) := by
    first
    | (have r₁ := eq506
       have r₂ := eq13 y x
       grind)
    | exact resolve eq506 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq506
  have eq510 : (N.op x x) ≠ (N.op x x) ∨ x = (M.op x y) ∨ y = (M.op x x) ∨ (M.op x x) = (M.op y x) := by
    first
    | (have i₁ := eq509
       have i₂ := eq13 y x
       grind)
    | exact superpose eq13 eq509
    | exact resolve eq509 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq509
  have eq511 : x = (M.op x y) ∨ y = (M.op x x) ∨ (M.op x x) = (M.op y x) := by grind
  clear eq510
  have eq512 : x ≠ x ∨ (N.op x x) ≠ (N.op x (N.op x x)) ∨ y = (M.op x x) ∨ y = (M.op x x) ∨ (M.op x x) = (M.op y x) := by
    first
    | (have i₁ := eq90
       have i₂ := eq511
       grind)
    | exact superpose eq511 eq90
    | (have r₁ := eq90
       have r₂ := eq511
       grind)
    | exact resolve eq90 eq511
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq90 eq511
  have eq526 : x ≠ x ∨ (N.op x x) ≠ (N.op x (N.op x x)) ∨ y = (M.op x x) ∨ (M.op x x) = (M.op y x) := by grind
  clear eq512
  have eq527 : (N.op x x) ≠ (N.op x (N.op x x)) ∨ y = (M.op x x) ∨ (M.op x x) = (M.op y x) := by grind
  clear eq526
  have eq582 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ y = (M.op x x) ∨ (M.op x x) = (M.op y x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq527
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq527
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq527 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq586 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ y = (M.op x x) ∨ (M.op x x) = (M.op y x) ∨ x = (M.op x x) := by grind
  clear eq582
  have eq587 : (M.op x x) = (M.op y x) ∨ y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq586
  have eq589 : (N.op x x) ≠ (N.op x (N.op x (M.op x x))) ∨ x = (M.op x y) ∨ y = (M.op x x) ∨ y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq95
       have i₂ := eq587
       grind)
    | exact superpose eq587 eq95
    | exact resolve eq95 eq587
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq603 : (N.op x x) ≠ (N.op x (N.op x (M.op x x))) ∨ x = (M.op x y) ∨ y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq589
  have eq605 : (N.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x y) ∨ y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq603
  have eq606 : (N.op x x) ≠ (M.op x x) ∨ x = (M.op x y) ∨ y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq605
  have eq992 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x y) ∨ y = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq606
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq606
    | (have j1 := eq9 x y
       grind)
    | (have r₁ := eq606
       have r₂ := eq9 x x
       grind)
    | (have r₁ := eq606
       have r₂ := eq9 x (N.op x x)
       grind)
    | exact resolve eq606 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq606
  have eq995 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x y) ∨ y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq992
  have eq996 : x = (M.op x y) ∨ y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq995
  have eq1000 : y = (M.op y x) ∨ y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq13 x y
       have i₂ := eq996
       grind)
    | exact superpose eq996 eq13
    | exact resolve eq13 eq996
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq996
  have eq1019 : y = (M.op x x) ∨ y = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq587
       have i₂ := eq1000
       grind)
    | exact superpose eq1000 eq587
    | exact resolve eq587 eq1000
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq587 eq1000
  have eq1042 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq1019
  have eq1047 : y = (N.op x y) ∨ x = (M.op x x) := by grind
  have eq1092 : (N.op x x) ≠ (N.op x (N.op x y)) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1047
       grind)
    | exact superpose eq1047 eq10
    | exact resolve eq10 eq1047
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1269 : (N.op x x) ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1092
       have i₂ := eq1047
       grind)
    | exact superpose eq1047 eq1092
    | exact resolve eq1092 eq1047
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1092
  have eq1274 : (N.op x x) ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq1269
  have eq1276 : (N.op x x) ≠ y ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1274
       have i₂ := eq1047
       grind)
    | exact superpose eq1047 eq1274
    | exact resolve eq1274 eq1047
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1047 eq1274
  have eq1279 : (N.op x x) ≠ y ∨ x = (M.op x x) := by grind
  clear eq1276
  have eq1281 : y ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1279
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq1279
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq1279 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1279
  have eq1284 : y ≠ (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq1281
  have eq1285 : x = (M.op x x) := by
    first
    | (have r₁ := eq1284
       have r₂ := eq1042
       grind)
    | exact resolve eq1284 eq1042
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1042 eq1284
  have eq1288 : x = (N.op x x) := by grind
  have eq1338 : x ≠ (N.op x x) ∨ y = (M.op x x) ∨ (M.op x x) = (M.op y x) := by
    first
    | (have i₁ := eq527
       have i₂ := eq1288
       grind)
    | exact superpose eq1288 eq527
    | exact resolve eq527 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq527
  have eq1345 : y = (M.op x x) ∨ (M.op x x) = (M.op y x) := by
    first
    | (have r₁ := eq1338
       have r₂ := eq1288
       grind)
    | exact resolve eq1338 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1338
  have eq1346 : x = y ∨ (M.op x x) = (M.op y x) := by
    first
    | (have i₁ := eq1345
       have i₂ := eq1285
       grind)
    | exact superpose eq1285 eq1345
    | exact resolve eq1345 eq1285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1345
  have eq1347 : x = y ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq1346
       have i₂ := eq1285
       grind)
    | exact superpose eq1285 eq1346
    | exact resolve eq1346 eq1285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1346
  have eq1348 : (N.op x x) ≠ (N.op x (N.op x (N.op x x))) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1347
       grind)
    | exact superpose eq1347 eq10
    | exact resolve eq10 eq1347
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1347
  have eq1371 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq1348
       have i₂ := eq1288
       grind)
    | exact superpose eq1288 eq1348
    | exact resolve eq1348 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1348
  have eq1373 : x ≠ (N.op x x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq1371
       have i₂ := eq1288
       grind)
    | exact superpose eq1288 eq1371
    | exact resolve eq1371 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1371
  have eq1375 : x = (M.op y x) := by
    first
    | (have r₁ := eq1373
       have r₂ := eq1288
       grind)
    | exact resolve eq1373 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1373
  have eq1384 : (N.op x x) ≠ (N.op x (N.op x x)) ∨ x = (M.op x y) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq95
       have i₂ := eq1375
       grind)
    | exact superpose eq1375 eq95
    | exact resolve eq95 eq1375
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq95
  have eq1409 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq1384
       have i₂ := eq1288
       grind)
    | exact superpose eq1288 eq1384
    | exact resolve eq1384 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1384
  have eq1412 : x = (M.op x y) ∨ y = (M.op x x) := by
    first
    | (have r₁ := eq1409
       have r₂ := eq1288
       grind)
    | exact resolve eq1409 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1409
  have eq1414 : x = y ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq1412
       have i₂ := eq1285
       grind)
    | exact superpose eq1285 eq1412
    | exact resolve eq1412 eq1285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1285 eq1412
  have eq1415 : (N.op x x) ≠ (N.op x (N.op x (N.op x x))) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1414
       grind)
    | exact superpose eq1414 eq10
    | exact resolve eq10 eq1414
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1414
  have eq1439 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq1415
       have i₂ := eq1288
       grind)
    | exact superpose eq1288 eq1415
    | exact resolve eq1415 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1415
  have eq1441 : x ≠ (N.op x x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq1439
       have i₂ := eq1288
       grind)
    | exact superpose eq1288 eq1439
    | exact resolve eq1439 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1439
  have eq1443 : x = (M.op x y) := by
    first
    | (have r₁ := eq1441
       have r₂ := eq1288
       grind)
    | exact resolve eq1441 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1441
  have eq1450 : y = (M.op y x) := by
    first
    | (have i₁ := eq13 x y
       have i₂ := eq1443
       grind)
    | exact superpose eq1443 eq13
    | exact resolve eq13 eq1443
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13 eq1443
  have eq1465 : x = y := by
    first
    | (have i₁ := eq1450
       have i₂ := eq1375
       grind)
    | exact superpose eq1375 eq1450
    | exact resolve eq1450 eq1375
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1375 eq1450
  have eq1469 : (N.op x x) ≠ (N.op x (N.op x (N.op x x))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1465
       grind)
    | exact superpose eq1465 eq10
    | exact resolve eq10 eq1465
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1465
  have eq1494 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq1469
       have i₂ := eq1288
       grind)
    | exact superpose eq1288 eq1469
    | exact resolve eq1469 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1469
  have eq1496 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq1494
       have i₂ := eq1288
       grind)
    | exact superpose eq1288 eq1494
    | exact resolve eq1494 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1494
  have eq1498 : False := by grind
  exact eq1498
