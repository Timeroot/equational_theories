import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation2430` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation2430` is `x = (y ◇ (z ◇ (w ◇ w))) ◇ x`, and the companion is
`x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`.  Its automorphism half is `GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2430`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches none of these on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation307` from `Equation2430`: `x = (y ◇ (z ◇ (w ◇ w))) ◇ x` ⊢ `x ◇ x = x ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation307_StructuralFromFin_Equation2430 : Law307.StructuralFromFin Law2430 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2430 (fun {G} M hM ↦ ?_)
  have hm := (@Law2430.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law307 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law307.models_iff G N]
  intro x
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X1 (M.op X2 (M.op X3 X3))) X0) = X0 := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X5 : G, (M.op (M.op X0 (M.op X1 X1)) X5) = X5 := by
    intro X0 X1 X5
    first
    | (have i₁ := eq6 X5 (M.op x (M.op x (M.op x x))) X0 X1
       have i₂ := eq6 (M.op X0 (M.op X1 X1)) x x x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq20 : ∀ X0 X3 : G, (M.op (M.op X0 X0) X3) = X3 := by
    intro X0 X3
    first
    | (have i₁ := eq13 (M.op x (M.op x x)) X0 X3
       have i₂ := eq13 x x (M.op X0 X0)
       grind)
    | exact superpose eq13 eq13
    | exact resolve eq13 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq27 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq29 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq27
  have eq33 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op X0 X0)
       have i₂ := eq20 X0 x
       grind)
    | exact superpose eq20 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X0)
       have r₂ := eq20 X0 x
       grind)
    | exact resolve eq8 eq20
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq34 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq20 X1 X0
       grind)
    | exact superpose eq20 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq20 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq20
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20
  have eq40 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq33 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33
  have eq45 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq34 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34
  have eq161 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq29
       have i₂ := eq40 x x
       grind)
    | exact superpose eq40 eq29
    | (have j1 := eq40 x x
       grind)
    | (have r₁ := eq29
       have r₂ := eq40 x x
       grind)
    | (have r₁ := eq29
       have r₂ := eq40 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq29 eq40
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29 eq40
  have eq164 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq161
  have eq165 : x = (M.op x x) := by grind
  clear eq164
  have eq189 : x = (N.op x x) := by
    first
    | (have i₁ := eq45 x
       have i₂ := eq165
       grind)
    | exact superpose eq165 eq45
    | exact resolve eq45 eq165
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45 eq165
  have eq385 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq189
       grind)
    | exact superpose eq189 eq10
    | exact resolve eq10 eq189
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq389 : False := by grind
  exact eq389

/-- `Equation312` from `Equation2430`: `x = (y ◇ (z ◇ (w ◇ w))) ◇ x` ⊢ `x ◇ x = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation312_StructuralFromFin_Equation2430 : Law312.StructuralFromFin Law2430 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2430 (fun {G} M hM ↦ ?_)
  have hm := (@Law2430.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law312 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law312.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X1 (M.op X2 (M.op X3 X3))) X0) = X0 := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X5 : G, (M.op (M.op X0 (M.op X1 X1)) X5) = X5 := by
    intro X0 X1 X5
    first
    | (have i₁ := eq6 X5 (M.op x (M.op x (M.op x x))) X0 X1
       have i₂ := eq6 (M.op X0 (M.op X1 X1)) x x x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq20 : ∀ X0 X3 : G, (M.op (M.op X0 X0) X3) = X3 := by
    intro X0 X3
    first
    | (have i₁ := eq13 (M.op x (M.op x x)) X0 X3
       have i₂ := eq13 x x (M.op X0 X0)
       grind)
    | exact superpose eq13 eq13
    | exact resolve eq13 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq27 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq29 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq27
  have eq33 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op X0 X0)
       have i₂ := eq20 X0 x
       grind)
    | exact superpose eq20 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X0)
       have r₂ := eq20 X0 x
       grind)
    | exact resolve eq8 eq20
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq34 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq20 X1 X0
       grind)
    | exact superpose eq20 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq20 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq20
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20
  have eq40 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq33 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33
  have eq45 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq34 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34
  have eq162 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq29
       have i₂ := eq40 y x
       grind)
    | exact superpose eq40 eq29
    | (have j1 := eq40 x x
       grind)
    | (have r₁ := eq29
       have r₂ := eq40 y x
       grind)
    | (have r₁ := eq29
       have r₂ := eq40 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq29 eq40
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq163 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq162
  have eq177 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq29
       have i₂ := eq163
       grind)
    | exact superpose eq163 eq29
    | exact resolve eq29 eq163
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29
  have eq190 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq45 x
       have i₂ := eq163
       grind)
    | exact superpose eq163 eq45
    | exact resolve eq45 eq163
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq191 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq163
  have eq192 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq177
       have r₂ := eq190
       grind)
    | exact resolve eq177 eq190
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq177 eq190
  have eq193 : x = (M.op x x) := by
    first
    | (have r₁ := eq192
       have r₂ := eq191
       grind)
    | exact resolve eq192 eq191
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq191 eq192
  have eq271 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq40 X0 x
       have i₂ := eq193
       grind)
    | exact superpose eq193 eq40
    | exact resolve eq40 eq193
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq40
  have eq272 : x = (N.op x x) := by
    first
    | (have i₁ := eq45 x
       have i₂ := eq193
       grind)
    | exact superpose eq193 eq45
    | exact resolve eq45 eq193
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45 eq193
  have eq427 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq272
       grind)
    | exact superpose eq272 eq10
    | exact resolve eq10 eq272
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq875 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq427
       have i₂ := eq271 y
       grind)
    | exact superpose eq271 eq427
    | (have j1 := eq271 y
       grind)
    | (have r₁ := eq427
       have r₂ := eq271 y
       grind)
    | (have r₁ := eq427
       have r₂ := eq271 (N.op y x)
       grind)
    | exact resolve eq427 eq271
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq271
  have eq876 : x = y := by grind
  clear eq875
  have eq1047 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq427
       have i₂ := eq876
       grind)
    | exact superpose eq876 eq427
    | exact resolve eq427 eq876
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq427 eq876
  have eq1052 : False := by grind
  exact eq1052

/-- `Equation3258` from `Equation2430`: `x = (y ◇ (z ◇ (w ◇ w))) ◇ x` ⊢ `x ◇ x = x ◇ (y ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3258_StructuralFromFin_Equation2430 : Law3258.StructuralFromFin Law2430 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2430 (fun {G} M hM ↦ ?_)
  have hm := (@Law2430.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3258 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3258.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X1 (M.op X2 (M.op X3 X3))) X0) = X0 := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op y (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X5 : G, (M.op (M.op X0 (M.op X1 X1)) X5) = X5 := by
    intro X0 X1 X5
    first
    | (have i₁ := eq6 X5 (M.op x (M.op x (M.op x x))) X0 X1
       have i₂ := eq6 (M.op X0 (M.op X1 X1)) x x x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X3 : G, (M.op (M.op X0 X0) X3) = X3 := by
    intro X0 X3
    first
    | (have i₁ := eq13 (M.op x (M.op x x)) X0 X3
       have i₂ := eq13 x x (M.op X0 X0)
       grind)
    | exact superpose eq13 eq13
    | exact resolve eq13 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq26 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq34 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op X0 X0)
       have i₂ := eq18 X0 x
       grind)
    | exact superpose eq18 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X0)
       have r₂ := eq18 X0 x
       grind)
    | exact resolve eq8 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq35 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq34 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34
  have eq41 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq26 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26
  have eq42 : (M.op x x) ≠ (N.op x (N.op y (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq45 : (M.op x x) ≠ (N.op x (N.op y (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq42
  have eq129 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq45
       have i₂ := eq35 y x
       grind)
    | exact superpose eq35 eq45
    | (have j1 := eq35 x x
       grind)
    | (have r₁ := eq45
       have r₂ := eq35 (N.op x (N.op y (M.op x x))) x
       grind)
    | exact resolve eq45 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq132 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq35 y x
       grind)
    | (have r₁ := eq129
       have r₂ := eq35 x x
       grind)
    | (have r₁ := eq129
       have r₂ := eq35 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq129 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq129
  have eq149 : y ≠ (N.op x (N.op y y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq45
       have i₂ := eq132
       grind)
    | exact superpose eq132 eq45
    | exact resolve eq45 eq132
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45
  have eq159 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq132
       grind)
    | exact superpose eq132 eq35
    | (have j0 := eq35 x x
       grind)
    | exact resolve eq35 eq132
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq160 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq41 x
       have i₂ := eq132
       grind)
    | exact superpose eq132 eq41
    | exact resolve eq41 eq132
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq41
  have eq161 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq132
  have eq162 : y ≠ (N.op x (N.op y y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq149
       have r₂ := eq161
       grind)
    | exact resolve eq149 eq161
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq149
  have eq351 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq162
       have i₂ := eq160
       grind)
    | exact superpose eq160 eq162
    | exact resolve eq162 eq160
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq160 eq162
  have eq355 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq351
  have eq619 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq355
       have i₂ := eq159 x
       grind)
    | exact superpose eq159 eq355
    | (have j1 := eq159 x
       grind)
    | (have r₁ := eq355
       have r₂ := eq159 x
       grind)
    | (have r₁ := eq355
       have r₂ := eq159 (N.op x y)
       grind)
    | exact resolve eq355 eq159
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq159 eq355
  have eq620 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq619
  have eq621 : x = (M.op x x) ∨ x = y := by grind
  clear eq620
  have eq624 : x = (M.op x x) := by
    first
    | (have r₁ := eq621
       have r₂ := eq161
       grind)
    | exact resolve eq621 eq161
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq161 eq621
  have eq706 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq624
       grind)
    | exact superpose eq624 eq35
    | exact resolve eq35 eq624
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq35
  have eq714 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq624
       grind)
    | exact superpose eq624 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq624
       grind)
    | exact resolve eq7 eq624
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq624
  have eq715 : x = (N.op x x) := by grind
  clear eq714
  have eq845 : x ≠ (N.op x (N.op y x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq715
       grind)
    | exact superpose eq715 eq10
    | exact resolve eq10 eq715
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1431 : x ≠ (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq845
       have i₂ := eq706 y
       grind)
    | exact superpose eq706 eq845
    | (have j1 := eq706 y
       grind)
    | (have r₁ := eq845
       have r₂ := eq706 (N.op x (N.op y x))
       grind)
    | exact resolve eq845 eq706
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq706
  have eq1434 : x = y := by
    first
    | (have r₁ := eq1431
       have r₂ := eq715
       grind)
    | exact resolve eq1431 eq715
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1431
  have eq1708 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq845
       have i₂ := eq1434
       grind)
    | exact superpose eq1434 eq845
    | exact resolve eq845 eq1434
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq845 eq1434
  have eq1710 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq1708
       have i₂ := eq715
       grind)
    | exact superpose eq715 eq1708
    | exact resolve eq1708 eq715
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1708
  have eq1714 : False := by grind
  exact eq1714

/-- `Equation3268` from `Equation2430`: `x = (y ◇ (z ◇ (w ◇ w))) ◇ x` ⊢ `x ◇ x = y ◇ (x ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3268_StructuralFromFin_Equation2430 : Law3268.StructuralFromFin Law2430 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2430 (fun {G} M hM ↦ ?_)
  have hm := (@Law2430.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3268 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3268.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X1 (M.op X2 (M.op X3 X3))) X0) = X0 := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X5 : G, (M.op (M.op X0 (M.op X1 X1)) X5) = X5 := by
    intro X0 X1 X5
    first
    | (have i₁ := eq6 X5 (M.op x (M.op x (M.op x x))) X0 X1
       have i₂ := eq6 (M.op X0 (M.op X1 X1)) x x x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X3 : G, (M.op (M.op X0 X0) X3) = X3 := by
    intro X0 X3
    first
    | (have i₁ := eq13 (M.op x (M.op x x)) X0 X3
       have i₂ := eq13 x x (M.op X0 X0)
       grind)
    | exact superpose eq13 eq13
    | exact resolve eq13 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq26 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq34 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op X0 X0)
       have i₂ := eq18 X0 x
       grind)
    | exact superpose eq18 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X0)
       have r₂ := eq18 X0 x
       grind)
    | exact resolve eq8 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq35 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq34 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34
  have eq41 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq26 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26
  have eq42 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq45 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq42
  have eq129 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq45
       have i₂ := eq35 x x
       grind)
    | exact superpose eq35 eq45
    | (have j1 := eq35 x x
       grind)
    | (have r₁ := eq45
       have r₂ := eq35 (N.op y (N.op x (M.op x x))) x
       grind)
    | exact resolve eq45 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45
  have eq130 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq129
  have eq149 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq130
       have i₂ := eq35 y x
       grind)
    | exact superpose eq35 eq130
    | (have j1 := eq35 x x
       grind)
    | (have r₁ := eq130
       have r₂ := eq35 y x
       grind)
    | (have r₁ := eq130
       have r₂ := eq35 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq130 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq151 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq149
  have eq171 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq130
       have i₂ := eq151
       grind)
    | exact superpose eq151 eq130
    | exact resolve eq130 eq151
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq130
  have eq184 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq41 x
       have i₂ := eq151
       grind)
    | exact superpose eq151 eq41
    | exact resolve eq41 eq151
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq185 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq151
  have eq187 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq171
       have r₂ := eq184
       grind)
    | exact resolve eq171 eq184
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq171 eq184
  have eq188 : x = (M.op x x) := by
    first
    | (have r₁ := eq187
       have r₂ := eq185
       grind)
    | exact resolve eq187 eq185
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq185 eq187
  have eq201 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq188
       grind)
    | exact superpose eq188 eq35
    | exact resolve eq35 eq188
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq35
  have eq203 : x = (N.op x x) := by
    first
    | (have i₁ := eq41 x
       have i₂ := eq188
       grind)
    | exact superpose eq188 eq41
    | exact resolve eq41 eq188
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq41 eq188
  have eq254 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq203
       grind)
    | exact superpose eq203 eq10
    | exact resolve eq10 eq203
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq259 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq254
       have i₂ := eq203
       grind)
    | exact superpose eq203 eq254
    | exact resolve eq254 eq203
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq254
  have eq720 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq259
       have i₂ := eq201 y
       grind)
    | exact superpose eq201 eq259
    | (have j1 := eq201 y
       grind)
    | (have r₁ := eq259
       have r₂ := eq201 y
       grind)
    | (have r₁ := eq259
       have r₂ := eq201 (N.op y x)
       grind)
    | exact resolve eq259 eq201
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq201
  have eq721 : x = y := by grind
  clear eq720
  have eq786 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq259
       have i₂ := eq721
       grind)
    | exact superpose eq721 eq259
    | exact resolve eq259 eq721
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq259 eq721
  have eq788 : False := by grind
  exact eq788

/-- `Equation3288` from `Equation2430`: `x = (y ◇ (z ◇ (w ◇ w))) ◇ x` ⊢ `x ◇ x = y ◇ (z ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3288_StructuralFromFin_Equation2430 : Law3288.StructuralFromFin Law2430 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2430 (fun {G} M hM ↦ ?_)
  have hm := (@Law2430.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3288 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3288.models_iff G N]
  intro x y z
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X1 (M.op X2 (M.op X3 X3))) X0) = X0 := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op z (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X5 : G, (M.op (M.op X0 (M.op X1 X1)) X5) = X5 := by
    intro X0 X1 X5
    first
    | (have i₁ := eq6 X5 (M.op x (M.op x (M.op x x))) X0 X1
       have i₂ := eq6 (M.op X0 (M.op X1 X1)) x x x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X3 : G, (M.op (M.op X0 X0) X3) = X3 := by
    intro X0 X3
    first
    | (have i₁ := eq13 (M.op x (M.op x x)) X0 X3
       have i₂ := eq13 x x (M.op X0 X0)
       grind)
    | exact superpose eq13 eq13
    | exact resolve eq13 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq26 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq34 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op X0 X0)
       have i₂ := eq18 X0 x
       grind)
    | exact superpose eq18 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X0)
       have r₂ := eq18 X0 x
       grind)
    | exact resolve eq8 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq35 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq34 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34
  have eq41 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq26 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26
  have eq42 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq45 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq42
  have eq129 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ z = (M.op x x) := by
    first
    | (have i₁ := eq45
       have i₂ := eq35 z x
       grind)
    | exact superpose eq35 eq45
    | (have j1 := eq35 x x
       grind)
    | (have r₁ := eq45
       have r₂ := eq35 (N.op y (N.op z (M.op x x))) x
       grind)
    | exact resolve eq45 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq369 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ z = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq129
       have i₂ := eq35 y x
       grind)
    | exact superpose eq35 eq129
    | (have j1 := eq35 x x
       grind)
    | (have r₁ := eq129
       have r₂ := eq35 y x
       grind)
    | (have r₁ := eq129
       have r₂ := eq35 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq129 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq371 : z = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq369
  have eq490 : z ≠ (N.op y (N.op z z)) ∨ x = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq45
       have i₂ := eq371
       grind)
    | exact superpose eq371 eq45
    | exact resolve eq45 eq371
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq501 : ∀ X0 : G, z = (N.op X0 z) ∨ z = X0 ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq371
       grind)
    | exact superpose eq371 eq35
    | (have j0 := eq35 x x
       grind)
    | exact resolve eq35 eq371
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq504 : z = (N.op z z) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq41 x
       have i₂ := eq371
       grind)
    | exact superpose eq371 eq41
    | exact resolve eq41 eq371
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq509 : x ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  have eq510 : y ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq371
  have eq511 : z ≠ (N.op y (N.op z z)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have r₁ := eq490
       have r₂ := eq509
       grind)
    | exact resolve eq490 eq509
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq490 eq509
  have eq1010 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq511
       have i₂ := eq504
       grind)
    | exact superpose eq504 eq511
    | exact resolve eq511 eq504
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq504 eq511
  have eq1014 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq1010
  have eq1733 : z ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq1014
       have i₂ := eq501 y
       grind)
    | exact superpose eq501 eq1014
    | (have j1 := eq501 y
       grind)
    | (have r₁ := eq1014
       have r₂ := eq501 y
       grind)
    | (have r₁ := eq1014
       have r₂ := eq501 (N.op y z)
       grind)
    | exact resolve eq1014 eq501
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq501 eq1014
  have eq1734 : z ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq1733
  have eq1735 : x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq1734
  have eq1738 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1735
       have r₂ := eq510
       grind)
    | exact resolve eq1735 eq510
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq510 eq1735
  have eq2053 : y ≠ (N.op y y) ∨ x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq129
       have i₂ := eq1738
       grind)
    | exact superpose eq1738 eq129
    | exact resolve eq129 eq1738
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq129
  have eq2054 : y ≠ (N.op y (N.op z y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq45
       have i₂ := eq1738
       grind)
    | exact superpose eq1738 eq45
    | exact resolve eq45 eq1738
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45
  have eq2066 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq41 x
       have i₂ := eq1738
       grind)
    | exact superpose eq1738 eq41
    | exact resolve eq41 eq1738
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq41
  have eq2081 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq1738
  have eq2082 : y ≠ (N.op y (N.op z y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq2054
       have r₂ := eq2081
       grind)
    | exact resolve eq2054 eq2081
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2054
  have eq2083 : x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq2053
       have r₂ := eq2066
       grind)
    | exact resolve eq2053 eq2066
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2053
  have eq2084 : x = (M.op x x) ∨ y = z := by
    first
    | (have r₁ := eq2083
       have r₂ := eq2081
       grind)
    | exact resolve eq2083 eq2081
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2083
  have eq2254 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ y = z := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq2084
       grind)
    | exact superpose eq2084 eq35
    | exact resolve eq35 eq2084
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2272 : x ≠ x ∨ x = (N.op x x) ∨ y = z := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq2084
       grind)
    | exact superpose eq2084 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq2084
       grind)
    | exact resolve eq7 eq2084
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2084
  have eq2273 : x = (N.op x x) ∨ y = z := by grind
  clear eq2272
  have eq3225 : x ≠ (N.op y (N.op z x)) ∨ y = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq2273
       grind)
    | exact superpose eq2273 eq10
    | exact resolve eq10 eq2273
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq5077 : x ≠ (N.op y x) ∨ y = z ∨ x = z ∨ y = z := by
    first
    | (have i₁ := eq3225
       have i₂ := eq2254 z
       grind)
    | exact superpose eq2254 eq3225
    | (have j1 := eq2254 z
       grind)
    | (have r₁ := eq3225
       have r₂ := eq2254 (N.op y (N.op z x))
       grind)
    | exact resolve eq3225 eq2254
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq5078 : x ≠ (N.op y x) ∨ y = z ∨ x = z := by grind
  clear eq5077
  have eq5256 : x ≠ x ∨ y = z ∨ x = z ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq5078
       have i₂ := eq2254 y
       grind)
    | exact superpose eq2254 eq5078
    | (have j1 := eq2254 z
       grind)
    | (have r₁ := eq5078
       have r₂ := eq2254 y
       grind)
    | (have r₁ := eq5078
       have r₂ := eq2254 (N.op y x)
       grind)
    | exact resolve eq5078 eq2254
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5078
  have eq5258 : x ≠ x ∨ y = z ∨ x = z ∨ x = y := by grind
  clear eq5256
  have eq5259 : y = z ∨ x = z ∨ x = y := by grind
  clear eq5258
  have eq6307 : y ≠ (N.op y (N.op y y)) ∨ x = (M.op x x) ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq2082
       have i₂ := eq5259
       grind)
    | exact superpose eq5259 eq2082
    | exact resolve eq2082 eq5259
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2082 eq5259
  have eq6310 : y ≠ (N.op y (N.op y y)) ∨ x = (M.op x x) ∨ x = z := by
    first
    | (have r₁ := eq6307
       have r₂ := eq2081
       grind)
    | exact resolve eq6307 eq2081
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq6307
  have eq9597 : y ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq6310
       have i₂ := eq2066
       grind)
    | exact superpose eq2066 eq6310
    | exact resolve eq6310 eq2066
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq6310
  have eq9601 : y ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = z := by grind
  clear eq9597
  have eq9602 : x = (M.op x x) ∨ x = z := by
    first
    | (have r₁ := eq9601
       have r₂ := eq2066
       grind)
    | exact resolve eq9601 eq2066
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2066 eq9601
  have eq10404 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq9602
       grind)
    | exact superpose eq9602 eq35
    | exact resolve eq35 eq9602
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq35
  have eq10443 : x ≠ x ∨ x = (N.op x x) ∨ x = z := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq9602
       grind)
    | exact superpose eq9602 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq9602
       grind)
    | exact resolve eq7 eq9602
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9602
  have eq10444 : x = (N.op x x) ∨ x = z := by grind
  clear eq10443
  have eq12683 : x ≠ (N.op y (N.op z x)) ∨ x = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq10444
       grind)
    | exact superpose eq10444 eq10
    | exact resolve eq10 eq10444
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq17778 : x ≠ (N.op y x) ∨ x = z ∨ x = z ∨ x = z := by
    first
    | (have i₁ := eq12683
       have i₂ := eq10404 z
       grind)
    | exact superpose eq10404 eq12683
    | (have j1 := eq10404 z
       grind)
    | (have r₁ := eq12683
       have r₂ := eq10404 (N.op y (N.op z x))
       grind)
    | exact resolve eq12683 eq10404
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12683
  have eq17781 : x ≠ (N.op y x) ∨ x = z := by grind
  clear eq17778
  have eq19137 : x ≠ x ∨ x = z ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq17781
       have i₂ := eq10404 y
       grind)
    | exact superpose eq10404 eq17781
    | (have j1 := eq10404 z
       grind)
    | (have r₁ := eq17781
       have r₂ := eq10404 y
       grind)
    | (have r₁ := eq17781
       have r₂ := eq10404 (N.op y x)
       grind)
    | exact resolve eq17781 eq10404
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq10404
  have eq19141 : x ≠ x ∨ x = z ∨ x = y := by grind
  clear eq19137
  have eq19142 : x = z ∨ x = y := by grind
  clear eq19141
  have eq20595 : x ≠ (N.op y (N.op x x)) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq3225
       have i₂ := eq19142
       grind)
    | exact superpose eq19142 eq3225
    | exact resolve eq3225 eq19142
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3225
  have eq20596 : x ≠ (N.op y (N.op x x)) ∨ x = y := by grind
  clear eq20595
  have eq22125 : x ≠ (N.op y x) ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq20596
       have i₂ := eq2273
       grind)
    | exact superpose eq2273 eq20596
    | exact resolve eq20596 eq2273
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2273 eq20596
  have eq22130 : y = z ∨ x = y := by
    first
    | (have j1 := eq2254 y
       grind)
    | (have r₁ := eq22125
       have r₂ := eq2254 y
       grind)
    | (have r₁ := eq22125
       have r₂ := eq2254 (N.op y x)
       grind)
    | exact resolve eq22125 eq2254
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2254 eq22125
  have eq23761 : x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq19142
       have i₂ := eq22130
       grind)
    | exact superpose eq22130 eq19142
    | exact resolve eq19142 eq22130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19142 eq22130
  have eq23762 : x = y := by grind
  clear eq23761
  have eq25496 : x ≠ x ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq2081
       have i₂ := eq23762
       grind)
    | exact superpose eq23762 eq2081
    | (have r₁ := eq2081
       have r₂ := eq23762
       grind)
    | exact resolve eq2081 eq23762
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2081
  have eq25502 : x ≠ (N.op x x) ∨ x = z := by
    first
    | (have i₁ := eq17781
       have i₂ := eq23762
       grind)
    | exact superpose eq23762 eq17781
    | exact resolve eq17781 eq23762
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq17781
  have eq25504 : x = (M.op x x) := by grind
  clear eq25496
  have eq25507 : x = z := by
    first
    | (have r₁ := eq25502
       have r₂ := eq10444
       grind)
    | exact resolve eq25502 eq10444
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq10444 eq25502
  have eq27977 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq25504
       grind)
    | exact superpose eq25504 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq25504
       grind)
    | exact resolve eq7 eq25504
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq25504
  have eq27978 : x = (N.op x x) := by grind
  clear eq27977
  have eq32662 : x ≠ (N.op y (N.op z x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq27978
       grind)
    | exact superpose eq27978 eq10
    | exact resolve eq10 eq27978
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq32671 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq32662
       have i₂ := eq25507
       grind)
    | exact superpose eq25507 eq32662
    | exact resolve eq32662 eq25507
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq25507 eq32662
  have eq32673 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq32671
       have i₂ := eq27978
       grind)
    | exact superpose eq27978 eq32671
    | exact resolve eq32671 eq27978
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32671
  have eq32675 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq32673
       have i₂ := eq23762
       grind)
    | exact superpose eq23762 eq32673
    | exact resolve eq32673 eq23762
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq23762 eq32673
  have eq32677 : False := by grind
  exact eq32677

/-- `Equation4272` from `Equation2430`: `x = (y ◇ (z ◇ (w ◇ w))) ◇ x` ⊢ `x ◇ (x ◇ x) = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation4272_StructuralFromFin_Equation2430 : Law4272.StructuralFromFin Law2430 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2430 (fun {G} M hM ↦ ?_)
  have hm := (@Law2430.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law4272 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law4272.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X1 (M.op X2 (M.op X3 X3))) X0) = X0 := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x (N.op x x)) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X5 : G, (M.op (M.op X0 (M.op X1 X1)) X5) = X5 := by
    intro X0 X1 X5
    first
    | (have i₁ := eq6 X5 (M.op x (M.op x (M.op x x))) X0 X1
       have i₂ := eq6 (M.op X0 (M.op X1 X1)) x x x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X3 : G, (M.op (M.op X0 X0) X3) = X3 := by
    intro X0 X3
    first
    | (have i₁ := eq13 (M.op x (M.op x x)) X0 X3
       have i₂ := eq13 x x (M.op X0 X0)
       grind)
    | exact superpose eq13 eq13
    | exact resolve eq13 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq26 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq34 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op X0 X0)
       have i₂ := eq18 X0 x
       grind)
    | exact superpose eq18 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X0)
       have r₂ := eq18 X0 x
       grind)
    | exact resolve eq8 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq35 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq34 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34
  have eq41 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq26 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26
  have eq42 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq44 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq42
  have eq127 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq44
       have i₂ := eq35 y x
       grind)
    | exact superpose eq35 eq44
    | (have j1 := eq35 x x
       grind)
    | exact resolve eq44 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq130 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq35 y x
       grind)
    | (have r₁ := eq127
       have r₂ := eq35 x x
       grind)
    | (have r₁ := eq127
       have r₂ := eq35 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq127 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq127
  have eq147 : (N.op x y) ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq44
       have i₂ := eq130
       grind)
    | exact superpose eq130 eq44
    | exact resolve eq44 eq130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq44
  have eq157 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq130
       grind)
    | exact superpose eq130 eq35
    | (have j0 := eq35 x x
       grind)
    | exact resolve eq35 eq130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq158 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq41 x
       have i₂ := eq130
       grind)
    | exact superpose eq130 eq41
    | exact resolve eq41 eq130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq41
  have eq159 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq130
  have eq160 : (N.op x y) ≠ (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq147
       have r₂ := eq159
       grind)
    | exact resolve eq147 eq159
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq147
  have eq446 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq160
       have i₂ := eq158
       grind)
    | exact superpose eq158 eq160
    | exact resolve eq160 eq158
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq158 eq160
  have eq449 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq446
  have eq685 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq449
       have i₂ := eq157 x
       grind)
    | exact superpose eq157 eq449
    | (have j1 := eq157 x
       grind)
    | (have r₁ := eq449
       have r₂ := eq157 x
       grind)
    | (have r₁ := eq449
       have r₂ := eq157 (N.op x y)
       grind)
    | exact resolve eq449 eq157
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq157 eq449
  have eq686 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq685
  have eq687 : x = (M.op x x) ∨ x = y := by grind
  clear eq686
  have eq690 : x = (M.op x x) := by
    first
    | (have r₁ := eq687
       have r₂ := eq159
       grind)
    | exact resolve eq687 eq159
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq159 eq687
  have eq755 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq690
       grind)
    | exact superpose eq690 eq35
    | exact resolve eq35 eq690
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq35
  have eq764 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq690
       grind)
    | exact superpose eq690 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq690
       grind)
    | exact resolve eq7 eq690
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq690
  have eq765 : x = (N.op x x) := by grind
  clear eq764
  have eq921 : (N.op x x) ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq765
       grind)
    | exact superpose eq765 eq10
    | exact resolve eq10 eq765
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq926 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq921
       have i₂ := eq765
       grind)
    | exact superpose eq765 eq921
    | exact resolve eq921 eq765
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq765 eq921
  have eq1978 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq926
       have i₂ := eq755 y
       grind)
    | exact superpose eq755 eq926
    | (have j1 := eq755 y
       grind)
    | (have r₁ := eq926
       have r₂ := eq755 y
       grind)
    | (have r₁ := eq926
       have r₂ := eq755 (N.op y x)
       grind)
    | exact resolve eq926 eq755
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq755 eq926
  have eq1979 : x = y := by grind
  clear eq1978
  have eq2320 : (N.op x (N.op x x)) ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1979
       grind)
    | exact superpose eq1979 eq10
    | exact resolve eq10 eq1979
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1979
  have eq2337 : False := by grind
  exact eq2337
