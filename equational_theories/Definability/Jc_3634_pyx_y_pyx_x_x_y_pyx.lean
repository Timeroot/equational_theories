import equational_theories.Definability.AutBox2
import equational_theories.Definability.Kc_03

/-!
# Structurality from `Equation3634` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3634` is `x ◇ y = z ◇ ((w ◇ x) ◇ y)`, and the companion is
`x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`.  Its automorphism half is `GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3634`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches none of these on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation622` from `Equation3634`: `x ◇ y = z ◇ ((w ◇ x) ◇ y)` ⊢ `x = x ◇ (x ◇ ((y ◇ y) ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation622_StructuralFromFin_Equation3634 : Law622.StructuralFromFin Law3634 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3634 (fun {G} M hM ↦ ?_)
  have hm := (@Law3634.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law622 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law622.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X3 X0) X1)) := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op x (N.op x (N.op (N.op y y) x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 X4 : G, (M.op X2 (M.op (M.op X3 X0) X1)) = (M.op X4 (M.op X0 X1)) := by
    intro X0 X1 X2 X3 X4
    first
    | (have i₁ := eq6 X2 (M.op (M.op X3 X0) X1) X4 x
       have i₂ := eq6 X0 X1 (M.op x X2) X3
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X4 : G, (M.op X0 X1) = (M.op X4 (M.op X0 X1)) := by
    intro X0 X1 X4
    first
    | (have i₁ := eq12 X0 X1 x x X4
       have i₂ := eq6 X0 X1 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq19 : ∀ X0 X1 X2 : G, (M.op X1 X2) = (M.op (M.op X0 X1) X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X1 X2 x X0
       have i₂ := eq14 (M.op X0 X1) X2 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq21 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ X2 ∨ (M.op X0 X1) = (N.op (M.op X0 X1) X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op X0 X1) X2
       have i₂ := eq14 X0 X1 X2
       grind)
    | exact superpose eq14 eq7
    | (have j0 := eq7 (M.op X0 X1) X2
       grind)
    | (have r₁ := eq7 (M.op X0 X1) (M.op X0 X1)
       have r₂ := eq14 X0 X1 (M.op X0 X1)
       grind)
    | exact resolve eq7 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq31 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X2 X0) ∨ (N.op X1 (M.op X2 X0)) = X1 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 X1 (M.op X2 X0)
       have i₂ := eq19 X2 X0 X1
       grind)
    | exact superpose eq19 eq7
    | (have j0 := eq7 X1 (M.op X2 X0)
       grind)
    | (have r₁ := eq7 X2 (M.op X2 X2)
       have r₂ := eq19 X2 X2 X2
       grind)
    | exact resolve eq7 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq36 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X0 X1) = X2 ∨ (N.op (M.op X0 X1) X2) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 (M.op X0 X1) x
       have i₂ := eq14 X0 X1 x
       grind)
    | exact superpose eq14 eq8
    | (have j0 := eq8 (M.op X0 X1) X2
       grind)
    | (have r₁ := eq8 (M.op X0 X1) x
       have r₂ := eq14 X0 X1 x
       grind)
    | exact resolve eq8 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq38 : ∀ X0 X1 X2 : G, (N.op (M.op X0 X1) X2) = X2 ∨ (M.op X0 X1) = X2 := by
    intro X0 X1 X2
    first
    | (have j0 := eq36 X0 X1 X2
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36
  have eq60 : x ≠ (N.op x (M.op (N.op (N.op y y) x) x)) ∨ x = (M.op (N.op (N.op y y) x) x) ∨ (N.op (N.op y y) x) = (M.op (N.op (N.op y y) x) x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x (N.op (N.op y y) x)
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x (N.op (N.op y y) x)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq62 : x ≠ (N.op x (N.op x (N.op (M.op y y) x))) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq63 : x ≠ (N.op x (N.op x (N.op (M.op y y) x))) ∨ y = (M.op y y) := by grind
  clear eq62
  have eq72 : x ≠ (N.op x (N.op x x)) ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq63
       have i₂ := eq38 y y x
       grind)
    | exact superpose eq38 eq63
    | (have j1 := eq38 y y y
       grind)
    | exact resolve eq63 eq38
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq74 : x ≠ (N.op x (M.op (N.op (M.op y y) x) x)) ∨ y = (M.op y y) ∨ x = (M.op (N.op (M.op y y) x) x) ∨ (N.op (M.op y y) x) = (M.op (N.op (M.op y y) x) x) := by
    first
    | (have i₁ := eq63
       have i₂ := eq9 x (N.op (M.op y y) x)
       grind)
    | exact superpose eq9 eq63
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq63 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq80 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op (M.op X0 X1) (M.op X0 X1)) := by
    intro X0 X1
    first
    | (have j0 := eq21 X0 X1 (M.op X0 X1)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq114 : ∀ X0 : G, (N.op X0 (M.op X0 X0)) = X0 := by
    intro X0
    first
    | (have j0 := eq31 X0 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq31
  have eq143 : x ≠ (N.op x (M.op x x)) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq74
       have i₂ := eq38 y y x
       grind)
    | exact superpose eq38 eq74
    | (have j1 := eq38 y y y
       grind)
    | exact resolve eq74 eq38
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq74
  have eq146 : x ≠ (N.op x (M.op x x)) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op y y) := by grind
  clear eq143
  have eq147 : y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    first
    | (have r₁ := eq146
       have r₂ := eq114 x
       grind)
    | exact resolve eq146 eq114
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq146
  have eq151 : ∀ X0 : G, y = (M.op X0 y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq147
       grind)
    | exact superpose eq147 eq14
    | exact resolve eq14 eq147
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14 eq147
  have eq201 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq80 y y
       have i₂ := eq151 X0
       grind)
    | exact superpose eq151 eq80
    | (have j1 := eq151 X0
       grind)
    | exact resolve eq80 eq151
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq151
  have eq206 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq201 X0
       have j1 := eq7 x x
       grind)
    | (have r₁ := eq201 y
       have r₂ := eq7 y y
       grind)
    | (have r₁ := eq201 X0
       have r₂ := eq7 x x
       grind)
    | exact resolve eq201 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq201
  have eq229 : ∀ X0 : G, x ≠ (N.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq72
       have i₂ := eq206 X0
       grind)
    | exact superpose eq206 eq72
    | (have j1 := eq206 y
       grind)
    | exact resolve eq72 eq206
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq72
  have eq242 : y = (N.op y y) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq80 x y
       have i₂ := eq206 x
       grind)
    | exact superpose eq206 eq80
    | exact resolve eq80 eq206
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq244 : ∀ X0 X1 : G, (M.op X1 y) = (M.op X0 y) ∨ x = (N.op x x) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 y X0 x
       have i₂ := eq206 (M.op x X1)
       grind)
    | exact superpose eq206 eq6
    | exact resolve eq6 eq206
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq251 : ∀ X0 : G, y = (M.op y y) ∨ y = (M.op X0 y) ∨ x = (M.op y y) := by
    intro X0
    first
    | (have j0 := eq229 y
       have j1 := eq206 y
       grind)
    | (have r₁ := eq229 X0
       have r₂ := eq206 X0
       grind)
    | exact resolve eq229 eq206
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq229
  have eq255 : x ≠ (N.op x (M.op (N.op y x) x)) ∨ x = (M.op (N.op y x) x) ∨ (N.op y x) = (M.op (N.op y x) x) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq60
       have i₂ := eq242
       grind)
    | exact superpose eq242 eq60
    | exact resolve eq60 eq242
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq60 eq242
  have eq503 : ∀ X0 X1 : G, y ≠ (M.op X0 y) ∨ (M.op X0 y) = X1 ∨ (N.op y X1) = X1 ∨ x = (N.op x x) := by
    intro X0 X1
    first
    | (have i₁ := eq8 y X1
       have i₂ := eq244 X0 X1
       grind)
    | (have i₁ := eq8 y X0
       have i₂ := eq244 X0 X1
       grind)
    | exact superpose eq244 eq8
    | (have j0 := eq8 y X0
       grind)
    | exact resolve eq8 eq244
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq244
  have eq538 : ∀ X0 X1 : G, (N.op y X1) = X1 ∨ (M.op X0 y) = X1 ∨ x = (N.op x x) := by
    intro X0 X1
    first
    | (have j0 := eq503 X0 X1
       grind)
    | (have r₁ := eq503 X0 X1
       have r₂ := eq206 X0
       grind)
    | exact resolve eq503 eq206
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq206 eq503
  have eq2096 : y ≠ y ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have j0 := eq251 y
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq251
  have eq2098 : y = (M.op y y) ∨ x = (M.op y y) := by grind
  clear eq2096
  have eq2150 : ∀ X0 : G, (N.op y X0) = X0 ∨ y = X0 ∨ x = (M.op y y) := by
    intro X0
    first
    | (have i₁ := eq38 y y x
       have i₂ := eq2098
       grind)
    | exact superpose eq2098 eq38
    | (have j0 := eq38 y y x
       grind)
    | exact resolve eq38 eq2098
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq38
  have eq2151 : y = (N.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq80 y y
       have i₂ := eq2098
       grind)
    | exact superpose eq2098 eq80
    | exact resolve eq80 eq2098
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2152 : x ≠ y ∨ x = (M.op y y) := by grind
  clear eq2098
  have eq2374 : x ≠ (N.op x (N.op x (N.op y x))) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq2151
       grind)
    | exact superpose eq2151 eq10
    | exact resolve eq10 eq2151
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2151
  have eq6125 : ∀ X0 : G, x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (N.op x x) ∨ x = (M.op X0 y) ∨ x = (N.op x x) := by
    intro X0
    first
    | (have i₁ := eq255
       have i₂ := eq538 X0 x
       grind)
    | exact superpose eq538 eq255
    | (have j1 := eq538 X0 x
       grind)
    | exact resolve eq255 eq538
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq255 eq538
  have eq6150 : ∀ X0 : G, x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (N.op x x) ∨ x = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq6125 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq6125
  have eq6156 : ∀ X0 : G, x = (M.op x x) ∨ x = (N.op x x) ∨ x = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq6150 X0
       grind)
    | (have r₁ := eq6150 X0
       have r₂ := eq114 x
       grind)
    | exact resolve eq6150 eq114
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq114 eq6150
  have eq6162 : ∀ X0 : G, x = (N.op x x) ∨ x = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq6156 X0
       have j1 := eq7 x x
       grind)
    | (have r₁ := eq6156 X0
       have r₂ := eq7 x x
       grind)
    | (have r₁ := eq6156 x
       have r₂ := eq7 y x
       grind)
    | exact resolve eq6156 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq6156
  have eq6285 : x = (N.op x x) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq80 x y
       have i₂ := eq6162 x
       grind)
    | exact superpose eq6162 eq80
    | exact resolve eq80 eq6162
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq80 eq6162
  have eq6299 : x = (N.op x x) := by grind
  clear eq6285
  have eq8903 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op y y) ∨ x = y ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq2374
       have i₂ := eq2150 x
       grind)
    | exact superpose eq2150 eq2374
    | (have j1 := eq2150 x
       grind)
    | exact resolve eq2374 eq2150
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2150 eq2374
  have eq8922 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op y y) ∨ x = y := by grind
  clear eq8903
  have eq8925 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op y y) := by
    first
    | (have r₁ := eq8922
       have r₂ := eq2152
       grind)
    | exact resolve eq8922 eq2152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2152 eq8922
  have eq8927 : x ≠ (N.op x x) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq8925
       have i₂ := eq6299
       grind)
    | exact superpose eq6299 eq8925
    | exact resolve eq8925 eq6299
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8925
  have eq8928 : x = (M.op y y) := by
    first
    | (have r₁ := eq8927
       have r₂ := eq6299
       grind)
    | exact resolve eq8927 eq6299
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8927
  have eq9075 : x ≠ (N.op x (N.op x (N.op x x))) ∨ x = y := by
    first
    | (have i₁ := eq63
       have i₂ := eq8928
       grind)
    | exact superpose eq8928 eq63
    | exact resolve eq63 eq8928
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq63 eq8928
  have eq9141 : x ≠ (N.op x (N.op x x)) ∨ x = y := by
    first
    | (have i₁ := eq9075
       have i₂ := eq6299
       grind)
    | exact superpose eq6299 eq9075
    | exact resolve eq9075 eq6299
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9075
  have eq9148 : x ≠ (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq9141
       have i₂ := eq6299
       grind)
    | exact superpose eq6299 eq9141
    | exact resolve eq9141 eq6299
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9141
  have eq9149 : x = y := by
    first
    | (have r₁ := eq9148
       have r₂ := eq6299
       grind)
    | exact resolve eq9148 eq6299
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9148
  have eq9302 : x ≠ (N.op x (N.op x (N.op (N.op x x) x))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9149
       grind)
    | exact superpose eq9149 eq10
    | exact resolve eq10 eq9149
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9149
  have eq9465 : x ≠ (N.op x (N.op x (N.op x x))) := by
    first
    | (have i₁ := eq9302
       have i₂ := eq6299
       grind)
    | exact superpose eq6299 eq9302
    | exact resolve eq9302 eq6299
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9302
  have eq9467 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq9465
       have i₂ := eq6299
       grind)
    | exact superpose eq6299 eq9465
    | exact resolve eq9465 eq6299
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9465
  have eq9469 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq9467
       have i₂ := eq6299
       grind)
    | exact superpose eq6299 eq9467
    | exact resolve eq9467 eq6299
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9467
  have eq9471 : False := by grind
  exact eq9471

/-- `Equation1515` from `Equation3634`: `x ◇ y = z ◇ ((w ◇ x) ◇ y)` ⊢ `x = (y ◇ y) ◇ (x ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation1515_StructuralFromFin_Equation3634 : Law1515.StructuralFromFin Law3634 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3634 (fun {G} M hM ↦ ?_)
  have hm := (@Law3634.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law1515 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law1515.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X3 X0) X1)) := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op (N.op y y) (N.op x (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 X4 : G, (M.op X2 (M.op (M.op X3 X0) X1)) = (M.op X4 (M.op X0 X1)) := by
    intro X0 X1 X2 X3 X4
    first
    | (have i₁ := eq6 X2 (M.op (M.op X3 X0) X1) X4 x
       have i₂ := eq6 X0 X1 (M.op x X2) X3
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X4 : G, (M.op X0 X1) = (M.op X4 (M.op X0 X1)) := by
    intro X0 X1 X4
    first
    | (have i₁ := eq12 X0 X1 x x X4
       have i₂ := eq6 X0 X1 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq19 : ∀ X0 X1 X2 : G, (M.op X1 X2) = (M.op (M.op X0 X1) X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X1 X2 x X0
       have i₂ := eq14 (M.op X0 X1) X2 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq21 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ X2 ∨ (M.op X0 X1) = (N.op (M.op X0 X1) X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op X0 X1) X2
       have i₂ := eq14 X0 X1 X2
       grind)
    | exact superpose eq14 eq7
    | (have j0 := eq7 (M.op X0 X1) X2
       grind)
    | (have r₁ := eq7 (M.op X0 X1) (M.op X0 X1)
       have r₂ := eq14 X0 X1 (M.op X0 X1)
       grind)
    | exact resolve eq7 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq31 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X2 X0) ∨ (N.op X1 (M.op X2 X0)) = X1 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 X1 (M.op X2 X0)
       have i₂ := eq19 X2 X0 X1
       grind)
    | exact superpose eq19 eq7
    | (have j0 := eq7 X1 (M.op X2 X0)
       grind)
    | (have r₁ := eq7 X2 (M.op X2 X2)
       have r₂ := eq19 X2 X2 X2
       grind)
    | exact resolve eq7 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq36 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X0 X1) = X2 ∨ (N.op (M.op X0 X1) X2) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 (M.op X0 X1) x
       have i₂ := eq14 X0 X1 x
       grind)
    | exact superpose eq14 eq8
    | (have j0 := eq8 (M.op X0 X1) X2
       grind)
    | (have r₁ := eq8 (M.op X0 X1) x
       have r₂ := eq14 X0 X1 x
       grind)
    | exact resolve eq8 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq38 : ∀ X0 X1 X2 : G, (N.op (M.op X0 X1) X2) = X2 ∨ (M.op X0 X1) = X2 := by
    intro X0 X1 X2
    first
    | (have j0 := eq36 X0 X1 X2
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36
  have eq60 : x ≠ (N.op (N.op y y) (N.op x (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq62 : x ≠ (N.op (M.op y y) (N.op x (N.op x x))) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq63 : x ≠ (N.op (M.op y y) (N.op x (N.op x x))) ∨ y = (M.op y y) := by grind
  clear eq62
  have eq64 : x ≠ (N.op (N.op y y) (N.op x (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq60
  have eq73 : x ≠ (N.op (M.op y y) (N.op x (M.op x x))) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq63
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq63
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq63 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq75 : x ≠ (N.op x (N.op x x)) ∨ y = (M.op y y) ∨ (N.op x (N.op x x)) = (M.op y y) := by
    first
    | (have i₁ := eq63
       have i₂ := eq38 y y (N.op x (N.op x x))
       grind)
    | exact superpose eq38 eq63
    | (have j1 := eq38 y y y
       grind)
    | exact resolve eq63 eq38
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq77 : x ≠ (N.op (M.op y y) (N.op x (M.op x x))) ∨ y = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq73
  have eq82 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op (M.op X0 X1) (M.op X0 X1)) := by
    intro X0 X1
    first
    | (have j0 := eq21 X0 X1 (M.op X0 X1)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq113 : ∀ X0 : G, (N.op X0 (M.op X0 X0)) = X0 := by
    intro X0
    first
    | (have j0 := eq31 X0 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq31
  have eq135 : x ≠ (N.op (N.op y y) x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq64
       have i₂ := eq113 x
       grind)
    | exact superpose eq113 eq64
    | exact resolve eq64 eq113
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64
  have eq157 : x ≠ (N.op x (M.op x x)) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ (N.op x (M.op x x)) = (M.op y y) := by
    first
    | (have i₁ := eq77
       have i₂ := eq38 y y (N.op x (M.op x x))
       grind)
    | exact superpose eq38 eq77
    | (have j1 := eq38 y y y
       grind)
    | exact resolve eq77 eq38
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq77
  have eq159 : y = (M.op y y) ∨ x = (M.op x x) ∨ (N.op x (M.op x x)) = (M.op y y) := by
    first
    | (have r₁ := eq157
       have r₂ := eq113 x
       grind)
    | exact resolve eq157 eq113
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq157
  have eq160 : y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq159
       have i₂ := eq113 x
       grind)
    | exact superpose eq113 eq159
    | exact resolve eq159 eq113
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq159
  have eq164 : ∀ X0 : G, y = (M.op X0 y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq160
       grind)
    | exact superpose eq160 eq14
    | exact resolve eq14 eq160
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq169 : ∀ X0 : G, (N.op y X0) = X0 ∨ y = X0 ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq38 y y x
       have i₂ := eq160
       grind)
    | exact superpose eq160 eq38
    | (have j0 := eq38 y y x
       grind)
    | exact resolve eq38 eq160
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq172 : x ≠ y ∨ x = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq160
  have eq215 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq82 y y
       have i₂ := eq164 X0
       grind)
    | exact superpose eq164 eq82
    | (have j1 := eq164 X0
       grind)
    | exact resolve eq82 eq164
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq164
  have eq221 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq215 X0
       have j1 := eq7 x x
       grind)
    | (have r₁ := eq215 y
       have r₂ := eq7 y y
       grind)
    | (have r₁ := eq215 X0
       have r₂ := eq7 x x
       grind)
    | exact resolve eq215 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq215
  have eq250 : y = (N.op y y) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq82 x y
       have i₂ := eq221 x
       grind)
    | exact superpose eq221 eq82
    | exact resolve eq82 eq221
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq221
  have eq285 : x ≠ (N.op y x) ∨ x = (M.op x x) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq135
       have i₂ := eq250
       grind)
    | exact superpose eq250 eq135
    | exact resolve eq135 eq250
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq135 eq250
  have eq289 : x ≠ (N.op y x) ∨ x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq285
       have r₂ := eq7 x x
       grind)
    | exact resolve eq285 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq285
  have eq341 : x ≠ x ∨ x = (N.op x x) ∨ x = y ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq289
       have i₂ := eq169 x
       grind)
    | exact superpose eq169 eq289
    | (have j1 := eq169 x
       grind)
    | (have r₁ := eq289
       have r₂ := eq169 x
       grind)
    | exact resolve eq289 eq169
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq169 eq289
  have eq346 : x = (N.op x x) ∨ x = y ∨ x = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq341
  have eq349 : x = (N.op x x) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq346
       have r₂ := eq172
       grind)
    | exact resolve eq346 eq172
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq172 eq346
  have eq350 : x = (N.op x x) ∨ x = (M.op y y) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq349
       have r₂ := eq7 x x
       grind)
    | exact resolve eq349 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq349
  have eq433 : x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq9 x x
       have i₂ := eq350
       grind)
    | exact superpose eq350 eq9
    | (have j0 := eq9 x x
       grind)
    | exact resolve eq9 eq350
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq350
  have eq434 : x = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq433
  have eq587 : x = (N.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq82 y y
       have i₂ := eq434
       grind)
    | exact superpose eq434 eq82
    | exact resolve eq82 eq434
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq434
  have eq595 : x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq587
       have r₂ := eq7 x x
       grind)
    | exact resolve eq587 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq587
  have eq667 : x ≠ (N.op (N.op y y) (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq595
       grind)
    | exact superpose eq595 eq10
    | exact resolve eq10 eq595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq672 : x ≠ (N.op x x) ∨ y = (M.op y y) ∨ (N.op x x) = (M.op y y) := by
    first
    | (have i₁ := eq75
       have i₂ := eq595
       grind)
    | exact superpose eq595 eq75
    | exact resolve eq75 eq595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq75
  have eq678 : y = (M.op y y) ∨ (N.op x x) = (M.op y y) := by
    first
    | (have r₁ := eq672
       have r₂ := eq595
       grind)
    | exact resolve eq672 eq595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq672
  have eq681 : x ≠ (N.op (N.op y y) x) := by
    first
    | (have i₁ := eq667
       have i₂ := eq595
       grind)
    | exact superpose eq595 eq667
    | exact resolve eq667 eq595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq667
  have eq682 : y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq678
       have i₂ := eq595
       grind)
    | exact superpose eq595 eq678
    | exact resolve eq678 eq595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq678
  have eq1288 : ∀ X0 : G, y = (M.op X0 y) ∨ x = (M.op y y) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq682
       grind)
    | exact superpose eq682 eq14
    | exact resolve eq14 eq682
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14 eq682
  have eq1346 : ∀ X0 : G, x ≠ (N.op x (N.op x (N.op x x))) ∨ x = y ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq63
       have i₂ := eq1288 X0
       grind)
    | exact superpose eq1288 eq63
    | (have j1 := eq1288 X0
       grind)
    | exact resolve eq63 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq63
  have eq1357 : ∀ X0 : G, y = (N.op y x) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq113 y
       have i₂ := eq1288 X0
       grind)
    | exact superpose eq1288 eq113
    | (have j1 := eq1288 X0
       grind)
    | exact resolve eq113 eq1288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq113 eq1288
  have eq1386 : ∀ X0 : G, x ≠ (N.op x (N.op x x)) ∨ x = y ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq1346 X0
       have i₂ := eq595
       grind)
    | exact superpose eq595 eq1346
    | (have j0 := eq1346 X0
       grind)
    | exact resolve eq1346 eq595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1346
  have eq1388 : ∀ X0 : G, x ≠ (N.op x x) ∨ x = y ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq1386 X0
       have i₂ := eq595
       grind)
    | exact superpose eq595 eq1386
    | (have j0 := eq1386 X0
       grind)
    | exact resolve eq1386 eq595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1386
  have eq1389 : ∀ X0 : G, y = (M.op X0 y) ∨ x = y := by
    intro X0
    first
    | (have j0 := eq1388 X0
       grind)
    | (have r₁ := eq1388 X0
       have r₂ := eq595
       grind)
    | exact resolve eq1388 eq595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1388
  have eq1580 : ∀ X1 : G, (N.op y X1) = X1 ∨ y = X1 ∨ x = y := by
    intro X1
    first
    | (have i₁ := eq38 x y x
       have i₂ := eq1389 x
       grind)
    | exact superpose eq1389 eq38
    | exact resolve eq38 eq1389
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq38 eq1389
  have eq1730 : y = (N.op y y) ∨ y = (N.op y x) := by
    first
    | (have i₁ := eq82 x y
       have i₂ := eq1357 x
       grind)
    | exact superpose eq1357 eq82
    | exact resolve eq82 eq1357
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq82 eq1357
  have eq2069 : x ≠ (N.op y x) ∨ y = (N.op y x) := by
    first
    | (have i₁ := eq681
       have i₂ := eq1730
       grind)
    | exact superpose eq1730 eq681
    | exact resolve eq681 eq1730
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1730
  have eq2446 : x ≠ x ∨ x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq2069
       have i₂ := eq1580 x
       grind)
    | exact superpose eq1580 eq2069
    | (have j1 := eq1580 x
       grind)
    | (have r₁ := eq2069
       have r₂ := eq1580 x
       grind)
    | exact resolve eq2069 eq1580
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1580 eq2069
  have eq2451 : x ≠ x ∨ x = y := by grind
  clear eq2446
  have eq2452 : x = y := by grind
  clear eq2451
  have eq2585 : x ≠ (N.op (N.op x x) x) := by
    first
    | (have i₁ := eq681
       have i₂ := eq2452
       grind)
    | exact superpose eq2452 eq681
    | exact resolve eq681 eq2452
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq681 eq2452
  have eq2619 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq2585
       have i₂ := eq595
       grind)
    | exact superpose eq595 eq2585
    | exact resolve eq2585 eq595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2585
  have eq2624 : False := by grind
  exact eq2624

/-- `Equation1691` from `Equation3634`: `x ◇ y = z ◇ ((w ◇ x) ◇ y)` ⊢ `x = (y ◇ x) ◇ ((y ◇ x) ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation1691_StructuralFromFin_Equation3634 : Law1691.StructuralFromFin Law3634 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3634 (fun {G} M hM ↦ ?_)
  have hm := (@Law3634.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law1691 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law1691.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X3 X0) X1)) := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op (N.op y x) (N.op (N.op y x) x)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 X4 : G, (M.op X2 (M.op (M.op X3 X0) X1)) = (M.op X4 (M.op X0 X1)) := by
    intro X0 X1 X2 X3 X4
    first
    | (have i₁ := eq6 X2 (M.op (M.op X3 X0) X1) X4 x
       have i₂ := eq6 X0 X1 (M.op x X2) X3
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) ≠ X2 ∨ (M.op (M.op X3 X0) X1) = (N.op (M.op (M.op X3 X0) X1) X2) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq7 (M.op (M.op X3 X0) X1) X2
       have i₂ := eq6 X0 X1 X2 X3
       grind)
    | exact superpose eq6 eq7
    | (have j0 := eq7 (M.op (M.op X3 X0) X1) X2
       grind)
    | (have r₁ := eq7 (M.op (M.op X3 X0) X1) (M.op X0 X1)
       have r₂ := eq6 X0 X1 (M.op X0 X1) X3
       grind)
    | exact resolve eq7 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X4 : G, (M.op X0 X1) = (M.op X4 (M.op X0 X1)) := by
    intro X0 X1 X4
    first
    | (have i₁ := eq12 X0 X1 x x X4
       have i₂ := eq6 X0 X1 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq19 : ∀ X0 X1 X2 : G, (M.op X1 X2) = (M.op (M.op X0 X1) X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X1 X2 x X0
       have i₂ := eq14 (M.op X0 X1) X2 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq31 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X2 X0) ∨ (N.op X1 (M.op X2 X0)) = X1 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 X1 (M.op X2 X0)
       have i₂ := eq19 X2 X0 X1
       grind)
    | exact superpose eq19 eq7
    | (have j0 := eq7 X1 (M.op X2 X0)
       grind)
    | (have r₁ := eq7 X2 (M.op X2 X2)
       have r₂ := eq19 X2 X2 X2
       grind)
    | exact resolve eq7 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq36 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X0 X1) = X2 ∨ (N.op (M.op X0 X1) X2) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 (M.op X0 X1) x
       have i₂ := eq14 X0 X1 x
       grind)
    | exact superpose eq14 eq8
    | (have j0 := eq8 (M.op X0 X1) X2
       grind)
    | (have r₁ := eq8 (M.op X0 X1) x
       have r₂ := eq14 X0 X1 x
       grind)
    | exact resolve eq8 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq38 : ∀ X0 X1 X2 : G, (N.op (M.op X0 X1) X2) = X2 ∨ (M.op X0 X1) = X2 := by
    intro X0 X1 X2
    first
    | (have j0 := eq36 X0 X1 X2
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36
  have eq59 : x ≠ (N.op (M.op x y) (N.op (M.op x y) x)) ∨ y = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq60 : x ≠ (N.op (N.op y x) (M.op x (N.op y x))) ∨ (N.op y x) = (M.op x (N.op y x)) ∨ x = (M.op x (N.op y x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 (N.op y x) x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 (N.op y x) x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq61 : x ≠ (M.op (N.op (N.op y x) x) (N.op y x)) ∨ (N.op y x) = (M.op (N.op (N.op y x) x) (N.op y x)) ∨ (N.op (N.op y x) x) = (M.op (N.op (N.op y x) x) (N.op y x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 (N.op y x) (N.op (N.op y x) x)
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 (N.op y x) (N.op (N.op y x) x)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq74 : ∀ X0 X1 X2 : G, (M.op (M.op X0 X1) X2) = (N.op (M.op (M.op X0 X1) X2) (M.op X1 X2)) := by
    intro X0 X1 X2
    first
    | (have j0 := eq13 X1 X2 (M.op X1 X2) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq75 : ∀ X1 X2 : G, (M.op X1 X2) = (N.op (M.op X1 X2) (M.op X1 X2)) := by
    intro X1 X2
    first
    | (have i₁ := eq74 x X1 X2
       have i₂ := eq19 x X1 X2
       grind)
    | exact superpose eq19 eq74
    | exact resolve eq74 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq74
  have eq88 : x ≠ (N.op (M.op x y) x) ∨ y = (M.op x y) ∨ x = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq59
       have i₂ := eq38 x y x
       grind)
    | exact superpose eq38 eq59
    | (have j1 := eq38 x y y
       grind)
    | exact resolve eq59 eq38
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq59
  have eq92 : x ≠ (N.op (M.op x y) x) ∨ y = (M.op x y) ∨ x = (M.op x y) := by grind
  clear eq88
  have eq94 : y = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have j1 := eq38 x y y
       grind)
    | (have r₁ := eq92
       have r₂ := eq38 x y x
       grind)
    | exact resolve eq92 eq38
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq92
  have eq102 : ∀ X0 : G, (N.op y X0) = X0 ∨ y = X0 ∨ x = (M.op x y) := by
    intro X0
    first
    | (have i₁ := eq38 x y x
       have i₂ := eq94
       grind)
    | exact superpose eq94 eq38
    | (have j0 := eq38 x y x
       grind)
    | exact resolve eq38 eq94
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq38
  have eq104 : x ≠ y ∨ x = (M.op x y) := by grind
  clear eq94
  have eq209 : ∀ X0 : G, (N.op X0 (M.op X0 X0)) = X0 := by
    intro X0
    first
    | (have j0 := eq31 X0 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq31
  have eq325 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq60
       have i₂ := eq102 x
       grind)
    | exact superpose eq102 eq60
    | (have j1 := eq102 x
       grind)
    | exact resolve eq60 eq102
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq60
  have eq326 : x ≠ (N.op x (N.op x x)) ∨ x = y ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq102 x
       grind)
    | exact superpose eq102 eq10
    | (have j1 := eq102 x
       grind)
    | exact resolve eq10 eq102
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq102
  have eq327 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x y) := by grind
  clear eq325
  have eq334 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op x y) := by
    first
    | (have r₁ := eq326
       have r₂ := eq104
       grind)
    | exact resolve eq326 eq104
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq326
  have eq335 : x = (M.op x x) ∨ x = y ∨ x = (M.op x y) := by
    first
    | (have r₁ := eq327
       have r₂ := eq209 x
       grind)
    | exact resolve eq327 eq209
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq209 eq327
  have eq336 : x = (M.op x y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq335
       have r₂ := eq104
       grind)
    | exact resolve eq335 eq104
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq104 eq335
  have eq352 : x = (N.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq75 x y
       have i₂ := eq336
       grind)
    | exact superpose eq336 eq75
    | exact resolve eq75 eq336
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq75 eq336
  have eq354 : x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq352
       have r₂ := eq7 x x
       grind)
    | exact resolve eq352 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq352
  have eq624 : x ≠ (N.op x x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq334
       have i₂ := eq354
       grind)
    | exact superpose eq354 eq334
    | exact resolve eq334 eq354
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq334
  have eq629 : x = (M.op x y) := by
    first
    | (have r₁ := eq624
       have r₂ := eq354
       grind)
    | exact resolve eq624 eq354
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq624
  have eq638 : x ≠ x ∨ y = (N.op y x) := by
    first
    | (have i₁ := eq7 y x
       have i₂ := eq629
       grind)
    | exact superpose eq629 eq7
    | (have j0 := eq7 y x
       grind)
    | (have r₁ := eq7 y x
       have r₂ := eq629
       grind)
    | exact resolve eq7 eq629
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq642 : ∀ X0 : G, (M.op y X0) = (M.op x X0) := by
    intro X0
    first
    | (have i₁ := eq19 x y X0
       have i₂ := eq629
       grind)
    | exact superpose eq629 eq19
    | exact resolve eq19 eq629
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq648 : y = (N.op y x) := by grind
  clear eq638
  have eq658 : x ≠ (M.op (N.op y x) y) ∨ y = (M.op (N.op y x) y) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq61
       have i₂ := eq648
       grind)
    | exact superpose eq648 eq61
    | exact resolve eq61 eq648
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq61
  have eq668 : x ≠ (M.op y y) ∨ y = (M.op (N.op y x) y) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq658
       have i₂ := eq648
       grind)
    | exact superpose eq648 eq658
    | exact resolve eq658 eq648
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq658
  have eq669 : x ≠ (M.op x y) ∨ y = (M.op (N.op y x) y) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq668
       have i₂ := eq642 y
       grind)
    | exact superpose eq642 eq668
    | exact resolve eq668 eq642
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq668
  have eq670 : y = (M.op (N.op y x) y) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have r₁ := eq669
       have r₂ := eq629
       grind)
    | exact resolve eq669 eq629
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq669
  have eq671 : y = (M.op y y) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq670
       have i₂ := eq648
       grind)
    | exact superpose eq648 eq670
    | exact resolve eq670 eq648
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq670
  have eq672 : y = (M.op x y) ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq671
       have i₂ := eq642 y
       grind)
    | exact superpose eq642 eq671
    | exact resolve eq671 eq642
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq671
  have eq673 : x = y ∨ (N.op y x) = (M.op (N.op y x) y) := by
    first
    | (have i₁ := eq672
       have i₂ := eq629
       grind)
    | exact superpose eq629 eq672
    | exact resolve eq672 eq629
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq672
  have eq674 : y = (M.op y y) ∨ x = y := by
    first
    | (have i₁ := eq673
       have i₂ := eq648
       grind)
    | exact superpose eq648 eq673
    | exact resolve eq673 eq648
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq648 eq673
  have eq675 : y = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq674
       have i₂ := eq642 y
       grind)
    | exact superpose eq642 eq674
    | exact resolve eq674 eq642
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq642 eq674
  have eq676 : x = y ∨ x = y := by
    first
    | (have i₁ := eq675
       have i₂ := eq629
       grind)
    | exact superpose eq629 eq675
    | exact resolve eq675 eq629
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq629 eq675
  have eq677 : x = y := by grind
  clear eq676
  have eq710 : x ≠ (N.op (N.op x x) (N.op (N.op x x) x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq677
       grind)
    | exact superpose eq677 eq10
    | exact resolve eq10 eq677
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq677
  have eq743 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq710
       have i₂ := eq354
       grind)
    | exact superpose eq354 eq710
    | exact resolve eq710 eq354
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq710
  have eq747 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq743
       have i₂ := eq354
       grind)
    | exact superpose eq354 eq743
    | exact resolve eq743 eq354
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq743
  have eq751 : False := by grind
  exact eq751
