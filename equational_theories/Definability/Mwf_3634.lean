import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation3634` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3634` is `x ◇ y = z ◇ ((w ◇ x) ◇ y)`, and the companion is
`x □ y = if (M.op a b) = a then b else if (M.op a b) = b then a else (M.op a b)`.  Its automorphism half is `GuardAut2_pxy_x_pxy_y_y_x_pxy_Equation3634`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches it on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation1429` from `Equation3634`: `x ◇ y = z ◇ ((w ◇ x) ◇ y)` ⊢ `x = (x ◇ x) ◇ (x ◇ (y ◇ y))`, through the
companion `x □ y = if (M.op a b) = a then b else if (M.op a b) = b then a else (M.op a b)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation1429_StructuralFromFin_Equation3634 : Law1429.StructuralFromFin Law3634 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 1)) (Lf 1) (Lf 1) (Lf 0) ((Lf 0) ⋆ (Lf 1))
    GuardAut2_pxy_x_pxy_y_y_x_pxy_Equation3634 (fun {G} M hM ↦ ?_)
  have hm := (@Law3634.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op a b) = a ∨ (M.op a b) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op a b) = a ∨ (M.op a b) = b ∨ N.op a b = (M.op a b)) → @satisfies _ G N Law1429 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 1)) (Lf 1) (Lf 1) (Lf 0) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 1)) (Lf 1) (Lf 1) (Lf 0) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (a)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (b)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 1)) (Lf 1) (Lf 1) (Lf 0) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (a)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (b)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law1429.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X3 X0) X1)) := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X1 ∨ (M.op X0 X1) = X0 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op X0 X1) ∨ (M.op X0 X1) = X1 ∨ (M.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op (N.op x x) (N.op x (N.op y y))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 X4 : G, (M.op X2 (M.op (M.op X3 X0) X1)) = (M.op X4 (M.op X0 X1)) := by
    intro X0 X1 X2 X3 X4
    first
    | (have i₁ := eq6 X2 (M.op (M.op X3 X0) X1) X4 x
       have i₂ := eq6 X0 X1 (M.op x X2) X3
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) ≠ X2 ∨ (M.op (M.op X3 X0) X1) = (N.op X2 (M.op (M.op X3 X0) X1)) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq7 X2 (M.op (M.op X3 X0) X1)
       have i₂ := eq6 X0 X1 X2 X3
       grind)
    | exact superpose eq6 eq7
    | (have j0 := eq7 X2 (M.op (M.op X3 X0) X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X1) (M.op (M.op X3 X0) X1)
       have r₂ := eq6 X0 X1 (M.op X0 X1) X3
       grind)
    | exact resolve eq7 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X4 : G, (M.op X0 X1) = (M.op X4 (M.op X0 X1)) := by
    intro X0 X1 X4
    first
    | (have i₁ := eq12 X0 X1 x x X4
       have i₂ := eq6 X0 X1 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq19 : ∀ X0 X1 X2 : G, (M.op X1 X2) = (M.op (M.op X0 X1) X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X1 X2 x X0
       have i₂ := eq14 (M.op X0 X1) X2 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq21 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (N.op X2 (M.op X0 X1)) ∨ (M.op X0 X1) ≠ X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 X2 (M.op X0 X1)
       have i₂ := eq14 X0 X1 X2
       grind)
    | exact superpose eq14 eq7
    | (have j0 := eq7 X2 (M.op X0 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X1) (M.op X0 X1)
       have r₂ := eq14 X0 X1 (M.op X0 X1)
       grind)
    | exact resolve eq7 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq31 : ∀ X0 X1 X2 : G, (N.op (M.op X2 X0) X1) = X1 ∨ (M.op X0 X1) ≠ (M.op X2 X0) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op X2 X0) X1
       have i₂ := eq19 X2 X0 X1
       grind)
    | exact superpose eq19 eq7
    | (have j0 := eq7 (M.op X2 X0) X1
       grind)
    | (have r₁ := eq7 (M.op X2 X2) X2
       have r₂ := eq19 X2 X2 X2
       grind)
    | exact resolve eq7 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq62 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X0 X1) = X2 ∨ (N.op X2 (M.op X0 X1)) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 x (M.op X0 X1)
       have i₂ := eq14 X0 X1 x
       grind)
    | exact superpose eq14 eq8
    | (have j0 := eq8 X2 (M.op X0 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X1)
       have r₂ := eq14 X0 X1 x
       grind)
    | exact resolve eq8 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq65 : ∀ X0 X1 X2 : G, (N.op X2 (M.op X0 X1)) = X2 ∨ (M.op X0 X1) = X2 := by
    intro X0 X1 X2
    first
    | (have j0 := eq62 X0 X1 X2
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq62
  have eq93 : x ≠ (N.op (N.op x x) (N.op x (M.op y y))) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq94 : x ≠ (N.op (N.op x x) (N.op x (M.op y y))) ∨ y = (M.op y y) := by grind
  clear eq93
  have eq107 : x ≠ (N.op (N.op x x) x) ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq94
       have i₂ := eq65 y y x
       grind)
    | exact superpose eq65 eq94
    | (have j1 := eq65 y y y
       grind)
    | exact resolve eq94 eq65
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq108 : x ≠ (M.op y y) ∨ y = (M.op y y) ∨ x ≠ (N.op (N.op x x) (M.op y y)) := by
    first
    | (have i₁ := eq94
       have i₂ := eq21 y y x
       grind)
    | exact superpose eq21 eq94
    | (have j1 := eq21 y y x
       grind)
    | (have r₁ := eq94
       have r₂ := eq21 y y y
       grind)
    | exact resolve eq94 eq21
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq94
  have eq120 : x ≠ (N.op (M.op x x) x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq107
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq107
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq107 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq122 : x ≠ (N.op (M.op x x) x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq120
  have eq127 : ∀ X0 X1 X2 : G, (M.op (M.op X0 X1) X2) = (N.op (M.op X1 X2) (M.op (M.op X0 X1) X2)) := by
    intro X0 X1 X2
    first
    | (have j0 := eq13 X1 X2 (M.op X1 X2) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq128 : ∀ X1 X2 : G, (M.op X1 X2) = (N.op (M.op X1 X2) (M.op X1 X2)) := by
    intro X1 X2
    first
    | (have i₁ := eq127 x X1 X2
       have i₂ := eq19 x X1 X2
       grind)
    | exact superpose eq19 eq127
    | exact resolve eq127 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq127
  have eq160 : x ≠ x ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) ∨ (M.op x x) ≠ (M.op x x) := by
    first
    | (have i₁ := eq122
       have i₂ := eq31 x x x
       grind)
    | exact superpose eq31 eq122
    | (have j1 := eq31 x x x
       grind)
    | (have r₁ := eq122
       have r₂ := eq31 x x x
       grind)
    | exact resolve eq122 eq31
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq122
  have eq162 : y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq160
  have eq166 : ∀ X0 : G, x = (M.op x x) ∨ x = (M.op y y) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq162
       grind)
    | exact superpose eq162 eq14
    | exact resolve eq14 eq162
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq162
  have eq186 : ∀ X0 : G, x = (N.op x x) ∨ x = (M.op y y) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq128 x x
       have i₂ := eq166 X0
       grind)
    | exact superpose eq166 eq128
    | (have j1 := eq166 X0
       grind)
    | exact resolve eq128 eq166
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq166
  have eq218 : ∀ X0 : G, x ≠ (N.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op y y) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq107
       have i₂ := eq186 X0
       grind)
    | exact superpose eq186 eq107
    | (have j1 := eq186 y
       grind)
    | exact resolve eq107 eq186
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq107
  have eq223 : ∀ X0 : G, x ≠ (N.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq218 y
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq218
  have eq225 : ∀ X0 : G, y = (M.op y y) ∨ x = (M.op y y) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have j0 := eq223 y
       have j1 := eq186 y
       grind)
    | (have r₁ := eq223 X0
       have r₂ := eq186 X0
       grind)
    | exact resolve eq223 eq186
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq186 eq223
  have eq417 : y ≠ y ∨ x = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have j0 := eq225 y
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq225
  have eq418 : y = (M.op y y) ∨ x = (M.op y y) := by grind
  clear eq417
  have eq430 : ∀ X0 : G, x = (M.op y y) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq418
       grind)
    | exact superpose eq418 eq14
    | exact resolve eq14 eq418
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq436 : y = (N.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq128 y y
       have i₂ := eq418
       grind)
    | exact superpose eq418 eq128
    | exact resolve eq128 eq418
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq437 : x ≠ y ∨ x = (M.op y y) := by grind
  clear eq418
  have eq459 : ∀ X0 X1 : G, y = (M.op X1 y) ∨ (M.op y X0) = (M.op x X0) := by
    intro X0 X1
    first
    | (have i₁ := eq19 y y X0
       have i₂ := eq430 X1
       grind)
    | exact superpose eq430 eq19
    | (have j1 := eq430 X1
       grind)
    | exact resolve eq19 eq430
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq464 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq128 y y
       have i₂ := eq430 X0
       grind)
    | exact superpose eq430 eq128
    | (have j1 := eq430 X0
       grind)
    | exact resolve eq128 eq430
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq472 : x ≠ (N.op (N.op x x) (N.op x y)) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq436
       grind)
    | exact superpose eq436 eq10
    | exact resolve eq10 eq436
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq436
  have eq510 : y ≠ (M.op x y) ∨ (M.op y y) = (M.op x y) := by
    first
    | (have j0 := eq459 y x
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq513 : (M.op y y) = (M.op x y) := by
    first
    | (have j1 := eq459 y x
       grind)
    | (have r₁ := eq510
       have r₂ := eq459 x x
       grind)
    | exact resolve eq510 eq459
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq459 eq510
  have eq525 : x ≠ (M.op x y) ∨ y = (M.op x y) ∨ x ≠ (N.op (N.op x x) (M.op x y)) := by
    first
    | (have i₁ := eq108
       have i₂ := eq513
       grind)
    | exact superpose eq513 eq108
    | exact resolve eq108 eq513
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq108
  have eq531 : ∀ X0 : G, x = (M.op x y) ∨ y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq430 X0
       have i₂ := eq513
       grind)
    | exact superpose eq513 eq430
    | (have j0 := eq430 X0
       grind)
    | exact resolve eq430 eq513
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq430 eq513
  have eq549 : x ≠ (N.op (N.op x x) (M.op x y)) ∨ y = (M.op x y) := by
    first
    | (have j1 := eq531 x
       grind)
    | (have r₁ := eq525
       have r₂ := eq531 x
       grind)
    | exact resolve eq525 eq531
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq525
  have eq776 : x ≠ (N.op x x) ∨ y = (M.op x y) ∨ (N.op x x) = (M.op x y) := by
    first
    | (have i₁ := eq549
       have i₂ := eq65 x y (N.op x x)
       grind)
    | exact superpose eq65 eq549
    | (have j1 := eq65 x y y
       grind)
    | exact resolve eq549 eq65
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq777 : x ≠ (M.op x y) ∨ y = (M.op x y) ∨ (N.op x x) ≠ (M.op x y) := by
    first
    | (have i₁ := eq549
       have i₂ := eq21 x y (N.op x x)
       grind)
    | exact superpose eq21 eq549
    | (have j1 := eq21 x y x
       grind)
    | (have r₁ := eq549
       have r₂ := eq21 x y y
       grind)
    | exact resolve eq549 eq21
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21 eq549
  have eq780 : (N.op x x) ≠ (M.op x y) ∨ y = (M.op x y) := by
    first
    | (have j1 := eq531 x
       grind)
    | (have r₁ := eq777
       have r₂ := eq531 x
       grind)
    | exact resolve eq777 eq531
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq531 eq777
  have eq781 : y = (M.op x y) ∨ (N.op x x) = (M.op x y) := by
    first
    | (have j1 := eq464 x
       grind)
    | (have r₁ := eq776
       have r₂ := eq464 x
       grind)
    | exact resolve eq776 eq464
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq464 eq776
  have eq782 : y = (M.op x y) := by
    first
    | (have r₁ := eq781
       have r₂ := eq780
       grind)
    | exact resolve eq781 eq780
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq780 eq781
  have eq796 : ∀ X0 : G, y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq14 x y x
       have i₂ := eq782
       grind)
    | exact superpose eq782 eq14
    | exact resolve eq14 eq782
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq801 : ∀ X0 : G, (N.op X0 y) = X0 ∨ y = X0 := by
    intro X0
    first
    | (have i₁ := eq65 x y x
       have i₂ := eq782
       grind)
    | exact superpose eq782 eq65
    | exact resolve eq65 eq782
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq65
  have eq802 : y = (N.op y y) := by
    first
    | (have i₁ := eq128 x y
       have i₂ := eq782
       grind)
    | exact superpose eq782 eq128
    | exact resolve eq128 eq782
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq782
  have eq887 : x ≠ (N.op (N.op x x) x) ∨ x = (M.op y y) ∨ x = y := by
    first
    | (have i₁ := eq472
       have i₂ := eq801 x
       grind)
    | exact superpose eq801 eq472
    | (have j1 := eq801 x
       grind)
    | exact resolve eq472 eq801
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq472 eq801
  have eq898 : x ≠ (N.op (N.op x x) x) ∨ x = (M.op y y) := by
    first
    | (have r₁ := eq887
       have r₂ := eq437
       grind)
    | exact resolve eq887 eq437
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq437 eq887
  have eq903 : x ≠ (N.op (N.op x x) x) ∨ x = y := by
    first
    | (have i₁ := eq898
       have i₂ := eq796 y
       grind)
    | exact superpose eq796 eq898
    | exact resolve eq898 eq796
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq796 eq898
  have eq923 : x ≠ (N.op (M.op x x) x) ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq903
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq903
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq903 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq926 : x ≠ (N.op (M.op x x) x) ∨ x = y ∨ x = (M.op x x) := by grind
  clear eq923
  have eq1071 : x ≠ x ∨ x = y ∨ x = (M.op x x) ∨ (M.op x x) ≠ (M.op x x) := by
    first
    | (have i₁ := eq926
       have i₂ := eq31 x x x
       grind)
    | exact superpose eq31 eq926
    | (have j1 := eq31 x x x
       grind)
    | (have r₁ := eq926
       have r₂ := eq31 x x x
       grind)
    | exact resolve eq926 eq31
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq31 eq926
  have eq1077 : x = (M.op x x) ∨ x = y := by grind
  clear eq1071
  have eq1087 : x = (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq128 x x
       have i₂ := eq1077
       grind)
    | exact superpose eq1077 eq128
    | exact resolve eq128 eq1077
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq128 eq1077
  have eq1128 : x ≠ (N.op x x) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq903
       have i₂ := eq1087
       grind)
    | exact superpose eq1087 eq903
    | exact resolve eq903 eq1087
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq903
  have eq1131 : x ≠ (N.op x x) ∨ x = y := by grind
  clear eq1128
  have eq1135 : x = y := by
    first
    | (have r₁ := eq1131
       have r₂ := eq1087
       grind)
    | exact resolve eq1131 eq1087
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1087 eq1131
  have eq1139 : x ≠ (N.op (N.op x x) (N.op x (N.op x x))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1135
       grind)
    | exact superpose eq1135 eq10
    | exact resolve eq10 eq1135
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1185 : x = (N.op x x) := by
    first
    | (have i₁ := eq802
       have i₂ := eq1135
       grind)
    | exact superpose eq1135 eq802
    | exact resolve eq802 eq1135
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq802 eq1135
  have eq1206 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq1139
       have i₂ := eq1185
       grind)
    | exact superpose eq1185 eq1139
    | exact resolve eq1139 eq1185
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1139
  have eq1210 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq1206
       have i₂ := eq1185
       grind)
    | exact superpose eq1185 eq1206
    | exact resolve eq1206 eq1185
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1206
  have eq1213 : False := by grind
  exact eq1213
