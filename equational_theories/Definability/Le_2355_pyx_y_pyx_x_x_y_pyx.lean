import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation2355` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation2355` is `x = (y ◇ (y ◇ (z ◇ z))) ◇ x`, and the companion is
`x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`.  Its automorphism half is `GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2355`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches none of these on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation307` from `Equation2355`: `x = (y ◇ (y ◇ (z ◇ z))) ◇ x` ⊢ `x ◇ x = x ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation307_StructuralFromFin_Equation2355 : Law307.StructuralFromFin Law2355 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2355 (fun {G} M hM ↦ ?_)
  have hm := (@Law2355.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law307 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law307.models_iff G N]
  intro x
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X1 (M.op X2 X2))) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op (M.op (M.op X0 (M.op X0 (M.op X1 X1))) (M.op X2 X2)) X3) = X3 := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X3 (M.op X0 (M.op X0 (M.op X1 X1))) X2
       have i₂ := eq6 (M.op (M.op X0 (M.op X0 (M.op X1 X1))) (M.op X2 X2)) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq15 : ∀ X2 X3 : G, (M.op (M.op X2 X2) X3) = X3 := by
    intro X2 X3
    first
    | (have i₁ := eq13 x x X2 X3
       have i₂ := eq6 (M.op X2 X2) x x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq21 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op x x)
       have i₂ := eq15 x x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq26 : ∀ X0 X1 : G, (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have j0 := eq21 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq29 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq31 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq29
  have eq32 : x = (M.op x x) := by
    first
    | (have j1 := eq26 x x
       grind)
    | (have r₁ := eq31
       have r₂ := eq26 (N.op x (M.op x x)) x
       grind)
    | (have r₁ := eq31
       have r₂ := eq26 x x
       grind)
    | exact resolve eq31 eq26
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26 eq31
  have eq33 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq32
       grind)
    | exact superpose eq32 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq32
       grind)
    | exact resolve eq7 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq36 : x = (N.op x x) := by grind
  clear eq33
  have eq123 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq36
       grind)
    | exact superpose eq36 eq10
    | exact resolve eq10 eq36
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq127 : False := by grind
  exact eq127

/-- `Equation312` from `Equation2355`: `x = (y ◇ (y ◇ (z ◇ z))) ◇ x` ⊢ `x ◇ x = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation312_StructuralFromFin_Equation2355 : Law312.StructuralFromFin Law2355 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2355 (fun {G} M hM ↦ ?_)
  have hm := (@Law2355.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law312 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law312.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X1 (M.op X2 X2))) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op (M.op (M.op X0 (M.op X0 (M.op X1 X1))) (M.op X2 X2)) X3) = X3 := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X3 (M.op X0 (M.op X0 (M.op X1 X1))) X2
       have i₂ := eq6 (M.op (M.op X0 (M.op X0 (M.op X1 X1))) (M.op X2 X2)) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq15 : ∀ X2 X3 : G, (M.op (M.op X2 X2) X3) = X3 := by
    intro X2 X3
    first
    | (have i₁ := eq13 x x X2 X3
       have i₂ := eq6 (M.op X2 X2) x x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq21 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op x x)
       have i₂ := eq15 x x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq22 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq15 X1 X0
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op x x) (M.op x x)
       have r₂ := eq15 x (M.op x x)
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq26 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq21 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq29 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq31 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq29
  have eq34 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq22 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq22
  have eq85 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq31
       have i₂ := eq26 y x
       grind)
    | exact superpose eq26 eq31
    | (have j1 := eq26 x x
       grind)
    | (have r₁ := eq31
       have r₂ := eq26 y x
       grind)
    | (have r₁ := eq31
       have r₂ := eq26 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq31 eq26
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq86 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq85
  have eq111 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq31
       have i₂ := eq86
       grind)
    | exact superpose eq86 eq31
    | exact resolve eq31 eq86
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq31
  have eq120 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq34 x
       have i₂ := eq86
       grind)
    | exact superpose eq86 eq34
    | exact resolve eq34 eq86
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq121 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq86
  have eq122 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq111
       have r₂ := eq120
       grind)
    | exact resolve eq111 eq120
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq111 eq120
  have eq123 : x = (M.op x x) := by
    first
    | (have r₁ := eq122
       have r₂ := eq121
       grind)
    | exact resolve eq122 eq121
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq121 eq122
  have eq158 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq26 X0 x
       have i₂ := eq123
       grind)
    | exact superpose eq123 eq26
    | exact resolve eq26 eq123
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26
  have eq159 : x = (N.op x x) := by
    first
    | (have i₁ := eq34 x
       have i₂ := eq123
       grind)
    | exact superpose eq123 eq34
    | exact resolve eq34 eq123
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34 eq123
  have eq452 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq159
       grind)
    | exact superpose eq159 eq10
    | exact resolve eq10 eq159
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1046 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq452
       have i₂ := eq158 y
       grind)
    | exact superpose eq158 eq452
    | (have j1 := eq158 y
       grind)
    | (have r₁ := eq452
       have r₂ := eq158 y
       grind)
    | (have r₁ := eq452
       have r₂ := eq158 (N.op y x)
       grind)
    | exact resolve eq452 eq158
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq158
  have eq1047 : x = y := by grind
  clear eq1046
  have eq1145 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq452
       have i₂ := eq1047
       grind)
    | exact superpose eq1047 eq452
    | exact resolve eq452 eq1047
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq452 eq1047
  have eq1150 : False := by grind
  exact eq1150

/-- `Equation3258` from `Equation2355`: `x = (y ◇ (y ◇ (z ◇ z))) ◇ x` ⊢ `x ◇ x = x ◇ (y ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3258_StructuralFromFin_Equation2355 : Law3258.StructuralFromFin Law2355 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2355 (fun {G} M hM ↦ ?_)
  have hm := (@Law2355.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3258 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3258.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X1 (M.op X2 X2))) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op y (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op (M.op (M.op X0 (M.op X0 (M.op X1 X1))) (M.op X2 X2)) X3) = X3 := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X3 (M.op X0 (M.op X0 (M.op X1 X1))) X2
       have i₂ := eq6 (M.op (M.op X0 (M.op X0 (M.op X1 X1))) (M.op X2 X2)) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq15 : ∀ X2 X3 : G, (M.op (M.op X2 X2) X3) = X3 := by
    intro X2 X3
    first
    | (have i₁ := eq13 x x X2 X3
       have i₂ := eq6 (M.op X2 X2) x x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq19 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq15 X1 X0
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op x x) (M.op x x)
       have r₂ := eq15 x (M.op x x)
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq27 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq19 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq29 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op x x)
       have i₂ := eq15 x x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq30 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq29 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29
  have eq36 : (M.op x x) ≠ (N.op x (N.op y (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq39 : (M.op x x) ≠ (N.op x (N.op y (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq36
  have eq87 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq39
       have i₂ := eq30 y x
       grind)
    | exact superpose eq30 eq39
    | (have j1 := eq30 x x
       grind)
    | (have r₁ := eq39
       have r₂ := eq30 (N.op x (N.op y (M.op x x))) x
       grind)
    | exact resolve eq39 eq30
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq90 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq30 y x
       grind)
    | (have r₁ := eq87
       have r₂ := eq30 x x
       grind)
    | (have r₁ := eq87
       have r₂ := eq30 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq87 eq30
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq87
  have eq113 : y ≠ (N.op x (N.op y y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq39
       have i₂ := eq90
       grind)
    | exact superpose eq90 eq39
    | exact resolve eq39 eq90
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq121 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq27 x
       have i₂ := eq90
       grind)
    | exact superpose eq90 eq27
    | exact resolve eq27 eq90
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq122 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq30 X0 x
       have i₂ := eq90
       grind)
    | exact superpose eq90 eq30
    | (have j0 := eq30 x x
       grind)
    | exact resolve eq30 eq90
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq123 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq90
  have eq124 : y ≠ (N.op x (N.op y y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq113
       have r₂ := eq123
       grind)
    | exact resolve eq113 eq123
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq113
  have eq277 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq124
       have i₂ := eq121
       grind)
    | exact superpose eq121 eq124
    | exact resolve eq124 eq121
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq121 eq124
  have eq281 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq277
  have eq536 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq281
       have i₂ := eq122 x
       grind)
    | exact superpose eq122 eq281
    | (have j1 := eq122 x
       grind)
    | (have r₁ := eq281
       have r₂ := eq122 x
       grind)
    | (have r₁ := eq281
       have r₂ := eq122 (N.op x y)
       grind)
    | exact resolve eq281 eq122
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq122 eq281
  have eq537 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq536
  have eq538 : x = (M.op x x) ∨ x = y := by grind
  clear eq537
  have eq541 : x = (M.op x x) := by
    first
    | (have r₁ := eq538
       have r₂ := eq123
       grind)
    | exact resolve eq538 eq123
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq123 eq538
  have eq551 : x = (N.op x x) := by
    first
    | (have i₁ := eq27 x
       have i₂ := eq541
       grind)
    | exact superpose eq541 eq27
    | exact resolve eq27 eq541
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq27
  have eq552 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq30 X0 x
       have i₂ := eq541
       grind)
    | exact superpose eq541 eq30
    | exact resolve eq30 eq541
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30 eq541
  have eq1037 : x ≠ (N.op x (N.op y x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq551
       grind)
    | exact superpose eq551 eq10
    | exact resolve eq10 eq551
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1937 : x ≠ (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq1037
       have i₂ := eq552 y
       grind)
    | exact superpose eq552 eq1037
    | (have j1 := eq552 y
       grind)
    | (have r₁ := eq1037
       have r₂ := eq552 (N.op x (N.op y x))
       grind)
    | exact resolve eq1037 eq552
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq552
  have eq1940 : x = y := by
    first
    | (have r₁ := eq1937
       have r₂ := eq551
       grind)
    | exact resolve eq1937 eq551
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1937
  have eq2082 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq1037
       have i₂ := eq1940
       grind)
    | exact superpose eq1940 eq1037
    | exact resolve eq1037 eq1940
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1037 eq1940
  have eq2084 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq2082
       have i₂ := eq551
       grind)
    | exact superpose eq551 eq2082
    | exact resolve eq2082 eq551
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2082
  have eq2089 : False := by grind
  exact eq2089

/-- `Equation3268` from `Equation2355`: `x = (y ◇ (y ◇ (z ◇ z))) ◇ x` ⊢ `x ◇ x = y ◇ (x ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3268_StructuralFromFin_Equation2355 : Law3268.StructuralFromFin Law2355 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2355 (fun {G} M hM ↦ ?_)
  have hm := (@Law2355.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3268 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3268.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X1 (M.op X2 X2))) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op (M.op (M.op X0 (M.op X0 (M.op X1 X1))) (M.op X2 X2)) X3) = X3 := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X3 (M.op X0 (M.op X0 (M.op X1 X1))) X2
       have i₂ := eq6 (M.op (M.op X0 (M.op X0 (M.op X1 X1))) (M.op X2 X2)) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq15 : ∀ X2 X3 : G, (M.op (M.op X2 X2) X3) = X3 := by
    intro X2 X3
    first
    | (have i₁ := eq13 x x X2 X3
       have i₂ := eq6 (M.op X2 X2) x x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq19 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq15 X1 X0
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op x x) (M.op x x)
       have r₂ := eq15 x (M.op x x)
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq27 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq19 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq29 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op x x)
       have i₂ := eq15 x x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq30 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq29 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29
  have eq36 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq39 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq36
  have eq87 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq39
       have i₂ := eq30 x x
       grind)
    | exact superpose eq30 eq39
    | (have j1 := eq30 x x
       grind)
    | (have r₁ := eq39
       have r₂ := eq30 (N.op y (N.op x (M.op x x))) x
       grind)
    | exact resolve eq39 eq30
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq90 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq87
  have eq113 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq90
       have i₂ := eq30 y x
       grind)
    | exact superpose eq30 eq90
    | (have j1 := eq30 x x
       grind)
    | (have r₁ := eq90
       have r₂ := eq30 y x
       grind)
    | (have r₁ := eq90
       have r₂ := eq30 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq90 eq30
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq115 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq113
  have eq141 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq90
       have i₂ := eq115
       grind)
    | exact superpose eq115 eq90
    | exact resolve eq90 eq115
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq90
  have eq150 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq27 x
       have i₂ := eq115
       grind)
    | exact superpose eq115 eq27
    | exact resolve eq27 eq115
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq153 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq115
  have eq155 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq141
       have r₂ := eq150
       grind)
    | exact resolve eq141 eq150
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq141 eq150
  have eq156 : x = (M.op x x) := by
    first
    | (have r₁ := eq155
       have r₂ := eq153
       grind)
    | exact resolve eq155 eq153
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq153 eq155
  have eq167 : x = (N.op x x) := by
    first
    | (have i₁ := eq27 x
       have i₂ := eq156
       grind)
    | exact superpose eq156 eq27
    | exact resolve eq27 eq156
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq27
  have eq168 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq30 X0 x
       have i₂ := eq156
       grind)
    | exact superpose eq156 eq30
    | exact resolve eq30 eq156
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30 eq156
  have eq315 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq167
       grind)
    | exact superpose eq167 eq10
    | exact resolve eq10 eq167
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq320 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq315
       have i₂ := eq167
       grind)
    | exact superpose eq167 eq315
    | exact resolve eq315 eq167
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq315
  have eq605 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq320
       have i₂ := eq168 y
       grind)
    | exact superpose eq168 eq320
    | (have j1 := eq168 y
       grind)
    | (have r₁ := eq320
       have r₂ := eq168 y
       grind)
    | (have r₁ := eq320
       have r₂ := eq168 (N.op y x)
       grind)
    | exact resolve eq320 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq168
  have eq606 : x = y := by grind
  clear eq605
  have eq737 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq320
       have i₂ := eq606
       grind)
    | exact superpose eq606 eq320
    | exact resolve eq320 eq606
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq320 eq606
  have eq739 : False := by grind
  exact eq739

/-- `Equation3288` from `Equation2355`: `x = (y ◇ (y ◇ (z ◇ z))) ◇ x` ⊢ `x ◇ x = y ◇ (z ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3288_StructuralFromFin_Equation2355 : Law3288.StructuralFromFin Law2355 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2355 (fun {G} M hM ↦ ?_)
  have hm := (@Law2355.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3288 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3288.models_iff G N]
  intro x y z
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X1 (M.op X2 X2))) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op z (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op (M.op (M.op X0 (M.op X0 (M.op X1 X1))) (M.op X2 X2)) X3) = X3 := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X3 (M.op X0 (M.op X0 (M.op X1 X1))) X2
       have i₂ := eq6 (M.op (M.op X0 (M.op X0 (M.op X1 X1))) (M.op X2 X2)) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq15 : ∀ X2 X3 : G, (M.op (M.op X2 X2) X3) = X3 := by
    intro X2 X3
    first
    | (have i₁ := eq13 x x X2 X3
       have i₂ := eq6 (M.op X2 X2) x x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq19 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq15 X1 X0
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op x x) (M.op x x)
       have r₂ := eq15 x (M.op x x)
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq27 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq19 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq29 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op x x)
       have i₂ := eq15 x x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq30 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq29 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29
  have eq36 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq39 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq36
  have eq87 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ z = (M.op x x) := by
    first
    | (have i₁ := eq39
       have i₂ := eq30 z x
       grind)
    | exact superpose eq30 eq39
    | (have j1 := eq30 x x
       grind)
    | (have r₁ := eq39
       have r₂ := eq30 (N.op y (N.op z (M.op x x))) x
       grind)
    | exact resolve eq39 eq30
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq224 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ z = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq87
       have i₂ := eq30 y x
       grind)
    | exact superpose eq30 eq87
    | (have j1 := eq30 x x
       grind)
    | (have r₁ := eq87
       have r₂ := eq30 y x
       grind)
    | (have r₁ := eq87
       have r₂ := eq30 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq87 eq30
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq226 : z = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq224
  have eq230 : z ≠ (N.op y (N.op z z)) ∨ x = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq39
       have i₂ := eq226
       grind)
    | exact superpose eq226 eq39
    | exact resolve eq39 eq226
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq238 : z = (N.op z z) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq27 x
       have i₂ := eq226
       grind)
    | exact superpose eq226 eq27
    | exact resolve eq27 eq226
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq239 : ∀ X0 : G, z = (N.op X0 z) ∨ z = X0 ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq30 X0 x
       have i₂ := eq226
       grind)
    | exact superpose eq226 eq30
    | (have j0 := eq30 x x
       grind)
    | exact resolve eq30 eq226
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq242 : x ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  have eq243 : y ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq226
  have eq244 : z ≠ (N.op y (N.op z z)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have r₁ := eq230
       have r₂ := eq242
       grind)
    | exact resolve eq230 eq242
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq230 eq242
  have eq533 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq244
       have i₂ := eq238
       grind)
    | exact superpose eq238 eq244
    | exact resolve eq244 eq238
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq238 eq244
  have eq537 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq533
  have eq1156 : z ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq537
       have i₂ := eq239 y
       grind)
    | exact superpose eq239 eq537
    | (have j1 := eq239 y
       grind)
    | (have r₁ := eq537
       have r₂ := eq239 y
       grind)
    | (have r₁ := eq537
       have r₂ := eq239 (N.op y z)
       grind)
    | exact resolve eq537 eq239
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq239 eq537
  have eq1157 : z ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq1156
  have eq1158 : x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq1157
  have eq1161 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1158
       have r₂ := eq243
       grind)
    | exact resolve eq1158 eq243
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq243 eq1158
  have eq1555 : y ≠ (N.op y y) ∨ x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq87
       have i₂ := eq1161
       grind)
    | exact superpose eq1161 eq87
    | exact resolve eq87 eq1161
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq87
  have eq1556 : y ≠ (N.op y (N.op z y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq39
       have i₂ := eq1161
       grind)
    | exact superpose eq1161 eq39
    | exact resolve eq39 eq1161
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq1564 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq27 x
       have i₂ := eq1161
       grind)
    | exact superpose eq1161 eq27
    | exact resolve eq27 eq1161
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1574 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq1161
  have eq1575 : y ≠ (N.op y (N.op z y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1556
       have r₂ := eq1574
       grind)
    | exact resolve eq1556 eq1574
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1556
  have eq1576 : x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1555
       have r₂ := eq1564
       grind)
    | exact resolve eq1555 eq1564
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1555
  have eq1577 : x = (M.op x x) ∨ y = z := by
    first
    | (have r₁ := eq1576
       have r₂ := eq1574
       grind)
    | exact resolve eq1576 eq1574
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1576
  have eq2202 : x = (N.op x x) ∨ y = z := by
    first
    | (have i₁ := eq27 x
       have i₂ := eq1577
       grind)
    | exact superpose eq1577 eq27
    | exact resolve eq27 eq1577
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq27
  have eq2203 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ y = z := by
    intro X0
    first
    | (have i₁ := eq30 X0 x
       have i₂ := eq1577
       grind)
    | exact superpose eq1577 eq30
    | exact resolve eq30 eq1577
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1577
  have eq2770 : x ≠ (N.op y (N.op z x)) ∨ y = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq2202
       grind)
    | exact superpose eq2202 eq10
    | exact resolve eq10 eq2202
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq4615 : x ≠ (N.op y x) ∨ y = z ∨ x = z ∨ y = z := by
    first
    | (have i₁ := eq2770
       have i₂ := eq2203 z
       grind)
    | exact superpose eq2203 eq2770
    | (have j1 := eq2203 z
       grind)
    | (have r₁ := eq2770
       have r₂ := eq2203 (N.op y (N.op z x))
       grind)
    | exact resolve eq2770 eq2203
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq4616 : x ≠ (N.op y x) ∨ y = z ∨ x = z := by grind
  clear eq4615
  have eq4680 : x ≠ x ∨ y = z ∨ x = z ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq4616
       have i₂ := eq2203 y
       grind)
    | exact superpose eq2203 eq4616
    | (have j1 := eq2203 z
       grind)
    | (have r₁ := eq4616
       have r₂ := eq2203 y
       grind)
    | (have r₁ := eq4616
       have r₂ := eq2203 (N.op y x)
       grind)
    | exact resolve eq4616 eq2203
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4616
  have eq4682 : x ≠ x ∨ y = z ∨ x = z ∨ x = y := by grind
  clear eq4680
  have eq4683 : y = z ∨ x = z ∨ x = y := by grind
  clear eq4682
  have eq5678 : y ≠ (N.op y (N.op y y)) ∨ x = (M.op x x) ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq1575
       have i₂ := eq4683
       grind)
    | exact superpose eq4683 eq1575
    | exact resolve eq1575 eq4683
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1575 eq4683
  have eq5681 : y ≠ (N.op y (N.op y y)) ∨ x = (M.op x x) ∨ x = z := by
    first
    | (have r₁ := eq5678
       have r₂ := eq1574
       grind)
    | exact resolve eq5678 eq1574
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5678
  have eq20447 : y ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq5681
       have i₂ := eq1564
       grind)
    | exact superpose eq1564 eq5681
    | exact resolve eq5681 eq1564
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5681
  have eq20451 : y ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = z := by grind
  clear eq20447
  have eq20452 : x = (M.op x x) ∨ x = z := by
    first
    | (have r₁ := eq20451
       have r₂ := eq1564
       grind)
    | exact resolve eq20451 eq1564
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1564 eq20451
  have eq21471 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq30 X0 x
       have i₂ := eq20452
       grind)
    | exact superpose eq20452 eq30
    | exact resolve eq30 eq20452
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq21505 : x ≠ x ∨ x = (N.op x x) ∨ x = z := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq20452
       grind)
    | exact superpose eq20452 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq20452
       grind)
    | exact resolve eq7 eq20452
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20452
  have eq21506 : x = (N.op x x) ∨ x = z := by grind
  clear eq21505
  have eq23704 : x ≠ (N.op y (N.op z x)) ∨ x = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq21506
       grind)
    | exact superpose eq21506 eq10
    | exact resolve eq10 eq21506
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq26437 : x ≠ (N.op y x) ∨ x = z ∨ x = z ∨ x = z := by
    first
    | (have i₁ := eq23704
       have i₂ := eq21471 z
       grind)
    | exact superpose eq21471 eq23704
    | (have j1 := eq21471 z
       grind)
    | (have r₁ := eq23704
       have r₂ := eq21471 (N.op y (N.op z x))
       grind)
    | exact resolve eq23704 eq21471
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq23704
  have eq26440 : x ≠ (N.op y x) ∨ x = z := by grind
  clear eq26437
  have eq27349 : x ≠ x ∨ x = z ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq26440
       have i₂ := eq21471 y
       grind)
    | exact superpose eq21471 eq26440
    | (have j1 := eq21471 z
       grind)
    | (have r₁ := eq26440
       have r₂ := eq21471 y
       grind)
    | (have r₁ := eq26440
       have r₂ := eq21471 (N.op y x)
       grind)
    | exact resolve eq26440 eq21471
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21471
  have eq27353 : x ≠ x ∨ x = z ∨ x = y := by grind
  clear eq27349
  have eq27354 : x = z ∨ x = y := by grind
  clear eq27353
  have eq28768 : x ≠ (N.op y (N.op x x)) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq2770
       have i₂ := eq27354
       grind)
    | exact superpose eq27354 eq2770
    | exact resolve eq2770 eq27354
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2770
  have eq28769 : x ≠ (N.op y (N.op x x)) ∨ x = y := by grind
  clear eq28768
  have eq29532 : x ≠ (N.op y x) ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq28769
       have i₂ := eq2202
       grind)
    | exact superpose eq2202 eq28769
    | exact resolve eq28769 eq2202
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2202 eq28769
  have eq29537 : y = z ∨ x = y := by
    first
    | (have j1 := eq2203 y
       grind)
    | (have r₁ := eq29532
       have r₂ := eq2203 y
       grind)
    | (have r₁ := eq29532
       have r₂ := eq2203 (N.op y x)
       grind)
    | exact resolve eq29532 eq2203
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2203 eq29532
  have eq31076 : x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq27354
       have i₂ := eq29537
       grind)
    | exact superpose eq29537 eq27354
    | exact resolve eq27354 eq29537
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq27354 eq29537
  have eq31077 : x = y := by grind
  clear eq31076
  have eq32326 : x ≠ x ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1574
       have i₂ := eq31077
       grind)
    | exact superpose eq31077 eq1574
    | (have r₁ := eq1574
       have r₂ := eq31077
       grind)
    | exact resolve eq1574 eq31077
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1574
  have eq32333 : x ≠ (N.op x x) ∨ x = z := by
    first
    | (have i₁ := eq26440
       have i₂ := eq31077
       grind)
    | exact superpose eq31077 eq26440
    | exact resolve eq26440 eq31077
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26440
  have eq32335 : x = (M.op x x) := by grind
  clear eq32326
  have eq32338 : x = z := by
    first
    | (have r₁ := eq32333
       have r₂ := eq21506
       grind)
    | exact resolve eq32333 eq21506
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21506 eq32333
  have eq35499 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq32335
       grind)
    | exact superpose eq32335 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq32335
       grind)
    | exact resolve eq7 eq32335
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32335
  have eq35500 : x = (N.op x x) := by grind
  clear eq35499
  have eq42223 : x ≠ (N.op y (N.op z x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq35500
       grind)
    | exact superpose eq35500 eq10
    | exact resolve eq10 eq35500
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq42232 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq42223
       have i₂ := eq32338
       grind)
    | exact superpose eq32338 eq42223
    | exact resolve eq42223 eq32338
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32338 eq42223
  have eq42234 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq42232
       have i₂ := eq35500
       grind)
    | exact superpose eq35500 eq42232
    | exact resolve eq42232 eq35500
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq42232
  have eq42236 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq42234
       have i₂ := eq31077
       grind)
    | exact superpose eq31077 eq42234
    | exact resolve eq42234 eq31077
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq31077 eq42234
  have eq42238 : False := by grind
  exact eq42238

/-- `Equation4272` from `Equation2355`: `x = (y ◇ (y ◇ (z ◇ z))) ◇ x` ⊢ `x ◇ (x ◇ x) = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation4272_StructuralFromFin_Equation2355 : Law4272.StructuralFromFin Law2355 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2355 (fun {G} M hM ↦ ?_)
  have hm := (@Law2355.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law4272 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law4272.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X1 (M.op X2 X2))) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x (N.op x x)) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op (M.op (M.op X0 (M.op X0 (M.op X1 X1))) (M.op X2 X2)) X3) = X3 := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X3 (M.op X0 (M.op X0 (M.op X1 X1))) X2
       have i₂ := eq6 (M.op (M.op X0 (M.op X0 (M.op X1 X1))) (M.op X2 X2)) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq15 : ∀ X2 X3 : G, (M.op (M.op X2 X2) X3) = X3 := by
    intro X2 X3
    first
    | (have i₁ := eq13 x x X2 X3
       have i₂ := eq6 (M.op X2 X2) x x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq19 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq15 X1 X0
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op x x) (M.op x x)
       have r₂ := eq15 x (M.op x x)
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq27 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq19 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq29 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op x x)
       have i₂ := eq15 x x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq30 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq29 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29
  have eq36 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq38 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq36
  have eq86 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq38
       have i₂ := eq30 y x
       grind)
    | exact superpose eq30 eq38
    | (have j1 := eq30 x x
       grind)
    | exact resolve eq38 eq30
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq88 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq30 y x
       grind)
    | (have r₁ := eq86
       have r₂ := eq30 x x
       grind)
    | (have r₁ := eq86
       have r₂ := eq30 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq86 eq30
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq86
  have eq111 : (N.op x y) ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq38
       have i₂ := eq88
       grind)
    | exact superpose eq88 eq38
    | exact resolve eq38 eq88
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq38
  have eq119 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq27 x
       have i₂ := eq88
       grind)
    | exact superpose eq88 eq27
    | exact resolve eq27 eq88
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq120 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq30 X0 x
       have i₂ := eq88
       grind)
    | exact superpose eq88 eq30
    | (have j0 := eq30 x x
       grind)
    | exact resolve eq30 eq88
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq121 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq88
  have eq122 : (N.op x y) ≠ (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq111
       have r₂ := eq121
       grind)
    | exact resolve eq111 eq121
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq111
  have eq415 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq122
       have i₂ := eq119
       grind)
    | exact superpose eq119 eq122
    | exact resolve eq122 eq119
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq119 eq122
  have eq418 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq415
  have eq636 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq418
       have i₂ := eq120 x
       grind)
    | exact superpose eq120 eq418
    | (have j1 := eq120 x
       grind)
    | (have r₁ := eq418
       have r₂ := eq120 x
       grind)
    | (have r₁ := eq418
       have r₂ := eq120 (N.op x y)
       grind)
    | exact resolve eq418 eq120
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq120 eq418
  have eq637 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq636
  have eq638 : x = (M.op x x) ∨ x = y := by grind
  clear eq637
  have eq641 : x = (M.op x x) := by
    first
    | (have r₁ := eq638
       have r₂ := eq121
       grind)
    | exact resolve eq638 eq121
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq121 eq638
  have eq890 : x = (N.op x x) := by
    first
    | (have i₁ := eq27 x
       have i₂ := eq641
       grind)
    | exact superpose eq641 eq27
    | exact resolve eq27 eq641
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq27
  have eq891 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq30 X0 x
       have i₂ := eq641
       grind)
    | exact superpose eq641 eq30
    | exact resolve eq30 eq641
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30 eq641
  have eq1478 : (N.op x x) ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq890
       grind)
    | exact superpose eq890 eq10
    | exact resolve eq10 eq890
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1483 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq1478
       have i₂ := eq890
       grind)
    | exact superpose eq890 eq1478
    | exact resolve eq1478 eq890
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq890 eq1478
  have eq2609 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq1483
       have i₂ := eq891 y
       grind)
    | exact superpose eq891 eq1483
    | (have j1 := eq891 y
       grind)
    | (have r₁ := eq1483
       have r₂ := eq891 y
       grind)
    | (have r₁ := eq1483
       have r₂ := eq891 (N.op y x)
       grind)
    | exact resolve eq1483 eq891
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq891 eq1483
  have eq2610 : x = y := by grind
  clear eq2609
  have eq2875 : (N.op x (N.op x x)) ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq2610
       grind)
    | exact superpose eq2610 eq10
    | exact resolve eq10 eq2610
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2610
  have eq2891 : False := by grind
  exact eq2891
