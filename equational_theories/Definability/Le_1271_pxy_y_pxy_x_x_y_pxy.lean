import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation1271` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation1271` is `x = x ◇ (((y ◇ z) ◇ w) ◇ y)`, and the companion is
`x □ y = if (M.op a b) = b then a else if (M.op a b) = a then b else (M.op a b)`.  Its automorphism half is `GuardAut2_pxy_y_pxy_x_x_y_pxy_Equation1271`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches none of these on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation312` from `Equation1271`: `x = x ◇ (((y ◇ z) ◇ w) ◇ y)` ⊢ `x ◇ x = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op a b) = b then a else if (M.op a b) = a then b else (M.op a b)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation312_StructuralFromFin_Equation1271 : Law312.StructuralFromFin Law1271 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1))
    GuardAut2_pxy_y_pxy_x_x_y_pxy_Equation1271 (fun {G} M hM ↦ ?_)
  have hm := (@Law1271.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op a b) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) = a ∨ N.op a b = (M.op a b)) → @satisfies _ G N Law312 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law312.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 (M.op (M.op (M.op X1 X2) X3) X1)) = X0 := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (M.op X0 X1) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 : G, (M.op X2 (M.op (M.op X0 X1) X0)) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X2 X0 X1 (M.op (M.op (M.op x x) x) x)
       have i₂ := eq6 (M.op X0 X1) x x x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 (M.op X0 X0)) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq12 X0 (M.op (M.op x x) x) X1
       have i₂ := eq12 x x X0
       grind)
    | exact superpose eq12 eq12
    | exact resolve eq12 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq27 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq29 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq27
  have eq34 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq18 X0 X1
       grind)
    | exact superpose eq18 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq18 X0 X1
       grind)
    | exact resolve eq8 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq36 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq40 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq34 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34
  have eq223 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq36 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36
  have eq478 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq29
       have i₂ := eq40 y x
       grind)
    | exact superpose eq40 eq29
    | (have j1 := eq40 x x
       grind)
    | (have r₁ := eq29
       have r₂ := eq40 y x
       grind)
    | (have r₁ := eq29
       have r₂ := eq40 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq29 eq40
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq479 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq478
  have eq593 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq29
       have i₂ := eq479
       grind)
    | exact superpose eq479 eq29
    | exact resolve eq29 eq479
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29
  have eq612 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq223 x
       have i₂ := eq479
       grind)
    | exact superpose eq479 eq223
    | exact resolve eq223 eq479
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq613 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq479
  have eq614 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq593
       have r₂ := eq612
       grind)
    | exact resolve eq593 eq612
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq593 eq612
  have eq615 : x = (M.op x x) := by
    first
    | (have r₁ := eq614
       have r₂ := eq613
       grind)
    | exact resolve eq614 eq613
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq613 eq614
  have eq757 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq40 X0 x
       have i₂ := eq615
       grind)
    | exact superpose eq615 eq40
    | exact resolve eq40 eq615
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq40
  have eq759 : x = (N.op x x) := by
    first
    | (have i₁ := eq223 x
       have i₂ := eq615
       grind)
    | exact superpose eq615 eq223
    | exact resolve eq223 eq615
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq223 eq615
  have eq1011 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq759
       grind)
    | exact superpose eq759 eq10
    | exact resolve eq10 eq759
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1589 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq1011
       have i₂ := eq757 y
       grind)
    | exact superpose eq757 eq1011
    | (have j1 := eq757 y
       grind)
    | (have r₁ := eq1011
       have r₂ := eq757 y
       grind)
    | (have r₁ := eq1011
       have r₂ := eq757 (N.op y x)
       grind)
    | exact resolve eq1011 eq757
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq757
  have eq1590 : x = y := by grind
  clear eq1589
  have eq1709 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq1011
       have i₂ := eq1590
       grind)
    | exact superpose eq1590 eq1011
    | exact resolve eq1011 eq1590
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1011 eq1590
  have eq1714 : False := by grind
  exact eq1714

/-- `Equation3268` from `Equation1271`: `x = x ◇ (((y ◇ z) ◇ w) ◇ y)` ⊢ `x ◇ x = y ◇ (x ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op a b) = b then a else if (M.op a b) = a then b else (M.op a b)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3268_StructuralFromFin_Equation1271 : Law3268.StructuralFromFin Law1271 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1))
    GuardAut2_pxy_y_pxy_x_x_y_pxy_Equation1271 (fun {G} M hM ↦ ?_)
  have hm := (@Law1271.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op a b) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) = a ∨ N.op a b = (M.op a b)) → @satisfies _ G N Law3268 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3268.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 (M.op (M.op (M.op X1 X2) X3) X1)) = X0 := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (M.op X0 X1) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 : G, (M.op X2 (M.op (M.op X0 X1) X0)) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X2 X0 X1 (M.op (M.op (M.op x x) x) x)
       have i₂ := eq6 (M.op X0 X1) x x x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq16 : ∀ X0 X1 : G, (M.op X1 (M.op X0 X0)) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq12 X0 (M.op (M.op x x) x) X1
       have i₂ := eq12 x x X0
       grind)
    | exact superpose eq12 eq12
    | exact resolve eq12 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq28 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq16 X1 X0
       grind)
    | exact superpose eq16 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq16 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq16
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq34 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq16 X0 X1
       grind)
    | exact superpose eq16 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq16 X0 X1
       grind)
    | exact resolve eq8 eq16
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq16
  have eq35 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq34 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34
  have eq55 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq58 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq55
  have eq178 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq28 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq28
  have eq486 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq58
       have i₂ := eq35 x x
       grind)
    | exact superpose eq35 eq58
    | (have j1 := eq35 x x
       grind)
    | (have r₁ := eq58
       have r₂ := eq35 (N.op y (N.op x (M.op x x))) x
       grind)
    | exact resolve eq58 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq58
  have eq487 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq486
  have eq601 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq487
       have i₂ := eq35 y x
       grind)
    | exact superpose eq35 eq487
    | (have j1 := eq35 x x
       grind)
    | (have r₁ := eq487
       have r₂ := eq35 y x
       grind)
    | (have r₁ := eq487
       have r₂ := eq35 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq487 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq603 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq601
  have eq727 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq487
       have i₂ := eq603
       grind)
    | exact superpose eq603 eq487
    | exact resolve eq487 eq603
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq487
  have eq748 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq178 x
       have i₂ := eq603
       grind)
    | exact superpose eq603 eq178
    | exact resolve eq178 eq603
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq749 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq603
  have eq751 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq727
       have r₂ := eq748
       grind)
    | exact resolve eq727 eq748
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq727 eq748
  have eq752 : x = (M.op x x) := by
    first
    | (have r₁ := eq751
       have r₂ := eq749
       grind)
    | exact resolve eq751 eq749
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq749 eq751
  have eq800 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq752
       grind)
    | exact superpose eq752 eq35
    | exact resolve eq35 eq752
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq35
  have eq802 : x = (N.op x x) := by
    first
    | (have i₁ := eq178 x
       have i₂ := eq752
       grind)
    | exact superpose eq752 eq178
    | exact resolve eq178 eq752
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq178 eq752
  have eq1102 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq802
       grind)
    | exact superpose eq802 eq10
    | exact resolve eq10 eq802
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1107 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq1102
       have i₂ := eq802
       grind)
    | exact superpose eq802 eq1102
    | exact resolve eq1102 eq802
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1102
  have eq1599 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq1107
       have i₂ := eq800 y
       grind)
    | exact superpose eq800 eq1107
    | (have j1 := eq800 y
       grind)
    | (have r₁ := eq1107
       have r₂ := eq800 y
       grind)
    | (have r₁ := eq1107
       have r₂ := eq800 (N.op y x)
       grind)
    | exact resolve eq1107 eq800
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq800
  have eq1600 : x = y := by grind
  clear eq1599
  have eq1614 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq1107
       have i₂ := eq1600
       grind)
    | exact superpose eq1600 eq1107
    | exact resolve eq1107 eq1600
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1107 eq1600
  have eq1616 : False := by grind
  exact eq1616

/-- `Equation3288` from `Equation1271`: `x = x ◇ (((y ◇ z) ◇ w) ◇ y)` ⊢ `x ◇ x = y ◇ (z ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op a b) = b then a else if (M.op a b) = a then b else (M.op a b)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3288_StructuralFromFin_Equation1271 : Law3288.StructuralFromFin Law1271 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1))
    GuardAut2_pxy_y_pxy_x_x_y_pxy_Equation1271 (fun {G} M hM ↦ ?_)
  have hm := (@Law1271.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op a b) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) = a ∨ N.op a b = (M.op a b)) → @satisfies _ G N Law3288 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3288.models_iff G N]
  intro x y z
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 (M.op (M.op (M.op X1 X2) X3) X1)) = X0 := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (M.op X0 X1) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op z (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 : G, (M.op X2 (M.op (M.op X0 X1) X0)) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X2 X0 X1 (M.op (M.op (M.op x x) x) x)
       have i₂ := eq6 (M.op X0 X1) x x x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq16 : ∀ X0 X1 : G, (M.op X1 (M.op X0 X0)) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq12 X0 (M.op (M.op x x) x) X1
       have i₂ := eq12 x x X0
       grind)
    | exact superpose eq12 eq12
    | exact resolve eq12 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq28 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq16 X1 X0
       grind)
    | exact superpose eq16 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq16 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq16
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq34 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq16 X0 X1
       grind)
    | exact superpose eq16 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq16 X0 X1
       grind)
    | exact resolve eq8 eq16
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq35 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq34 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34
  have eq55 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq58 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq55
  have eq178 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq28 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq28
  have eq486 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ z = (M.op x x) := by
    first
    | (have i₁ := eq58
       have i₂ := eq35 z x
       grind)
    | exact superpose eq35 eq58
    | (have j1 := eq35 x x
       grind)
    | (have r₁ := eq58
       have r₂ := eq35 (N.op y (N.op z (M.op x x))) x
       grind)
    | exact resolve eq58 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2708 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ z = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq486
       have i₂ := eq35 y x
       grind)
    | exact superpose eq35 eq486
    | (have j1 := eq35 x x
       grind)
    | (have r₁ := eq486
       have r₂ := eq35 y x
       grind)
    | (have r₁ := eq486
       have r₂ := eq35 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq486 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2710 : z = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq2708
  have eq2714 : z ≠ (N.op y (N.op z z)) ∨ x = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq58
       have i₂ := eq2710
       grind)
    | exact superpose eq2710 eq58
    | exact resolve eq58 eq2710
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2740 : ∀ X0 : G, y = (M.op x x) ∨ (M.op X0 z) = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq16 x X0
       have i₂ := eq2710
       grind)
    | exact superpose eq2710 eq16
    | exact resolve eq16 eq2710
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq16
  have eq2744 : ∀ X0 : G, z = (N.op X0 z) ∨ z = X0 ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq2710
       grind)
    | exact superpose eq2710 eq35
    | (have j0 := eq35 x x
       grind)
    | exact resolve eq35 eq2710
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2752 : x ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  have eq2753 : y ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq2710
  have eq2754 : z ≠ (N.op y (N.op z z)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have r₁ := eq2714
       have r₂ := eq2752
       grind)
    | exact resolve eq2714 eq2752
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2714 eq2752
  have eq2957 : ∀ X0 : G, y ≠ (N.op y y) ∨ x = y ∨ y = z ∨ (M.op X0 z) = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq486
       have i₂ := eq2740 X0
       grind)
    | exact superpose eq2740 eq486
    | (have j1 := eq2740 X0
       grind)
    | exact resolve eq486 eq2740
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2969 : ∀ X0 : G, y = (N.op y y) ∨ (M.op X0 z) = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq178 x
       have i₂ := eq2740 X0
       grind)
    | exact superpose eq2740 eq178
    | (have j1 := eq2740 X0
       grind)
    | exact resolve eq178 eq2740
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq3146 : ∀ X0 : G, x ≠ y ∨ (M.op X0 z) = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq2740 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2740
  have eq3149 : ∀ X0 : G, x = y ∨ y = z ∨ (M.op X0 z) = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq2957 X0
       have j1 := eq2969 X0
       grind)
    | (have r₁ := eq2957 X0
       have r₂ := eq2969 X0
       grind)
    | exact resolve eq2957 eq2969
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2957 eq2969
  have eq3150 : ∀ X0 : G, x = (M.op x x) ∨ (M.op X0 z) = X0 ∨ y = z := by
    intro X0
    first
    | (have j0 := eq3149 X0
       have j1 := eq3146 X0
       grind)
    | (have r₁ := eq3149 X0
       have r₂ := eq3146 X0
       grind)
    | exact resolve eq3149 eq3146
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3146 eq3149
  have eq3820 : ∀ X0 : G, x ≠ x ∨ x = (N.op x x) ∨ (M.op X0 z) = X0 ∨ y = z := by
    intro X0
    first
    | (have i₁ := eq7 x x
       have i₂ := eq3150 X0
       grind)
    | exact superpose eq3150 eq7
    | (have j0 := eq7 x x
       have j1 := eq3150 X0
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq3150 X0
       grind)
    | (have r₁ := eq7 z z
       have r₂ := eq3150 z
       grind)
    | exact resolve eq7 eq3150
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq3857 : z = (N.op z z) ∨ x = (M.op x x) ∨ y = z := by
    first
    | (have i₁ := eq178 z
       have i₂ := eq3150 z
       grind)
    | exact superpose eq3150 eq178
    | exact resolve eq178 eq3150
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3150
  have eq4012 : ∀ X0 : G, x = (N.op x x) ∨ (M.op X0 z) = X0 ∨ y = z := by
    intro X0
    first
    | (have j0 := eq3820 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3820
  have eq5672 : ∀ X0 : G, z ≠ X0 ∨ (N.op X0 z) = X0 ∨ x = (N.op x x) ∨ y = z := by
    intro X0
    first
    | (have i₁ := eq7 X0 z
       have i₂ := eq4012 X0
       grind)
    | exact superpose eq4012 eq7
    | (have j0 := eq7 X0 z
       grind)
    | (have r₁ := eq7 z z
       have r₂ := eq4012 z
       grind)
    | exact resolve eq7 eq4012
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4012
  have eq23286 : z = (N.op z z) ∨ x = (N.op x x) ∨ y = z := by
    first
    | (have j0 := eq5672 z
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5672
  have eq27098 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ x = (M.op x x) ∨ y = z := by
    first
    | (have i₁ := eq2754
       have i₂ := eq3857
       grind)
    | exact superpose eq3857 eq2754
    | exact resolve eq2754 eq3857
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2754 eq3857
  have eq27108 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq27098
  have eq27113 : x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by
    first
    | (have j1 := eq2744 y
       grind)
    | (have r₁ := eq27108
       have r₂ := eq2744 y
       grind)
    | (have r₁ := eq27108
       have r₂ := eq2744 (N.op y z)
       grind)
    | exact resolve eq27108 eq2744
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2744 eq27108
  have eq27117 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq27113
       have r₂ := eq2753
       grind)
    | exact resolve eq27113 eq2753
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2753 eq27113
  have eq27845 : y ≠ (N.op y (N.op z y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq58
       have i₂ := eq27117
       grind)
    | exact superpose eq27117 eq58
    | exact resolve eq58 eq27117
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq58
  have eq27847 : y ≠ (N.op y y) ∨ x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq486
       have i₂ := eq27117
       grind)
    | exact superpose eq27117 eq486
    | exact resolve eq486 eq27117
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq486
  have eq27864 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq178 x
       have i₂ := eq27117
       grind)
    | exact superpose eq27117 eq178
    | exact resolve eq178 eq27117
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq178
  have eq27927 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq27117
  have eq27928 : x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq27847
       have r₂ := eq27864
       grind)
    | exact resolve eq27847 eq27864
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq27847
  have eq27929 : y ≠ (N.op y (N.op z y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq27845
       have r₂ := eq27927
       grind)
    | exact resolve eq27845 eq27927
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq27845
  have eq27930 : x = (M.op x x) ∨ y = z := by
    first
    | (have r₁ := eq27928
       have r₂ := eq27927
       grind)
    | exact resolve eq27928 eq27927
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq27928
  have eq29335 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ y = z := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq27930
       grind)
    | exact superpose eq27930 eq35
    | exact resolve eq35 eq27930
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq29344 : x ≠ x ∨ x = (N.op x x) ∨ y = z := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq27930
       grind)
    | exact superpose eq27930 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq27930
       grind)
    | exact resolve eq7 eq27930
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq27930
  have eq29405 : x = (N.op x x) ∨ y = z := by grind
  clear eq29344
  have eq31501 : x ≠ (N.op y (N.op z x)) ∨ y = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq29405
       grind)
    | exact superpose eq29405 eq10
    | exact resolve eq10 eq29405
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29405
  have eq41096 : x ≠ (N.op y x) ∨ y = z ∨ x = z ∨ y = z := by
    first
    | (have i₁ := eq31501
       have i₂ := eq29335 z
       grind)
    | exact superpose eq29335 eq31501
    | (have j1 := eq29335 z
       grind)
    | (have r₁ := eq31501
       have r₂ := eq29335 (N.op y (N.op z x))
       grind)
    | exact resolve eq31501 eq29335
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq41099 : x ≠ (N.op y x) ∨ y = z ∨ x = z := by grind
  clear eq41096
  have eq41301 : x ≠ x ∨ y = z ∨ x = z ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq41099
       have i₂ := eq29335 y
       grind)
    | exact superpose eq29335 eq41099
    | (have j1 := eq29335 z
       grind)
    | (have r₁ := eq41099
       have r₂ := eq29335 y
       grind)
    | (have r₁ := eq41099
       have r₂ := eq29335 (N.op y x)
       grind)
    | exact resolve eq41099 eq29335
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29335 eq41099
  have eq41306 : x ≠ x ∨ y = z ∨ x = z ∨ x = y := by grind
  clear eq41301
  have eq41307 : y = z ∨ x = z ∨ x = y := by grind
  clear eq41306
  have eq47437 : y ≠ (N.op y (N.op y y)) ∨ x = (M.op x x) ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq27929
       have i₂ := eq41307
       grind)
    | exact superpose eq41307 eq27929
    | exact resolve eq27929 eq41307
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq27929 eq41307
  have eq47440 : y ≠ (N.op y (N.op y y)) ∨ x = (M.op x x) ∨ x = z := by
    first
    | (have r₁ := eq47437
       have r₂ := eq27927
       grind)
    | exact resolve eq47437 eq27927
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq47437
  have eq141645 : y ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq47440
       have i₂ := eq27864
       grind)
    | exact superpose eq27864 eq47440
    | exact resolve eq47440 eq27864
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq47440
  have eq141651 : y ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = z := by grind
  clear eq141645
  have eq141654 : x = (M.op x x) ∨ x = z := by
    first
    | (have r₁ := eq141651
       have r₂ := eq27864
       grind)
    | exact resolve eq141651 eq27864
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq27864 eq141651
  have eq142568 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq141654
       grind)
    | exact superpose eq141654 eq35
    | exact resolve eq35 eq141654
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq142583 : x ≠ x ∨ x = (N.op x x) ∨ x = z := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq141654
       grind)
    | exact superpose eq141654 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq141654
       grind)
    | exact resolve eq7 eq141654
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq141654
  have eq142612 : x = (N.op x x) ∨ x = z := by grind
  clear eq142583
  have eq149433 : x ≠ (N.op y (N.op z x)) ∨ x = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq142612
       grind)
    | exact superpose eq142612 eq10
    | exact resolve eq10 eq142612
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq142612
  have eq153043 : x ≠ (N.op y x) ∨ x = z ∨ x = z ∨ x = z := by
    first
    | (have i₁ := eq149433
       have i₂ := eq142568 z
       grind)
    | exact superpose eq142568 eq149433
    | (have j1 := eq142568 z
       grind)
    | (have r₁ := eq149433
       have r₂ := eq142568 (N.op y (N.op z x))
       grind)
    | exact resolve eq149433 eq142568
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq149433
  have eq153054 : x ≠ (N.op y x) ∨ x = z := by grind
  clear eq153043
  have eq153770 : x ≠ x ∨ x = z ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq153054
       have i₂ := eq142568 y
       grind)
    | exact superpose eq142568 eq153054
    | (have j1 := eq142568 z
       grind)
    | (have r₁ := eq153054
       have r₂ := eq142568 y
       grind)
    | (have r₁ := eq153054
       have r₂ := eq142568 (N.op y x)
       grind)
    | exact resolve eq153054 eq142568
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq142568
  have eq153772 : x ≠ x ∨ x = z ∨ x = y := by grind
  clear eq153770
  have eq153773 : x = z ∨ x = y := by grind
  clear eq153772
  have eq157384 : x = (N.op x x) ∨ x = (N.op x x) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq23286
       have i₂ := eq153773
       grind)
    | exact superpose eq153773 eq23286
    | exact resolve eq23286 eq153773
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq23286
  have eq157387 : x ≠ (N.op y (N.op x x)) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq31501
       have i₂ := eq153773
       grind)
    | exact superpose eq153773 eq31501
    | exact resolve eq31501 eq153773
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq31501 eq153773
  have eq157391 : x ≠ (N.op y (N.op x x)) ∨ x = y := by grind
  clear eq157387
  have eq157392 : x = (N.op x x) ∨ x = y := by grind
  clear eq157384
  have eq161054 : x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = y := by
    first
    | (have i₁ := eq9 x x
       have i₂ := eq157392
       grind)
    | exact superpose eq157392 eq9
    | (have j0 := eq9 x x
       grind)
    | exact resolve eq9 eq157392
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq157392
  have eq161055 : x = (M.op x x) ∨ x = y := by grind
  clear eq161054
  have eq161057 : x = (M.op x x) := by
    first
    | (have r₁ := eq161055
       have r₂ := eq27927
       grind)
    | exact resolve eq161055 eq27927
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq27927 eq161055
  have eq161767 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq161057
       grind)
    | exact superpose eq161057 eq35
    | exact resolve eq35 eq161057
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq35
  have eq161782 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq161057
       grind)
    | exact superpose eq161057 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq161057
       grind)
    | exact resolve eq7 eq161057
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq161057
  have eq161811 : x = (N.op x x) := by grind
  clear eq161782
  have eq163861 : x ≠ (N.op y (N.op z x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq161811
       grind)
    | exact superpose eq161811 eq10
    | exact resolve eq10 eq161811
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq173515 : x ≠ (N.op y x) ∨ x = y := by
    first
    | (have i₁ := eq157391
       have i₂ := eq161811
       grind)
    | exact superpose eq161811 eq157391
    | exact resolve eq157391 eq161811
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq157391
  have eq173519 : x = y := by
    first
    | (have j1 := eq161767 y
       grind)
    | (have r₁ := eq173515
       have r₂ := eq161767 y
       grind)
    | (have r₁ := eq173515
       have r₂ := eq161767 (N.op y x)
       grind)
    | exact resolve eq173515 eq161767
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq161767 eq173515
  have eq174756 : x ≠ (N.op x x) ∨ x = z := by
    first
    | (have i₁ := eq153054
       have i₂ := eq173519
       grind)
    | exact superpose eq173519 eq153054
    | exact resolve eq153054 eq173519
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq153054
  have eq174764 : x = z := by
    first
    | (have r₁ := eq174756
       have r₂ := eq161811
       grind)
    | exact resolve eq174756 eq161811
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq174756
  have eq175642 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq163861
       have i₂ := eq174764
       grind)
    | exact superpose eq174764 eq163861
    | exact resolve eq163861 eq174764
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq163861 eq174764
  have eq175646 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq175642
       have i₂ := eq161811
       grind)
    | exact superpose eq161811 eq175642
    | exact resolve eq175642 eq161811
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq175642
  have eq175651 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq175646
       have i₂ := eq173519
       grind)
    | exact superpose eq173519 eq175646
    | exact resolve eq175646 eq173519
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq173519 eq175646
  have eq175656 : False := by grind
  exact eq175656

/-- `Equation4272` from `Equation1271`: `x = x ◇ (((y ◇ z) ◇ w) ◇ y)` ⊢ `x ◇ (x ◇ x) = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op a b) = b then a else if (M.op a b) = a then b else (M.op a b)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation4272_StructuralFromFin_Equation1271 : Law4272.StructuralFromFin Law1271 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1))
    GuardAut2_pxy_y_pxy_x_x_y_pxy_Equation1271 (fun {G} M hM ↦ ?_)
  have hm := (@Law1271.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op a b) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) = a ∨ N.op a b = (M.op a b)) → @satisfies _ G N Law4272 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law4272.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 (M.op (M.op (M.op X1 X2) X3) X1)) = X0 := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (M.op X0 X1) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x (N.op x x)) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 : G, (M.op X2 (M.op (M.op X0 X1) X0)) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X2 X0 X1 (M.op (M.op (M.op x x) x) x)
       have i₂ := eq6 (M.op X0 X1) x x x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq16 : ∀ X0 X1 : G, (M.op X1 (M.op X0 X0)) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq12 X0 (M.op (M.op x x) x) X1
       have i₂ := eq12 x x X0
       grind)
    | exact superpose eq12 eq12
    | exact resolve eq12 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq28 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq16 X1 X0
       grind)
    | exact superpose eq16 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq16 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq16
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq34 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq16 X0 X1
       grind)
    | exact superpose eq16 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq16 X0 X1
       grind)
    | exact resolve eq8 eq16
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq16
  have eq35 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq34 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34
  have eq55 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq57 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq55
  have eq176 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq28 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq28
  have eq592 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq57
       have i₂ := eq35 y x
       grind)
    | exact superpose eq35 eq57
    | (have j1 := eq35 x x
       grind)
    | exact resolve eq57 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq595 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq35 y x
       grind)
    | (have r₁ := eq592
       have r₂ := eq35 x x
       grind)
    | (have r₁ := eq592
       have r₂ := eq35 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq592 eq35
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq592
  have eq719 : (N.op x y) ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq57
       have i₂ := eq595
       grind)
    | exact superpose eq595 eq57
    | exact resolve eq57 eq595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq57
  have eq737 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq595
       grind)
    | exact superpose eq595 eq35
    | (have j0 := eq35 x x
       grind)
    | exact resolve eq35 eq595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq739 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq176 x
       have i₂ := eq595
       grind)
    | exact superpose eq595 eq176
    | exact resolve eq176 eq595
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq176
  have eq740 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq595
  have eq741 : (N.op x y) ≠ (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq719
       have r₂ := eq740
       grind)
    | exact resolve eq719 eq740
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq719
  have eq1959 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq741
       have i₂ := eq739
       grind)
    | exact superpose eq739 eq741
    | exact resolve eq741 eq739
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq739 eq741
  have eq1962 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq1959
  have eq4408 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1962
       have i₂ := eq737 x
       grind)
    | exact superpose eq737 eq1962
    | (have j1 := eq737 x
       grind)
    | (have r₁ := eq1962
       have r₂ := eq737 x
       grind)
    | (have r₁ := eq1962
       have r₂ := eq737 (N.op x y)
       grind)
    | exact resolve eq1962 eq737
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq737 eq1962
  have eq4409 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq4408
  have eq4410 : x = (M.op x x) ∨ x = y := by grind
  clear eq4409
  have eq4413 : x = (M.op x x) := by
    first
    | (have r₁ := eq4410
       have r₂ := eq740
       grind)
    | exact resolve eq4410 eq740
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq740 eq4410
  have eq4814 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq35 X0 x
       have i₂ := eq4413
       grind)
    | exact superpose eq4413 eq35
    | exact resolve eq35 eq4413
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq35
  have eq4819 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq4413
       grind)
    | exact superpose eq4413 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq4413
       grind)
    | exact resolve eq7 eq4413
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4413
  have eq4845 : x = (N.op x x) := by grind
  clear eq4819
  have eq5955 : (N.op x x) ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq4845
       grind)
    | exact superpose eq4845 eq10
    | exact resolve eq10 eq4845
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq5960 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq5955
       have i₂ := eq4845
       grind)
    | exact superpose eq4845 eq5955
    | exact resolve eq5955 eq4845
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4845 eq5955
  have eq8028 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq5960
       have i₂ := eq4814 y
       grind)
    | exact superpose eq4814 eq5960
    | (have j1 := eq4814 y
       grind)
    | (have r₁ := eq5960
       have r₂ := eq4814 y
       grind)
    | (have r₁ := eq5960
       have r₂ := eq4814 (N.op y x)
       grind)
    | exact resolve eq5960 eq4814
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4814 eq5960
  have eq8030 : x = y := by grind
  clear eq8028
  have eq8228 : (N.op x (N.op x x)) ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq8030
       grind)
    | exact superpose eq8030 eq10
    | exact resolve eq10 eq8030
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8030
  have eq8253 : False := by grind
  exact eq8253
