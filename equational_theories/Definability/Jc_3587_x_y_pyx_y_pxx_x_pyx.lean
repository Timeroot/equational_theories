import equational_theories.Definability.AutBox2
import equational_theories.Definability.Kax_13

/-!
# Structurality from `Equation3587` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3587` is `x ◇ y = z ◇ ((x ◇ y) ◇ y)`, and the companion is
`x □ y = if a = b then (M.op a a) else if (M.op b a) = b then a else (M.op b a)`.  Its automorphism half is `GuardAut2_x_y_pyx_y_pxx_x_pyx_Equation3587`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches it on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation3318` from `Equation3587`: `x ◇ y = z ◇ ((x ◇ y) ◇ y)` ⊢ `x ◇ y = x ◇ (y ◇ (y ◇ x))`, through the
companion `x □ y = if a = b then (M.op a a) else if (M.op b a) = b then a else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3318_StructuralFromFin_Equation3587 : Law3318.StructuralFromFin Law3587 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_x_y_pyx_y_pxx_x_pyx_Equation3587 (fun {G} M hM ↦ ?_)
  have hm := (@Law3587.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, a ≠ b ∨ N.op a b = (M.op a a)) →
      (∀ a b : G, a = b ∨ (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, a = b ∨ (M.op b a) = b ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3318 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (b)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3318.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X0 X1) X1)) := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, X0 ≠ X1 ∨ (N.op X0 X1) = (M.op X0 X0) := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ X0 = X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (N.op X0 X1) = (M.op X1 X0) ∨ (M.op X1 X0) = X1 ∨ X0 = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x y) ≠ (N.op x (N.op y (N.op y x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op X2 (M.op (M.op X0 X1) X1)) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X1) X1) X3
       have i₂ := eq6 X0 X1 (M.op X2 (M.op (M.op X0 X1) X1))
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X3 : G, (M.op X0 X1) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X3
    first
    | (have i₁ := eq13 X0 X1 x X3
       have i₂ := eq6 X0 X1 x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq19 : ∀ X0 X1 : G, (M.op X0 X1) = (M.op (M.op X0 X1) X1) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 X1 x
       have i₂ := eq14 (M.op X0 X1) X1 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq36 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X0 X1) = X1 ∨ (N.op X1 (M.op X0 X1)) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X1)
       have i₂ := eq19 X0 X1
       grind)
    | exact superpose eq19 eq8
    | (have j0 := eq8 (M.op X0 X1) X1
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X1)
       have r₂ := eq19 X0 X1
       grind)
    | exact resolve eq8 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq37 : ∀ X0 X1 : G, (N.op X1 (M.op X0 X1)) = X1 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq36 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36
  have eq43 : (N.op x y) ≠ (N.op x (N.op y (M.op x y))) ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y x
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op x y) (N.op x (N.op y (N.op y x)))
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op x (N.op y (N.op y x))) (N.op x y)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq44 : (N.op x y) ≠ (N.op x (M.op (N.op y x) y)) ∨ (N.op y x) = (M.op (N.op y x) y) ∨ y = (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y (N.op y x)
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y (N.op y x)
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op x y) (N.op x (N.op y (N.op y x)))
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op x (N.op y (N.op y x))) (N.op x y)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq45 : (N.op x y) ≠ (M.op (N.op y (N.op y x)) x) ∨ (N.op y (N.op y x)) = (M.op (N.op y (N.op y x)) x) ∨ x = (N.op y (N.op y x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x (N.op y (N.op y x))
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x (N.op y (N.op y x))
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op x y) (N.op x (N.op y (N.op y x)))
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op x (N.op y (N.op y x))) (N.op x y)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq48 : (N.op x y) ≠ (N.op x y) ∨ x = (M.op x y) ∨ x = y ∨ y = (M.op x y) := by
    first
    | (have i₁ := eq43
       have i₂ := eq37 x y
       grind)
    | exact superpose eq37 eq43
    | (have j1 := eq37 x y
       grind)
    | exact resolve eq43 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq43
  have eq51 : y = (M.op x y) ∨ x = y ∨ x = (M.op x y) := by grind
  clear eq48
  have eq57 : ∀ X0 : G, y = (M.op X0 y) ∨ x = (M.op x y) ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq14 x y x
       have i₂ := eq51
       grind)
    | exact superpose eq51 eq14
    | exact resolve eq14 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq70 : (N.op x y) ≠ (N.op x y) ∨ y = (N.op y x) ∨ y = (N.op y x) ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq44
       have i₂ := eq57 (N.op y x)
       grind)
    | exact superpose eq57 eq44
    | exact resolve eq44 eq57
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq44
  have eq86 : (N.op x y) ≠ (N.op x y) ∨ y = (N.op y x) ∨ x = (M.op x y) ∨ x = y := by grind
  clear eq70
  have eq87 : y = (N.op y x) ∨ x = (M.op x y) ∨ x = y := by grind
  clear eq86
  have eq93 : y = (N.op y x) ∨ x = y := by
    first
    | (have j1 := eq8 y (N.op y x)
       grind)
    | (have r₁ := eq87
       have r₂ := eq8 y x
       grind)
    | exact resolve eq87 eq8
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq87
  have eq110 : (N.op x y) ≠ (N.op x (N.op y y)) ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq93
       grind)
    | exact superpose eq93 eq10
    | exact resolve eq10 eq93
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq93
  have eq114 : (N.op x y) ≠ (N.op x (M.op y y)) ∨ x = y := by grind
  clear eq110
  have eq122 : (N.op x y) ≠ (N.op x y) ∨ x = y ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq114
       have i₂ := eq57 y
       grind)
    | exact superpose eq57 eq114
    | exact resolve eq114 eq57
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq57 eq114
  have eq124 : (N.op x y) ≠ (N.op x y) ∨ x = y ∨ x = (M.op x y) := by grind
  clear eq122
  have eq125 : x = (M.op x y) ∨ x = y := by grind
  clear eq124
  have eq135 : ∀ X0 : G, x = (M.op X0 x) ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq14 x y x
       have i₂ := eq125
       grind)
    | exact superpose eq125 eq14
    | exact resolve eq14 eq125
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq125
  have eq188 : x ≠ (N.op x y) ∨ x = (N.op y (N.op y x)) ∨ x = (N.op y (N.op y x)) ∨ x = y := by
    first
    | (have i₁ := eq45
       have i₂ := eq135 (N.op y (N.op y x))
       grind)
    | exact superpose eq135 eq45
    | exact resolve eq45 eq135
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45
  have eq196 : ∀ X0 X1 : G, (M.op X0 x) = (M.op X1 x) ∨ x = y := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 x X1
       have i₂ := eq135 (M.op X0 x)
       grind)
    | exact superpose eq135 eq6
    | exact resolve eq6 eq135
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq199 : x ≠ (N.op x y) ∨ x = (N.op y (N.op y x)) ∨ x = y := by grind
  clear eq188
  have eq516 : x ≠ (M.op y x) ∨ x = (N.op y (N.op y x)) ∨ x = y ∨ y = (M.op y x) ∨ x = y := by
    first
    | (have i₁ := eq199
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq199
    | (have j1 := eq9 x y
       grind)
    | (have r₁ := eq199
       have r₂ := eq9 x (N.op x y)
       grind)
    | (have r₁ := eq199
       have r₂ := eq9 (N.op x y) x
       grind)
    | exact resolve eq199 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq199
  have eq517 : x ≠ (M.op y x) ∨ x = (N.op y (N.op y x)) ∨ x = y ∨ y = (M.op y x) := by grind
  clear eq516
  have eq518 : x = (N.op y (N.op y x)) ∨ x = y ∨ y = (M.op y x) := by
    first
    | (have r₁ := eq517
       have r₂ := eq135 y
       grind)
    | exact resolve eq517 eq135
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq517
  have eq525 : (N.op x y) ≠ (N.op x x) ∨ x = y ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq518
       grind)
    | exact superpose eq518 eq10
    | exact resolve eq10 eq518
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq518
  have eq530 : (N.op x y) ≠ (M.op x x) ∨ x = y ∨ y = (M.op y x) := by grind
  clear eq525
  have eq604 : (M.op y x) ≠ (M.op x x) ∨ x = y ∨ y = (M.op y x) ∨ y = (M.op y x) ∨ x = y := by
    first
    | (have i₁ := eq530
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq530
    | (have j1 := eq9 x y
       grind)
    | (have r₁ := eq530
       have r₂ := eq9 (N.op x y) (M.op x x)
       grind)
    | (have r₁ := eq530
       have r₂ := eq9 (M.op x x) (N.op x y)
       grind)
    | exact resolve eq530 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq530
  have eq605 : (M.op y x) ≠ (M.op x x) ∨ x = y ∨ y = (M.op y x) := by grind
  clear eq604
  have eq606 : y = (M.op y x) ∨ x = y := by
    first
    | (have r₁ := eq605
       have r₂ := eq196 y x
       grind)
    | (have r₁ := eq605
       have r₂ := eq196 x y
       grind)
    | exact resolve eq605 eq196
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq196 eq605
  have eq629 : x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq135 y
       have i₂ := eq606
       grind)
    | exact superpose eq606 eq135
    | exact resolve eq135 eq606
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq135 eq606
  have eq648 : x = y := by grind
  clear eq629
  have eq672 : (N.op x x) ≠ (N.op x (N.op x (N.op x x))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq648
       grind)
    | exact superpose eq648 eq10
    | exact resolve eq10 eq648
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq648
  have eq678 : (M.op x x) ≠ (N.op x (N.op x (M.op x x))) := by grind
  clear eq672
  have eq809 : (N.op x x) ≠ (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq678
       have i₂ := eq37 x x
       grind)
    | exact superpose eq37 eq678
    | (have j1 := eq37 x x
       grind)
    | exact resolve eq678 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq37
  have eq812 : x = (M.op x x) := by grind
  clear eq809
  have eq820 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq678
       have i₂ := eq812
       grind)
    | exact superpose eq812 eq678
    | exact resolve eq678 eq812
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq678
  have eq828 : ∀ X0 : G, x = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 x x x
       have i₂ := eq812
       grind)
    | exact superpose eq812 eq14
    | exact resolve eq14 eq812
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq833 : x ≠ (N.op x (M.op x x)) := by grind
  clear eq820
  have eq834 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq833
       have i₂ := eq828 x
       grind)
    | exact superpose eq828 eq833
    | exact resolve eq833 eq828
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq828 eq833
  have eq835 : x ≠ (M.op x x) := by grind
  clear eq834
  have eq836 : False := by grind
  exact eq836
