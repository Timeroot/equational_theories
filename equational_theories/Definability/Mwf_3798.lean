import equational_theories.Definability.AutBox2
import equational_theories.Definability.Mwc_00

/-!
# Structurality from `Equation3798` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3798` is `x ◇ y = (z ◇ x) ◇ (w ◇ y)`, and the companion is
`x □ y = if a = b then (M.op a a) else if (M.op b a) = b then a else (M.op a b)`.  Its automorphism half is `GuardAut2_x_y_pyx_y_pxx_x_pxy_Equation3798`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches it on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation835` from `Equation3798`: `x ◇ y = (z ◇ x) ◇ (w ◇ y)` ⊢ `x = x ◇ ((y ◇ x) ◇ (y ◇ x))`, through the
companion `x □ y = if a = b then (M.op a a) else if (M.op b a) = b then a else (M.op a b)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation835_StructuralFromFin_Equation3798 : Law835.StructuralFromFin Law3798 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 0)) (Lf 0) ((Lf 0) ⋆ (Lf 1))
    GuardAut2_x_y_pyx_y_pxx_x_pxy_Equation3798 (fun {G} M hM ↦ ?_)
  have hm := (@Law3798.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, a ≠ b ∨ N.op a b = (M.op a a)) →
      (∀ a b : G, a = b ∨ (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, a = b ∨ (M.op b a) = b ∨ N.op a b = (M.op a b)) → @satisfies _ G N Law835 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 0)) (Lf 0) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 0)) (Lf 0) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 0)) (Lf 0) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (b)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law835.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) = (M.op (M.op X2 X0) (M.op X3 X1)) := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, X0 ≠ X1 ∨ (N.op X0 X1) = (M.op X0 X0) := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ X0 = X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op X0 X1) ∨ (M.op X1 X0) = X1 ∨ X0 = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op x (N.op (N.op y x) (N.op y x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 X5 : G, (M.op (M.op X2 X1) X3) = (M.op (M.op X0 X1) (M.op X5 X3)) := by
    intro X0 X1 X2 X3 X5
    first
    | (have i₁ := eq6 (M.op X2 X1) X3 (M.op x X0) X5
       have i₂ := eq6 X0 X1 x X2
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq13 : ∀ X0 X1 X2 X3 X4 : G, (M.op X2 (M.op X3 X1)) = (M.op (M.op X4 X2) (M.op X0 X1)) := by
    intro X0 X1 X2 X3 X4
    first
    | (have i₁ := eq6 X2 (M.op X3 X1) X4 (M.op x X0)
       have i₂ := eq6 X0 X1 x X3
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X1 X2 X3 : G, (M.op X2 X1) = (M.op X2 (M.op X3 X1)) := by
    intro X1 X2 X3
    first
    | (have i₁ := eq13 x X1 X2 X3 x
       have i₂ := eq6 X2 X1 x x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq15 : ∀ X1 X2 X3 : G, (M.op X1 X3) = (M.op (M.op X2 X1) X3) := by
    intro X1 X2 X3
    first
    | (have i₁ := eq12 x X1 X2 X3 x
       have i₂ := eq6 X1 X3 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq40 : x ≠ (N.op x (M.op (N.op y x) (N.op y x))) := by grind
  have eq43 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X2 X0) ∨ (M.op X2 X0) = X1 ∨ (N.op X1 (M.op X2 X0)) = X1 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 X1 (M.op X2 X0)
       have i₂ := eq15 X0 X2 X1
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 (M.op X2 X0) X1
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq46 : x ≠ (N.op x (M.op (M.op y x) (M.op y x))) ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq40
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq40
    | (have j1 := eq9 y x
       grind)
    | (have r₁ := eq40
       have r₂ := eq9 x (N.op x (M.op (N.op y x) (N.op y x)))
       grind)
    | (have r₁ := eq40
       have r₂ := eq9 (N.op x (M.op (N.op y x) (N.op y x))) x
       grind)
    | exact resolve eq40 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq49 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq46
       have i₂ := eq6 x x y y
       grind)
    | exact superpose eq6 eq46
    | exact resolve eq46 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq46
  have eq72 : ∀ X0 : G, (N.op X0 (M.op X0 X0)) = X0 ∨ (M.op X0 X0) = X0 := by
    intro X0
    first
    | (have j0 := eq43 X0 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq43
  have eq81 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op (M.op X1 X0) (M.op X0 X0)) ∨ (M.op X0 X0) = (M.op X1 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq72 (M.op x X1)
       have i₂ := eq6 X1 X1 x x
       grind)
    | exact superpose eq6 eq72
    | exact resolve eq72 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq86 : x ≠ x ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq49
       have i₂ := eq72 x
       grind)
    | exact superpose eq72 eq49
    | (have j1 := eq72 x
       grind)
    | (have r₁ := eq49
       have r₂ := eq72 x
       grind)
    | exact resolve eq49 eq72
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq87 : x = y ∨ x = (M.op x y) ∨ x = (M.op x x) := by grind
  clear eq86
  have eq94 : x ≠ (N.op x (M.op (N.op x x) (N.op x x))) ∨ x = (M.op x y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq40
       have i₂ := eq87
       grind)
    | exact superpose eq87 eq40
    | exact resolve eq40 eq87
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq87
  have eq97 : x ≠ (N.op x (M.op (M.op x x) (M.op x x))) ∨ x = (M.op x y) ∨ x = (M.op x x) := by grind
  clear eq94
  have eq99 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq97
       have i₂ := eq6 x x x x
       grind)
    | exact superpose eq6 eq97
    | exact resolve eq97 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq97
  have eq101 : x = (M.op x y) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq72 x
       grind)
    | (have r₁ := eq99
       have r₂ := eq72 x
       grind)
    | exact resolve eq99 eq72
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq99
  have eq106 : x ≠ x ∨ x = y ∨ y = (N.op y x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq8 y x
       have i₂ := eq101
       grind)
    | exact superpose eq101 eq8
    | (have j0 := eq8 x y
       grind)
    | (have r₁ := eq8 y x
       have r₂ := eq101
       grind)
    | (have r₁ := eq8 x x
       have r₂ := eq101
       grind)
    | exact resolve eq8 eq101
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq112 : y = (N.op y x) ∨ x = y ∨ x = (M.op x x) := by grind
  clear eq106
  have eq135 : x ≠ (N.op x (M.op y y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq40
       have i₂ := eq112
       grind)
    | exact superpose eq112 eq40
    | exact resolve eq40 eq112
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq112
  have eq166 : x = (N.op x (M.op y y)) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq81 y x
       have i₂ := eq101
       grind)
    | exact superpose eq101 eq81
    | exact resolve eq81 eq101
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq81
  have eq194 : x ≠ x ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq135
       have i₂ := eq166
       grind)
    | exact superpose eq166 eq135
    | (have r₁ := eq135
       have r₂ := eq166
       grind)
    | exact resolve eq135 eq166
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq135 eq166
  have eq197 : x ≠ x ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op y y) := by grind
  clear eq194
  have eq198 : x = y ∨ x = (M.op x x) ∨ x = (M.op y y) := by grind
  clear eq197
  have eq207 : x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq101
       have i₂ := eq198
       grind)
    | exact superpose eq198 eq101
    | exact resolve eq101 eq198
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq198
  have eq210 : x = (M.op x x) ∨ x = (M.op y y) := by grind
  clear eq207
  have eq211 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq49
       have i₂ := eq210
       grind)
    | exact superpose eq210 eq49
    | exact resolve eq49 eq210
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq49
  have eq219 : x ≠ (M.op x x) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op y y) := by grind
  clear eq211
  have eq220 : x = y ∨ x = (M.op x y) ∨ x = (M.op y y) := by
    first
    | (have r₁ := eq219
       have r₂ := eq101
       grind)
    | exact resolve eq219 eq101
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq219
  have eq222 : x ≠ (N.op x (M.op (N.op x x) (N.op x x))) ∨ x = (M.op x y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq40
       have i₂ := eq220
       grind)
    | exact superpose eq220 eq40
    | exact resolve eq40 eq220
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq220
  have eq227 : x ≠ (N.op x (M.op (M.op x x) (M.op x x))) ∨ x = (M.op x y) ∨ x = (M.op y y) := by grind
  clear eq222
  have eq229 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq227
       have i₂ := eq6 x x x x
       grind)
    | exact superpose eq6 eq227
    | exact resolve eq227 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq227
  have eq232 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ x = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq229
       have i₂ := eq210
       grind)
    | exact superpose eq210 eq229
    | exact resolve eq229 eq210
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq210 eq229
  have eq238 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ x = (M.op y y) := by grind
  clear eq232
  have eq241 : x ≠ (M.op x x) ∨ x = (M.op x y) ∨ x = (M.op y y) := by grind
  clear eq238
  have eq244 : x = (M.op x y) ∨ x = (M.op y y) := by
    first
    | (have r₁ := eq241
       have r₂ := eq101
       grind)
    | exact resolve eq241 eq101
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq241
  have eq254 : ∀ X0 : G, x = (M.op y y) ∨ (M.op y X0) = (M.op x X0) := by
    intro X0
    first
    | (have i₁ := eq15 y x X0
       have i₂ := eq244
       grind)
    | exact superpose eq244 eq15
    | exact resolve eq15 eq244
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq291 : x ≠ (M.op x y) ∨ (M.op x y) = (M.op y y) := by
    first
    | (have j0 := eq254 y
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq254
  have eq309 : x ≠ x ∨ x = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq291
       have i₂ := eq244
       grind)
    | exact superpose eq244 eq291
    | (have r₁ := eq291
       have r₂ := eq244
       grind)
    | exact resolve eq291 eq244
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq244 eq291
  have eq312 : x ≠ x ∨ x = (M.op y y) := by grind
  clear eq309
  have eq313 : x = (M.op y y) := by grind
  clear eq312
  have eq322 : ∀ X0 : G, (M.op X0 y) = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 y X0 y
       have i₂ := eq313
       grind)
    | exact superpose eq313 eq14
    | exact resolve eq14 eq313
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq323 : ∀ X0 : G, (M.op y X0) = (M.op x X0) := by
    intro X0
    first
    | (have i₁ := eq15 y y X0
       have i₂ := eq313
       grind)
    | exact superpose eq313 eq15
    | exact resolve eq15 eq313
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq328 : y = (N.op y x) ∨ x = y := by
    first
    | (have i₁ := eq72 y
       have i₂ := eq313
       grind)
    | exact superpose eq313 eq72
    | exact resolve eq72 eq313
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq72 eq313
  have eq354 : x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq101
       have i₂ := eq322 x
       grind)
    | exact superpose eq322 eq101
    | exact resolve eq101 eq322
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq101
  have eq361 : x = (M.op x x) := by grind
  clear eq354
  have eq510 : x ≠ (N.op x (M.op y y)) ∨ x = y := by
    first
    | (have i₁ := eq40
       have i₂ := eq328
       grind)
    | exact superpose eq328 eq40
    | exact resolve eq40 eq328
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq328
  have eq517 : x ≠ (N.op x (M.op y x)) ∨ x = y := by
    first
    | (have i₁ := eq510
       have i₂ := eq322 y
       grind)
    | exact superpose eq322 eq510
    | exact resolve eq510 eq322
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq322 eq510
  have eq522 : x ≠ (N.op x (M.op x x)) ∨ x = y := by
    first
    | (have i₁ := eq517
       have i₂ := eq323 x
       grind)
    | exact superpose eq323 eq517
    | exact resolve eq517 eq323
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq323 eq517
  have eq527 : x ≠ (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq522
       have i₂ := eq361
       grind)
    | exact superpose eq361 eq522
    | exact resolve eq522 eq361
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq522
  have eq530 : x ≠ (M.op x x) ∨ x = y := by grind
  clear eq527
  have eq532 : x = y := by
    first
    | (have r₁ := eq530
       have r₂ := eq361
       grind)
    | exact resolve eq530 eq361
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq530
  have eq535 : x ≠ (N.op x (M.op (N.op x x) (N.op x x))) := by
    first
    | (have i₁ := eq40
       have i₂ := eq532
       grind)
    | exact superpose eq532 eq40
    | exact resolve eq40 eq532
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq40 eq532
  have eq544 : x ≠ (N.op x (M.op (M.op x x) (M.op x x))) := by grind
  clear eq535
  have eq546 : x ≠ (N.op x (M.op x x)) := by
    first
    | (have i₁ := eq544
       have i₂ := eq6 x x x x
       grind)
    | exact superpose eq6 eq544
    | exact resolve eq544 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq544
  have eq548 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq546
       have i₂ := eq361
       grind)
    | exact superpose eq361 eq546
    | exact resolve eq546 eq361
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq546
  have eq550 : x ≠ (M.op x x) := by grind
  clear eq548
  have eq552 : False := by grind
  exact eq552
