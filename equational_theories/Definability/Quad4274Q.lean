import equational_theories.Definability.Quad4274

/-!
# `StructuralFrom Law4274`, operation `1623822`

Generated by `defwork/emitquad4274k.py`.  These are the targets settled by operation `1623822` of
`Definability/Quad4274.lean`, the one that copies the column of `T ∖ {k}` into itself, so `k` is the
right zero while `h` needs no rearranging.
-/

set_option linter.unusedVariables false

open Law Law.MagmaLaw Quad4274

namespace Law.MagmaLaw

/-- `x ◇ y = y ◇ (x ◇ y)` -/
theorem Equation333_StructuralFrom_Equation4274 : Law333.StructuralFrom Law4274 := by
  intro G M hM
  rcases subsingleton_or_nontrivial G with hs | hs
  · exact structuralOnMagma_subsingleton _ M
  obtain ⟨x₀⟩ : Nonempty G := inferInstance
  have h : Equation4274 G := Law4274.models_iff.mp hM
  obtain ⟨K⟩ : Nonempty (Ker M) := ⟨kernel M h x₀⟩
  refine structural_q1623822 M K ?_
  rw [@Law333.models_iff]
  intro x y
  have h1 := K.tk
  have h2 := K.tim
  have h3 := K.hin
  simp only [q1623822_apply M K]
  grind

end Law.MagmaLaw
