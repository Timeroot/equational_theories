import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation3738` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3738` is `x ◇ y = (x ◇ z) ◇ (y ◇ w)`, and the companion is
`x □ y = if (M.op b a) = a then b else if (M.op b a) = b then a else (M.op b a)`.  Its automorphism half is `GuardAut2_pyx_x_pyx_y_y_x_pyx_Equation3738`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches none of these on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation820` from `Equation3738`: `x ◇ y = (x ◇ z) ◇ (y ◇ w)` ⊢ `x = x ◇ ((x ◇ x) ◇ (y ◇ y))`, through the
companion `x □ y = if (M.op b a) = a then b else if (M.op b a) = b then a else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation820_StructuralFromFin_Equation3738 : Law820.StructuralFromFin Law3738 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_x_pyx_y_y_x_pyx_Equation3738 (fun {G} M hM ↦ ?_)
  have hm := (@Law3738.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = a ∨ (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = a ∨ (M.op b a) = b ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law820 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (a)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (a)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (b)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law820.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) = (M.op (M.op X0 X2) (M.op X1 X3)) := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (M.op X1 X0) = X0 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X1 ∨ (M.op X1 X0) = X0 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op x (N.op (N.op x x) (N.op y y))) := by grind
  clear hm ht hu hv nh hM
  have eq11 : ∀ X0 X1 X2 X3 X5 : G, (M.op (M.op X0 X2) X3) = (M.op (M.op X0 X1) (M.op X3 X5)) := by
    intro X0 X1 X2 X3 X5
    first
    | (have i₁ := eq6 (M.op X0 X2) X3 (M.op X1 x) X5
       have i₂ := eq6 X0 X1 X2 x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq12 : ∀ X0 X1 X2 X3 X4 : G, (M.op X2 (M.op X0 X3)) = (M.op (M.op X2 X4) (M.op X0 X1)) := by
    intro X0 X1 X2 X3 X4
    first
    | (have i₁ := eq6 X2 (M.op X0 X3) X4 (M.op X1 x)
       have i₂ := eq6 X0 X1 X3 x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X2 X3 : G, (M.op X2 (M.op X0 X3)) = (M.op X2 X0) := by
    intro X0 X2 X3
    first
    | (have i₁ := eq12 X0 x X2 X3 x
       have i₂ := eq6 X2 X0 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq15 : ∀ X0 X2 X3 : G, (M.op (M.op X0 X2) X3) = (M.op X0 X3) := by
    intro X0 X2 X3
    first
    | (have i₁ := eq11 X0 x X2 X3 x
       have i₂ := eq6 X0 X3 x x
       grind)
    | exact superpose eq6 eq11
    | exact resolve eq11 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq11
  have eq21 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X1 X2) ∨ (N.op (M.op X1 X2) X0) = X0 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op X1 X2) X0
       have i₂ := eq14 X1 X0 X2
       grind)
    | exact superpose eq14 eq7
    | (have j0 := eq7 (M.op X1 X2) X0
       grind)
    | (have r₁ := eq7 (M.op X2 X2) X2
       have r₂ := eq14 X2 X2 X2
       grind)
    | exact resolve eq7 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq33 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ X1 ∨ (M.op X0 X2) = (N.op X1 (M.op X0 X2)) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 X1 (M.op X0 X2)
       have i₂ := eq15 X0 X2 X1
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 X1 X0
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq42 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) ≠ (M.op X0 X2) ∨ (M.op X0 X1) = (M.op X1 X3) ∨ (M.op X1 X3) = (N.op (M.op X1 X3) (M.op X0 X2)) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq8 (M.op X1 X3) (M.op X0 X2)
       have i₂ := eq6 X0 X1 X2 X3
       grind)
    | exact superpose eq6 eq8
    | (have j0 := eq8 (M.op X1 X3) (M.op X0 X2)
       grind)
    | (have r₁ := eq8 (M.op X1 X3) (M.op X0 X1)
       have r₂ := eq6 X0 X1 X1 X3
       grind)
    | exact resolve eq8 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq44 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X0 X2) ∨ (M.op X0 X1) = X1 ∨ (N.op X1 (M.op X0 X2)) = X1 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 x (M.op X0 X2)
       have i₂ := eq15 X0 X2 x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X1 X0
       grind)
    | (have r₁ := eq8 x (M.op X0 x)
       have r₂ := eq15 X0 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq51 : ∀ X0 : G, (N.op (M.op X0 X0) X0) = X0 := by
    intro X0
    first
    | (have j0 := eq21 X0 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq58 : x ≠ (N.op x (M.op (N.op y y) (N.op x x))) ∨ (N.op y y) = (M.op (N.op y y) (N.op x x)) ∨ (N.op x x) = (M.op (N.op y y) (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 (N.op x x) (N.op y y)
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 (N.op x x) (N.op y y)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq59 : x ≠ (N.op x (N.op (M.op x x) (N.op y y))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq61 : x ≠ (N.op x (N.op (N.op x x) (M.op y y))) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq62 : x ≠ (N.op x (N.op (N.op x x) (M.op y y))) ∨ y = (M.op y y) := by grind
  clear eq61
  have eq63 : x ≠ (N.op x (N.op (M.op x x) (N.op y y))) ∨ x = (M.op x x) := by grind
  clear eq59
  have eq64 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op (M.op X0 X0) (M.op X0 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq51 (M.op X0 X1)
       have i₂ := eq6 X0 X0 X1 X1
       grind)
    | exact superpose eq6 eq51
    | exact resolve eq51 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq73 : x ≠ (N.op x (N.op (M.op x x) (M.op y y))) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq62
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq62
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq62 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq76 : x ≠ (N.op x (N.op (M.op x x) (M.op y y))) ∨ y = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq73
  have eq98 : x ≠ (N.op x (M.op (N.op y y) (M.op x x))) ∨ x = (M.op x x) ∨ (N.op y y) = (M.op (N.op y y) (M.op x x)) ∨ (M.op x x) = (M.op (N.op y y) (M.op x x)) := by
    first
    | (have i₁ := eq63
       have i₂ := eq9 (M.op x x) (N.op y y)
       grind)
    | exact superpose eq9 eq63
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq63 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq101 : x ≠ (N.op x (M.op (N.op y y) x)) ∨ x = (M.op x x) ∨ (N.op y y) = (M.op (N.op y y) (M.op x x)) ∨ (M.op x x) = (M.op (N.op y y) (M.op x x)) := by
    first
    | (have i₁ := eq98
       have i₂ := eq14 x (N.op y y) x
       grind)
    | exact superpose eq14 eq98
    | exact resolve eq98 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq98
  have eq102 : (N.op y y) = (M.op (N.op y y) x) ∨ x ≠ (N.op x (M.op (N.op y y) x)) ∨ x = (M.op x x) ∨ (M.op x x) = (M.op (N.op y y) (M.op x x)) := by
    first
    | (have i₁ := eq101
       have i₂ := eq14 x (N.op y y) x
       grind)
    | exact superpose eq14 eq101
    | exact resolve eq101 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq101
  have eq103 : x ≠ (N.op x (M.op (N.op y y) x)) ∨ (N.op y y) = (M.op (N.op y y) x) ∨ (M.op x x) = (M.op (N.op y y) x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq102
       have i₂ := eq14 x (N.op y y) x
       grind)
    | exact superpose eq14 eq102
    | exact resolve eq102 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq102
  have eq130 : ∀ X0 X1 : G, (N.op X1 (M.op X0 X1)) = X1 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq44 X0 X1 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq44
  have eq145 : ∀ X0 X1 X2 : G, (M.op X1 X2) = (N.op (M.op X1 X2) (M.op X0 X1)) ∨ (M.op X0 X1) = (M.op X1 X2) := by
    intro X0 X1 X2
    first
    | (have j0 := eq42 X0 X1 X1 X2
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq42
  have eq205 : x ≠ (N.op x (M.op (M.op y y) (M.op x x))) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ (M.op y y) = (M.op (M.op y y) (M.op x x)) ∨ (M.op x x) = (M.op (M.op y y) (M.op x x)) := by
    first
    | (have i₁ := eq76
       have i₂ := eq9 (M.op x x) (M.op y y)
       grind)
    | exact superpose eq9 eq76
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq76 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq207 : x ≠ (N.op x (M.op y x)) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ (M.op y y) = (M.op (M.op y y) (M.op x x)) ∨ (M.op x x) = (M.op (M.op y y) (M.op x x)) := by
    first
    | (have i₁ := eq205
       have i₂ := eq6 y x y x
       grind)
    | exact superpose eq6 eq205
    | exact resolve eq205 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq205
  have eq208 : (M.op y y) = (M.op y x) ∨ x ≠ (N.op x (M.op y x)) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ (M.op x x) = (M.op (M.op y y) (M.op x x)) := by
    first
    | (have i₁ := eq207
       have i₂ := eq6 y x y x
       grind)
    | exact superpose eq6 eq207
    | exact resolve eq207 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq207
  have eq209 : x ≠ (N.op x (M.op y x)) ∨ (M.op y y) = (M.op y x) ∨ (M.op x x) = (M.op y x) ∨ y = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq208
       have i₂ := eq6 y x y x
       grind)
    | exact superpose eq6 eq208
    | exact resolve eq208 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq208
  have eq216 : x ≠ x ∨ (M.op y y) = (M.op y x) ∨ (M.op x x) = (M.op y x) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq209
       have i₂ := eq130 y x
       grind)
    | exact superpose eq130 eq209
    | (have j1 := eq130 y y
       grind)
    | (have r₁ := eq209
       have r₂ := eq130 y x
       grind)
    | exact resolve eq209 eq130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq209
  have eq218 : (M.op y y) = (M.op y x) ∨ (M.op x x) = (M.op y x) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op y x) := by grind
  clear eq216
  have eq220 : x ≠ x ∨ (N.op y y) = (M.op (N.op y y) x) ∨ (M.op x x) = (M.op (N.op y y) x) ∨ x = (M.op x x) ∨ x = (M.op (N.op y y) x) := by
    first
    | (have i₁ := eq103
       have i₂ := eq130 (N.op y y) x
       grind)
    | exact superpose eq130 eq103
    | (have j1 := eq130 x x
       grind)
    | (have r₁ := eq103
       have r₂ := eq130 (N.op y y) x
       grind)
    | exact resolve eq103 eq130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq103
  have eq222 : (N.op y y) = (M.op (N.op y y) x) ∨ (M.op x x) = (M.op (N.op y y) x) ∨ x = (M.op x x) ∨ x = (M.op (N.op y y) x) := by grind
  clear eq220
  have eq227 : x ≠ (N.op x (N.op (M.op x x) (M.op y x))) ∨ y = (M.op y x) ∨ x = (M.op x x) ∨ (M.op x x) = (M.op y x) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq76
       have i₂ := eq218
       grind)
    | exact superpose eq218 eq76
    | exact resolve eq76 eq218
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq251 : y ≠ (M.op y x) ∨ (M.op x x) = (M.op y x) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op y x) := by grind
  clear eq218
  have eq252 : x ≠ (N.op x (N.op (M.op x x) (M.op y x))) ∨ y = (M.op y x) ∨ x = (M.op x x) ∨ (M.op x x) = (M.op y x) ∨ y = (M.op y y) ∨ x = (M.op y x) := by grind
  clear eq227
  have eq256 : x ≠ (N.op x (N.op (M.op x x) (M.op y x))) ∨ x = (M.op x x) ∨ (M.op x x) = (M.op y x) ∨ y = (M.op y y) ∨ x = (M.op y x) := by
    first
    | (have r₁ := eq252
       have r₂ := eq251
       grind)
    | exact resolve eq252 eq251
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq251 eq252
  have eq274 : ∀ X0 : G, (M.op x X0) = (N.op (M.op x X0) (N.op y y)) ∨ (N.op y y) = (M.op x X0) ∨ (M.op x x) = (M.op (N.op y y) x) ∨ x = (M.op x x) ∨ x = (M.op (N.op y y) x) := by
    intro X0
    first
    | (have i₁ := eq145 (N.op y y) x x
       have i₂ := eq222
       grind)
    | exact superpose eq222 eq145
    | (have j0 := eq145 (N.op y y) x x
       grind)
    | exact resolve eq145 eq222
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq276 : (N.op y y) ≠ (M.op x x) ∨ (M.op x x) = (M.op (N.op y y) x) ∨ x = (M.op x x) ∨ x = (M.op (N.op y y) x) := by grind
  clear eq222
  have eq368 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ (M.op x x) = (M.op y x) ∨ y = (M.op y y) ∨ x = (M.op y x) ∨ (M.op x x) = (M.op y x) := by
    first
    | (have i₁ := eq256
       have i₂ := eq145 y x x
       grind)
    | exact superpose eq145 eq256
    | (have j1 := eq145 y x x
       grind)
    | exact resolve eq256 eq145
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq145 eq256
  have eq371 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ (M.op x x) = (M.op y x) ∨ y = (M.op y y) ∨ x = (M.op y x) := by grind
  clear eq368
  have eq372 : y = (M.op y y) ∨ (M.op x x) = (M.op y x) ∨ x = (M.op x x) ∨ x = (M.op y x) := by
    first
    | (have j1 := eq130 y y
       grind)
    | (have r₁ := eq371
       have r₂ := eq130 x x
       grind)
    | exact resolve eq371 eq130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq371
  have eq390 : y = (N.op y y) ∨ (M.op x x) = (M.op y x) ∨ x = (M.op x x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq51 y
       have i₂ := eq372
       grind)
    | exact superpose eq372 eq51
    | exact resolve eq51 eq372
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq372
  have eq439 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ (N.op y y) = (M.op x x) ∨ (M.op x x) = (M.op (N.op y y) x) ∨ x = (M.op x x) ∨ x = (M.op (N.op y y) x) := by
    first
    | (have i₁ := eq63
       have i₂ := eq274 x
       grind)
    | exact superpose eq274 eq63
    | (have j1 := eq274 x
       grind)
    | exact resolve eq63 eq274
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq274
  have eq442 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ (N.op y y) = (M.op x x) ∨ (M.op x x) = (M.op (N.op y y) x) ∨ x = (M.op (N.op y y) x) := by grind
  clear eq439
  have eq447 : x = (M.op x x) ∨ (N.op y y) = (M.op x x) ∨ (M.op x x) = (M.op (N.op y y) x) ∨ x = (M.op (N.op y y) x) := by
    first
    | (have j1 := eq130 x x
       grind)
    | (have r₁ := eq442
       have r₂ := eq130 x x
       grind)
    | exact resolve eq442 eq130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq442
  have eq451 : (M.op x x) = (M.op (N.op y y) x) ∨ x = (M.op x x) ∨ x = (M.op (N.op y y) x) := by
    first
    | (have r₁ := eq447
       have r₂ := eq276
       grind)
    | exact resolve eq447 eq276
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq276 eq447
  have eq454 : (M.op x x) = (M.op y x) ∨ x = (M.op x x) ∨ x = (M.op y x) ∨ (M.op x x) = (M.op y x) ∨ x = (M.op x x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq451
       have i₂ := eq390
       grind)
    | exact superpose eq390 eq451
    | exact resolve eq451 eq390
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq390
  have eq463 : ∀ X0 : G, (M.op X0 (N.op y y)) = (M.op X0 (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op (N.op y y) x) := by
    intro X0
    first
    | (have i₁ := eq14 (N.op y y) X0 x
       have i₂ := eq451
       grind)
    | exact superpose eq451 eq14
    | exact resolve eq14 eq451
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq464 : ∀ X0 : G, (M.op (N.op y y) X0) = (M.op (M.op x x) X0) ∨ x = (M.op x x) ∨ x = (M.op (N.op y y) x) := by
    intro X0
    first
    | (have i₁ := eq15 (N.op y y) x X0
       have i₂ := eq451
       grind)
    | exact superpose eq451 eq15
    | exact resolve eq15 eq451
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq451
  have eq483 : (M.op x x) = (M.op y x) ∨ x = (M.op x x) ∨ x = (M.op y x) := by grind
  clear eq454
  have eq484 : ∀ X0 : G, (M.op (N.op y y) X0) = (M.op x X0) ∨ x = (M.op (N.op y y) x) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq464 X0
       have i₂ := eq15 x x X0
       grind)
    | exact superpose eq15 eq464
    | exact resolve eq464 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq464
  have eq485 : ∀ X0 : G, (M.op X0 (N.op y y)) = (M.op X0 x) ∨ x = (M.op (N.op y y) x) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq463 X0
       have i₂ := eq14 x X0 x
       grind)
    | exact superpose eq14 eq463
    | exact resolve eq463 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq463
  have eq508 : ∀ X0 : G, (M.op X0 y) = (M.op X0 (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op y x) := by
    intro X0
    first
    | (have i₁ := eq14 y X0 x
       have i₂ := eq483
       grind)
    | exact superpose eq483 eq14
    | exact resolve eq14 eq483
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq509 : ∀ X0 : G, (M.op y X0) = (M.op (M.op x x) X0) ∨ x = (M.op x x) ∨ x = (M.op y x) := by
    intro X0
    first
    | (have i₁ := eq15 y x X0
       have i₂ := eq483
       grind)
    | exact superpose eq483 eq15
    | exact resolve eq15 eq483
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq483
  have eq526 : ∀ X0 : G, (M.op y X0) = (M.op x X0) ∨ x = (M.op y x) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq509 X0
       have i₂ := eq15 x x X0
       grind)
    | exact superpose eq15 eq509
    | exact resolve eq509 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq509
  have eq527 : ∀ X0 : G, (M.op X0 y) = (M.op X0 x) ∨ x = (M.op y x) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq508 X0
       have i₂ := eq14 x X0 x
       grind)
    | exact superpose eq14 eq508
    | exact resolve eq508 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq508
  have eq582 : ∀ X0 X1 : G, (M.op y X1) = (M.op x X1) ∨ (M.op y X0) = (M.op x X0) ∨ x = (M.op x x) := by
    intro X0 X1
    first
    | (have i₁ := eq15 y x X1
       have i₂ := eq526 X0
       grind)
    | exact superpose eq526 eq15
    | (have j1 := eq526 X1
       grind)
    | exact resolve eq15 eq526
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq526
  have eq906 : ∀ X0 X1 : G, (M.op (N.op y y) X1) = (M.op x X1) ∨ (M.op (N.op y y) X0) = (M.op x X0) ∨ x = (M.op x x) := by
    intro X0 X1
    first
    | (have i₁ := eq15 (N.op y y) x X1
       have i₂ := eq484 X0
       grind)
    | exact superpose eq484 eq15
    | (have j1 := eq484 X1
       grind)
    | exact resolve eq15 eq484
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq484
  have eq1043 : (N.op y y) = (N.op (M.op (N.op y y) x) (N.op y y)) ∨ x = (M.op (N.op y y) x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq51 (N.op y y)
       have i₂ := eq485 (N.op y y)
       grind)
    | exact superpose eq485 eq51
    | exact resolve eq51 eq485
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51 eq485
  have eq1457 : ∀ X0 : G, (M.op x X0) ≠ (M.op x X0) ∨ (M.op y X0) = (M.op x X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq582 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq582
  have eq1458 : ∀ X0 : G, (M.op y X0) = (M.op x X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq1457 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1457
  have eq1557 : ∀ X0 : G, x = (M.op x x) ∨ (M.op X0 y) = (M.op X0 x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq527 X0
       have i₂ := eq1458 x
       grind)
    | exact superpose eq1458 eq527
    | (have j0 := eq527 X0
       grind)
    | exact resolve eq527 eq1458
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq527
  have eq1561 : x ≠ (N.op x (N.op (M.op x x) (M.op x y))) ∨ y = (M.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq76
       have i₂ := eq1458 y
       grind)
    | exact superpose eq1458 eq76
    | exact resolve eq76 eq1458
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq76 eq1458
  have eq1601 : x ≠ (N.op x (N.op (M.op x x) (M.op x y))) ∨ y = (M.op x y) ∨ x = (M.op x x) := by grind
  clear eq1561
  have eq1604 : ∀ X0 : G, x = (M.op x x) ∨ (M.op X0 y) = (M.op X0 x) := by
    intro X0
    first
    | (have j0 := eq1557 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1557
  have eq1628 : x ≠ (N.op x (M.op x y)) ∨ y = (M.op x y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1601
       have i₂ := eq64 x y
       grind)
    | exact superpose eq64 eq1601
    | exact resolve eq1601 eq64
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1601
  have eq2696 : ∀ X0 : G, (M.op x X0) ≠ (M.op x X0) ∨ (M.op (N.op y y) X0) = (M.op x X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq906 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq906
  have eq2697 : ∀ X0 : G, (M.op (N.op y y) X0) = (M.op x X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq2696 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2696
  have eq4186 : (N.op y y) = (N.op (M.op x x) (N.op y y)) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1043
       have i₂ := eq2697 x
       grind)
    | exact superpose eq2697 eq1043
    | exact resolve eq1043 eq2697
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1043 eq2697
  have eq4195 : (N.op y y) = (N.op (M.op x x) (N.op y y)) ∨ x = (M.op x x) := by grind
  clear eq4186
  have eq4214 : x ≠ (N.op x (N.op y y)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq63
       have i₂ := eq4195
       grind)
    | exact superpose eq4195 eq63
    | exact resolve eq63 eq4195
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq63 eq4195
  have eq4219 : x ≠ (N.op x (N.op y y)) ∨ x = (M.op x x) := by grind
  clear eq4214
  have eq4532 : x ≠ (N.op x (M.op x x)) ∨ y = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1628
       have i₂ := eq1604 x
       grind)
    | exact superpose eq1604 eq1628
    | exact resolve eq1628 eq1604
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1604 eq1628
  have eq4538 : x ≠ (N.op x (M.op x x)) ∨ y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq4532
  have eq4540 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq130 x x
       grind)
    | (have r₁ := eq4538
       have r₂ := eq130 x x
       grind)
    | exact resolve eq4538 eq130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4538
  have eq4640 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq64 x x
       have i₂ := eq4540
       grind)
    | exact superpose eq4540 eq64
    | exact resolve eq64 eq4540
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq4641 : x = (N.op x y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq130 x x
       have i₂ := eq4540
       grind)
    | exact superpose eq4540 eq130
    | (have j0 := eq130 x x
       grind)
    | exact resolve eq130 eq4540
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq4644 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq4540
  have eq4652 : x = (N.op x y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq4641
       have r₂ := eq4644
       grind)
    | exact resolve eq4641 eq4644
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4641 eq4644
  have eq4874 : x ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq4219
       have i₂ := eq4640
       grind)
    | exact superpose eq4640 eq4219
    | exact resolve eq4219 eq4640
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4219 eq4640
  have eq4877 : x ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq4874
  have eq4935 : x = (M.op x x) := by
    first
    | (have r₁ := eq4877
       have r₂ := eq4652
       grind)
    | exact resolve eq4877 eq4652
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4652 eq4877
  have eq5063 : x = (N.op x x) := by
    first
    | (have i₁ := eq64 x x
       have i₂ := eq4935
       grind)
    | exact superpose eq4935 eq64
    | exact resolve eq64 eq4935
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4935
  have eq5178 : x ≠ (N.op x (N.op x (N.op y y))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq5063
       grind)
    | exact superpose eq5063 eq10
    | exact resolve eq10 eq5063
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq5179 : x ≠ (N.op x (M.op (N.op y y) x)) ∨ (N.op y y) = (M.op (N.op y y) x) ∨ x = (M.op (N.op y y) x) := by
    first
    | (have i₁ := eq58
       have i₂ := eq5063
       grind)
    | exact superpose eq5063 eq58
    | exact resolve eq58 eq5063
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq58
  have eq5180 : x ≠ (N.op x (N.op x (M.op y y))) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq62
       have i₂ := eq5063
       grind)
    | exact superpose eq5063 eq62
    | exact resolve eq62 eq5063
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq62
  have eq5192 : (N.op y y) = (M.op (N.op y y) x) ∨ x = (M.op (N.op y y) x) := by
    first
    | (have j1 := eq130 (N.op y y) x
       grind)
    | (have r₁ := eq5179
       have r₂ := eq130 (N.op y y) x
       grind)
    | exact resolve eq5179 eq130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5179
  have eq7097 : x = (N.op x (N.op y y)) ∨ x = (N.op y y) ∨ x = (M.op (N.op y y) x) := by
    first
    | (have i₁ := eq130 (N.op y y) x
       have i₂ := eq5192
       grind)
    | exact superpose eq5192 eq130
    | (have j0 := eq130 (N.op y y) x
       grind)
    | exact resolve eq130 eq5192
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq130
  have eq7101 : x ≠ (N.op y y) ∨ x = (M.op (N.op y y) x) := by grind
  clear eq5192
  have eq7117 : x = (N.op x (N.op y y)) ∨ x = (M.op (N.op y y) x) := by
    first
    | (have r₁ := eq7097
       have r₂ := eq7101
       grind)
    | exact resolve eq7097 eq7101
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7097 eq7101
  have eq7195 : x ≠ (N.op x x) ∨ x = (M.op (N.op y y) x) := by
    first
    | (have i₁ := eq5178
       have i₂ := eq7117
       grind)
    | exact superpose eq7117 eq5178
    | exact resolve eq5178 eq7117
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7117
  have eq7202 : x = (M.op (N.op y y) x) := by
    first
    | (have r₁ := eq7195
       have r₂ := eq5063
       grind)
    | exact resolve eq7195 eq5063
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7195
  have eq7276 : x = (M.op (M.op y y) x) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq7202
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq7202
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq7202 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq7301 : x ≠ x ∨ (N.op y y) = (N.op x (N.op y y)) := by
    first
    | (have i₁ := eq7 x (N.op y y)
       have i₂ := eq7202
       grind)
    | exact superpose eq7202 eq7
    | (have j0 := eq7 x (N.op y y)
       grind)
    | (have r₁ := eq7 x (N.op y y)
       have r₂ := eq7202
       grind)
    | exact resolve eq7 eq7202
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq7317 : (N.op y y) = (N.op x (N.op y y)) := by grind
  clear eq7301
  have eq7329 : x = (M.op (M.op y y) x) ∨ y = (M.op y y) := by grind
  clear eq7276
  have eq7335 : y = (M.op y y) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq7329
       have i₂ := eq15 y y x
       grind)
    | exact superpose eq15 eq7329
    | exact resolve eq7329 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15 eq7329
  have eq7789 : x ≠ (N.op x (N.op y y)) := by
    first
    | (have i₁ := eq5178
       have i₂ := eq7317
       grind)
    | exact superpose eq7317 eq5178
    | exact resolve eq5178 eq7317
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5178
  have eq7795 : x ≠ (N.op y y) := by
    first
    | (have i₁ := eq7789
       have i₂ := eq7317
       grind)
    | exact superpose eq7317 eq7789
    | exact resolve eq7789 eq7317
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7317 eq7789
  have eq8028 : y = (N.op y y) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq64 y y
       have i₂ := eq7335
       grind)
    | exact superpose eq7335 eq64
    | exact resolve eq64 eq7335
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64 eq7335
  have eq8246 : x = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq7202
       have i₂ := eq8028
       grind)
    | exact superpose eq8028 eq7202
    | exact resolve eq7202 eq8028
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7202 eq8028
  have eq8253 : x = (M.op y x) := by grind
  clear eq8246
  have eq8353 : ∀ X0 : G, (M.op X0 y) = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 y X0 x
       have i₂ := eq8253
       grind)
    | exact superpose eq8253 eq14
    | exact resolve eq14 eq8253
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq8357 : ∀ X0 : G, x ≠ x ∨ (M.op y X0) = (N.op x (M.op y X0)) := by
    intro X0
    first
    | (have i₁ := eq33 y x X0
       have i₂ := eq8253
       grind)
    | exact superpose eq8253 eq33
    | (have j0 := eq33 y x X0
       grind)
    | (have r₁ := eq33 y x x
       have r₂ := eq8253
       grind)
    | exact resolve eq33 eq8253
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33
  have eq8367 : ∀ X0 : G, (M.op y X0) = (N.op x (M.op y X0)) := by
    intro X0
    first
    | (have j0 := eq8357 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8357
  have eq8609 : x ≠ (N.op x (N.op x (M.op y x))) ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq5180
       have i₂ := eq8353 y
       grind)
    | exact superpose eq8353 eq5180
    | exact resolve eq5180 eq8353
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5180 eq8353
  have eq8641 : x ≠ (N.op x (M.op y x)) ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq8609
       have i₂ := eq8367 x
       grind)
    | exact superpose eq8367 eq8609
    | exact resolve eq8609 eq8367
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8609
  have eq8658 : x ≠ (M.op y x) ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq8641
       have i₂ := eq8367 x
       grind)
    | exact superpose eq8367 eq8641
    | exact resolve eq8641 eq8367
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8367 eq8641
  have eq8667 : y = (M.op y x) := by
    first
    | (have r₁ := eq8658
       have r₂ := eq8253
       grind)
    | exact resolve eq8658 eq8253
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8658
  have eq8669 : x = y := by
    first
    | (have i₁ := eq8667
       have i₂ := eq8253
       grind)
    | exact superpose eq8253 eq8667
    | exact resolve eq8667 eq8253
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8253 eq8667
  have eq8871 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq7795
       have i₂ := eq8669
       grind)
    | exact superpose eq8669 eq7795
    | exact resolve eq7795 eq8669
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7795 eq8669
  have eq8890 : False := by grind
  exact eq8890

/-- `Equation1023` from `Equation3738`: `x ◇ y = (x ◇ z) ◇ (y ◇ w)` ⊢ `x = x ◇ ((x ◇ (x ◇ y)) ◇ y)`, through the
companion `x □ y = if (M.op b a) = a then b else if (M.op b a) = b then a else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation1023_StructuralFromFin_Equation3738 : Law1023.StructuralFromFin Law3738 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_x_pyx_y_y_x_pyx_Equation3738 (fun {G} M hM ↦ ?_)
  have hm := (@Law3738.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = a ∨ (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = a ∨ (M.op b a) = b ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law1023 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (a)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (a)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (b)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law1023.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) = (M.op (M.op X0 X2) (M.op X1 X3)) := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (M.op X1 X0) = X0 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X1 ∨ (M.op X1 X0) = X0 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op x (N.op (N.op x (N.op x y)) y)) := by grind
  clear hm ht hu hv nh hM
  have eq11 : ∀ X0 X1 X2 X3 X5 : G, (M.op (M.op X0 X2) X3) = (M.op (M.op X0 X1) (M.op X3 X5)) := by
    intro X0 X1 X2 X3 X5
    first
    | (have i₁ := eq6 (M.op X0 X2) X3 (M.op X1 x) X5
       have i₂ := eq6 X0 X1 X2 x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq12 : ∀ X0 X1 X2 X3 X4 : G, (M.op X2 (M.op X0 X3)) = (M.op (M.op X2 X4) (M.op X0 X1)) := by
    intro X0 X1 X2 X3 X4
    first
    | (have i₁ := eq6 X2 (M.op X0 X3) X4 (M.op X1 x)
       have i₂ := eq6 X0 X1 X3 x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X2 X3 : G, (M.op X2 (M.op X0 X3)) = (M.op X2 X0) := by
    intro X0 X2 X3
    first
    | (have i₁ := eq12 X0 x X2 X3 x
       have i₂ := eq6 X2 X0 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq15 : ∀ X0 X2 X3 : G, (M.op (M.op X0 X2) X3) = (M.op X0 X3) := by
    intro X0 X2 X3
    first
    | (have i₁ := eq11 X0 x X2 X3 x
       have i₂ := eq6 X0 X3 x x
       grind)
    | exact superpose eq6 eq11
    | exact resolve eq11 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq11
  have eq21 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X1 X2) ∨ (N.op (M.op X1 X2) X0) = X0 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op X1 X2) X0
       have i₂ := eq14 X1 X0 X2
       grind)
    | exact superpose eq14 eq7
    | (have j0 := eq7 (M.op X1 X2) X0
       grind)
    | (have r₁ := eq7 (M.op X2 X2) X2
       have r₂ := eq14 X2 X2 X2
       grind)
    | exact resolve eq7 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq33 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ X1 ∨ (M.op X0 X2) = (N.op X1 (M.op X0 X2)) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 X1 (M.op X0 X2)
       have i₂ := eq15 X0 X2 X1
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 X1 X0
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq42 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) ≠ (M.op X0 X2) ∨ (M.op X0 X1) = (M.op X1 X3) ∨ (M.op X1 X3) = (N.op (M.op X1 X3) (M.op X0 X2)) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq8 (M.op X1 X3) (M.op X0 X2)
       have i₂ := eq6 X0 X1 X2 X3
       grind)
    | exact superpose eq6 eq8
    | (have j0 := eq8 (M.op X1 X3) (M.op X0 X2)
       grind)
    | (have r₁ := eq8 (M.op X1 X3) (M.op X0 X1)
       have r₂ := eq6 X0 X1 X1 X3
       grind)
    | exact resolve eq8 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq44 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X0 X2) ∨ (M.op X0 X1) = X1 ∨ (N.op X1 (M.op X0 X2)) = X1 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 x (M.op X0 X2)
       have i₂ := eq15 X0 X2 x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X1 X0
       grind)
    | (have r₁ := eq8 x (M.op X0 x)
       have r₂ := eq15 X0 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq51 : ∀ X0 : G, (N.op (M.op X0 X0) X0) = X0 := by
    intro X0
    first
    | (have j0 := eq21 X0 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq58 : x ≠ (N.op x (N.op (N.op x (M.op y x)) y)) ∨ y = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq59 : x ≠ (N.op x (N.op (M.op (N.op x y) x) y)) ∨ (N.op x y) = (M.op (N.op x y) x) ∨ x = (M.op (N.op x y) x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x (N.op x y)
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x (N.op x y)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq61 : x ≠ (N.op x (M.op y (N.op x (N.op x y)))) ∨ y = (M.op y (N.op x (N.op x y))) ∨ (N.op x (N.op x y)) = (M.op y (N.op x (N.op x y))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 (N.op x (N.op x y)) y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 (N.op x (N.op x y)) y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq62 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op (M.op X0 X0) (M.op X0 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq51 (M.op X0 X1)
       have i₂ := eq6 X0 X0 X1 X1
       grind)
    | exact superpose eq6 eq51
    | exact resolve eq51 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq117 : ∀ X0 X1 X2 : G, (M.op X1 X2) = (N.op (M.op X1 X2) (M.op X0 X1)) ∨ (M.op X0 X1) = (M.op X1 X2) := by
    intro X0 X1 X2
    first
    | (have j0 := eq42 X0 X1 X1 X2
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq42
  have eq160 : ∀ X0 X1 : G, (N.op X1 (M.op X0 X1)) = X1 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq44 X0 X1 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq44
  have eq182 : x ≠ (N.op x (N.op x y)) ∨ y = (M.op y x) ∨ x = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq58
       have i₂ := eq160 y x
       grind)
    | exact superpose eq160 eq58
    | (have j1 := eq160 y x
       grind)
    | exact resolve eq58 eq160
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq58
  have eq183 : x ≠ (N.op x (N.op x y)) ∨ y = (M.op y x) ∨ x = (M.op y x) := by grind
  clear eq182
  have eq195 : x ≠ (N.op x (M.op y x)) ∨ y = (M.op y x) ∨ x = (M.op y x) ∨ y = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq183
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq183
    | (have j1 := eq9 x y
       grind)
    | exact resolve eq183 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq183
  have eq197 : x ≠ (N.op x (M.op y x)) ∨ y = (M.op y x) ∨ x = (M.op y x) := by grind
  clear eq195
  have eq198 : y = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have j1 := eq160 y x
       grind)
    | (have r₁ := eq197
       have r₂ := eq160 y x
       grind)
    | exact resolve eq197 eq160
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq197
  have eq218 : ∀ X0 : G, (M.op x X0) = (N.op (M.op x X0) y) ∨ y = (M.op x X0) ∨ x = (M.op y x) := by
    intro X0
    first
    | (have i₁ := eq117 y x x
       have i₂ := eq198
       grind)
    | exact superpose eq198 eq117
    | exact resolve eq117 eq198
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq117
  have eq220 : x = (N.op x y) ∨ x = y ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq160 y x
       have i₂ := eq198
       grind)
    | exact superpose eq198 eq160
    | (have j0 := eq160 y x
       grind)
    | exact resolve eq160 eq198
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq221 : x ≠ y ∨ x = (M.op y x) := by grind
  have eq224 : x = (N.op x y) ∨ x = (M.op y x) := by
    first
    | (have r₁ := eq220
       have r₂ := eq221
       grind)
    | exact resolve eq220 eq221
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq220 eq221
  have eq233 : x ≠ (N.op x (N.op (M.op x x) y)) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq59
       have i₂ := eq224
       grind)
    | exact superpose eq224 eq59
    | exact resolve eq59 eq224
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq59
  have eq234 : x ≠ (N.op x (N.op (N.op x x) y)) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq224
       grind)
    | exact superpose eq224 eq10
    | exact resolve eq10 eq224
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq237 : x ≠ (N.op x (N.op (M.op x x) y)) ∨ x = (M.op x x) ∨ x = (M.op y x) := by grind
  clear eq233
  have eq258 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op y x) ∨ y = (M.op x x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq237
       have i₂ := eq218 x
       grind)
    | exact superpose eq218 eq237
    | (have j1 := eq218 x
       grind)
    | exact resolve eq237 eq218
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq218
  have eq261 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op y x) ∨ y = (M.op x x) := by grind
  clear eq258
  have eq263 : x = (M.op y x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have j1 := eq160 y x
       grind)
    | (have r₁ := eq261
       have r₂ := eq160 x x
       grind)
    | exact resolve eq261 eq160
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq261
  have eq274 : ∀ X0 : G, (M.op y X0) = (M.op x X0) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq15 y x X0
       have i₂ := eq263
       grind)
    | exact superpose eq263 eq15
    | exact resolve eq15 eq263
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq263
  have eq379 : y = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq198
       have i₂ := eq274 x
       grind)
    | exact superpose eq274 eq198
    | exact resolve eq198 eq274
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq198 eq274
  have eq415 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq379
  have eq437 : ∀ X0 : G, (M.op y X0) = (M.op x X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq15 x x X0
       have i₂ := eq415
       grind)
    | exact superpose eq415 eq15
    | exact resolve eq15 eq415
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq446 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq62 x x
       have i₂ := eq415
       grind)
    | exact superpose eq415 eq62
    | exact resolve eq62 eq415
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq449 : x = (N.op x y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq160 x x
       have i₂ := eq415
       grind)
    | exact superpose eq415 eq160
    | (have j0 := eq160 x x
       grind)
    | exact resolve eq160 eq415
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq160
  have eq450 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq415
  have eq453 : x = (N.op x y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq449
       have r₂ := eq450
       grind)
    | exact resolve eq449 eq450
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq449 eq450
  have eq498 : y = (M.op y y) ∨ y = (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq9 y y
       have i₂ := eq446
       grind)
    | exact superpose eq446 eq9
    | (have j0 := eq9 y y
       grind)
    | exact resolve eq9 eq446
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq446
  have eq499 : y = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq498
  have eq734 : y = (M.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq499
       have i₂ := eq437 y
       grind)
    | exact superpose eq437 eq499
    | exact resolve eq499 eq437
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq499
  have eq770 : y = (M.op x y) ∨ x = (M.op x x) := by grind
  clear eq734
  have eq818 : y = (N.op (M.op x x) y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq62 x y
       have i₂ := eq770
       grind)
    | exact superpose eq770 eq62
    | exact resolve eq62 eq770
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq770
  have eq1018 : x ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op y x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq237
       have i₂ := eq818
       grind)
    | exact superpose eq818 eq237
    | exact resolve eq237 eq818
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq237 eq818
  have eq1023 : x ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op y x) := by grind
  clear eq1018
  have eq1027 : x = (M.op y x) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1023
       have r₂ := eq453
       grind)
    | exact resolve eq1023 eq453
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq453 eq1023
  have eq1074 : x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq437 x
       have i₂ := eq1027
       grind)
    | exact superpose eq1027 eq437
    | exact resolve eq437 eq1027
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq437 eq1027
  have eq1094 : x = (M.op x x) := by grind
  clear eq1074
  have eq1162 : x = (N.op x x) := by
    first
    | (have i₁ := eq62 x x
       have i₂ := eq1094
       grind)
    | exact superpose eq1094 eq62
    | exact resolve eq62 eq1094
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq62
  have eq1213 : x ≠ (N.op x (N.op x y)) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq234
       have i₂ := eq1162
       grind)
    | exact superpose eq1162 eq234
    | exact resolve eq234 eq1162
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq234
  have eq1722 : x ≠ (N.op x x) ∨ x = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq1213
       have i₂ := eq224
       grind)
    | exact superpose eq224 eq1213
    | exact resolve eq1213 eq224
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq224 eq1213
  have eq1726 : x ≠ (N.op x x) ∨ x = (M.op y x) := by grind
  clear eq1722
  have eq1728 : x = (M.op y x) := by
    first
    | (have r₁ := eq1726
       have r₂ := eq1162
       grind)
    | exact resolve eq1726 eq1162
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1726
  have eq1770 : x ≠ x ∨ y = (N.op x y) := by
    first
    | (have i₁ := eq7 x y
       have i₂ := eq1728
       grind)
    | exact superpose eq1728 eq7
    | (have j0 := eq7 x y
       grind)
    | (have r₁ := eq7 x y
       have r₂ := eq1728
       grind)
    | exact resolve eq7 eq1728
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1773 : ∀ X0 : G, (M.op X0 y) = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 y X0 x
       have i₂ := eq1728
       grind)
    | exact superpose eq1728 eq14
    | exact resolve eq14 eq1728
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq1774 : ∀ X0 : G, (M.op y X0) = (M.op x X0) := by
    intro X0
    first
    | (have i₁ := eq15 y x X0
       have i₂ := eq1728
       grind)
    | exact superpose eq1728 eq15
    | exact resolve eq15 eq1728
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq1777 : ∀ X0 : G, x ≠ x ∨ (M.op y X0) = (N.op x (M.op y X0)) := by
    intro X0
    first
    | (have i₁ := eq33 y x X0
       have i₂ := eq1728
       grind)
    | exact superpose eq1728 eq33
    | (have j0 := eq33 y x X0
       grind)
    | (have r₁ := eq33 y x x
       have r₂ := eq1728
       grind)
    | exact resolve eq33 eq1728
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33 eq1728
  have eq1785 : ∀ X0 : G, (M.op y X0) = (N.op x (M.op y X0)) := by
    intro X0
    first
    | (have j0 := eq1777 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1777
  have eq1786 : y = (N.op x y) := by grind
  clear eq1770
  have eq1817 : x ≠ (N.op x (M.op y (N.op x y))) ∨ y = (M.op y (N.op x y)) ∨ (N.op x y) = (M.op y (N.op x y)) := by
    first
    | (have i₁ := eq61
       have i₂ := eq1786
       grind)
    | exact superpose eq1786 eq61
    | exact resolve eq61 eq1786
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq61
  have eq1828 : x ≠ (M.op y (N.op x y)) ∨ y = (M.op y (N.op x y)) ∨ (N.op x y) = (M.op y (N.op x y)) := by
    first
    | (have i₁ := eq1817
       have i₂ := eq1785 (N.op x y)
       grind)
    | exact superpose eq1785 eq1817
    | exact resolve eq1817 eq1785
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1785 eq1817
  have eq1830 : x ≠ (M.op x (N.op x y)) ∨ y = (M.op y (N.op x y)) ∨ (N.op x y) = (M.op y (N.op x y)) := by
    first
    | (have i₁ := eq1828
       have i₂ := eq1774 (N.op x y)
       grind)
    | exact superpose eq1774 eq1828
    | exact resolve eq1828 eq1774
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1828
  have eq1831 : x ≠ (M.op x y) ∨ y = (M.op y (N.op x y)) ∨ (N.op x y) = (M.op y (N.op x y)) := by
    first
    | (have i₁ := eq1830
       have i₂ := eq1786
       grind)
    | exact superpose eq1786 eq1830
    | exact resolve eq1830 eq1786
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1830
  have eq1832 : x ≠ (M.op x x) ∨ y = (M.op y (N.op x y)) ∨ (N.op x y) = (M.op y (N.op x y)) := by
    first
    | (have i₁ := eq1831
       have i₂ := eq1773 x
       grind)
    | exact superpose eq1773 eq1831
    | exact resolve eq1831 eq1773
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1831
  have eq1833 : y = (M.op y (N.op x y)) ∨ (N.op x y) = (M.op y (N.op x y)) := by
    first
    | (have r₁ := eq1832
       have r₂ := eq1094
       grind)
    | exact resolve eq1832 eq1094
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1832
  have eq1834 : y = (M.op x (N.op x y)) ∨ (N.op x y) = (M.op y (N.op x y)) := by
    first
    | (have i₁ := eq1833
       have i₂ := eq1774 (N.op x y)
       grind)
    | exact superpose eq1774 eq1833
    | exact resolve eq1833 eq1774
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1833
  have eq1835 : y = (M.op x y) ∨ (N.op x y) = (M.op y (N.op x y)) := by
    first
    | (have i₁ := eq1834
       have i₂ := eq1786
       grind)
    | exact superpose eq1786 eq1834
    | exact resolve eq1834 eq1786
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1834
  have eq1836 : y = (M.op x x) ∨ (N.op x y) = (M.op y (N.op x y)) := by
    first
    | (have i₁ := eq1835
       have i₂ := eq1773 x
       grind)
    | exact superpose eq1773 eq1835
    | exact resolve eq1835 eq1773
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1835
  have eq1837 : x = y ∨ (N.op x y) = (M.op y (N.op x y)) := by
    first
    | (have i₁ := eq1836
       have i₂ := eq1094
       grind)
    | exact superpose eq1094 eq1836
    | exact resolve eq1836 eq1094
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1836
  have eq1838 : (N.op x y) = (M.op x (N.op x y)) ∨ x = y := by
    first
    | (have i₁ := eq1837
       have i₂ := eq1774 (N.op x y)
       grind)
    | exact superpose eq1774 eq1837
    | exact resolve eq1837 eq1774
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1774 eq1837
  have eq1839 : y = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq1838
       have i₂ := eq1786
       grind)
    | exact superpose eq1786 eq1838
    | exact resolve eq1838 eq1786
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1786 eq1838
  have eq1840 : y = (M.op x x) ∨ x = y := by
    first
    | (have i₁ := eq1839
       have i₂ := eq1773 x
       grind)
    | exact superpose eq1773 eq1839
    | exact resolve eq1839 eq1773
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1773 eq1839
  have eq1841 : x = y ∨ x = y := by
    first
    | (have i₁ := eq1840
       have i₂ := eq1094
       grind)
    | exact superpose eq1094 eq1840
    | exact resolve eq1840 eq1094
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1094 eq1840
  have eq1842 : x = y := by grind
  clear eq1841
  have eq1904 : x ≠ (N.op x (N.op (N.op x (N.op x x)) x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1842
       grind)
    | exact superpose eq1842 eq10
    | exact resolve eq10 eq1842
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1842
  have eq1988 : x ≠ (N.op x (N.op (N.op x x) x)) := by
    first
    | (have i₁ := eq1904
       have i₂ := eq1162
       grind)
    | exact superpose eq1162 eq1904
    | exact resolve eq1904 eq1162
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1904
  have eq1990 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq1988
       have i₂ := eq1162
       grind)
    | exact superpose eq1162 eq1988
    | exact resolve eq1988 eq1162
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1988
  have eq1992 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq1990
       have i₂ := eq1162
       grind)
    | exact superpose eq1162 eq1990
    | exact resolve eq1990 eq1162
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1990
  have eq1994 : False := by grind
  exact eq1994
