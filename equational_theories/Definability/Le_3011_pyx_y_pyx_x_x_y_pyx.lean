import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation3011` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3011` is `x = ((y ◇ (z ◇ z)) ◇ y) ◇ x`, and the companion is
`x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`.  Its automorphism half is `GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3011`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches none of these on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation307` from `Equation3011`: `x = ((y ◇ (z ◇ z)) ◇ y) ◇ x` ⊢ `x ◇ x = x ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation307_StructuralFromFin_Equation3011 : Law307.StructuralFromFin Law3011 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3011 (fun {G} M hM ↦ ?_)
  have hm := (@Law3011.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law307 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law307.models_iff G N]
  intro x
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op X1 (M.op X2 X2)) X1) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op X0 X0) (M.op X1 X1)) X2) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X0) (M.op X1 X1)) X0
       have i₂ := eq6 (M.op (M.op X0 X0) (M.op X1 X1)) (M.op X0 X0) X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq22 : ∀ X0 X2 : G, (M.op (M.op X0 X0) X2) = X2 := by
    intro X0 X2
    first
    | (have i₁ := eq6 X2 (M.op X0 X0) x
       have i₂ := eq13 X0 x (M.op X0 X0)
       grind)
    | exact superpose eq13 eq6
    | exact resolve eq6 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq30 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq32 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq30
  have eq38 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op X0 X0)
       have i₂ := eq22 X0 x
       grind)
    | exact superpose eq22 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X0)
       have r₂ := eq22 X0 x
       grind)
    | exact resolve eq8 eq22
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq39 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq22 X1 X0
       grind)
    | exact superpose eq22 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq22 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq22
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq22
  have eq44 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq38 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq38
  have eq49 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq39 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq174 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq32
       have i₂ := eq44 x x
       grind)
    | exact superpose eq44 eq32
    | (have j1 := eq44 x x
       grind)
    | (have r₁ := eq32
       have r₂ := eq44 x x
       grind)
    | (have r₁ := eq32
       have r₂ := eq44 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq32 eq44
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32 eq44
  have eq177 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq174
  have eq178 : x = (M.op x x) := by grind
  clear eq177
  have eq226 : x = (N.op x x) := by
    first
    | (have i₁ := eq49 x
       have i₂ := eq178
       grind)
    | exact superpose eq178 eq49
    | exact resolve eq49 eq178
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq49 eq178
  have eq433 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq226
       grind)
    | exact superpose eq226 eq10
    | exact resolve eq10 eq226
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq437 : False := by grind
  exact eq437

/-- `Equation312` from `Equation3011`: `x = ((y ◇ (z ◇ z)) ◇ y) ◇ x` ⊢ `x ◇ x = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation312_StructuralFromFin_Equation3011 : Law312.StructuralFromFin Law3011 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3011 (fun {G} M hM ↦ ?_)
  have hm := (@Law3011.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law312 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law312.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op X1 (M.op X2 X2)) X1) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op X0 X0) (M.op X1 X1)) X2) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X0) (M.op X1 X1)) X0
       have i₂ := eq6 (M.op (M.op X0 X0) (M.op X1 X1)) (M.op X0 X0) X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq22 : ∀ X0 X2 : G, (M.op (M.op X0 X0) X2) = X2 := by
    intro X0 X2
    first
    | (have i₁ := eq6 X2 (M.op X0 X0) x
       have i₂ := eq13 X0 x (M.op X0 X0)
       grind)
    | exact superpose eq13 eq6
    | exact resolve eq6 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq30 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq32 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq30
  have eq38 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op X0 X0)
       have i₂ := eq22 X0 x
       grind)
    | exact superpose eq22 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X0)
       have r₂ := eq22 X0 x
       grind)
    | exact resolve eq8 eq22
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq39 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq22 X1 X0
       grind)
    | exact superpose eq22 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq22 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq22
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq22
  have eq44 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq38 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq38
  have eq49 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq39 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq175 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq32
       have i₂ := eq44 y x
       grind)
    | exact superpose eq44 eq32
    | (have j1 := eq44 x x
       grind)
    | (have r₁ := eq32
       have r₂ := eq44 y x
       grind)
    | (have r₁ := eq32
       have r₂ := eq44 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq32 eq44
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq176 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq175
  have eq214 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq32
       have i₂ := eq176
       grind)
    | exact superpose eq176 eq32
    | exact resolve eq32 eq176
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq227 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq49 x
       have i₂ := eq176
       grind)
    | exact superpose eq176 eq49
    | exact resolve eq49 eq176
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq228 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq176
  have eq231 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq214
       have r₂ := eq227
       grind)
    | exact resolve eq214 eq227
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq214 eq227
  have eq232 : x = (M.op x x) := by
    first
    | (have r₁ := eq231
       have r₂ := eq228
       grind)
    | exact resolve eq231 eq228
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq228 eq231
  have eq282 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq44 X0 x
       have i₂ := eq232
       grind)
    | exact superpose eq232 eq44
    | exact resolve eq44 eq232
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq44
  have eq283 : x = (N.op x x) := by
    first
    | (have i₁ := eq49 x
       have i₂ := eq232
       grind)
    | exact superpose eq232 eq49
    | exact resolve eq49 eq232
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq49 eq232
  have eq620 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq283
       grind)
    | exact superpose eq283 eq10
    | exact resolve eq10 eq283
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1201 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq620
       have i₂ := eq282 y
       grind)
    | exact superpose eq282 eq620
    | (have j1 := eq282 y
       grind)
    | (have r₁ := eq620
       have r₂ := eq282 y
       grind)
    | (have r₁ := eq620
       have r₂ := eq282 (N.op y x)
       grind)
    | exact resolve eq620 eq282
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq282
  have eq1202 : x = y := by grind
  clear eq1201
  have eq1407 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq620
       have i₂ := eq1202
       grind)
    | exact superpose eq1202 eq620
    | exact resolve eq620 eq1202
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq620 eq1202
  have eq1412 : False := by grind
  exact eq1412

/-- `Equation3258` from `Equation3011`: `x = ((y ◇ (z ◇ z)) ◇ y) ◇ x` ⊢ `x ◇ x = x ◇ (y ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3258_StructuralFromFin_Equation3011 : Law3258.StructuralFromFin Law3011 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3011 (fun {G} M hM ↦ ?_)
  have hm := (@Law3011.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3258 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3258.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op X1 (M.op X2 X2)) X1) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op y (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op X0 X0) (M.op X1 X1)) X2) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X0) (M.op X1 X1)) X0
       have i₂ := eq6 (M.op (M.op X0 X0) (M.op X1 X1)) (M.op X0 X0) X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq22 : ∀ X0 X2 : G, (M.op (M.op X0 X0) X2) = X2 := by
    intro X0 X2
    first
    | (have i₁ := eq6 X2 (M.op X0 X0) x
       have i₂ := eq13 X0 x (M.op X0 X0)
       grind)
    | exact superpose eq13 eq6
    | exact resolve eq6 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq30 : (M.op x x) ≠ (N.op x (N.op y (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq33 : (M.op x x) ≠ (N.op x (N.op y (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq30
  have eq39 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op X0 X0)
       have i₂ := eq22 X0 x
       grind)
    | exact superpose eq22 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X0)
       have r₂ := eq22 X0 x
       grind)
    | exact resolve eq8 eq22
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq40 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq22 X1 X0
       grind)
    | exact superpose eq22 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq22 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq22
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq22
  have eq45 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq39 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq92 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq40 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq40
  have eq177 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq33
       have i₂ := eq45 y x
       grind)
    | exact superpose eq45 eq33
    | (have j1 := eq45 x x
       grind)
    | (have r₁ := eq33
       have r₂ := eq45 (N.op x (N.op y (M.op x x))) x
       grind)
    | exact resolve eq33 eq45
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq180 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq45 y x
       grind)
    | (have r₁ := eq177
       have r₂ := eq45 x x
       grind)
    | (have r₁ := eq177
       have r₂ := eq45 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq177 eq45
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq177
  have eq216 : y ≠ (N.op x (N.op y y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq33
       have i₂ := eq180
       grind)
    | exact superpose eq180 eq33
    | exact resolve eq33 eq180
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33
  have eq228 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq45 X0 x
       have i₂ := eq180
       grind)
    | exact superpose eq180 eq45
    | (have j0 := eq45 x x
       grind)
    | exact resolve eq45 eq180
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq229 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq92 x
       have i₂ := eq180
       grind)
    | exact superpose eq180 eq92
    | exact resolve eq92 eq180
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq230 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq180
  have eq233 : y ≠ (N.op x (N.op y y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq216
       have r₂ := eq230
       grind)
    | exact resolve eq216 eq230
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq216
  have eq440 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq233
       have i₂ := eq229
       grind)
    | exact superpose eq229 eq233
    | exact resolve eq233 eq229
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq229 eq233
  have eq444 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq440
  have eq849 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq444
       have i₂ := eq228 x
       grind)
    | exact superpose eq228 eq444
    | (have j1 := eq228 x
       grind)
    | (have r₁ := eq444
       have r₂ := eq228 x
       grind)
    | (have r₁ := eq444
       have r₂ := eq228 (N.op x y)
       grind)
    | exact resolve eq444 eq228
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq228 eq444
  have eq850 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq849
  have eq851 : x = (M.op x x) ∨ x = y := by grind
  clear eq850
  have eq854 : x = (M.op x x) := by
    first
    | (have r₁ := eq851
       have r₂ := eq230
       grind)
    | exact resolve eq851 eq230
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq230 eq851
  have eq1113 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq45 X0 x
       have i₂ := eq854
       grind)
    | exact superpose eq854 eq45
    | exact resolve eq45 eq854
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45
  have eq1116 : x = (N.op x x) := by
    first
    | (have i₁ := eq92 x
       have i₂ := eq854
       grind)
    | exact superpose eq854 eq92
    | exact resolve eq92 eq854
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq92 eq854
  have eq1431 : x ≠ (N.op x (N.op y x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1116
       grind)
    | exact superpose eq1116 eq10
    | exact resolve eq10 eq1116
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1797 : x ≠ (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq1431
       have i₂ := eq1113 y
       grind)
    | exact superpose eq1113 eq1431
    | (have j1 := eq1113 y
       grind)
    | (have r₁ := eq1431
       have r₂ := eq1113 (N.op x (N.op y x))
       grind)
    | exact resolve eq1431 eq1113
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1113
  have eq1800 : x = y := by
    first
    | (have r₁ := eq1797
       have r₂ := eq1116
       grind)
    | exact resolve eq1797 eq1116
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1797
  have eq1817 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq1431
       have i₂ := eq1800
       grind)
    | exact superpose eq1800 eq1431
    | exact resolve eq1431 eq1800
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1431 eq1800
  have eq1819 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq1817
       have i₂ := eq1116
       grind)
    | exact superpose eq1116 eq1817
    | exact resolve eq1817 eq1116
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1817
  have eq1824 : False := by grind
  exact eq1824

/-- `Equation3268` from `Equation3011`: `x = ((y ◇ (z ◇ z)) ◇ y) ◇ x` ⊢ `x ◇ x = y ◇ (x ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3268_StructuralFromFin_Equation3011 : Law3268.StructuralFromFin Law3011 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3011 (fun {G} M hM ↦ ?_)
  have hm := (@Law3011.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3268 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3268.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op X1 (M.op X2 X2)) X1) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op X0 X0) (M.op X1 X1)) X2) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X0) (M.op X1 X1)) X0
       have i₂ := eq6 (M.op (M.op X0 X0) (M.op X1 X1)) (M.op X0 X0) X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq22 : ∀ X0 X2 : G, (M.op (M.op X0 X0) X2) = X2 := by
    intro X0 X2
    first
    | (have i₁ := eq6 X2 (M.op X0 X0) x
       have i₂ := eq13 X0 x (M.op X0 X0)
       grind)
    | exact superpose eq13 eq6
    | exact resolve eq6 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq30 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq33 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq30
  have eq39 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op X0 X0)
       have i₂ := eq22 X0 x
       grind)
    | exact superpose eq22 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X0)
       have r₂ := eq22 X0 x
       grind)
    | exact resolve eq8 eq22
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq40 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq22 X1 X0
       grind)
    | exact superpose eq22 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq22 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq22
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq22
  have eq45 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq39 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq92 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq40 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq40
  have eq177 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq33
       have i₂ := eq45 x x
       grind)
    | exact superpose eq45 eq33
    | (have j1 := eq45 x x
       grind)
    | (have r₁ := eq33
       have r₂ := eq45 (N.op y (N.op x (M.op x x))) x
       grind)
    | exact resolve eq33 eq45
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33
  have eq178 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq177
  have eq216 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq178
       have i₂ := eq45 y x
       grind)
    | exact superpose eq45 eq178
    | (have j1 := eq45 x x
       grind)
    | (have r₁ := eq178
       have r₂ := eq45 y x
       grind)
    | (have r₁ := eq178
       have r₂ := eq45 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq178 eq45
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq218 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq216
  have eq255 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq178
       have i₂ := eq218
       grind)
    | exact superpose eq218 eq178
    | exact resolve eq178 eq218
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq178
  have eq270 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq92 x
       have i₂ := eq218
       grind)
    | exact superpose eq218 eq92
    | exact resolve eq92 eq218
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq271 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq218
  have eq275 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq255
       have r₂ := eq270
       grind)
    | exact resolve eq255 eq270
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq255 eq270
  have eq276 : x = (M.op x x) := by
    first
    | (have r₁ := eq275
       have r₂ := eq271
       grind)
    | exact resolve eq275 eq271
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq271 eq275
  have eq292 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq45 X0 x
       have i₂ := eq276
       grind)
    | exact superpose eq276 eq45
    | exact resolve eq45 eq276
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45
  have eq293 : x = (N.op x x) := by
    first
    | (have i₁ := eq92 x
       have i₂ := eq276
       grind)
    | exact superpose eq276 eq92
    | exact resolve eq92 eq276
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq92 eq276
  have eq471 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq293
       grind)
    | exact superpose eq293 eq10
    | exact resolve eq10 eq293
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq476 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq471
       have i₂ := eq293
       grind)
    | exact superpose eq293 eq471
    | exact resolve eq471 eq293
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq471
  have eq1140 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq476
       have i₂ := eq292 y
       grind)
    | exact superpose eq292 eq476
    | (have j1 := eq292 y
       grind)
    | (have r₁ := eq476
       have r₂ := eq292 y
       grind)
    | (have r₁ := eq476
       have r₂ := eq292 (N.op y x)
       grind)
    | exact resolve eq476 eq292
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq292
  have eq1141 : x = y := by grind
  clear eq1140
  have eq1231 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq476
       have i₂ := eq1141
       grind)
    | exact superpose eq1141 eq476
    | exact resolve eq476 eq1141
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq476 eq1141
  have eq1233 : False := by grind
  exact eq1233

/-- `Equation3288` from `Equation3011`: `x = ((y ◇ (z ◇ z)) ◇ y) ◇ x` ⊢ `x ◇ x = y ◇ (z ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3288_StructuralFromFin_Equation3011 : Law3288.StructuralFromFin Law3011 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3011 (fun {G} M hM ↦ ?_)
  have hm := (@Law3011.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3288 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3288.models_iff G N]
  intro x y z
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op X1 (M.op X2 X2)) X1) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op z (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op X0 X0) (M.op X1 X1)) X2) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X0) (M.op X1 X1)) X0
       have i₂ := eq6 (M.op (M.op X0 X0) (M.op X1 X1)) (M.op X0 X0) X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq22 : ∀ X0 X2 : G, (M.op (M.op X0 X0) X2) = X2 := by
    intro X0 X2
    first
    | (have i₁ := eq6 X2 (M.op X0 X0) x
       have i₂ := eq13 X0 x (M.op X0 X0)
       grind)
    | exact superpose eq13 eq6
    | exact resolve eq6 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq30 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq33 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq30
  have eq39 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op X0 X0)
       have i₂ := eq22 X0 x
       grind)
    | exact superpose eq22 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X0)
       have r₂ := eq22 X0 x
       grind)
    | exact resolve eq8 eq22
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq40 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq22 X1 X0
       grind)
    | exact superpose eq22 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq22 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq22
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq22
  have eq45 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq39 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq92 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq40 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq40
  have eq177 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ z = (M.op x x) := by
    first
    | (have i₁ := eq33
       have i₂ := eq45 z x
       grind)
    | exact superpose eq45 eq33
    | (have j1 := eq45 x x
       grind)
    | (have r₁ := eq33
       have r₂ := eq45 (N.op y (N.op z (M.op x x))) x
       grind)
    | exact resolve eq33 eq45
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq687 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ z = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq177
       have i₂ := eq45 y x
       grind)
    | exact superpose eq45 eq177
    | (have j1 := eq45 x x
       grind)
    | (have r₁ := eq177
       have r₂ := eq45 y x
       grind)
    | (have r₁ := eq177
       have r₂ := eq45 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq177 eq45
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq689 : z = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq687
  have eq749 : z ≠ (N.op y (N.op z z)) ∨ x = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq33
       have i₂ := eq689
       grind)
    | exact superpose eq689 eq33
    | exact resolve eq33 eq689
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq769 : ∀ X0 : G, z = (N.op X0 z) ∨ z = X0 ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq45 X0 x
       have i₂ := eq689
       grind)
    | exact superpose eq689 eq45
    | (have j0 := eq45 x x
       grind)
    | exact resolve eq45 eq689
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq771 : z = (N.op z z) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq92 x
       have i₂ := eq689
       grind)
    | exact superpose eq689 eq92
    | exact resolve eq92 eq689
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq772 : x ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  have eq773 : y ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq689
  have eq781 : z ≠ (N.op y (N.op z z)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have r₁ := eq749
       have r₂ := eq772
       grind)
    | exact resolve eq749 eq772
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq749 eq772
  have eq1648 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq781
       have i₂ := eq771
       grind)
    | exact superpose eq771 eq781
    | exact resolve eq781 eq771
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq771 eq781
  have eq1652 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq1648
  have eq1865 : z ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq1652
       have i₂ := eq769 y
       grind)
    | exact superpose eq769 eq1652
    | (have j1 := eq769 y
       grind)
    | (have r₁ := eq1652
       have r₂ := eq769 y
       grind)
    | (have r₁ := eq1652
       have r₂ := eq769 (N.op y z)
       grind)
    | exact resolve eq1652 eq769
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq769 eq1652
  have eq1866 : z ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq1865
  have eq1867 : x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq1866
  have eq1870 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1867
       have r₂ := eq773
       grind)
    | exact resolve eq1867 eq773
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq773 eq1867
  have eq2256 : y ≠ (N.op y y) ∨ x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq177
       have i₂ := eq1870
       grind)
    | exact superpose eq1870 eq177
    | exact resolve eq177 eq1870
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq177
  have eq2257 : y ≠ (N.op y (N.op z y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq33
       have i₂ := eq1870
       grind)
    | exact superpose eq1870 eq33
    | exact resolve eq33 eq1870
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33
  have eq2284 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq92 x
       have i₂ := eq1870
       grind)
    | exact superpose eq1870 eq92
    | exact resolve eq92 eq1870
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2288 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq1870
  have eq2297 : y ≠ (N.op y (N.op z y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq2257
       have r₂ := eq2288
       grind)
    | exact resolve eq2257 eq2288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2257
  have eq2298 : x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq2256
       have r₂ := eq2284
       grind)
    | exact resolve eq2256 eq2284
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2256
  have eq2302 : x = (M.op x x) ∨ y = z := by
    first
    | (have r₁ := eq2298
       have r₂ := eq2288
       grind)
    | exact resolve eq2298 eq2288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2298
  have eq3363 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ y = z := by
    intro X0
    first
    | (have i₁ := eq45 X0 x
       have i₂ := eq2302
       grind)
    | exact superpose eq2302 eq45
    | exact resolve eq45 eq2302
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq3368 : x = (N.op x x) ∨ y = z := by
    first
    | (have i₁ := eq92 x
       have i₂ := eq2302
       grind)
    | exact superpose eq2302 eq92
    | exact resolve eq92 eq2302
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq92 eq2302
  have eq4594 : x ≠ (N.op y (N.op z x)) ∨ y = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq3368
       grind)
    | exact superpose eq3368 eq10
    | exact resolve eq10 eq3368
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq7276 : x ≠ (N.op y x) ∨ y = z ∨ x = z ∨ y = z := by
    first
    | (have i₁ := eq4594
       have i₂ := eq3363 z
       grind)
    | exact superpose eq3363 eq4594
    | (have j1 := eq3363 z
       grind)
    | (have r₁ := eq4594
       have r₂ := eq3363 (N.op y (N.op z x))
       grind)
    | exact resolve eq4594 eq3363
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq7277 : x ≠ (N.op y x) ∨ y = z ∨ x = z := by grind
  clear eq7276
  have eq7833 : x ≠ x ∨ y = z ∨ x = z ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq7277
       have i₂ := eq3363 y
       grind)
    | exact superpose eq3363 eq7277
    | (have j1 := eq3363 z
       grind)
    | (have r₁ := eq7277
       have r₂ := eq3363 y
       grind)
    | (have r₁ := eq7277
       have r₂ := eq3363 (N.op y x)
       grind)
    | exact resolve eq7277 eq3363
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7277
  have eq7835 : x ≠ x ∨ y = z ∨ x = z ∨ x = y := by grind
  clear eq7833
  have eq7836 : y = z ∨ x = z ∨ x = y := by grind
  clear eq7835
  have eq9605 : y ≠ (N.op y (N.op y y)) ∨ x = (M.op x x) ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq2297
       have i₂ := eq7836
       grind)
    | exact superpose eq7836 eq2297
    | exact resolve eq2297 eq7836
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2297 eq7836
  have eq9608 : y ≠ (N.op y (N.op y y)) ∨ x = (M.op x x) ∨ x = z := by
    first
    | (have r₁ := eq9605
       have r₂ := eq2288
       grind)
    | exact resolve eq9605 eq2288
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9605
  have eq29039 : y ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq9608
       have i₂ := eq2284
       grind)
    | exact superpose eq2284 eq9608
    | exact resolve eq9608 eq2284
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9608
  have eq29043 : y ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = z := by grind
  clear eq29039
  have eq29044 : x = (M.op x x) ∨ x = z := by
    first
    | (have r₁ := eq29043
       have r₂ := eq2284
       grind)
    | exact resolve eq29043 eq2284
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2284 eq29043
  have eq30698 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq45 X0 x
       have i₂ := eq29044
       grind)
    | exact superpose eq29044 eq45
    | exact resolve eq45 eq29044
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45
  have eq30731 : x ≠ x ∨ x = (N.op x x) ∨ x = z := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq29044
       grind)
    | exact superpose eq29044 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq29044
       grind)
    | exact resolve eq7 eq29044
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29044
  have eq30732 : x = (N.op x x) ∨ x = z := by grind
  clear eq30731
  have eq35158 : x ≠ (N.op y (N.op z x)) ∨ x = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq30732
       grind)
    | exact superpose eq30732 eq10
    | exact resolve eq10 eq30732
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq40164 : x ≠ (N.op y x) ∨ x = z ∨ x = z ∨ x = z := by
    first
    | (have i₁ := eq35158
       have i₂ := eq30698 z
       grind)
    | exact superpose eq30698 eq35158
    | (have j1 := eq30698 z
       grind)
    | (have r₁ := eq35158
       have r₂ := eq30698 (N.op y (N.op z x))
       grind)
    | exact resolve eq35158 eq30698
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq35158
  have eq40167 : x ≠ (N.op y x) ∨ x = z := by grind
  clear eq40164
  have eq42478 : x ≠ x ∨ x = z ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq40167
       have i₂ := eq30698 y
       grind)
    | exact superpose eq30698 eq40167
    | (have j1 := eq30698 z
       grind)
    | (have r₁ := eq40167
       have r₂ := eq30698 y
       grind)
    | (have r₁ := eq40167
       have r₂ := eq30698 (N.op y x)
       grind)
    | exact resolve eq40167 eq30698
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30698
  have eq42482 : x ≠ x ∨ x = z ∨ x = y := by grind
  clear eq42478
  have eq42483 : x = z ∨ x = y := by grind
  clear eq42482
  have eq44967 : x ≠ (N.op y (N.op x x)) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq4594
       have i₂ := eq42483
       grind)
    | exact superpose eq42483 eq4594
    | exact resolve eq4594 eq42483
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4594
  have eq44968 : x ≠ (N.op y (N.op x x)) ∨ x = y := by grind
  clear eq44967
  have eq46640 : x ≠ (N.op y x) ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq44968
       have i₂ := eq3368
       grind)
    | exact superpose eq3368 eq44968
    | exact resolve eq44968 eq3368
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3368 eq44968
  have eq46645 : y = z ∨ x = y := by
    first
    | (have j1 := eq3363 y
       grind)
    | (have r₁ := eq46640
       have r₂ := eq3363 y
       grind)
    | (have r₁ := eq46640
       have r₂ := eq3363 (N.op y x)
       grind)
    | exact resolve eq46640 eq3363
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3363 eq46640
  have eq48489 : x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq42483
       have i₂ := eq46645
       grind)
    | exact superpose eq46645 eq42483
    | exact resolve eq42483 eq46645
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq42483 eq46645
  have eq48490 : x = y := by grind
  clear eq48489
  have eq50608 : x ≠ x ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq2288
       have i₂ := eq48490
       grind)
    | exact superpose eq48490 eq2288
    | (have r₁ := eq2288
       have r₂ := eq48490
       grind)
    | exact resolve eq2288 eq48490
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2288
  have eq50614 : x ≠ (N.op x x) ∨ x = z := by
    first
    | (have i₁ := eq40167
       have i₂ := eq48490
       grind)
    | exact superpose eq48490 eq40167
    | exact resolve eq40167 eq48490
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq40167
  have eq50616 : x = (M.op x x) := by grind
  clear eq50608
  have eq50619 : x = z := by
    first
    | (have r₁ := eq50614
       have r₂ := eq30732
       grind)
    | exact resolve eq50614 eq30732
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30732 eq50614
  have eq54934 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq50616
       grind)
    | exact superpose eq50616 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq50616
       grind)
    | exact resolve eq7 eq50616
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq50616
  have eq54935 : x = (N.op x x) := by grind
  clear eq54934
  have eq58652 : x ≠ (N.op y (N.op z x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq54935
       grind)
    | exact superpose eq54935 eq10
    | exact resolve eq10 eq54935
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq58661 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq58652
       have i₂ := eq50619
       grind)
    | exact superpose eq50619 eq58652
    | exact resolve eq58652 eq50619
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq50619 eq58652
  have eq58663 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq58661
       have i₂ := eq54935
       grind)
    | exact superpose eq54935 eq58661
    | exact resolve eq58661 eq54935
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq58661
  have eq58665 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq58663
       have i₂ := eq48490
       grind)
    | exact superpose eq48490 eq58663
    | exact resolve eq58663 eq48490
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq48490 eq58663
  have eq58667 : False := by grind
  exact eq58667

/-- `Equation4272` from `Equation3011`: `x = ((y ◇ (z ◇ z)) ◇ y) ◇ x` ⊢ `x ◇ (x ◇ x) = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation4272_StructuralFromFin_Equation3011 : Law4272.StructuralFromFin Law3011 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3011 (fun {G} M hM ↦ ?_)
  have hm := (@Law3011.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law4272 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law4272.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op X1 (M.op X2 X2)) X1) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x (N.op x x)) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op X0 X0) (M.op X1 X1)) X2) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X0) (M.op X1 X1)) X0
       have i₂ := eq6 (M.op (M.op X0 X0) (M.op X1 X1)) (M.op X0 X0) X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq22 : ∀ X0 X2 : G, (M.op (M.op X0 X0) X2) = X2 := by
    intro X0 X2
    first
    | (have i₁ := eq6 X2 (M.op X0 X0) x
       have i₂ := eq13 X0 x (M.op X0 X0)
       grind)
    | exact superpose eq13 eq6
    | exact resolve eq6 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq30 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq32 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq30
  have eq38 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op X0 X0)
       have i₂ := eq22 X0 x
       grind)
    | exact superpose eq22 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X0)
       have r₂ := eq22 X0 x
       grind)
    | exact resolve eq8 eq22
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq39 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq22 X1 X0
       grind)
    | exact superpose eq22 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq22 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq22
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq22
  have eq44 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq38 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq38
  have eq91 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq39 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq175 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq32
       have i₂ := eq44 y x
       grind)
    | exact superpose eq44 eq32
    | (have j1 := eq44 x x
       grind)
    | exact resolve eq32 eq44
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq178 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq44 y x
       grind)
    | (have r₁ := eq175
       have r₂ := eq44 x x
       grind)
    | (have r₁ := eq175
       have r₂ := eq44 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq175 eq44
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq175
  have eq214 : (N.op x y) ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq32
       have i₂ := eq178
       grind)
    | exact superpose eq178 eq32
    | exact resolve eq32 eq178
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq226 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq44 X0 x
       have i₂ := eq178
       grind)
    | exact superpose eq178 eq44
    | (have j0 := eq44 x x
       grind)
    | exact resolve eq44 eq178
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq227 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq91 x
       have i₂ := eq178
       grind)
    | exact superpose eq178 eq91
    | exact resolve eq91 eq178
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq228 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq178
  have eq231 : (N.op x y) ≠ (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq214
       have r₂ := eq228
       grind)
    | exact resolve eq214 eq228
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq214
  have eq594 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq231
       have i₂ := eq227
       grind)
    | exact superpose eq227 eq231
    | exact resolve eq231 eq227
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq227 eq231
  have eq597 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq594
  have eq1086 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq597
       have i₂ := eq226 x
       grind)
    | exact superpose eq226 eq597
    | (have j1 := eq226 x
       grind)
    | (have r₁ := eq597
       have r₂ := eq226 x
       grind)
    | (have r₁ := eq597
       have r₂ := eq226 (N.op x y)
       grind)
    | exact resolve eq597 eq226
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq226 eq597
  have eq1087 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq1086
  have eq1088 : x = (M.op x x) ∨ x = y := by grind
  clear eq1087
  have eq1091 : x = (M.op x x) := by
    first
    | (have r₁ := eq1088
       have r₂ := eq228
       grind)
    | exact resolve eq1088 eq228
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq228 eq1088
  have eq1184 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq44 X0 x
       have i₂ := eq1091
       grind)
    | exact superpose eq1091 eq44
    | exact resolve eq44 eq1091
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq44
  have eq1187 : x = (N.op x x) := by
    first
    | (have i₁ := eq91 x
       have i₂ := eq1091
       grind)
    | exact superpose eq1091 eq91
    | exact resolve eq91 eq1091
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq91 eq1091
  have eq1629 : (N.op x x) ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1187
       grind)
    | exact superpose eq1187 eq10
    | exact resolve eq10 eq1187
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1633 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq1629
       have i₂ := eq1187
       grind)
    | exact superpose eq1187 eq1629
    | exact resolve eq1629 eq1187
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1187 eq1629
  have eq1999 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq1633
       have i₂ := eq1184 y
       grind)
    | exact superpose eq1184 eq1633
    | (have j1 := eq1184 y
       grind)
    | (have r₁ := eq1633
       have r₂ := eq1184 y
       grind)
    | (have r₁ := eq1633
       have r₂ := eq1184 (N.op y x)
       grind)
    | exact resolve eq1633 eq1184
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1184 eq1633
  have eq2000 : x = y := by grind
  clear eq1999
  have eq2398 : (N.op x (N.op x x)) ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq2000
       grind)
    | exact superpose eq2000 eq10
    | exact resolve eq10 eq2000
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2000
  have eq2414 : False := by grind
  exact eq2414
