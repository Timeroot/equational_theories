import equational_theories.Definability.AutBox2
import equational_theories.Definability.Kc_03

/-!
# Structurality from `Equation3587` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3587` is `x ◇ y = z ◇ ((x ◇ y) ◇ y)`, and the companion is
`x □ y = if a = b then (M.op a a) else if (M.op a b) = a then b else (M.op b a)`.  Its automorphism half is `GuardAut2_x_y_pxy_x_pxx_y_pyx_Equation3587`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches none of these on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation1644` from `Equation3587`: `x ◇ y = z ◇ ((x ◇ y) ◇ y)` ⊢ `x = (x ◇ y) ◇ ((x ◇ x) ◇ x)`, through the
companion `x □ y = if a = b then (M.op a a) else if (M.op a b) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation1644_StructuralFromFin_Equation3587 : Law1644.StructuralFromFin Law3587 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_x_y_pxy_x_pxx_y_pyx_Equation3587 (fun {G} M hM ↦ ?_)
  have hm := (@Law3587.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, a ≠ b ∨ N.op a b = (M.op a a)) →
      (∀ a b : G, a = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, a = b ∨ (M.op a b) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law1644 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law1644.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X0 X1) X1)) := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, X0 ≠ X1 ∨ (N.op X0 X1) = (M.op X0 X0) := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ X0 = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (N.op X0 X1) = (M.op X1 X0) ∨ (M.op X0 X1) = X0 ∨ X0 = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op (N.op x y) (N.op (N.op x x) x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op X2 (M.op (M.op X0 X1) X1)) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X1) X1) X3
       have i₂ := eq6 X0 X1 (M.op X2 (M.op (M.op X0 X1) X1))
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X3 : G, (M.op X0 X1) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X3
    first
    | (have i₁ := eq13 X0 X1 x X3
       have i₂ := eq6 X0 X1 x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq19 : ∀ X0 X1 : G, (M.op X0 X1) = (M.op (M.op X0 X1) X1) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 X1 x
       have i₂ := eq14 (M.op X0 X1) X1 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq24 : x ≠ (N.op (N.op x y) (N.op (M.op x x) x)) := by grind
  have eq37 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X0 X1) = X1 ∨ (N.op (M.op X0 X1) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq8 (M.op X0 X1) X1
       have i₂ := eq19 X0 X1
       grind)
    | exact superpose eq19 eq8
    | (have j0 := eq8 (M.op X0 X1) X1
       grind)
    | (have r₁ := eq8 (M.op X0 X1) X1
       have r₂ := eq19 X0 X1
       grind)
    | exact resolve eq8 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq38 : ∀ X0 X1 : G, (N.op (M.op X0 X1) X1) = X1 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq37 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq37
  have eq43 : x ≠ (M.op (N.op (M.op x x) x) (N.op x y)) ∨ (N.op x y) = (M.op (N.op x y) (N.op (M.op x x) x)) ∨ (N.op x y) = (N.op (M.op x x) x) := by
    first
    | (have i₁ := eq24
       have i₂ := eq9 (N.op x y) (N.op (M.op x x) x)
       grind)
    | exact superpose eq9 eq24
    | (have j1 := eq9 (N.op x y) (N.op (M.op x x) x)
       grind)
    | (have r₁ := eq24
       have r₂ := eq9 x (N.op (N.op x y) (N.op (M.op x x) x))
       grind)
    | (have r₁ := eq24
       have r₂ := eq9 (N.op (N.op x y) (N.op (M.op x x) x)) x
       grind)
    | exact resolve eq24 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq44 : x ≠ (N.op (M.op y x) (N.op (M.op x x) x)) ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq24
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq24
    | (have j1 := eq9 x y
       grind)
    | (have r₁ := eq24
       have r₂ := eq9 x (N.op (N.op x y) (N.op (M.op x x) x))
       grind)
    | (have r₁ := eq24
       have r₂ := eq9 (N.op (N.op x y) (N.op (M.op x x) x)) x
       grind)
    | exact resolve eq24 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq56 : x ≠ (N.op (N.op x y) x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq24
       have i₂ := eq38 x x
       grind)
    | exact superpose eq38 eq24
    | (have j1 := eq38 x x
       grind)
    | exact resolve eq24 eq38
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq60 : x ≠ (N.op (M.op y x) x) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq44
       have i₂ := eq38 x x
       grind)
    | exact superpose eq38 eq44
    | (have j1 := eq38 x x
       grind)
    | exact resolve eq44 eq38
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq70 : x ≠ x ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq60
       have i₂ := eq38 y x
       grind)
    | exact superpose eq38 eq60
    | (have j1 := eq38 x x
       grind)
    | (have r₁ := eq60
       have r₂ := eq38 y x
       grind)
    | exact resolve eq60 eq38
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq60
  have eq72 : x = (M.op y x) ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op x y) := by grind
  clear eq70
  have eq77 : x = (M.op x x) ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq19 y x
       have i₂ := eq72
       grind)
    | exact superpose eq72 eq19
    | exact resolve eq19 eq72
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19 eq72
  have eq80 : x = (M.op x y) ∨ x = y ∨ x = (M.op x x) := by grind
  clear eq77
  have eq92 : ∀ X0 : G, x = (M.op x x) ∨ x = (M.op X0 x) ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq14 x y x
       have i₂ := eq80
       grind)
    | exact superpose eq80 eq14
    | exact resolve eq14 eq80
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq138 : x ≠ x ∨ x = (M.op x x) ∨ x = y := by
    first
    | (have j0 := eq92 x
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq92
  have eq139 : x = (M.op x x) ∨ x = y := by grind
  clear eq138
  have eq166 : x ≠ (M.op (N.op x x) (N.op x y)) ∨ (N.op x y) = (M.op (N.op x y) (N.op x x)) ∨ (N.op x y) = (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq43
       have i₂ := eq139
       grind)
    | exact superpose eq139 eq43
    | exact resolve eq43 eq139
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq43
  have eq167 : x ≠ (N.op (M.op y x) (N.op x x)) ∨ x = (M.op x y) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq44
       have i₂ := eq139
       grind)
    | exact superpose eq139 eq44
    | exact resolve eq44 eq139
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq44
  have eq170 : ∀ X0 : G, x = (M.op X0 x) ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq14 x x x
       have i₂ := eq139
       grind)
    | exact superpose eq139 eq14
    | exact resolve eq14 eq139
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq139
  have eq172 : x ≠ (N.op (M.op y x) (N.op x x)) ∨ x = (M.op x y) ∨ x = y := by grind
  clear eq167
  have eq175 : x ≠ (N.op (M.op y x) (M.op x x)) ∨ x = (M.op x y) ∨ x = y := by grind
  clear eq172
  have eq176 : x ≠ (M.op (M.op x x) (N.op x y)) ∨ (N.op x y) = (M.op (N.op x y) (N.op x x)) ∨ (N.op x y) = (N.op x x) ∨ x = y := by grind
  clear eq166
  have eq177 : (N.op x y) = (M.op (N.op x y) (M.op x x)) ∨ x ≠ (M.op (M.op x x) (N.op x y)) ∨ (N.op x y) = (N.op x x) ∨ x = y := by grind
  clear eq176
  have eq178 : (N.op x y) = (M.op x x) ∨ x ≠ (M.op (M.op x x) (N.op x y)) ∨ (N.op x y) = (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq177
       have i₂ := eq14 x x (N.op x y)
       grind)
    | exact superpose eq14 eq177
    | exact resolve eq177 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq177
  have eq179 : (N.op x y) = (M.op x x) ∨ (N.op x y) = (M.op x x) ∨ x ≠ (M.op (M.op x x) (N.op x y)) ∨ x = y := by grind
  clear eq178
  have eq180 : x ≠ (M.op (M.op x x) (N.op x y)) ∨ (N.op x y) = (M.op x x) ∨ x = y := by grind
  clear eq179
  have eq454 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x y) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq175
       have i₂ := eq170 y
       grind)
    | exact superpose eq170 eq175
    | exact resolve eq175 eq170
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq175
  have eq474 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x y) ∨ x = y := by grind
  clear eq454
  have eq512 : x ≠ (M.op x (N.op x y)) ∨ x = (N.op x y) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq180
       have i₂ := eq170 x
       grind)
    | exact superpose eq170 eq180
    | exact resolve eq180 eq170
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq180
  have eq521 : x ≠ (M.op x (N.op x y)) ∨ x = (N.op x y) ∨ x = y := by grind
  clear eq512
  have eq560 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq474
       have i₂ := eq170 x
       grind)
    | exact superpose eq170 eq474
    | exact resolve eq474 eq170
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq170 eq474
  have eq566 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ x = y := by grind
  clear eq560
  have eq571 : x ≠ (M.op x x) ∨ x = (M.op x y) ∨ x = y := by grind
  clear eq566
  have eq573 : x = (M.op x y) ∨ x = y := by
    first
    | (have r₁ := eq571
       have r₂ := eq80
       grind)
    | exact resolve eq571 eq80
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq80 eq571
  have eq576 : x ≠ x ∨ x = y ∨ y = (N.op x y) ∨ x = y := by
    first
    | (have i₁ := eq8 x y
       have i₂ := eq573
       grind)
    | exact superpose eq573 eq8
    | (have j0 := eq8 x y
       grind)
    | (have r₁ := eq8 x y
       have r₂ := eq573
       grind)
    | exact resolve eq8 eq573
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq580 : x ≠ x ∨ x = y ∨ y = (N.op x y) := by grind
  clear eq576
  have eq581 : y = (N.op x y) ∨ x = y := by grind
  clear eq580
  have eq601 : x ≠ (M.op x y) ∨ x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq521
       have i₂ := eq581
       grind)
    | exact superpose eq581 eq521
    | exact resolve eq521 eq581
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq521 eq581
  have eq604 : x ≠ (M.op x y) ∨ x = y := by grind
  clear eq601
  have eq614 : x = y := by
    first
    | (have r₁ := eq604
       have r₂ := eq573
       grind)
    | exact resolve eq604 eq573
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq573 eq604
  have eq636 : x ≠ (N.op (N.op x x) x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq56
       have i₂ := eq614
       grind)
    | exact superpose eq614 eq56
    | exact resolve eq56 eq614
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq56
  have eq639 : x ≠ (N.op (M.op x x) x) ∨ x = (M.op x x) := by grind
  clear eq636
  have eq644 : x = (M.op x x) := by
    first
    | (have j1 := eq38 x x
       grind)
    | (have r₁ := eq639
       have r₂ := eq38 x x
       grind)
    | exact resolve eq639 eq38
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq38 eq639
  have eq674 : x ≠ (N.op (N.op x y) (N.op x x)) := by
    first
    | (have i₁ := eq24
       have i₂ := eq644
       grind)
    | exact superpose eq644 eq24
    | exact resolve eq24 eq644
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq24
  have eq691 : ∀ X0 : G, x = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 x x x
       have i₂ := eq644
       grind)
    | exact superpose eq644 eq14
    | exact resolve eq14 eq644
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq695 : x ≠ (N.op (N.op x y) (M.op x x)) := by grind
  clear eq674
  have eq697 : x ≠ (N.op (N.op x y) x) := by
    first
    | (have i₁ := eq695
       have i₂ := eq691 x
       grind)
    | exact superpose eq691 eq695
    | exact resolve eq695 eq691
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq695
  have eq699 : x ≠ (N.op (N.op x x) x) := by
    first
    | (have i₁ := eq697
       have i₂ := eq614
       grind)
    | exact superpose eq614 eq697
    | exact resolve eq697 eq614
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq614 eq697
  have eq701 : x ≠ (N.op (M.op x x) x) := by grind
  clear eq699
  have eq703 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq701
       have i₂ := eq691 x
       grind)
    | exact superpose eq691 eq701
    | exact resolve eq701 eq691
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq691 eq701
  have eq705 : x ≠ (M.op x x) := by grind
  clear eq703
  have eq707 : False := by grind
  exact eq707

/-- `Equation1647` from `Equation3587`: `x ◇ y = z ◇ ((x ◇ y) ◇ y)` ⊢ `x = (x ◇ y) ◇ ((x ◇ y) ◇ x)`, through the
companion `x □ y = if a = b then (M.op a a) else if (M.op a b) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation1647_StructuralFromFin_Equation3587 : Law1647.StructuralFromFin Law3587 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_x_y_pxy_x_pxx_y_pyx_Equation3587 (fun {G} M hM ↦ ?_)
  have hm := (@Law3587.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, a ≠ b ∨ N.op a b = (M.op a a)) →
      (∀ a b : G, a = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, a = b ∨ (M.op a b) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law1647 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law1647.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X0 X1) X1)) := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, X0 ≠ X1 ∨ (N.op X0 X1) = (M.op X0 X0) := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ X0 = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (N.op X0 X1) = (M.op X1 X0) ∨ (M.op X0 X1) = X0 ∨ X0 = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op (N.op x y) (N.op (N.op x y) x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op X2 (M.op (M.op X0 X1) X1)) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X1) X1) X3
       have i₂ := eq6 X0 X1 (M.op X2 (M.op (M.op X0 X1) X1))
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X3 : G, (M.op X0 X1) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X3
    first
    | (have i₁ := eq13 X0 X1 x X3
       have i₂ := eq6 X0 X1 x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq19 : ∀ X0 X1 : G, (M.op X0 X1) = (M.op (M.op X0 X1) X1) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 X1 x
       have i₂ := eq14 (M.op X0 X1) X1 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq36 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X0 X1) = X1 ∨ (N.op (M.op X0 X1) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq8 (M.op X0 X1) X1
       have i₂ := eq19 X0 X1
       grind)
    | exact superpose eq19 eq8
    | (have j0 := eq8 (M.op X0 X1) X1
       grind)
    | (have r₁ := eq8 (M.op X0 X1) X1
       have r₂ := eq19 X0 X1
       grind)
    | exact resolve eq8 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq37 : ∀ X0 X1 : G, (N.op (M.op X0 X1) X1) = X1 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq36 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36
  have eq43 : x ≠ (N.op (N.op x y) (M.op x (N.op x y))) ∨ (N.op x y) = (M.op (N.op x y) x) ∨ x = (N.op x y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 (N.op x y) x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 (N.op x y) x
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 x (N.op (N.op x y) (N.op (N.op x y) x))
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op (N.op x y) (N.op (N.op x y) x)) x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq45 : x ≠ (N.op (M.op y x) (N.op (M.op y x) x)) ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x y
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 x (N.op (N.op x y) (N.op (N.op x y) x))
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op (N.op x y) (N.op (N.op x y) x)) x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq48 : x ≠ (N.op (M.op y x) x) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq45
       have i₂ := eq37 y x
       grind)
    | exact superpose eq37 eq45
    | (have j1 := eq37 y x
       grind)
    | exact resolve eq45 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq52 : x = (M.op y x) ∨ x = y ∨ x = (M.op x y) := by
    first
    | (have j1 := eq37 y x
       grind)
    | (have r₁ := eq48
       have r₂ := eq37 y x
       grind)
    | exact resolve eq48 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq48
  have eq54 : x ≠ (M.op (M.op x (N.op x y)) (N.op x y)) ∨ (N.op x y) = (M.op (N.op x y) x) ∨ x = (N.op x y) ∨ (N.op x y) = (M.op (N.op x y) (M.op x (N.op x y))) ∨ (N.op x y) = (M.op x (N.op x y)) := by
    first
    | (have i₁ := eq43
       have i₂ := eq9 (N.op x y) (M.op x (N.op x y))
       grind)
    | exact superpose eq9 eq43
    | (have j1 := eq9 (N.op x y) x
       grind)
    | (have r₁ := eq43
       have r₂ := eq9 x (N.op (N.op x y) (M.op x (N.op x y)))
       grind)
    | (have r₁ := eq43
       have r₂ := eq9 (N.op (N.op x y) (M.op x (N.op x y))) x
       grind)
    | exact resolve eq43 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq43
  have eq55 : x ≠ (M.op x (N.op x y)) ∨ (N.op x y) = (M.op (N.op x y) x) ∨ x = (N.op x y) ∨ (N.op x y) = (M.op (N.op x y) (M.op x (N.op x y))) ∨ (N.op x y) = (M.op x (N.op x y)) := by
    first
    | (have i₁ := eq54
       have i₂ := eq19 x (N.op x y)
       grind)
    | exact superpose eq19 eq54
    | exact resolve eq54 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19 eq54
  have eq56 : (N.op x y) = (M.op x (N.op x y)) ∨ x ≠ (M.op x (N.op x y)) ∨ (N.op x y) = (M.op (N.op x y) x) ∨ x = (N.op x y) ∨ (N.op x y) = (M.op x (N.op x y)) := by
    first
    | (have i₁ := eq55
       have i₂ := eq14 x (N.op x y) (N.op x y)
       grind)
    | exact superpose eq14 eq55
    | exact resolve eq55 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq55
  have eq57 : x ≠ (M.op x (N.op x y)) ∨ (N.op x y) = (M.op x (N.op x y)) ∨ (N.op x y) = (M.op (N.op x y) x) ∨ x = (N.op x y) := by grind
  clear eq56
  have eq58 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op x y) ∨ x = y ∨ x = y ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq45
       have i₂ := eq52
       grind)
    | exact superpose eq52 eq45
    | exact resolve eq45 eq52
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45
  have eq61 : ∀ X0 : G, x = (M.op x y) ∨ x = (M.op X0 x) ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq14 y x x
       have i₂ := eq52
       grind)
    | exact superpose eq52 eq14
    | exact resolve eq14 eq52
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq52
  have eq64 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op x y) ∨ x = y := by grind
  clear eq58
  have eq66 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x y) ∨ x = y := by grind
  clear eq64
  have eq75 : ∀ X0 X1 : G, x = (M.op X1 x) ∨ x = (M.op X0 x) ∨ x = y := by
    intro X0 X1
    first
    | (have i₁ := eq14 x y x
       have i₂ := eq61 X0
       grind)
    | exact superpose eq61 eq14
    | (have j1 := eq61 X1
       grind)
    | exact resolve eq14 eq61
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq109 : ∀ X0 : G, x ≠ x ∨ x = (M.op X0 x) ∨ x = y := by
    intro X0
    first
    | (have j0 := eq75 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq75
  have eq110 : ∀ X0 : G, x = (M.op X0 x) ∨ x = y := by
    intro X0
    first
    | (have j0 := eq109 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq109
  have eq146 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq66
       have i₂ := eq110 x
       grind)
    | exact superpose eq110 eq66
    | exact resolve eq66 eq110
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq66
  have eq152 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ x = y := by grind
  clear eq146
  have eq153 : x ≠ (M.op x x) ∨ x = (M.op x y) ∨ x = y := by grind
  clear eq152
  have eq154 : x = (M.op x y) ∨ x = y := by
    first
    | (have r₁ := eq153
       have r₂ := eq61 x
       grind)
    | exact resolve eq153 eq61
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq61 eq153
  have eq156 : x ≠ x ∨ x = y ∨ y = (N.op x y) ∨ x = y := by
    first
    | (have i₁ := eq8 x y
       have i₂ := eq154
       grind)
    | exact superpose eq154 eq8
    | (have j0 := eq8 x y
       grind)
    | (have r₁ := eq8 x y
       have r₂ := eq154
       grind)
    | exact resolve eq8 eq154
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq160 : x ≠ x ∨ x = y ∨ y = (N.op x y) := by grind
  clear eq156
  have eq161 : y = (N.op x y) ∨ x = y := by grind
  clear eq160
  have eq182 : x ≠ (M.op x y) ∨ y = (M.op x y) ∨ y = (M.op y x) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq57
       have i₂ := eq161
       grind)
    | exact superpose eq161 eq57
    | exact resolve eq57 eq161
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq57 eq161
  have eq189 : x ≠ (M.op x y) ∨ y = (M.op x y) ∨ y = (M.op y x) ∨ x = y := by grind
  clear eq182
  have eq191 : y = (M.op y x) ∨ y = (M.op x y) ∨ x = y := by
    first
    | (have r₁ := eq189
       have r₂ := eq154
       grind)
    | exact resolve eq189 eq154
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq189
  have eq430 : x = y ∨ x = y ∨ y = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq110 y
       have i₂ := eq191
       grind)
    | exact superpose eq191 eq110
    | exact resolve eq110 eq191
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq110 eq191
  have eq445 : y = (M.op x y) ∨ x = y := by grind
  clear eq430
  have eq468 : x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq154
       have i₂ := eq445
       grind)
    | exact superpose eq445 eq154
    | exact resolve eq154 eq445
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq154 eq445
  have eq476 : x = y := by grind
  clear eq468
  have eq491 : x ≠ (N.op (N.op x x) (N.op (N.op x x) x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq476
       grind)
    | exact superpose eq476 eq10
    | exact resolve eq10 eq476
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq476
  have eq498 : x ≠ (N.op (M.op x x) (N.op (M.op x x) x)) := by grind
  clear eq491
  have eq578 : x ≠ (N.op (M.op x x) x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq498
       have i₂ := eq37 x x
       grind)
    | exact superpose eq37 eq498
    | (have j1 := eq37 x x
       grind)
    | exact resolve eq498 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq582 : x = (M.op x x) := by
    first
    | (have j1 := eq37 x x
       grind)
    | (have r₁ := eq578
       have r₂ := eq37 x x
       grind)
    | exact resolve eq578 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq37 eq578
  have eq595 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq498
       have i₂ := eq582
       grind)
    | exact superpose eq582 eq498
    | exact resolve eq498 eq582
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq498
  have eq603 : ∀ X0 : G, x = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 x x x
       have i₂ := eq582
       grind)
    | exact superpose eq582 eq14
    | exact resolve eq14 eq582
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq606 : x ≠ (N.op x (M.op x x)) := by grind
  clear eq595
  have eq607 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq606
       have i₂ := eq603 x
       grind)
    | exact superpose eq603 eq606
    | exact resolve eq606 eq603
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq603 eq606
  have eq608 : x ≠ (M.op x x) := by grind
  clear eq607
  have eq609 : False := by grind
  exact eq609

/-- `Equation1650` from `Equation3587`: `x ◇ y = z ◇ ((x ◇ y) ◇ y)` ⊢ `x = (x ◇ y) ◇ ((x ◇ z) ◇ x)`, through the
companion `x □ y = if a = b then (M.op a a) else if (M.op a b) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation1650_StructuralFromFin_Equation3587 : Law1650.StructuralFromFin Law3587 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_x_y_pxy_x_pxx_y_pyx_Equation3587 (fun {G} M hM ↦ ?_)
  have hm := (@Law3587.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, a ≠ b ∨ N.op a b = (M.op a a)) →
      (∀ a b : G, a = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, a = b ∨ (M.op a b) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law1650 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law1650.models_iff G N]
  intro x y z
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X0 X1) X1)) := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, X0 ≠ X1 ∨ (N.op X0 X1) = (M.op X0 X0) := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ X0 = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (N.op X0 X1) = (M.op X1 X0) ∨ (M.op X0 X1) = X0 ∨ X0 = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op (N.op x y) (N.op (N.op x z) x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op X2 (M.op (M.op X0 X1) X1)) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X1) X1) X3
       have i₂ := eq6 X0 X1 (M.op X2 (M.op (M.op X0 X1) X1))
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X3 : G, (M.op X0 X1) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X3
    first
    | (have i₁ := eq13 X0 X1 x X3
       have i₂ := eq6 X0 X1 x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq19 : ∀ X0 X1 : G, (M.op X0 X1) = (M.op (M.op X0 X1) X1) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 X1 x
       have i₂ := eq14 (M.op X0 X1) X1 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq36 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X0 X1) = X1 ∨ (N.op (M.op X0 X1) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq8 (M.op X0 X1) X1
       have i₂ := eq19 X0 X1
       grind)
    | exact superpose eq19 eq8
    | (have j0 := eq8 (M.op X0 X1) X1
       grind)
    | (have r₁ := eq8 (M.op X0 X1) X1
       have r₂ := eq19 X0 X1
       grind)
    | exact resolve eq8 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq37 : ∀ X0 X1 : G, (N.op (M.op X0 X1) X1) = X1 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq36 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36
  have eq45 : x ≠ (N.op (M.op y x) (N.op (N.op x z) x)) ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x y
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 x (N.op (N.op x y) (N.op (N.op x z) x))
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op (N.op x y) (N.op (N.op x z) x)) x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq46 : x ≠ (N.op (N.op x y) (N.op (M.op z x) x)) ∨ x = (M.op x z) ∨ x = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x z
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x z
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 x (N.op (N.op x y) (N.op (N.op x z) x))
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op (N.op x y) (N.op (N.op x z) x)) x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq49 : x ≠ (N.op (M.op y x) (N.op (M.op z x) x)) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x z) ∨ x = z := by
    first
    | (have i₁ := eq45
       have i₂ := eq9 x z
       grind)
    | exact superpose eq9 eq45
    | (have j1 := eq9 x y
       grind)
    | (have r₁ := eq45
       have r₂ := eq9 x (N.op (M.op y x) (N.op (N.op x z) x))
       grind)
    | (have r₁ := eq45
       have r₂ := eq9 (N.op (M.op y x) (N.op (N.op x z) x)) x
       grind)
    | exact resolve eq45 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq50 : x ≠ (N.op (M.op y x) (M.op x (N.op x z))) ∨ x = (M.op x y) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) := by
    first
    | (have i₁ := eq45
       have i₂ := eq9 (N.op x z) x
       grind)
    | exact superpose eq9 eq45
    | (have j1 := eq9 x y
       grind)
    | (have r₁ := eq45
       have r₂ := eq9 x (N.op (M.op y x) (N.op (N.op x z) x))
       grind)
    | (have r₁ := eq45
       have r₂ := eq9 (N.op (M.op y x) (N.op (N.op x z) x)) x
       grind)
    | exact resolve eq45 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45
  have eq54 : x ≠ (N.op (N.op x y) x) ∨ x = (M.op x z) ∨ x = z ∨ x = (M.op z x) := by
    first
    | (have i₁ := eq46
       have i₂ := eq37 z x
       grind)
    | exact superpose eq37 eq46
    | (have j1 := eq37 z x
       grind)
    | exact resolve eq46 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq57 : x ≠ (N.op (M.op y x) x) ∨ x = (M.op x z) ∨ x = z ∨ x = (M.op z x) ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq54
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq54
    | (have j1 := eq9 x z
       grind)
    | (have r₁ := eq54
       have r₂ := eq9 x (N.op (N.op x y) x)
       grind)
    | (have r₁ := eq54
       have r₂ := eq9 (N.op (N.op x y) x) x
       grind)
    | exact resolve eq54 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq72 : x ≠ x ∨ x = (M.op x z) ∨ x = z ∨ x = (M.op z x) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq57
       have i₂ := eq37 y x
       grind)
    | exact superpose eq37 eq57
    | (have j1 := eq37 z x
       grind)
    | (have r₁ := eq57
       have r₂ := eq37 y x
       grind)
    | exact resolve eq57 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq57
  have eq74 : x = (M.op z x) ∨ x = z ∨ x = (M.op x z) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op y x) := by grind
  clear eq72
  have eq76 : x ≠ (M.op (M.op x (N.op x z)) (M.op y x)) ∨ x = (M.op x y) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) ∨ (M.op y x) = (M.op (M.op y x) (M.op x (N.op x z))) ∨ (M.op x (N.op x z)) = (M.op y x) := by
    first
    | (have i₁ := eq50
       have i₂ := eq9 (M.op y x) (M.op x (N.op x z))
       grind)
    | exact superpose eq9 eq50
    | (have j1 := eq9 x y
       grind)
    | (have r₁ := eq50
       have r₂ := eq9 x (N.op (M.op y x) (M.op x (N.op x z)))
       grind)
    | (have r₁ := eq50
       have r₂ := eq9 (N.op (M.op y x) (M.op x (N.op x z))) x
       grind)
    | exact resolve eq50 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq77 : x ≠ (M.op y x) ∨ x = (M.op x y) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) ∨ (M.op y x) = (M.op (M.op y x) (M.op x (N.op x z))) ∨ (M.op x (N.op x z)) = (M.op y x) := by
    first
    | (have i₁ := eq76
       have i₂ := eq14 y x (M.op x (N.op x z))
       grind)
    | exact superpose eq14 eq76
    | exact resolve eq76 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq76
  have eq78 : (M.op x (N.op x z)) = (M.op y x) ∨ x ≠ (M.op y x) ∨ x = (M.op x y) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) ∨ (M.op x (N.op x z)) = (M.op y x) := by
    first
    | (have i₁ := eq77
       have i₂ := eq14 x (N.op x z) (M.op y x)
       grind)
    | exact superpose eq14 eq77
    | exact resolve eq77 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq77
  have eq79 : x ≠ (M.op y x) ∨ (M.op x (N.op x z)) = (M.op y x) ∨ x = (M.op x y) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) := by grind
  clear eq78
  have eq84 : ∀ X0 : G, x = (M.op y x) ∨ x = (M.op X0 x) ∨ x = (M.op x z) ∨ x = (M.op x y) ∨ x = y ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq14 z x x
       have i₂ := eq74
       grind)
    | exact superpose eq74 eq14
    | exact resolve eq14 eq74
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq74
  have eq113 : x ≠ x ∨ x = (M.op y x) ∨ x = (M.op x z) ∨ x = (M.op x y) ∨ x = y ∨ x = z := by
    first
    | (have j0 := eq84 y
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq84
  have eq114 : x = (M.op y x) ∨ x = (M.op x z) ∨ x = (M.op x y) ∨ x = y ∨ x = z := by grind
  clear eq113
  have eq128 : x ≠ (N.op x (N.op (M.op z x) x)) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x z) ∨ x = z ∨ x = (M.op x z) ∨ x = (M.op x y) ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq49
       have i₂ := eq114
       grind)
    | exact superpose eq114 eq49
    | exact resolve eq49 eq114
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq49
  have eq134 : ∀ X0 : G, x = (M.op x z) ∨ x = (M.op X0 x) ∨ x = (M.op x y) ∨ x = y ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq14 y x x
       have i₂ := eq114
       grind)
    | exact superpose eq114 eq14
    | exact resolve eq14 eq114
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq114
  have eq141 : x ≠ (N.op x (N.op (M.op z x) x)) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x z) ∨ x = z := by grind
  clear eq128
  have eq156 : ∀ X0 X1 : G, x = (M.op x y) ∨ x = (M.op X1 x) ∨ x = (M.op X0 x) ∨ x = y ∨ x = z := by
    intro X0 X1
    first
    | (have i₁ := eq14 x z x
       have i₂ := eq134 X0
       grind)
    | exact superpose eq134 eq14
    | (have j1 := eq134 X1
       grind)
    | exact resolve eq14 eq134
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq240 : ∀ X0 : G, x ≠ x ∨ x = (M.op x y) ∨ x = (M.op X0 x) ∨ x = y ∨ x = z := by
    intro X0
    first
    | (have j0 := eq156 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq156
  have eq241 : ∀ X0 : G, x = (M.op x y) ∨ x = (M.op X0 x) ∨ x = y ∨ x = z := by
    intro X0
    first
    | (have j0 := eq240 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq240
  have eq286 : ∀ X0 : G, x ≠ x ∨ x = y ∨ y = (N.op x y) ∨ x = (M.op X0 x) ∨ x = y ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq8 x y
       have i₂ := eq241 X0
       grind)
    | exact superpose eq241 eq8
    | (have j0 := eq8 x y
       have j1 := eq241 X0
       grind)
    | (have r₁ := eq8 x y
       have r₂ := eq241 X0
       grind)
    | (have r₁ := eq8 x x
       have r₂ := eq241 x
       grind)
    | exact resolve eq8 eq241
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq288 : ∀ X0 X1 : G, x = (M.op X1 x) ∨ x = (M.op X0 x) ∨ x = y ∨ x = z := by
    intro X0 X1
    first
    | (have i₁ := eq14 x y x
       have i₂ := eq241 X0
       grind)
    | exact superpose eq241 eq14
    | (have j1 := eq241 X1
       grind)
    | exact resolve eq14 eq241
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq292 : x ≠ (N.op x (M.op x (N.op x z))) ∨ x = (M.op x y) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) ∨ x = (M.op x y) ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq50
       have i₂ := eq241 y
       grind)
    | exact superpose eq241 eq50
    | exact resolve eq50 eq241
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq50
  have eq296 : x ≠ x ∨ x = (M.op x (N.op x z)) ∨ x = (M.op x y) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) ∨ x = (M.op x y) ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq79
       have i₂ := eq241 y
       grind)
    | exact superpose eq241 eq79
    | (have r₁ := eq79
       have r₂ := eq241 y
       grind)
    | exact resolve eq79 eq241
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq79 eq241
  have eq309 : x ≠ x ∨ x = (M.op x (N.op x z)) ∨ x = (M.op x y) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) ∨ x = z := by grind
  clear eq296
  have eq310 : (N.op x z) = (M.op (N.op x z) x) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x (N.op x z)) ∨ x = (N.op x z) ∨ x = z := by grind
  clear eq309
  have eq316 : x ≠ (N.op x (M.op x (N.op x z))) ∨ x = (M.op x y) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) ∨ x = z := by grind
  clear eq292
  have eq319 : ∀ X0 : G, x ≠ x ∨ x = y ∨ y = (N.op x y) ∨ x = (M.op X0 x) ∨ x = z := by
    intro X0
    first
    | (have j0 := eq286 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq286
  have eq320 : ∀ X0 : G, y = (N.op x y) ∨ x = (M.op X0 x) ∨ x = y ∨ x = z := by
    intro X0
    first
    | (have j0 := eq319 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq319
  have eq365 : ∀ X0 : G, x ≠ x ∨ x = (M.op X0 x) ∨ x = y ∨ x = z := by
    intro X0
    first
    | (have j0 := eq288 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq288
  have eq366 : ∀ X0 : G, x = (M.op X0 x) ∨ x = y ∨ x = z := by
    intro X0
    first
    | (have j0 := eq365 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq365
  have eq412 : x ≠ (N.op (N.op x y) (N.op x x)) ∨ x = (M.op x z) ∨ x = z ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq46
       have i₂ := eq366 z
       grind)
    | exact superpose eq366 eq46
    | exact resolve eq46 eq366
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq419 : x ≠ (N.op (N.op x y) (N.op x x)) ∨ x = (M.op x z) ∨ x = z ∨ x = y := by grind
  clear eq412
  have eq434 : x ≠ (N.op (N.op x y) (M.op x x)) ∨ x = (M.op x z) ∨ x = z ∨ x = y := by grind
  clear eq419
  have eq1082 : x ≠ (N.op (N.op x y) x) ∨ x = (M.op x z) ∨ x = z ∨ x = y ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq434
       have i₂ := eq366 x
       grind)
    | exact superpose eq366 eq434
    | exact resolve eq434 eq366
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq434
  have eq1088 : x ≠ (N.op (N.op x y) x) ∨ x = (M.op x z) ∨ x = z ∨ x = y := by grind
  clear eq1082
  have eq1107 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x z) ∨ x = z ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq141
       have i₂ := eq366 z
       grind)
    | exact superpose eq366 eq141
    | exact resolve eq141 eq366
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq141
  have eq1115 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x z) ∨ x = z := by grind
  clear eq1107
  have eq1128 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x z) ∨ x = z := by grind
  clear eq1115
  have eq2116 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x z) ∨ x = z ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq1128
       have i₂ := eq366 x
       grind)
    | exact superpose eq366 eq1128
    | exact resolve eq1128 eq366
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1128
  have eq2122 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x z) ∨ x = z := by grind
  clear eq2116
  have eq2132 : x ≠ (M.op x x) ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x z) ∨ x = z := by grind
  clear eq2122
  have eq2133 : x = (M.op x z) ∨ x = y ∨ x = (M.op x y) ∨ x = z := by
    first
    | (have r₁ := eq2132
       have r₂ := eq134 x
       grind)
    | exact resolve eq2132 eq134
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq134 eq2132
  have eq2184 : x ≠ x ∨ x = z ∨ z = (N.op x z) ∨ x = y ∨ x = (M.op x y) ∨ x = z := by
    first
    | (have i₁ := eq8 x z
       have i₂ := eq2133
       grind)
    | exact superpose eq2133 eq8
    | (have j0 := eq8 x z
       grind)
    | (have r₁ := eq8 x z
       have r₂ := eq2133
       grind)
    | (have r₁ := eq8 x y
       have r₂ := eq2133
       grind)
    | exact resolve eq8 eq2133
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2188 : x ≠ x ∨ x = z ∨ z = (N.op x z) ∨ x = y ∨ x = (M.op x y) := by grind
  clear eq2184
  have eq2189 : z = (N.op x z) ∨ x = z ∨ x = y ∨ x = (M.op x y) := by grind
  clear eq2188
  have eq2431 : x = (N.op x z) ∨ y = (N.op x y) ∨ x = y ∨ x = z ∨ x = (M.op x y) ∨ x = y ∨ x = (M.op x (N.op x z)) ∨ x = (N.op x z) ∨ x = z := by
    first
    | (have i₁ := eq320 (N.op x z)
       have i₂ := eq310
       grind)
    | exact superpose eq310 eq320
    | exact resolve eq320 eq310
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq310 eq320
  have eq2454 : x = (N.op x z) ∨ y = (N.op x y) ∨ x = y ∨ x = z ∨ x = (M.op x y) ∨ x = (M.op x (N.op x z)) := by grind
  clear eq2431
  have eq2501 : x = (M.op x (N.op x z)) ∨ y = (N.op x y) ∨ x = y ∨ x = z ∨ x = (N.op x z) := by
    first
    | (have j1 := eq8 x y
       grind)
    | (have r₁ := eq2454
       have r₂ := eq8 x y
       grind)
    | (have r₁ := eq2454
       have r₂ := eq8 x (N.op x z)
       grind)
    | exact resolve eq2454 eq8
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2454
  have eq2575 : x = (M.op x z) ∨ y = (N.op x y) ∨ x = y ∨ x = z ∨ x = z ∨ x = z ∨ x = y ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq2501
       have i₂ := eq2189
       grind)
    | exact superpose eq2189 eq2501
    | exact resolve eq2501 eq2189
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2189
  have eq2583 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) ∨ x = z ∨ y = (N.op x y) ∨ x = y ∨ x = z ∨ x = (N.op x z) := by
    first
    | (have i₁ := eq316
       have i₂ := eq2501
       grind)
    | exact superpose eq2501 eq316
    | exact resolve eq316 eq2501
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq316 eq2501
  have eq2592 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) ∨ x = z ∨ y = (N.op x y) := by grind
  clear eq2583
  have eq2600 : x = (M.op x z) ∨ y = (N.op x y) ∨ x = y ∨ x = z ∨ x = (M.op x y) := by grind
  clear eq2575
  have eq2602 : x ≠ (N.op x x) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) ∨ x = z ∨ y = (N.op x y) := by
    first
    | (have j1 := eq8 x y
       grind)
    | (have r₁ := eq2592
       have r₂ := eq8 x (N.op x x)
       grind)
    | (have r₁ := eq2592
       have r₂ := eq8 (N.op x x) x
       grind)
    | (have r₁ := eq2592
       have r₂ := eq8 x x
       grind)
    | exact resolve eq2592 eq8
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2592
  have eq2605 : y = (N.op x y) ∨ x = (M.op x z) ∨ x = y ∨ x = z := by
    first
    | (have j1 := eq8 x y
       grind)
    | (have r₁ := eq2600
       have r₂ := eq8 x z
       grind)
    | (have r₁ := eq2600
       have r₂ := eq8 x y
       grind)
    | exact resolve eq2600 eq8
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2600
  have eq2606 : x ≠ (M.op x x) ∨ x = y ∨ (N.op x z) = (M.op (N.op x z) x) ∨ x = (N.op x z) ∨ x = z ∨ y = (N.op x y) := by grind
  clear eq2602
  have eq2607 : (N.op x z) = (M.op (N.op x z) x) ∨ x = y ∨ x = (N.op x z) ∨ x = z ∨ y = (N.op x y) := by
    first
    | (have r₁ := eq2606
       have r₂ := eq366 x
       grind)
    | exact resolve eq2606 eq366
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2606
  have eq2649 : x ≠ (N.op y x) ∨ x = (M.op x z) ∨ x = z ∨ x = y ∨ x = (M.op x z) ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq1088
       have i₂ := eq2605
       grind)
    | exact superpose eq2605 eq1088
    | exact resolve eq1088 eq2605
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1088 eq2605
  have eq2654 : x ≠ (N.op y x) ∨ x = (M.op x z) ∨ x = z ∨ x = y := by grind
  clear eq2649
  have eq2688 : x ≠ (M.op x y) ∨ x = (M.op x z) ∨ x = z ∨ x = y ∨ y = (M.op y x) ∨ x = y := by
    first
    | (have i₁ := eq2654
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq2654
    | (have j1 := eq9 x z
       grind)
    | (have r₁ := eq2654
       have r₂ := eq9 x (N.op y x)
       grind)
    | (have r₁ := eq2654
       have r₂ := eq9 (N.op y x) x
       grind)
    | exact resolve eq2654 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2654
  have eq2689 : x ≠ (M.op x y) ∨ x = (M.op x z) ∨ x = z ∨ x = y ∨ y = (M.op y x) := by grind
  clear eq2688
  have eq2690 : y = (M.op y x) ∨ x = z ∨ x = y ∨ x = (M.op x z) := by
    first
    | (have r₁ := eq2689
       have r₂ := eq2133
       grind)
    | exact resolve eq2689 eq2133
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2133 eq2689
  have eq2755 : x = y ∨ x = y ∨ x = z ∨ x = z ∨ x = y ∨ x = (M.op x z) := by
    first
    | (have i₁ := eq366 y
       have i₂ := eq2690
       grind)
    | exact superpose eq2690 eq366
    | exact resolve eq366 eq2690
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2690
  have eq2778 : x = (M.op x z) ∨ x = z ∨ x = y := by grind
  clear eq2755
  have eq2893 : x ≠ x ∨ x = z ∨ z = (N.op x z) ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq8 x z
       have i₂ := eq2778
       grind)
    | exact superpose eq2778 eq8
    | (have j0 := eq8 x z
       grind)
    | (have r₁ := eq8 x z
       have r₂ := eq2778
       grind)
    | exact resolve eq8 eq2778
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2897 : x ≠ x ∨ x = z ∨ z = (N.op x z) ∨ x = y := by grind
  clear eq2893
  have eq2898 : z = (N.op x z) ∨ x = z ∨ x = y := by grind
  clear eq2897
  have eq2933 : x ≠ (N.op (N.op x y) (N.op z x)) ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq2898
       grind)
    | exact superpose eq2898 eq10
    | exact resolve eq10 eq2898
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq3058 : x = (N.op x z) ∨ x = y ∨ x = z ∨ x = y ∨ x = (N.op x z) ∨ x = z ∨ y = (N.op x y) := by
    first
    | (have i₁ := eq366 (N.op x z)
       have i₂ := eq2607
       grind)
    | exact superpose eq2607 eq366
    | exact resolve eq366 eq2607
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2607
  have eq3081 : x = (N.op x z) ∨ x = y ∨ x = z ∨ y = (N.op x y) := by grind
  clear eq3058
  have eq3153 : x = z ∨ x = z ∨ x = y ∨ x = y ∨ x = z ∨ y = (N.op x y) := by
    first
    | (have i₁ := eq2898
       have i₂ := eq3081
       grind)
    | exact superpose eq3081 eq2898
    | exact resolve eq2898 eq3081
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2898 eq3081
  have eq3156 : y = (N.op x y) ∨ x = y ∨ x = z := by grind
  clear eq3153
  have eq3225 : x ≠ (N.op y (N.op z x)) ∨ x = z ∨ x = y ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq2933
       have i₂ := eq3156
       grind)
    | exact superpose eq3156 eq2933
    | exact resolve eq2933 eq3156
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2933
  have eq3226 : y = (M.op y x) ∨ x = (M.op x y) ∨ x = y ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq9 x y
       have i₂ := eq3156
       grind)
    | exact superpose eq3156 eq9
    | (have j0 := eq9 y x
       grind)
    | exact resolve eq9 eq3156
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3156
  have eq3227 : y = (M.op y x) ∨ x = (M.op x y) ∨ x = y ∨ x = z := by grind
  clear eq3226
  have eq3228 : x ≠ (N.op y (N.op z x)) ∨ x = z ∨ x = y := by grind
  clear eq3225
  have eq3253 : x ≠ (N.op y (M.op x z)) ∨ x = z ∨ x = y ∨ z = (M.op z x) ∨ x = z := by
    first
    | (have i₁ := eq3228
       have i₂ := eq9 z x
       grind)
    | exact superpose eq9 eq3228
    | (have j1 := eq9 z x
       grind)
    | (have r₁ := eq3228
       have r₂ := eq9 x (N.op y (N.op z x))
       grind)
    | (have r₁ := eq3228
       have r₂ := eq9 (N.op y (N.op z x)) x
       grind)
    | exact resolve eq3228 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3228
  have eq3255 : x ≠ (N.op y (M.op x z)) ∨ x = z ∨ x = y ∨ z = (M.op z x) := by grind
  clear eq3253
  have eq3770 : x = y ∨ x = y ∨ x = z ∨ x = (M.op x y) ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq366 y
       have i₂ := eq3227
       grind)
    | exact superpose eq3227 eq366
    | exact resolve eq366 eq3227
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3227
  have eq3793 : x = (M.op x y) ∨ x = z ∨ x = y := by grind
  clear eq3770
  have eq4628 : x ≠ (N.op y x) ∨ x = z ∨ x = y ∨ z = (M.op z x) ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq3255
       have i₂ := eq2778
       grind)
    | exact superpose eq2778 eq3255
    | exact resolve eq3255 eq2778
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2778 eq3255
  have eq4630 : x ≠ (N.op y x) ∨ x = z ∨ x = y ∨ z = (M.op z x) := by grind
  clear eq4628
  have eq4739 : x ≠ (M.op x y) ∨ x = z ∨ x = y ∨ z = (M.op z x) ∨ y = (M.op y x) ∨ x = y := by
    first
    | (have i₁ := eq4630
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq4630
    | (have j1 := eq9 z x
       grind)
    | (have r₁ := eq4630
       have r₂ := eq9 x (N.op y x)
       grind)
    | (have r₁ := eq4630
       have r₂ := eq9 (N.op y x) x
       grind)
    | exact resolve eq4630 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4630
  have eq4740 : x ≠ (M.op x y) ∨ x = z ∨ x = y ∨ z = (M.op z x) ∨ y = (M.op y x) := by grind
  clear eq4739
  have eq4743 : z = (M.op z x) ∨ x = y ∨ x = z ∨ y = (M.op y x) := by
    first
    | (have r₁ := eq4740
       have r₂ := eq3793
       grind)
    | exact resolve eq4740 eq3793
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3793 eq4740
  have eq4803 : x = z ∨ x = y ∨ x = z ∨ x = y ∨ x = z ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq366 z
       have i₂ := eq4743
       grind)
    | exact superpose eq4743 eq366
    | exact resolve eq366 eq4743
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4743
  have eq4838 : y = (M.op y x) ∨ x = y ∨ x = z := by grind
  clear eq4803
  have eq5073 : x = y ∨ x = y ∨ x = z ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq366 y
       have i₂ := eq4838
       grind)
    | exact superpose eq4838 eq366
    | exact resolve eq366 eq4838
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq366 eq4838
  have eq5104 : x = z ∨ x = y := by grind
  clear eq5073
  have eq5200 : x ≠ (N.op (N.op x y) (N.op (N.op x x) x)) ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq5104
       grind)
    | exact superpose eq5104 eq10
    | exact resolve eq10 eq5104
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5104
  have eq5213 : x ≠ (N.op (N.op x y) (N.op (M.op x x) x)) ∨ x = y := by grind
  clear eq5200
  have eq6099 : x ≠ (N.op (N.op x y) x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq5213
       have i₂ := eq37 x x
       grind)
    | exact superpose eq37 eq5213
    | (have j1 := eq37 x x
       grind)
    | exact resolve eq5213 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq6181 : x ≠ (N.op (M.op y x) x) ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq6099
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq6099
    | (have j1 := eq9 x y
       grind)
    | (have r₁ := eq6099
       have r₂ := eq9 x (N.op (N.op x y) x)
       grind)
    | (have r₁ := eq6099
       have r₂ := eq9 (N.op (N.op x y) x) x
       grind)
    | exact resolve eq6099 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq6099
  have eq6183 : x ≠ (N.op (M.op y x) x) ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op x y) := by grind
  clear eq6181
  have eq8418 : x ≠ x ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op x y) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq6183
       have i₂ := eq37 y x
       grind)
    | exact superpose eq37 eq6183
    | (have j1 := eq37 x x
       grind)
    | (have r₁ := eq6183
       have r₂ := eq37 y x
       grind)
    | exact resolve eq6183 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq6183
  have eq8420 : x = (M.op y x) ∨ x = (M.op x x) ∨ x = (M.op x y) ∨ x = y := by grind
  clear eq8418
  have eq8573 : x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq19 y x
       have i₂ := eq8420
       grind)
    | exact superpose eq8420 eq19
    | exact resolve eq19 eq8420
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8420
  have eq8576 : x = (M.op x y) ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq8573
  have eq8809 : ∀ X0 : G, x = (M.op x x) ∨ x = (M.op X0 x) ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq14 x y x
       have i₂ := eq8576
       grind)
    | exact superpose eq8576 eq14
    | exact resolve eq14 eq8576
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8576
  have eq9004 : x ≠ x ∨ x = (M.op x x) ∨ x = y := by
    first
    | (have j0 := eq8809 x
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8809
  have eq9005 : x = (M.op x x) ∨ x = y := by grind
  clear eq9004
  have eq9281 : x ≠ (N.op (N.op x y) (N.op x x)) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq5213
       have i₂ := eq9005
       grind)
    | exact superpose eq9005 eq5213
    | exact resolve eq5213 eq9005
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5213
  have eq9308 : ∀ X0 : G, x = (M.op X0 x) ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq14 x x x
       have i₂ := eq9005
       grind)
    | exact superpose eq9005 eq14
    | exact resolve eq14 eq9005
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq9335 : x ≠ (N.op (N.op x y) (N.op x x)) ∨ x = y := by grind
  clear eq9281
  have eq9355 : x ≠ (N.op (N.op x y) (M.op x x)) ∨ x = y := by grind
  clear eq9335
  have eq10314 : x ≠ (N.op (N.op x y) x) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq9355
       have i₂ := eq9308 x
       grind)
    | exact superpose eq9308 eq9355
    | exact resolve eq9355 eq9308
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9355
  have eq10320 : x ≠ (N.op (N.op x y) x) ∨ x = y := by grind
  clear eq10314
  have eq10365 : x ≠ (N.op (M.op y x) x) ∨ x = y ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq10320
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq10320
    | (have j1 := eq9 x y
       grind)
    | (have r₁ := eq10320
       have r₂ := eq9 x (N.op (N.op x y) x)
       grind)
    | (have r₁ := eq10320
       have r₂ := eq9 (N.op (N.op x y) x) x
       grind)
    | exact resolve eq10320 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq10367 : x ≠ (N.op (M.op y x) x) ∨ x = y ∨ x = (M.op x y) := by grind
  clear eq10365
  have eq11539 : x ≠ (N.op x x) ∨ x = y ∨ x = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq10367
       have i₂ := eq9308 y
       grind)
    | exact superpose eq9308 eq10367
    | exact resolve eq10367 eq9308
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq10367
  have eq11547 : x ≠ (N.op x x) ∨ x = y ∨ x = (M.op x y) := by grind
  clear eq11539
  have eq11581 : x ≠ (M.op x x) ∨ x = y ∨ x = (M.op x y) := by grind
  clear eq11547
  have eq11582 : x = (M.op x y) ∨ x = y := by
    first
    | (have r₁ := eq11581
       have r₂ := eq9005
       grind)
    | exact resolve eq11581 eq9005
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9005 eq11581
  have eq11595 : x ≠ x ∨ x = y ∨ y = (N.op x y) ∨ x = y := by
    first
    | (have i₁ := eq8 x y
       have i₂ := eq11582
       grind)
    | exact superpose eq11582 eq8
    | (have j0 := eq8 x y
       grind)
    | (have r₁ := eq8 x y
       have r₂ := eq11582
       grind)
    | exact resolve eq8 eq11582
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq11599 : x ≠ x ∨ x = y ∨ y = (N.op x y) := by grind
  clear eq11595
  have eq11600 : y = (N.op x y) ∨ x = y := by grind
  clear eq11599
  have eq11672 : x ≠ (N.op y x) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq10320
       have i₂ := eq11600
       grind)
    | exact superpose eq11600 eq10320
    | exact resolve eq10320 eq11600
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq10320 eq11600
  have eq11675 : x ≠ (N.op y x) ∨ x = y := by grind
  clear eq11672
  have eq11729 : x ≠ (M.op x y) ∨ x = y ∨ y = (M.op y x) ∨ x = y := by
    first
    | (have i₁ := eq11675
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq11675
    | (have j1 := eq9 y x
       grind)
    | (have r₁ := eq11675
       have r₂ := eq9 x (N.op y x)
       grind)
    | (have r₁ := eq11675
       have r₂ := eq9 (N.op y x) x
       grind)
    | exact resolve eq11675 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq11675
  have eq11730 : x ≠ (M.op x y) ∨ x = y ∨ y = (M.op y x) := by grind
  clear eq11729
  have eq11735 : y = (M.op y x) ∨ x = y := by
    first
    | (have r₁ := eq11730
       have r₂ := eq11582
       grind)
    | exact resolve eq11730 eq11582
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq11582 eq11730
  have eq11841 : x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq9308 y
       have i₂ := eq11735
       grind)
    | exact superpose eq11735 eq9308
    | exact resolve eq9308 eq11735
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9308 eq11735
  have eq11858 : x = y := by grind
  clear eq11841
  have eq11962 : x ≠ (N.op (N.op x x) (N.op (N.op x z) x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq11858
       grind)
    | exact superpose eq11858 eq10
    | exact resolve eq10 eq11858
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq11965 : x ≠ (N.op (N.op x x) (N.op (M.op z x) x)) ∨ x = (M.op x z) ∨ x = z := by
    first
    | (have i₁ := eq46
       have i₂ := eq11858
       grind)
    | exact superpose eq11858 eq46
    | exact resolve eq46 eq11858
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq46
  have eq11966 : x ≠ (N.op (N.op x x) x) ∨ x = (M.op x z) ∨ x = z ∨ x = (M.op z x) := by
    first
    | (have i₁ := eq54
       have i₂ := eq11858
       grind)
    | exact superpose eq11858 eq54
    | exact resolve eq54 eq11858
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq54 eq11858
  have eq11973 : x ≠ (N.op (M.op x x) x) ∨ x = (M.op x z) ∨ x = z ∨ x = (M.op z x) := by grind
  clear eq11966
  have eq11974 : x ≠ (N.op (M.op x x) (N.op (M.op z x) x)) ∨ x = (M.op x z) ∨ x = z := by grind
  clear eq11965
  have eq11977 : x ≠ (N.op (M.op x x) (N.op (N.op x z) x)) := by grind
  clear eq11962
  have eq15706 : x ≠ x ∨ x = (M.op x z) ∨ x = z ∨ x = (M.op z x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq11973
       have i₂ := eq37 x x
       grind)
    | exact superpose eq37 eq11973
    | (have j1 := eq37 z x
       grind)
    | (have r₁ := eq11973
       have r₂ := eq37 x x
       grind)
    | exact resolve eq11973 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq11973
  have eq15708 : x = (M.op z x) ∨ x = z ∨ x = (M.op x z) ∨ x = (M.op x x) := by grind
  clear eq15706
  have eq15834 : x = (M.op x x) ∨ x = z ∨ x = (M.op x z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq19 z x
       have i₂ := eq15708
       grind)
    | exact superpose eq15708 eq19
    | exact resolve eq19 eq15708
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19 eq15708
  have eq15837 : x = (M.op x z) ∨ x = z ∨ x = (M.op x x) := by grind
  clear eq15834
  have eq15925 : ∀ X0 : G, x = (M.op x x) ∨ x = (M.op X0 x) ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq14 x z x
       have i₂ := eq15837
       grind)
    | exact superpose eq15837 eq14
    | exact resolve eq14 eq15837
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq16311 : x ≠ x ∨ x = (M.op x x) ∨ x = z := by
    first
    | (have j0 := eq15925 x
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15925
  have eq16312 : x = (M.op x x) ∨ x = z := by grind
  clear eq16311
  have eq17080 : x ≠ (N.op x (N.op (N.op x z) x)) ∨ x = z := by
    first
    | (have i₁ := eq11977
       have i₂ := eq16312
       grind)
    | exact superpose eq16312 eq11977
    | exact resolve eq11977 eq16312
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq17124 : ∀ X0 : G, x = (M.op X0 x) ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq14 x x x
       have i₂ := eq16312
       grind)
    | exact superpose eq16312 eq14
    | exact resolve eq14 eq16312
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq17489 : x ≠ (N.op (M.op x x) (N.op x x)) ∨ x = (M.op x z) ∨ x = z ∨ x = z := by
    first
    | (have i₁ := eq11974
       have i₂ := eq17124 z
       grind)
    | exact superpose eq17124 eq11974
    | exact resolve eq11974 eq17124
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq11974
  have eq17496 : x ≠ (N.op (M.op x x) (N.op x x)) ∨ x = (M.op x z) ∨ x = z := by grind
  clear eq17489
  have eq17616 : x ≠ (N.op (M.op x x) (M.op x x)) ∨ x = (M.op x z) ∨ x = z := by grind
  clear eq17496
  have eq17619 : x ≠ (M.op (M.op x x) (M.op x x)) ∨ x = (M.op x z) ∨ x = z := by grind
  clear eq17616
  have eq17622 : x ≠ (M.op x x) ∨ x = (M.op x z) ∨ x = z := by
    first
    | (have i₁ := eq17619
       have i₂ := eq14 x x (M.op x x)
       grind)
    | exact superpose eq14 eq17619
    | exact resolve eq17619 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq17619
  have eq17625 : x = (M.op x z) ∨ x = z := by
    first
    | (have r₁ := eq17622
       have r₂ := eq15837
       grind)
    | exact resolve eq17622 eq15837
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15837 eq17622
  have eq17788 : x ≠ x ∨ x = z ∨ z = (N.op x z) ∨ x = z := by
    first
    | (have i₁ := eq8 x z
       have i₂ := eq17625
       grind)
    | exact superpose eq17625 eq8
    | (have j0 := eq8 x z
       grind)
    | (have r₁ := eq8 x z
       have r₂ := eq17625
       grind)
    | exact resolve eq8 eq17625
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq17792 : x ≠ x ∨ x = z ∨ z = (N.op x z) := by grind
  clear eq17788
  have eq17793 : z = (N.op x z) ∨ x = z := by grind
  clear eq17792
  have eq18913 : x ≠ (N.op x (N.op z x)) ∨ x = z ∨ x = z := by
    first
    | (have i₁ := eq17080
       have i₂ := eq17793
       grind)
    | exact superpose eq17793 eq17080
    | exact resolve eq17080 eq17793
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq17080 eq17793
  have eq18928 : x ≠ (N.op x (N.op z x)) ∨ x = z := by grind
  clear eq18913
  have eq19005 : x ≠ (N.op x (M.op x z)) ∨ x = z ∨ z = (M.op z x) ∨ x = z := by
    first
    | (have i₁ := eq18928
       have i₂ := eq9 z x
       grind)
    | exact superpose eq9 eq18928
    | (have j1 := eq9 z x
       grind)
    | (have r₁ := eq18928
       have r₂ := eq9 x (N.op x (N.op z x))
       grind)
    | (have r₁ := eq18928
       have r₂ := eq9 (N.op x (N.op z x)) x
       grind)
    | exact resolve eq18928 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18928
  have eq19009 : x ≠ (N.op x (M.op x z)) ∨ x = z ∨ z = (M.op z x) := by grind
  clear eq19005
  have eq21833 : x ≠ (N.op x x) ∨ x = z ∨ z = (M.op z x) ∨ x = z := by
    first
    | (have i₁ := eq19009
       have i₂ := eq17625
       grind)
    | exact superpose eq17625 eq19009
    | exact resolve eq19009 eq17625
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq17625 eq19009
  have eq21837 : x ≠ (N.op x x) ∨ x = z ∨ z = (M.op z x) := by grind
  clear eq21833
  have eq21847 : x ≠ (M.op x x) ∨ x = z ∨ z = (M.op z x) := by grind
  clear eq21837
  have eq21848 : z = (M.op z x) ∨ x = z := by
    first
    | (have r₁ := eq21847
       have r₂ := eq16312
       grind)
    | exact resolve eq21847 eq16312
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq16312 eq21847
  have eq22029 : x = z ∨ x = z ∨ x = z := by
    first
    | (have i₁ := eq17124 z
       have i₂ := eq21848
       grind)
    | exact superpose eq21848 eq17124
    | exact resolve eq17124 eq21848
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq17124 eq21848
  have eq22050 : x = z := by grind
  clear eq22029
  have eq22698 : x ≠ (N.op (M.op x x) (N.op (N.op x x) x)) := by
    first
    | (have i₁ := eq11977
       have i₂ := eq22050
       grind)
    | exact superpose eq22050 eq11977
    | exact resolve eq11977 eq22050
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq11977 eq22050
  have eq22699 : x ≠ (N.op (M.op x x) (N.op (M.op x x) x)) := by grind
  clear eq22698
  have eq24569 : x ≠ (N.op (M.op x x) x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq22699
       have i₂ := eq37 x x
       grind)
    | exact superpose eq37 eq22699
    | (have j1 := eq37 x x
       grind)
    | exact resolve eq22699 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq24573 : x = (M.op x x) := by
    first
    | (have j1 := eq37 x x
       grind)
    | (have r₁ := eq24569
       have r₂ := eq37 x x
       grind)
    | exact resolve eq24569 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq37 eq24569
  have eq24692 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq22699
       have i₂ := eq24573
       grind)
    | exact superpose eq24573 eq22699
    | exact resolve eq22699 eq24573
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq22699
  have eq24747 : ∀ X0 : G, x = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 x x x
       have i₂ := eq24573
       grind)
    | exact superpose eq24573 eq14
    | exact resolve eq14 eq24573
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq24759 : x ≠ (N.op x (M.op x x)) := by grind
  clear eq24692
  have eq24764 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq24759
       have i₂ := eq24747 x
       grind)
    | exact superpose eq24747 eq24759
    | exact resolve eq24759 eq24747
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq24747 eq24759
  have eq24769 : x ≠ (M.op x x) := by grind
  clear eq24764
  have eq24774 : False := by grind
  exact eq24774

/-- `Equation3525` from `Equation3587`: `x ◇ y = z ◇ ((x ◇ y) ◇ y)` ⊢ `x ◇ y = x ◇ ((y ◇ z) ◇ y)`, through the
companion `x □ y = if a = b then (M.op a a) else if (M.op a b) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3525_StructuralFromFin_Equation3587 : Law3525.StructuralFromFin Law3587 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_x_y_pxy_x_pxx_y_pyx_Equation3587 (fun {G} M hM ↦ ?_)
  have hm := (@Law3587.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, a ≠ b ∨ N.op a b = (M.op a a)) →
      (∀ a b : G, a = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, a = b ∨ (M.op a b) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3525 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3525.models_iff G N]
  intro x y z
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (M.op X2 (M.op (M.op X0 X1) X1)) := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, X0 ≠ X1 ∨ (N.op X0 X1) = (M.op X0 X0) := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ X0 = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (N.op X0 X1) = (M.op X1 X0) ∨ (M.op X0 X1) = X0 ∨ X0 = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x y) ≠ (N.op x (N.op (N.op y z) y)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 X2 X3 : G, (M.op X2 (M.op (M.op X0 X1) X1)) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X2 (M.op (M.op X0 X1) X1) X3
       have i₂ := eq6 X0 X1 (M.op X2 (M.op (M.op X0 X1) X1))
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X3 : G, (M.op X0 X1) = (M.op X3 (M.op X0 X1)) := by
    intro X0 X1 X3
    first
    | (have i₁ := eq13 X0 X1 x X3
       have i₂ := eq6 X0 X1 x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq19 : ∀ X0 X1 : G, (M.op X0 X1) = (M.op (M.op X0 X1) X1) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 X1 x
       have i₂ := eq14 (M.op X0 X1) X1 x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq36 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X0 X1) = X1 ∨ (N.op (M.op X0 X1) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq8 (M.op X0 X1) X1
       have i₂ := eq19 X0 X1
       grind)
    | exact superpose eq19 eq8
    | (have j0 := eq8 (M.op X0 X1) X1
       grind)
    | (have r₁ := eq8 (M.op X0 X1) X1
       have r₂ := eq19 X0 X1
       grind)
    | exact resolve eq8 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq37 : ∀ X0 X1 : G, (N.op (M.op X0 X1) X1) = X1 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq36 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36
  have eq43 : (N.op x y) ≠ (N.op x (M.op y (N.op y z))) ∨ (N.op y z) = (M.op (N.op y z) y) ∨ y = (N.op y z) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 (N.op y z) y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 (N.op y z) y
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op x y) (N.op x (N.op (N.op y z) y))
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op x (N.op (N.op y z) y)) (N.op x y)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq45 : (N.op x y) ≠ (N.op x (N.op (M.op z y) y)) ∨ y = (M.op y z) ∨ y = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y z
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y z
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op x y) (N.op x (N.op (N.op y z) y))
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op x (N.op (N.op y z) y)) (N.op x y)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq48 : (N.op x y) ≠ (N.op x y) ∨ y = (M.op y z) ∨ y = z ∨ y = (M.op z y) := by
    first
    | (have i₁ := eq45
       have i₂ := eq37 z y
       grind)
    | exact superpose eq37 eq45
    | (have j1 := eq37 z y
       grind)
    | exact resolve eq45 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq51 : y = (M.op z y) ∨ y = z ∨ y = (M.op y z) := by grind
  clear eq48
  have eq56 : (N.op x y) ≠ (N.op x (N.op y y)) ∨ y = (M.op y z) ∨ y = z ∨ y = z ∨ y = (M.op y z) := by
    first
    | (have i₁ := eq45
       have i₂ := eq51
       grind)
    | exact superpose eq51 eq45
    | exact resolve eq45 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq45
  have eq59 : ∀ X0 : G, y = (M.op y z) ∨ y = (M.op X0 y) ∨ y = z := by
    intro X0
    first
    | (have i₁ := eq14 z y x
       have i₂ := eq51
       grind)
    | exact superpose eq51 eq14
    | exact resolve eq14 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq62 : (N.op x y) ≠ (N.op x (N.op y y)) ∨ y = (M.op y z) ∨ y = z := by grind
  clear eq56
  have eq64 : (N.op x y) ≠ (N.op x (M.op y y)) ∨ y = (M.op y z) ∨ y = z := by grind
  clear eq62
  have eq71 : ∀ X0 X1 : G, y = (M.op X1 y) ∨ y = (M.op X0 y) ∨ y = z := by
    intro X0 X1
    first
    | (have i₁ := eq14 y z x
       have i₂ := eq59 X0
       grind)
    | exact superpose eq59 eq14
    | (have j1 := eq59 X1
       grind)
    | exact resolve eq14 eq59
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq59
  have eq105 : ∀ X0 : G, y ≠ y ∨ y = (M.op X0 y) ∨ y = z := by
    intro X0
    first
    | (have j0 := eq71 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq71
  have eq106 : ∀ X0 : G, y = (M.op X0 y) ∨ y = z := by
    intro X0
    first
    | (have j0 := eq105 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq105
  have eq146 : (N.op x y) ≠ (N.op x y) ∨ y = (M.op y z) ∨ y = z ∨ y = z := by
    first
    | (have i₁ := eq64
       have i₂ := eq106 y
       grind)
    | exact superpose eq106 eq64
    | exact resolve eq64 eq106
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64
  have eq154 : (N.op x y) ≠ (N.op x y) ∨ y = (M.op y z) ∨ y = z := by grind
  clear eq146
  have eq155 : y = (M.op y z) ∨ y = z := by grind
  clear eq154
  have eq157 : y ≠ y ∨ y = z ∨ z = (N.op y z) ∨ y = z := by
    first
    | (have i₁ := eq8 y z
       have i₂ := eq155
       grind)
    | exact superpose eq155 eq8
    | (have j0 := eq8 y z
       grind)
    | (have r₁ := eq8 y z
       have r₂ := eq155
       grind)
    | exact resolve eq8 eq155
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq161 : y ≠ y ∨ y = z ∨ z = (N.op y z) := by grind
  clear eq157
  have eq162 : z = (N.op y z) ∨ y = z := by grind
  clear eq161
  have eq185 : (N.op x y) ≠ (N.op x (M.op y z)) ∨ z = (M.op z y) ∨ y = z ∨ y = z := by
    first
    | (have i₁ := eq43
       have i₂ := eq162
       grind)
    | exact superpose eq162 eq43
    | exact resolve eq43 eq162
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq43 eq162
  have eq189 : (N.op x y) ≠ (N.op x (M.op y z)) ∨ z = (M.op z y) ∨ y = z := by grind
  clear eq185
  have eq428 : (N.op x y) ≠ (N.op x y) ∨ z = (M.op z y) ∨ y = z ∨ y = z := by
    first
    | (have i₁ := eq189
       have i₂ := eq155
       grind)
    | exact superpose eq155 eq189
    | exact resolve eq189 eq155
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq155 eq189
  have eq433 : (N.op x y) ≠ (N.op x y) ∨ z = (M.op z y) ∨ y = z := by grind
  clear eq428
  have eq434 : z = (M.op z y) ∨ y = z := by grind
  clear eq433
  have eq460 : y = z ∨ y = z ∨ y = z := by
    first
    | (have i₁ := eq106 z
       have i₂ := eq434
       grind)
    | exact superpose eq434 eq106
    | exact resolve eq106 eq434
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq106 eq434
  have eq475 : y = z := by grind
  clear eq460
  have eq496 : (N.op x y) ≠ (N.op x (N.op (N.op y y) y)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq475
       grind)
    | exact superpose eq475 eq10
    | exact resolve eq10 eq475
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq475
  have eq503 : (N.op x y) ≠ (N.op x (N.op (M.op y y) y)) := by grind
  clear eq496
  have eq611 : (N.op x y) ≠ (N.op x y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq503
       have i₂ := eq37 y y
       grind)
    | exact superpose eq37 eq503
    | (have j1 := eq37 y y
       grind)
    | exact resolve eq503 eq37
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq37
  have eq614 : y = (M.op y y) := by grind
  clear eq611
  have eq622 : (N.op x y) ≠ (N.op x (N.op y y)) := by
    first
    | (have i₁ := eq503
       have i₂ := eq614
       grind)
    | exact superpose eq614 eq503
    | exact resolve eq503 eq614
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq503
  have eq632 : ∀ X0 : G, y = (M.op X0 y) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq614
       grind)
    | exact superpose eq614 eq14
    | exact resolve eq14 eq614
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14 eq614
  have eq638 : (N.op x y) ≠ (N.op x (M.op y y)) := by grind
  clear eq622
  have eq639 : (N.op x y) ≠ (N.op x y) := by
    first
    | (have i₁ := eq638
       have i₂ := eq632 y
       grind)
    | exact superpose eq632 eq638
    | exact resolve eq638 eq632
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq632 eq638
  have eq640 : False := by grind
  exact eq640
