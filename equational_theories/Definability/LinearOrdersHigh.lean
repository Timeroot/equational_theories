import equational_theories.Definability.PolyIrreducible
import equational_theories.Definability.LinearOrders

/-!
# Linear magmas over orders of degree four and five

The remaining blocks of `CERTINF` need orders `ℤ[θ]` whose defining polynomial has degree four or
five. Everything downstream of the ring is as in `Definability/LinearOrders.lean`: one residue map
per arithmetic fact, one `linear_combination` per source law. What is new is upstream --
`Polynomial.Monic.irreducible_iff_roots_eq_zero_of_degree_le_three` no longer applies, and none of
these polynomials is of Selmer's shape `X ^ n - X - 1`.

`Definability/PolyIrreducible.lean` fills that gap. Reducing modulo a prime of good reduction, a
proper monic factor would have degree one or two, and both are pinned down by their coefficients;
so the whole question becomes two quantified statements over a finite ring, and each is a
`decide`. The primes used here are as small as `2`.

This file is generated by `genord45.py`.
-/

open Law Law.MagmaLaw Polynomial

noncomputable section

/-! ### `ℤ[θ]` with `q θ = 0` for `q = X ^ 4 + 2 * X ^ 3 + 3 * X ^ 2 + X + 1`: a root of `X ^ 4 + 2 X ^ 3 + 3 X ^ 2 + X + 1` -/

namespace QuarticA

/-- The defining polynomial of the order. -/
abbrev q : ℤ[X] := X ^ 4 + 2 * X ^ 3 + 3 * X ^ 2 + X + 1

/-- `q` is irreducible because its reduction modulo `3` is: a proper monic factor
would have degree at most `4 / 2 = 2`, and there is none. -/
theorem q_irr : Irreducible q :=
  irreducible_quartic_int 2 3 1 1 3
    (by simp only [q, map_one, map_ofNat]; ring) (by decide) (by decide)

/-- The order `ℤ[θ]`, a domain because `q` is irreducible. -/
abbrev R : Type := AdjoinRoot q

instance : IsDomain R :=
  AdjoinRoot.isDomain_of_prime (UniqueFactorizationMonoid.irreducible_iff_prime.mp q_irr)

/-- The algebraic integer `θ`. -/
abbrev th : R := AdjoinRoot.root q

theorem hq : th ^ 4 + 2 * th ^ 3 + 3 * th ^ 2 + th + 1 = 0 := by
  have h := AdjoinRoot.eval₂_root q
  simp only [q, eval₂_add, eval₂_mul, eval₂_ofNat, eval₂_pow, eval₂_X, eval₂_one] at h
  linear_combination h

/-! The residue maps, each given by a root of `q` modulo `m`. -/

/-- Reduction modulo `17`, sending `θ` to `8`. -/
def r17_8 : R →+* ZMod 17 := AdjoinRoot.lift (Int.castRingHom (ZMod 17)) 8
  (by simp only [q, eval₂_add, eval₂_mul, eval₂_ofNat, eval₂_pow, eval₂_X, eval₂_one]; decide)

@[simp] theorem r17_8_th : r17_8 th = 8 := by simp [r17_8]

/-- Reduction modulo `2`, sending `θ` to `1`. -/
def r2_1 : R →+* ZMod 2 := AdjoinRoot.lift (Int.castRingHom (ZMod 2)) 1
  (by simp only [q, eval₂_add, eval₂_mul, eval₂_ofNat, eval₂_pow, eval₂_X, eval₂_one]; decide)

@[simp] theorem r2_1_th : r2_1 th = 1 := by simp [r2_1]

/-! #### The model `x ◇ y = (th ^ 3 + 2 * th ^ 2 + 2 * th) * x + (th) * y`, with coefficient modulus `c = th ^ 3 + 2 * th ^ 2 + 3 * th - 1`. -/

theorem hc (x : R) : (1 : R) + (th ^ 3 + 2 * th ^ 2 + 3 * th - 1) * x ≠ 0 :=
  Magma.add_mul_ne_zero_of_hom r17_8 1 _
    (by simp [map_ofNat]; decide)
    (by simp only [map_one]; decide) x

theorem hP3269 (z : R) : z ^ 3 - 3 * z ^ 2 + 2 * z - 1 ≠ 0 := by
  intro h
  have hr := congrArg r2_1 h
  simp only [map_sub, map_add, map_mul, map_pow, map_one, map_ofNat, map_zero] at hr
  revert hr
  generalize r2_1 z = y
  revert y
  decide

theorem hP3279 (z : R) : z ^ 3 - 2 * z ^ 2 + z - 1 ≠ 0 := by
  intro h
  have hr := congrArg r2_1 h
  simp only [map_sub, map_add, map_mul, map_pow, map_one, map_ofNat, map_zero] at hr
  revert hr
  generalize r2_1 z = y
  revert y
  decide

theorem hP3475 (z : R) : z ^ 3 - z ^ 2 + 1 ≠ 0 := by
  intro h
  have hr := congrArg r2_1 h
  simp only [map_sub, map_add, map_pow, map_one, map_zero] at hr
  revert hr
  generalize r2_1 z = y
  revert y
  decide

theorem hQ (z : R) : z ^ 2 + 3 * z + 1 ≠ 0 := by
  intro h
  have hr := congrArg r2_1 h
  simp only [map_add, map_mul, map_pow, map_one, map_ofNat, map_zero] at hr
  revert hr
  generalize r2_1 z = y
  revert y
  decide

/-- Equation `883`, `x = y ◇ ((x ◇ y) ◇ (y ◇ y))`, holds in the model. -/
theorem sat_883 : @Equation883 R (Magma.lin (th ^ 3 + 2 * th ^ 2 + 2 * th) (th)) := by
  intro x y
  simp only [Magma.lin_op]
  linear_combination (-th ^ 3 * x - 2 * th ^ 2 * x - th * x - 2 * th * y + x) * hq

/-- Equation `2304`, `x = (y ◇ (x ◇ (y ◇ y))) ◇ y`, holds in the model. -/
theorem sat_2304 : @Equation2304 R (Magma.lin (th ^ 3 + 2 * th ^ 2 + 2 * th) (th)) := by
  intro x y
  simp only [Magma.lin_op]
  linear_combination (-th ^ 4 * y - th ^ 3 * x - 2 * th ^ 3 * y - 2 * th ^ 2 * x - 3 * th ^ 2 * y - th * x - th * y + x) * hq

end QuarticA

/-! ### `ℤ[θ]` with `q θ = 0` for `q = X ^ 5 - X ^ 3 - X ^ 2 + X + 1`: a root of `X ^ 5 - X ^ 3 - X ^ 2 + X + 1` -/

namespace QuinticA

/-- The defining polynomial of the order. -/
abbrev q : ℤ[X] := X ^ 5 - X ^ 3 - X ^ 2 + X + 1

/-- `q` is irreducible because its reduction modulo `2` is: a proper monic factor
would have degree at most `5 / 2 = 2`, and there is none. -/
theorem q_irr : Irreducible q :=
  irreducible_quintic_int 0 (-1) (-1) 1 1 2
    (by simp only [q, map_zero, map_one, map_neg]; ring) (by decide) (by decide)

/-- The order `ℤ[θ]`, a domain because `q` is irreducible. -/
abbrev R : Type := AdjoinRoot q

instance : IsDomain R :=
  AdjoinRoot.isDomain_of_prime (UniqueFactorizationMonoid.irreducible_iff_prime.mp q_irr)

/-- The algebraic integer `θ`. -/
abbrev th : R := AdjoinRoot.root q

theorem hq : th ^ 5 - th ^ 3 - th ^ 2 + th + 1 = 0 := by
  have h := AdjoinRoot.eval₂_root q
  simp only [q, eval₂_sub, eval₂_add, eval₂_pow, eval₂_X, eval₂_one] at h
  linear_combination h

/-! The residue maps, each given by a root of `q` modulo `m`. -/

/-- Reduction modulo `43`, sending `θ` to `24`. -/
def r43_24 : R →+* ZMod 43 := AdjoinRoot.lift (Int.castRingHom (ZMod 43)) 24
  (by simp only [q, eval₂_sub, eval₂_add, eval₂_pow, eval₂_X, eval₂_one]; decide)

@[simp] theorem r43_24_th : r43_24 th = 24 := by simp [r43_24]

/-- Reduction modulo `11`, sending `θ` to `5`. -/
def r11_5 : R →+* ZMod 11 := AdjoinRoot.lift (Int.castRingHom (ZMod 11)) 5
  (by simp only [q, eval₂_sub, eval₂_add, eval₂_pow, eval₂_X, eval₂_one]; decide)

@[simp] theorem r11_5_th : r11_5 th = 5 := by simp [r11_5]

/-! #### The model `x ◇ y = (th ^ 3 - 1) * x + (th) * y`, with coefficient modulus `c = th ^ 3 + th - 2`. -/

theorem hc (x : R) : (1 : R) + (th ^ 3 + th - 2) * x ≠ 0 :=
  Magma.add_mul_ne_zero_of_hom r43_24 1 _
    (by simp [map_ofNat]; decide)
    (by simp only [map_one]; decide) x

theorem h₃ (x : R) : (3 : R) + (th ^ 3 + th - 2) * x ≠ 0 :=
  Magma.add_mul_ne_zero_of_hom r43_24 3 _
    (by simp [map_ofNat]; decide)
    (by simp only [map_ofNat]; decide) x

theorem hP3272 (z : R) : z ^ 2 - z + 1 ≠ 0 := by
  intro h
  have hr := congrArg r11_5 h
  simp only [map_sub, map_add, map_pow, map_one, map_zero] at hr
  revert hr
  generalize r11_5 z = y
  revert y
  decide

/-- Equation `1286`, `x = y ◇ (((x ◇ y) ◇ x) ◇ y)`, holds in the model. -/
theorem sat_1286 : @Equation1286 R (Magma.lin (th ^ 3 - 1) (th)) := by
  intro x y
  simp only [Magma.lin_op]
  linear_combination (-th ^ 5 * x - th ^ 3 * x - th ^ 3 * y + 2 * th ^ 2 * x - th * y + x + y) * hq

end QuinticA

/-! ### `ℤ[θ]` with `q θ = 0` for `q = X ^ 5 + X ^ 4 + X ^ 3 + 2 * X ^ 2 + X + 1`: a root of `X ^ 5 + X ^ 4 + X ^ 3 + 2 X ^ 2 + X + 1` -/

namespace QuinticB

/-- The defining polynomial of the order. -/
abbrev q : ℤ[X] := X ^ 5 + X ^ 4 + X ^ 3 + 2 * X ^ 2 + X + 1

/-- `q` is irreducible because its reduction modulo `2` is: a proper monic factor
would have degree at most `5 / 2 = 2`, and there is none. -/
theorem q_irr : Irreducible q :=
  irreducible_quintic_int 1 1 2 1 1 2
    (by simp only [q, map_one, map_ofNat]; ring) (by decide) (by decide)

/-- The order `ℤ[θ]`, a domain because `q` is irreducible. -/
abbrev R : Type := AdjoinRoot q

instance : IsDomain R :=
  AdjoinRoot.isDomain_of_prime (UniqueFactorizationMonoid.irreducible_iff_prime.mp q_irr)

/-- The algebraic integer `θ`. -/
abbrev th : R := AdjoinRoot.root q

theorem hq : th ^ 5 + th ^ 4 + th ^ 3 + 2 * th ^ 2 + th + 1 = 0 := by
  have h := AdjoinRoot.eval₂_root q
  simp only [q, eval₂_add, eval₂_mul, eval₂_ofNat, eval₂_pow, eval₂_X, eval₂_one] at h
  linear_combination h

/-! The residue maps, each given by a root of `q` modulo `m`. -/

/-- Reduction modulo `23`, sending `θ` to `8`. -/
def r23_8 : R →+* ZMod 23 := AdjoinRoot.lift (Int.castRingHom (ZMod 23)) 8
  (by simp only [q, eval₂_add, eval₂_mul, eval₂_ofNat, eval₂_pow, eval₂_X, eval₂_one]; decide)

@[simp] theorem r23_8_th : r23_8 th = 8 := by simp [r23_8]

/-- Reduction modulo `29`, sending `θ` to `6`. -/
def r29_6 : R →+* ZMod 29 := AdjoinRoot.lift (Int.castRingHom (ZMod 29)) 6
  (by simp only [q, eval₂_add, eval₂_mul, eval₂_ofNat, eval₂_pow, eval₂_X, eval₂_one]; decide)

@[simp] theorem r29_6_th : r29_6 th = 6 := by simp [r29_6]

/-- Reduction modulo `7`, sending `θ` to `1`. -/
def r7_1 : R →+* ZMod 7 := AdjoinRoot.lift (Int.castRingHom (ZMod 7)) 1
  (by simp only [q, eval₂_add, eval₂_mul, eval₂_ofNat, eval₂_pow, eval₂_X, eval₂_one]; decide)

@[simp] theorem r7_1_th : r7_1 th = 1 := by simp [r7_1]

/-! #### The model `x ◇ y = (th ^ 4 + th ^ 3 + th) * x + (th) * y`, with coefficient modulus `c = th ^ 4 + th ^ 3 + 2 * th - 1`. -/

theorem hc (x : R) : (1 : R) + (th ^ 4 + th ^ 3 + 2 * th - 1) * x ≠ 0 :=
  Magma.add_mul_ne_zero_of_hom r23_8 1 _
    (by simp [map_ofNat]; decide)
    (by simp only [map_one]; decide) x

theorem hP3269 (z : R) : z ^ 3 - 3 * z ^ 2 + 2 * z - 1 ≠ 0 := by
  intro h
  have hr := congrArg r29_6 h
  simp only [map_sub, map_add, map_mul, map_pow, map_one, map_ofNat, map_zero] at hr
  revert hr
  generalize r29_6 z = y
  revert y
  decide

theorem hP3279 (z : R) : z ^ 3 - 2 * z ^ 2 + z - 1 ≠ 0 := by
  intro h
  have hr := congrArg r29_6 h
  simp only [map_sub, map_add, map_mul, map_pow, map_one, map_ofNat, map_zero] at hr
  revert hr
  generalize r29_6 z = y
  revert y
  decide

theorem hP3475 (z : R) : z ^ 3 - z ^ 2 + 1 ≠ 0 := by
  intro h
  have hr := congrArg r29_6 h
  simp only [map_sub, map_add, map_pow, map_one, map_zero] at hr
  revert hr
  generalize r29_6 z = y
  revert y
  decide

theorem hQ (z : R) : z ^ 2 + 3 * z + 1 ≠ 0 := by
  intro h
  have hr := congrArg r7_1 h
  simp only [map_add, map_mul, map_pow, map_one, map_ofNat, map_zero] at hr
  revert hr
  generalize r7_1 z = y
  revert y
  decide

/-- Equation `1279`, `x = y ◇ (((x ◇ x) ◇ y) ◇ y)`, holds in the model. -/
theorem sat_1279 : @Equation1279 R (Magma.lin (th ^ 4 + th ^ 3 + th) (th)) := by
  intro x y
  simp only [Magma.lin_op]
  linear_combination (-th ^ 8 * x - 2 * th ^ 7 * x - th ^ 5 * x - 2 * th ^ 4 * x + 2 * th ^ 3 * x - th ^ 2 * x - th * x - th * y + x) * hq

end QuinticB

/-! ### `ℤ[θ]` with `q θ = 0` for `q = X ^ 4 + 2 * X ^ 3 + 2 * X ^ 2 + X + 1`: a root of `X ^ 4 + 2 X ^ 3 + 2 X ^ 2 + X + 1` -/

namespace QuarticB

/-- The defining polynomial of the order. -/
abbrev q : ℤ[X] := X ^ 4 + 2 * X ^ 3 + 2 * X ^ 2 + X + 1

/-- `q` is irreducible because its reduction modulo `2` is: a proper monic factor
would have degree at most `4 / 2 = 2`, and there is none. -/
theorem q_irr : Irreducible q :=
  irreducible_quartic_int 2 2 1 1 2
    (by simp only [q, map_one, map_ofNat]; ring) (by decide) (by decide)

/-- The order `ℤ[θ]`, a domain because `q` is irreducible. -/
abbrev R : Type := AdjoinRoot q

instance : IsDomain R :=
  AdjoinRoot.isDomain_of_prime (UniqueFactorizationMonoid.irreducible_iff_prime.mp q_irr)

/-- The algebraic integer `θ`. -/
abbrev th : R := AdjoinRoot.root q

theorem hq : th ^ 4 + 2 * th ^ 3 + 2 * th ^ 2 + th + 1 = 0 := by
  have h := AdjoinRoot.eval₂_root q
  simp only [q, eval₂_add, eval₂_mul, eval₂_ofNat, eval₂_pow, eval₂_X, eval₂_one] at h
  linear_combination h

/-! The residue maps, each given by a root of `q` modulo `m`. -/

/-- Reduction modulo `19`, sending `θ` to `13`. -/
def r19_13 : R →+* ZMod 19 := AdjoinRoot.lift (Int.castRingHom (ZMod 19)) 13
  (by simp only [q, eval₂_add, eval₂_mul, eval₂_ofNat, eval₂_pow, eval₂_X, eval₂_one]; decide)

@[simp] theorem r19_13_th : r19_13 th = 13 := by simp [r19_13]

/-- Reduction modulo `13`, sending `θ` to `6`. -/
def r13_6 : R →+* ZMod 13 := AdjoinRoot.lift (Int.castRingHom (ZMod 13)) 6
  (by simp only [q, eval₂_add, eval₂_mul, eval₂_ofNat, eval₂_pow, eval₂_X, eval₂_one]; decide)

@[simp] theorem r13_6_th : r13_6 th = 6 := by simp [r13_6]

/-- Reduction modulo `7`, sending `θ` to `1`. -/
def r7_1 : R →+* ZMod 7 := AdjoinRoot.lift (Int.castRingHom (ZMod 7)) 1
  (by simp only [q, eval₂_add, eval₂_mul, eval₂_ofNat, eval₂_pow, eval₂_X, eval₂_one]; decide)

@[simp] theorem r7_1_th : r7_1 th = 1 := by simp [r7_1]

/-! #### The model `x ◇ y = (-th ^ 2 - th - 1) * x + (th) * y`, with coefficient modulus `c = -th ^ 2 - 2`. -/

theorem hc (x : R) : (1 : R) + (-th ^ 2 - 2) * x ≠ 0 :=
  Magma.add_mul_ne_zero_of_hom r19_13 1 _
    (by simp [map_ofNat]; decide)
    (by simp only [map_one]; decide) x

theorem hP3269 (z : R) : z ^ 3 - 3 * z ^ 2 + 2 * z - 1 ≠ 0 := by
  intro h
  have hr := congrArg r13_6 h
  simp only [map_sub, map_add, map_mul, map_pow, map_one, map_ofNat, map_zero] at hr
  revert hr
  generalize r13_6 z = y
  revert y
  decide

theorem hP3279 (z : R) : z ^ 3 - 2 * z ^ 2 + z - 1 ≠ 0 := by
  intro h
  have hr := congrArg r13_6 h
  simp only [map_sub, map_add, map_mul, map_pow, map_one, map_ofNat, map_zero] at hr
  revert hr
  generalize r13_6 z = y
  revert y
  decide

theorem hP3475 (z : R) : z ^ 3 - z ^ 2 + 1 ≠ 0 := by
  intro h
  have hr := congrArg r13_6 h
  simp only [map_sub, map_add, map_pow, map_one, map_zero] at hr
  revert hr
  generalize r13_6 z = y
  revert y
  decide

theorem hQ (z : R) : z ^ 2 + 3 * z + 1 ≠ 0 := by
  intro h
  have hr := congrArg r7_1 h
  simp only [map_add, map_mul, map_pow, map_one, map_ofNat, map_zero] at hr
  revert hr
  generalize r7_1 z = y
  revert y
  decide

/-- Equation `1516`, `x = (y ◇ y) ◇ (x ◇ (x ◇ y))`, holds in the model. -/
theorem sat_1516 : @Equation1516 R (Magma.lin (-th ^ 2 - th - 1) (th)) := by
  intro x y
  simp only [Magma.lin_op]
  linear_combination (x - y) * hq

end QuarticB

end
