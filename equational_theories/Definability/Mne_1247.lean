import equational_theories.Definability.AutBox2
import equational_theories.Definability.Mnb_00

/-!
# Structurality from `Equation1247` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation1247` is `x = x ◇ (((y ◇ x) ◇ z) ◇ w)`, and the companion is
`x □ y = if a = b then (M.op a a) else if (M.op a b) = a then b else (M.op a b)`.  Its automorphism half is `GuardAut2_x_y_pxy_x_pxx_y_pxy_Equation1247`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches it on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation3461` from `Equation1247`: `x = x ◇ (((y ◇ x) ◇ z) ◇ w)` ⊢ `x ◇ x = x ◇ ((y ◇ x) ◇ x)`, through the
companion `x □ y = if a = b then (M.op a a) else if (M.op a b) = a then b else (M.op a b)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3461_StructuralFromFin_Equation1247 : Law3461.StructuralFromFin Law1247 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1))
    GuardAut2_x_y_pxy_x_pxx_y_pxy_Equation1247 (fun {G} M hM ↦ ?_)
  have hm := (@Law1247.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, a ≠ b ∨ N.op a b = (M.op a a)) →
      (∀ a b : G, a = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, a = b ∨ (M.op a b) = a ∨ N.op a b = (M.op a b)) → @satisfies _ G N Law3461 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 0)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3461.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 (M.op (M.op (M.op X1 X0) X2) X3)) = X0 := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, X0 ≠ X1 ∨ (N.op X0 X1) = (M.op X0 X0) := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (N.op X0 X1) = X1 ∨ (M.op X0 X1) ≠ X0 ∨ X0 = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (N.op X0 X1) = (M.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ X0 = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op (N.op y x) x)) := by grind
  clear hm ht hu hv nh hM
  have eq14 : ∀ X0 X1 X2 : G, (M.op X1 (M.op (M.op X0 X1) X2)) = X1 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X1 X0 X2 (M.op (M.op (M.op x (M.op (M.op X0 X1) X2)) x) x)
       have i₂ := eq6 (M.op (M.op X0 X1) X2) x x x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 (M.op X0 X1)) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq14 X0 X1 (M.op (M.op x (M.op X0 X1)) x)
       have i₂ := eq14 x (M.op X0 X1) x
       grind)
    | exact superpose eq14 eq14
    | exact resolve eq14 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq24 : ∀ X0 X1 : G, (M.op X1 X0) = (M.op (M.op X1 X0) X0) := by
    intro X0 X1
    first
    | (have i₁ := eq18 X0 (M.op X1 X0)
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq18
    | exact resolve eq18 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq78 : (N.op x x) ≠ (N.op x x) ∨ (N.op y x) ≠ (M.op (N.op y x) x) ∨ x = (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq8 (N.op y x) x
       grind)
    | exact superpose eq8 eq10
    | (have j1 := eq8 (N.op y x) x
       grind)
    | (have r₁ := eq10
       have r₂ := eq8 (N.op x x) (N.op x (N.op (N.op y x) x))
       grind)
    | (have r₁ := eq10
       have r₂ := eq8 (N.op x (N.op (N.op y x) x)) (N.op x x)
       grind)
    | exact resolve eq10 eq8
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq80 : (N.op x x) ≠ (N.op x (N.op x x)) ∨ y ≠ (M.op y x) ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq8 y x
       grind)
    | exact superpose eq8 eq10
    | (have j1 := eq8 y x
       grind)
    | (have r₁ := eq10
       have r₂ := eq8 (N.op x x) (N.op x (N.op (N.op y x) x))
       grind)
    | (have r₁ := eq10
       have r₂ := eq8 (N.op x (N.op (N.op y x) x)) (N.op x x)
       grind)
    | exact resolve eq10 eq8
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq81 : (N.op y x) ≠ (M.op (N.op y x) x) ∨ x = (N.op y x) := by grind
  clear eq78
  have eq82 : y ≠ (M.op y x) ∨ (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = y := by grind
  clear eq80
  have eq252 : (M.op y x) ≠ (M.op (M.op y x) x) ∨ x = (M.op y x) ∨ y = (M.op y x) ∨ x = y := by
    first
    | (have i₁ := eq81
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq81
    | (have j1 := eq9 y x
       grind)
    | (have r₁ := eq81
       have r₂ := eq9 (N.op y x) x
       grind)
    | (have r₁ := eq81
       have r₂ := eq9 (N.op y x) (M.op (N.op y x) x)
       grind)
    | (have r₁ := eq81
       have r₂ := eq9 (M.op (N.op y x) x) (N.op y x)
       grind)
    | exact resolve eq81 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq253 : (N.op x x) ≠ (N.op x (N.op (M.op y x) x)) ∨ y = (M.op y x) ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y x
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op x x) (N.op x (N.op (N.op y x) x))
       grind)
    | (have r₁ := eq10
       have r₂ := eq9 (N.op x (N.op (N.op y x) x)) (N.op x x)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq256 : (M.op x x) ≠ (N.op x (N.op (M.op y x) x)) ∨ y = (M.op y x) ∨ x = y := by grind
  clear eq253
  have eq257 : x = y ∨ y = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have r₁ := eq252
       have r₂ := eq24 x y
       grind)
    | exact resolve eq252 eq24
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq252
  have eq261 : (N.op x x) ≠ (M.op (N.op x x) x) ∨ x = (N.op x x) ∨ y = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq81
       have i₂ := eq257
       grind)
    | exact superpose eq257 eq81
    | exact resolve eq81 eq257
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq81
  have eq262 : (N.op x x) ≠ (N.op x (N.op (N.op x x) x)) ∨ y = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq257
       grind)
    | exact superpose eq257 eq10
    | exact resolve eq10 eq257
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq257
  have eq263 : (M.op x x) ≠ (N.op x (N.op (M.op x x) x)) ∨ y = (M.op y x) ∨ x = (M.op y x) := by grind
  clear eq262
  have eq264 : (M.op x x) ≠ (M.op (M.op x x) x) ∨ x = (N.op x x) ∨ y = (M.op y x) ∨ x = (M.op y x) := by grind
  clear eq261
  have eq265 : x = (N.op x x) ∨ y = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have r₁ := eq264
       have r₂ := eq24 x x
       grind)
    | exact resolve eq264 eq24
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq264
  have eq266 : x = (M.op x x) ∨ y = (M.op y x) ∨ x = (M.op y x) := by grind
  clear eq265
  have eq3849 : x ≠ (N.op x (N.op x x)) ∨ y = (M.op y x) ∨ x = (M.op y x) ∨ y = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq263
       have i₂ := eq266
       grind)
    | exact superpose eq266 eq263
    | exact resolve eq263 eq266
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq263
  have eq3854 : x ≠ (N.op x (N.op x x)) ∨ y = (M.op y x) ∨ x = (M.op y x) := by grind
  clear eq3849
  have eq3855 : x ≠ (N.op x (M.op x x)) ∨ y = (M.op y x) ∨ x = (M.op y x) := by grind
  clear eq3854
  have eq3858 : x ≠ (N.op x x) ∨ y = (M.op y x) ∨ x = (M.op y x) ∨ y = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq3855
       have i₂ := eq266
       grind)
    | exact superpose eq266 eq3855
    | exact resolve eq3855 eq266
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3855
  have eq3861 : x ≠ (N.op x x) ∨ y = (M.op y x) ∨ x = (M.op y x) := by grind
  clear eq3858
  have eq3862 : x ≠ (M.op x x) ∨ y = (M.op y x) ∨ x = (M.op y x) := by grind
  clear eq3861
  have eq3863 : y = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have r₁ := eq3862
       have r₂ := eq266
       grind)
    | exact resolve eq3862 eq266
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq266 eq3862
  have eq3865 : y ≠ y ∨ (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = y ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq82
       have i₂ := eq3863
       grind)
    | exact superpose eq3863 eq82
    | (have r₁ := eq82
       have r₂ := eq3863
       grind)
    | exact resolve eq82 eq3863
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq82
  have eq3916 : x ≠ y ∨ x = (M.op y x) := by grind
  clear eq3863
  have eq3917 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = y ∨ x = (M.op y x) := by grind
  clear eq3865
  have eq3919 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op y x) := by
    first
    | (have r₁ := eq3917
       have r₂ := eq3916
       grind)
    | exact resolve eq3917 eq3916
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3916 eq3917
  have eq4420 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op y x) ∨ x ≠ (M.op x (M.op x x)) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq3919
       have i₂ := eq8 x (M.op x x)
       grind)
    | exact superpose eq8 eq3919
    | (have j1 := eq8 x (M.op x x)
       grind)
    | (have r₁ := eq3919
       have r₂ := eq8 x (M.op x x)
       grind)
    | (have r₁ := eq3919
       have r₂ := eq8 (M.op x x) (N.op x (M.op x x))
       grind)
    | (have r₁ := eq3919
       have r₂ := eq8 (N.op x (M.op x x)) (M.op x x)
       grind)
    | exact resolve eq3919 eq8
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq4421 : x = (M.op y x) ∨ x ≠ (M.op x (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq4420
  have eq4423 : x = (M.op x x) ∨ x = (M.op y x) := by
    first
    | (have r₁ := eq4421
       have r₂ := eq18 x x
       grind)
    | exact resolve eq4421 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18 eq4421
  have eq4434 : x ≠ (N.op x x) ∨ x = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq3919
       have i₂ := eq4423
       grind)
    | exact superpose eq4423 eq3919
    | exact resolve eq3919 eq4423
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3919
  have eq4483 : x ≠ (N.op x x) ∨ x = (M.op y x) := by grind
  clear eq4434
  have eq4486 : x ≠ (M.op x x) ∨ x = (M.op y x) := by grind
  clear eq4483
  have eq4487 : x = (M.op y x) := by
    first
    | (have r₁ := eq4486
       have r₂ := eq4423
       grind)
    | exact resolve eq4486 eq4423
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4423 eq4486
  have eq4494 : (N.op x (N.op x x)) ≠ (M.op x x) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq256
       have i₂ := eq4487
       grind)
    | exact superpose eq4487 eq256
    | exact resolve eq256 eq4487
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq256
  have eq4508 : x = (M.op x x) := by
    first
    | (have i₁ := eq24 x y
       have i₂ := eq4487
       grind)
    | exact superpose eq4487 eq24
    | exact resolve eq24 eq4487
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq24 eq4487
  have eq4543 : (N.op x (N.op x x)) ≠ (M.op x x) ∨ x = y := by grind
  clear eq4494
  have eq4544 : x ≠ (N.op x (N.op x x)) ∨ x = y := by
    first
    | (have i₁ := eq4543
       have i₂ := eq4508
       grind)
    | exact superpose eq4508 eq4543
    | exact resolve eq4543 eq4508
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4543
  have eq4545 : x ≠ (N.op x (M.op x x)) ∨ x = y := by grind
  clear eq4544
  have eq4546 : x ≠ (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq4545
       have i₂ := eq4508
       grind)
    | exact superpose eq4508 eq4545
    | exact resolve eq4545 eq4508
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4545
  have eq4547 : x ≠ (M.op x x) ∨ x = y := by grind
  clear eq4546
  have eq4548 : x = y := by
    first
    | (have r₁ := eq4547
       have r₂ := eq4508
       grind)
    | exact resolve eq4547 eq4508
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4547
  have eq4549 : (N.op x x) ≠ (N.op x (N.op (N.op x x) x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq4548
       grind)
    | exact superpose eq4548 eq10
    | exact resolve eq10 eq4548
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4548
  have eq4591 : (M.op x x) ≠ (N.op x (N.op (M.op x x) x)) := by grind
  clear eq4549
  have eq4596 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq4591
       have i₂ := eq4508
       grind)
    | exact superpose eq4508 eq4591
    | exact resolve eq4591 eq4508
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4591
  have eq4601 : x ≠ (N.op x (M.op x x)) := by grind
  clear eq4596
  have eq4603 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq4601
       have i₂ := eq4508
       grind)
    | exact superpose eq4508 eq4601
    | exact resolve eq4601 eq4508
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4601
  have eq4605 : x ≠ (M.op x x) := by grind
  clear eq4603
  have eq4607 : False := by grind
  exact eq4607
