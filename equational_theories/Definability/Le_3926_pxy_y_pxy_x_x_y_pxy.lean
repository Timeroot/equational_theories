import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation3926` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3926` is `x ◇ y = (x ◇ (y ◇ x)) ◇ z`, and the companion is
`x □ y = if (M.op a b) = b then a else if (M.op a b) = a then b else (M.op a b)`.  Its automorphism half is `GuardAut2_pxy_y_pxy_x_x_y_pxy_Equation3926`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches none of these on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation622` from `Equation3926`: `x ◇ y = (x ◇ (y ◇ x)) ◇ z` ⊢ `x = x ◇ (x ◇ ((y ◇ y) ◇ x))`, through the
companion `x □ y = if (M.op a b) = b then a else if (M.op a b) = a then b else (M.op a b)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation622_StructuralFromFin_Equation3926 : Law622.StructuralFromFin Law3926 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1))
    GuardAut2_pxy_y_pxy_x_x_y_pxy_Equation3926 (fun {G} M hM ↦ ?_)
  have hm := (@Law3926.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op a b) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) = a ∨ N.op a b = (M.op a b)) → @satisfies _ G N Law622 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law622.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (M.op (M.op X0 (M.op X1 X0)) X2) := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (M.op X0 X1) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op x (N.op x (N.op (N.op y y) x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X0 (M.op X1 X0)) X2) = (M.op (M.op X0 X1) X3) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 (M.op X0 (M.op X1 X0)) X2 X3
       have i₂ := eq6 X0 X1 (M.op X2 (M.op X0 (M.op X1 X0)))
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X3 : G, (M.op X0 X1) = (M.op (M.op X0 X1) X3) := by
    intro X0 X1 X3
    first
    | (have i₁ := eq12 X0 X1 x X3
       have i₂ := eq6 X0 X1 x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq18 : ∀ X0 X1 : G, (M.op X0 X1) = (M.op X0 (M.op X1 X0)) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 X1 x
       have i₂ := eq14 X0 (M.op X1 X0) x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq20 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ X2 ∨ (M.op X0 X1) = (N.op (M.op X0 X1) X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op X0 X1) X2
       have i₂ := eq14 X0 X1 X2
       grind)
    | exact superpose eq14 eq7
    | (have j0 := eq7 (M.op X0 X1) X2
       grind)
    | (have r₁ := eq7 (M.op X0 X1) (M.op X0 X1)
       have r₂ := eq14 X0 X1 (M.op X0 X1)
       grind)
    | exact resolve eq7 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X1 X0) ∨ (N.op X0 (M.op X1 X0)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X0)
       have i₂ := eq18 X0 X1
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X0 (M.op X1 X0)
       grind)
    | (have r₁ := eq7 X0 (M.op X0 X0)
       have r₂ := eq18 X0 X0
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq37 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X0 X1) = X2 ∨ (N.op (M.op X0 X1) X2) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 (M.op X0 X1) x
       have i₂ := eq14 X0 X1 x
       grind)
    | exact superpose eq14 eq8
    | (have j0 := eq8 (M.op X0 X1) X2
       grind)
    | (have r₁ := eq8 (M.op X0 X1) x
       have r₂ := eq14 X0 X1 x
       grind)
    | exact resolve eq8 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq39 : ∀ X0 X1 X2 : G, (N.op (M.op X0 X1) X2) = X2 ∨ (M.op X0 X1) = X2 := by
    intro X0 X1 X2
    first
    | (have j0 := eq37 X0 X1 X2
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq37
  have eq48 : x ≠ (N.op x (M.op x (N.op (N.op y y) x))) ∨ x = (M.op x (N.op (N.op y y) x)) ∨ (N.op (N.op y y) x) = (M.op x (N.op (N.op y y) x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x (N.op (N.op y y) x)
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x (N.op (N.op y y) x)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq50 : x ≠ (N.op x (N.op x (N.op (M.op y y) x))) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq51 : x ≠ (N.op x (N.op x (N.op (M.op y y) x))) ∨ y = (M.op y y) := by grind
  clear eq50
  have eq57 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op (M.op X0 X1) (M.op X0 X1)) := by
    intro X0 X1
    first
    | (have j0 := eq20 X0 X1 (M.op X0 X1)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20
  have eq179 : x ≠ (N.op x (N.op x x)) ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq51
       have i₂ := eq39 y y x
       grind)
    | exact superpose eq39 eq51
    | (have j1 := eq39 y y y
       grind)
    | exact resolve eq51 eq39
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq191 : ∀ X0 : G, (N.op X0 (M.op X0 X0)) = X0 := by
    intro X0
    first
    | (have j0 := eq30 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq240 : x ≠ (N.op x (M.op x x)) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq179
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq179
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq179 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq242 : x ≠ (N.op x (M.op x x)) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq240
  have eq243 : y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq242
       have r₂ := eq191 x
       grind)
    | exact resolve eq242 eq191
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq242
  have eq256 : ∀ X0 : G, y = (M.op y X0) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq243
       grind)
    | exact superpose eq243 eq14
    | exact resolve eq14 eq243
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq261 : ∀ X0 : G, (N.op y X0) = X0 ∨ y = X0 ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq39 y y x
       have i₂ := eq243
       grind)
    | exact superpose eq243 eq39
    | (have j0 := eq39 y y x
       grind)
    | exact resolve eq39 eq243
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq263 : y = (N.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq191 y
       have i₂ := eq243
       grind)
    | exact superpose eq243 eq191
    | exact resolve eq191 eq243
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq243
  have eq313 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op y X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq57 y y
       have i₂ := eq256 X0
       grind)
    | exact superpose eq256 eq57
    | (have j1 := eq256 X0
       grind)
    | exact resolve eq57 eq256
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq256
  have eq321 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op y X0) := by
    intro X0
    first
    | (have j0 := eq313 X0
       have j1 := eq7 x x
       grind)
    | (have r₁ := eq313 y
       have r₂ := eq7 y y
       grind)
    | (have r₁ := eq313 X0
       have r₂ := eq7 x x
       grind)
    | exact resolve eq313 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq313
  have eq354 : ∀ X0 : G, x ≠ (N.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ y = (M.op y X0) := by
    intro X0
    first
    | (have i₁ := eq179
       have i₂ := eq321 X0
       grind)
    | exact superpose eq321 eq179
    | (have j1 := eq321 y
       grind)
    | exact resolve eq179 eq321
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq179
  have eq381 : ∀ X0 : G, y = (M.op y y) ∨ y = (M.op y X0) ∨ x = (M.op y y) := by
    intro X0
    first
    | (have j0 := eq354 y
       have j1 := eq321 y
       grind)
    | (have r₁ := eq354 X0
       have r₂ := eq321 X0
       grind)
    | exact resolve eq354 eq321
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq321 eq354
  have eq386 : x ≠ (N.op x (M.op x (N.op y x))) ∨ x = (M.op x (N.op y x)) ∨ (N.op y x) = (M.op x (N.op y x)) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq48
       have i₂ := eq263
       grind)
    | exact superpose eq263 eq48
    | exact resolve eq48 eq263
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq48 eq263
  have eq2020 : y ≠ y ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have j0 := eq381 y
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq381
  have eq2022 : y = (M.op y y) ∨ x = (M.op y y) := by grind
  clear eq2020
  have eq2072 : ∀ X0 : G, (N.op y X0) = X0 ∨ y = X0 ∨ x = (M.op y y) := by
    intro X0
    first
    | (have i₁ := eq39 y y x
       have i₂ := eq2022
       grind)
    | exact superpose eq2022 eq39
    | (have j0 := eq39 y y x
       grind)
    | exact resolve eq39 eq2022
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39
  have eq2074 : y = (N.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq191 y
       have i₂ := eq2022
       grind)
    | exact superpose eq2022 eq191
    | exact resolve eq191 eq2022
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2075 : x ≠ y ∨ x = (M.op y y) := by grind
  clear eq2022
  have eq2453 : x ≠ (N.op x (N.op x (N.op y x))) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq2074
       grind)
    | exact superpose eq2074 eq10
    | exact resolve eq10 eq2074
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2074
  have eq2737 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op y y) ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq386
       have i₂ := eq261 x
       grind)
    | exact superpose eq261 eq386
    | (have j1 := eq261 x
       grind)
    | exact resolve eq386 eq261
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq261 eq386
  have eq2742 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op y y) ∨ x = y := by grind
  clear eq2737
  have eq2746 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    first
    | (have r₁ := eq2742
       have r₂ := eq2075
       grind)
    | exact resolve eq2742 eq2075
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2742
  have eq2749 : x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq2746
       have r₂ := eq191 x
       grind)
    | exact resolve eq2746 eq191
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq191 eq2746
  have eq2796 : x = (N.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq57 y y
       have i₂ := eq2749
       grind)
    | exact superpose eq2749 eq57
    | exact resolve eq57 eq2749
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq57 eq2749
  have eq2815 : x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq2796
       have r₂ := eq7 x x
       grind)
    | exact resolve eq2796 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2796
  have eq20307 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op y y) ∨ x = y ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq2453
       have i₂ := eq2072 x
       grind)
    | exact superpose eq2072 eq2453
    | (have j1 := eq2072 x
       grind)
    | exact resolve eq2453 eq2072
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2072 eq2453
  have eq20321 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op y y) ∨ x = y := by grind
  clear eq20307
  have eq20326 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op y y) := by
    first
    | (have r₁ := eq20321
       have r₂ := eq2075
       grind)
    | exact resolve eq20321 eq2075
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2075 eq20321
  have eq20328 : x ≠ (N.op x x) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq20326
       have i₂ := eq2815
       grind)
    | exact superpose eq2815 eq20326
    | exact resolve eq20326 eq2815
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20326
  have eq20330 : x = (M.op y y) := by
    first
    | (have r₁ := eq20328
       have r₂ := eq2815
       grind)
    | exact resolve eq20328 eq2815
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20328
  have eq20376 : x ≠ (N.op x (N.op x (N.op x x))) ∨ x = y := by
    first
    | (have i₁ := eq51
       have i₂ := eq20330
       grind)
    | exact superpose eq20330 eq51
    | exact resolve eq51 eq20330
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51 eq20330
  have eq20476 : x ≠ (N.op x (N.op x x)) ∨ x = y := by
    first
    | (have i₁ := eq20376
       have i₂ := eq2815
       grind)
    | exact superpose eq2815 eq20376
    | exact resolve eq20376 eq2815
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20376
  have eq20482 : x ≠ (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq20476
       have i₂ := eq2815
       grind)
    | exact superpose eq2815 eq20476
    | exact resolve eq20476 eq2815
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20476
  have eq20483 : x = y := by
    first
    | (have r₁ := eq20482
       have r₂ := eq2815
       grind)
    | exact resolve eq20482 eq2815
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20482
  have eq20496 : x ≠ (N.op x (N.op x (N.op (N.op x x) x))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq20483
       grind)
    | exact superpose eq20483 eq10
    | exact resolve eq10 eq20483
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20483
  have eq20735 : x ≠ (N.op x (N.op x (N.op x x))) := by
    first
    | (have i₁ := eq20496
       have i₂ := eq2815
       grind)
    | exact superpose eq2815 eq20496
    | exact resolve eq20496 eq2815
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20496
  have eq20737 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq20735
       have i₂ := eq2815
       grind)
    | exact superpose eq2815 eq20735
    | exact resolve eq20735 eq2815
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20735
  have eq20739 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq20737
       have i₂ := eq2815
       grind)
    | exact superpose eq2815 eq20737
    | exact resolve eq20737 eq2815
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20737
  have eq20741 : False := by grind
  exact eq20741

/-- `Equation1515` from `Equation3926`: `x ◇ y = (x ◇ (y ◇ x)) ◇ z` ⊢ `x = (y ◇ y) ◇ (x ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op a b) = b then a else if (M.op a b) = a then b else (M.op a b)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation1515_StructuralFromFin_Equation3926 : Law1515.StructuralFromFin Law3926 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1))
    GuardAut2_pxy_y_pxy_x_x_y_pxy_Equation3926 (fun {G} M hM ↦ ?_)
  have hm := (@Law3926.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op a b) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) = a ∨ N.op a b = (M.op a b)) → @satisfies _ G N Law1515 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law1515.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (M.op (M.op X0 (M.op X1 X0)) X2) := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (M.op X0 X1) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op (N.op y y) (N.op x (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X0 (M.op X1 X0)) X2) = (M.op (M.op X0 X1) X3) := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 (M.op X0 (M.op X1 X0)) X2 X3
       have i₂ := eq6 X0 X1 (M.op X2 (M.op X0 (M.op X1 X0)))
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X3 : G, (M.op X0 X1) = (M.op (M.op X0 X1) X3) := by
    intro X0 X1 X3
    first
    | (have i₁ := eq12 X0 X1 x X3
       have i₂ := eq6 X0 X1 x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq18 : ∀ X0 X1 : G, (M.op X0 X1) = (M.op X0 (M.op X1 X0)) := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 X1 x
       have i₂ := eq14 X0 (M.op X1 X0) x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq20 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ X2 ∨ (M.op X0 X1) = (N.op (M.op X0 X1) X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op X0 X1) X2
       have i₂ := eq14 X0 X1 X2
       grind)
    | exact superpose eq14 eq7
    | (have j0 := eq7 (M.op X0 X1) X2
       grind)
    | (have r₁ := eq7 (M.op X0 X1) (M.op X0 X1)
       have r₂ := eq14 X0 X1 (M.op X0 X1)
       grind)
    | exact resolve eq7 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X1 X0) ∨ (N.op X0 (M.op X1 X0)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X0)
       have i₂ := eq18 X0 X1
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X0 (M.op X1 X0)
       grind)
    | (have r₁ := eq7 X0 (M.op X0 X0)
       have r₂ := eq18 X0 X0
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq37 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X0 X1) = X2 ∨ (N.op (M.op X0 X1) X2) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 (M.op X0 X1) x
       have i₂ := eq14 X0 X1 x
       grind)
    | exact superpose eq14 eq8
    | (have j0 := eq8 (M.op X0 X1) X2
       grind)
    | (have r₁ := eq8 (M.op X0 X1) x
       have r₂ := eq14 X0 X1 x
       grind)
    | exact resolve eq8 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq39 : ∀ X0 X1 X2 : G, (N.op (M.op X0 X1) X2) = X2 ∨ (M.op X0 X1) = X2 := by
    intro X0 X1 X2
    first
    | (have j0 := eq37 X0 X1 X2
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq37
  have eq48 : x ≠ (N.op (N.op y y) (N.op x (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq50 : x ≠ (N.op (M.op y y) (N.op x (N.op x x))) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq51 : x ≠ (N.op (M.op y y) (N.op x (N.op x x))) ∨ y = (M.op y y) := by grind
  clear eq50
  have eq52 : x ≠ (N.op (N.op y y) (N.op x (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq48
  have eq58 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op (M.op X0 X1) (M.op X0 X1)) := by
    intro X0 X1
    first
    | (have j0 := eq20 X0 X1 (M.op X0 X1)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20
  have eq182 : x ≠ (N.op x (N.op x x)) ∨ y = (M.op y y) ∨ (N.op x (N.op x x)) = (M.op y y) := by
    first
    | (have i₁ := eq51
       have i₂ := eq39 y y (N.op x (N.op x x))
       grind)
    | exact superpose eq39 eq51
    | (have j1 := eq39 y y y
       grind)
    | exact resolve eq51 eq39
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq193 : ∀ X0 : G, (N.op X0 (M.op X0 X0)) = X0 := by
    intro X0
    first
    | (have j0 := eq30 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq217 : x ≠ (N.op (N.op y y) x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq52
       have i₂ := eq193 x
       grind)
    | exact superpose eq193 eq52
    | exact resolve eq52 eq193
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq52
  have eq243 : x ≠ (N.op (M.op y y) x) ∨ x = (M.op x x) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq217
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq217
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq217 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq245 : x ≠ (N.op (M.op y y) x) ∨ x = (M.op x x) ∨ y = (M.op y y) := by grind
  clear eq243
  have eq251 : x ≠ x ∨ x = (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq245
       have i₂ := eq39 y y x
       grind)
    | exact superpose eq39 eq245
    | (have j1 := eq39 x x x
       grind)
    | (have r₁ := eq245
       have r₂ := eq39 y y x
       grind)
    | exact resolve eq245 eq39
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq245
  have eq253 : y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op y y) := by grind
  clear eq251
  have eq267 : ∀ X0 : G, y = (M.op y X0) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq253
       grind)
    | exact superpose eq253 eq14
    | exact resolve eq14 eq253
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq272 : ∀ X0 : G, (N.op y X0) = X0 ∨ y = X0 ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    intro X0
    first
    | (have i₁ := eq39 y y x
       have i₂ := eq253
       grind)
    | exact superpose eq253 eq39
    | (have j0 := eq39 x x x
       grind)
    | exact resolve eq39 eq253
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq275 : x ≠ y ∨ x = (M.op x x) ∨ x = (M.op y y) := by grind
  clear eq253
  have eq329 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op y X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq58 y y
       have i₂ := eq267 X0
       grind)
    | exact superpose eq267 eq58
    | (have j1 := eq267 X0
       grind)
    | exact resolve eq58 eq267
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq267
  have eq339 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op y X0) := by
    intro X0
    first
    | (have j0 := eq329 X0
       have j1 := eq7 x x
       grind)
    | (have r₁ := eq329 y
       have r₂ := eq7 y y
       grind)
    | (have r₁ := eq329 X0
       have r₂ := eq7 x x
       grind)
    | exact resolve eq329 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq329
  have eq363 : ∀ X0 : G, x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op y X0) := by
    intro X0
    first
    | (have i₁ := eq9 x x
       have i₂ := eq339 X0
       grind)
    | exact superpose eq339 eq9
    | (have j0 := eq9 x x
       have j1 := eq339 X0
       grind)
    | exact resolve eq9 eq339
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq339
  have eq381 : ∀ X0 : G, y = (M.op y X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq363 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq363
  have eq453 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq193 y
       have i₂ := eq381 y
       grind)
    | exact superpose eq381 eq193
    | exact resolve eq193 eq381
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq381
  have eq484 : x ≠ (N.op y x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq217
       have i₂ := eq453
       grind)
    | exact superpose eq453 eq217
    | exact resolve eq217 eq453
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq217 eq453
  have eq488 : x ≠ (N.op y x) ∨ x = (M.op x x) := by grind
  clear eq484
  have eq504 : x ≠ x ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq488
       have i₂ := eq272 x
       grind)
    | exact superpose eq272 eq488
    | (have j1 := eq272 x
       grind)
    | (have r₁ := eq488
       have r₂ := eq272 x
       grind)
    | exact resolve eq488 eq272
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq272 eq488
  have eq506 : x ≠ x ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op y y) := by grind
  clear eq504
  have eq507 : x = (M.op x x) ∨ x = y ∨ x = (M.op y y) := by grind
  clear eq506
  have eq508 : x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq507
       have r₂ := eq275
       grind)
    | exact resolve eq507 eq275
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq275 eq507
  have eq578 : x = (N.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq58 y y
       have i₂ := eq508
       grind)
    | exact superpose eq508 eq58
    | exact resolve eq58 eq508
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq508
  have eq588 : x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq578
       have r₂ := eq7 x x
       grind)
    | exact resolve eq578 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq578
  have eq652 : x ≠ (N.op (N.op y y) (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq588
       grind)
    | exact superpose eq588 eq10
    | exact resolve eq10 eq588
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq657 : x ≠ (N.op x x) ∨ y = (M.op y y) ∨ (N.op x x) = (M.op y y) := by
    first
    | (have i₁ := eq182
       have i₂ := eq588
       grind)
    | exact superpose eq588 eq182
    | exact resolve eq182 eq588
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq182
  have eq665 : y = (M.op y y) ∨ (N.op x x) = (M.op y y) := by
    first
    | (have r₁ := eq657
       have r₂ := eq588
       grind)
    | exact resolve eq657 eq588
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq657
  have eq668 : x ≠ (N.op (N.op y y) x) := by
    first
    | (have i₁ := eq652
       have i₂ := eq588
       grind)
    | exact superpose eq588 eq652
    | exact resolve eq652 eq588
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq652
  have eq669 : y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq665
       have i₂ := eq588
       grind)
    | exact superpose eq588 eq665
    | exact resolve eq665 eq588
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq665
  have eq1423 : ∀ X0 : G, y = (M.op y X0) ∨ x = (M.op y y) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq669
       grind)
    | exact superpose eq669 eq14
    | exact resolve eq14 eq669
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14 eq669
  have eq1506 : ∀ X0 : G, x ≠ (N.op x (N.op x (N.op x x))) ∨ x = y ∨ y = (M.op y X0) := by
    intro X0
    first
    | (have i₁ := eq51
       have i₂ := eq1423 X0
       grind)
    | exact superpose eq1423 eq51
    | (have j1 := eq1423 X0
       grind)
    | exact resolve eq51 eq1423
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq1526 : ∀ X0 : G, y = (N.op y x) ∨ y = (M.op y X0) := by
    intro X0
    first
    | (have i₁ := eq193 y
       have i₂ := eq1423 X0
       grind)
    | exact superpose eq1423 eq193
    | (have j1 := eq1423 X0
       grind)
    | exact resolve eq193 eq1423
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq193 eq1423
  have eq1543 : ∀ X0 : G, x ≠ (N.op x (N.op x x)) ∨ x = y ∨ y = (M.op y X0) := by
    intro X0
    first
    | (have i₁ := eq1506 X0
       have i₂ := eq588
       grind)
    | exact superpose eq588 eq1506
    | (have j0 := eq1506 X0
       grind)
    | exact resolve eq1506 eq588
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1506
  have eq1549 : ∀ X0 : G, x ≠ (N.op x x) ∨ x = y ∨ y = (M.op y X0) := by
    intro X0
    first
    | (have i₁ := eq1543 X0
       have i₂ := eq588
       grind)
    | exact superpose eq588 eq1543
    | (have j0 := eq1543 X0
       grind)
    | exact resolve eq1543 eq588
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1543
  have eq1551 : ∀ X0 : G, y = (M.op y X0) ∨ x = y := by
    intro X0
    first
    | (have j0 := eq1549 X0
       grind)
    | (have r₁ := eq1549 X0
       have r₂ := eq588
       grind)
    | exact resolve eq1549 eq588
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1549
  have eq1586 : ∀ X1 : G, (N.op y X1) = X1 ∨ y = X1 ∨ x = y := by
    intro X1
    first
    | (have i₁ := eq39 y x x
       have i₂ := eq1551 x
       grind)
    | exact superpose eq1551 eq39
    | exact resolve eq39 eq1551
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq39 eq1551
  have eq2026 : y = (N.op y y) ∨ y = (N.op y x) := by
    first
    | (have i₁ := eq58 y x
       have i₂ := eq1526 x
       grind)
    | exact superpose eq1526 eq58
    | exact resolve eq58 eq1526
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq58 eq1526
  have eq2522 : x ≠ (N.op y x) ∨ y = (N.op y x) := by
    first
    | (have i₁ := eq668
       have i₂ := eq2026
       grind)
    | exact superpose eq2026 eq668
    | exact resolve eq668 eq2026
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2026
  have eq2992 : x ≠ x ∨ x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq2522
       have i₂ := eq1586 x
       grind)
    | exact superpose eq1586 eq2522
    | (have j1 := eq1586 x
       grind)
    | (have r₁ := eq2522
       have r₂ := eq1586 x
       grind)
    | exact resolve eq2522 eq1586
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1586 eq2522
  have eq2997 : x ≠ x ∨ x = y := by grind
  clear eq2992
  have eq2998 : x = y := by grind
  clear eq2997
  have eq3097 : x ≠ (N.op (N.op x x) x) := by
    first
    | (have i₁ := eq668
       have i₂ := eq2998
       grind)
    | exact superpose eq2998 eq668
    | exact resolve eq668 eq2998
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq668 eq2998
  have eq3143 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq3097
       have i₂ := eq588
       grind)
    | exact superpose eq588 eq3097
    | exact resolve eq3097 eq588
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3097
  have eq3146 : False := by grind
  exact eq3146
