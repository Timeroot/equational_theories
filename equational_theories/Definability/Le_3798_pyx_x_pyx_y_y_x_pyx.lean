import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation3798` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3798` is `x ◇ y = (z ◇ x) ◇ (w ◇ y)`, and the companion is
`x □ y = if (M.op b a) = a then b else if (M.op b a) = b then a else (M.op b a)`.  Its automorphism half is `GuardAut2_pyx_x_pyx_y_y_x_pyx_Equation3798`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches it on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation1691` from `Equation3798`: `x ◇ y = (z ◇ x) ◇ (w ◇ y)` ⊢ `x = (y ◇ x) ◇ ((y ◇ x) ◇ x)`, through the
companion `x □ y = if (M.op b a) = a then b else if (M.op b a) = b then a else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation1691_StructuralFromFin_Equation3798 : Law1691.StructuralFromFin Law3798 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_x_pyx_y_y_x_pyx_Equation3798 (fun {G} M hM ↦ ?_)
  have hm := (@Law3798.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = a ∨ (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = a ∨ (M.op b a) = b ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law1691 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (a)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (a)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (b)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law1691.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) = (M.op (M.op X2 X0) (M.op X3 X1)) := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (M.op X1 X0) = X0 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X1 ∨ (M.op X1 X0) = X0 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op (N.op y x) (N.op (N.op y x) x)) := by grind
  clear hm ht hu hv nh hM
  have eq11 : ∀ X0 X1 X2 X3 X5 : G, (M.op (M.op X2 X1) X3) = (M.op (M.op X0 X1) (M.op X5 X3)) := by
    intro X0 X1 X2 X3 X5
    first
    | (have i₁ := eq6 (M.op X2 X1) X3 (M.op x X0) X5
       have i₂ := eq6 X0 X1 x X2
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq12 : ∀ X0 X1 X2 X3 X4 : G, (M.op X2 (M.op X3 X1)) = (M.op (M.op X4 X2) (M.op X0 X1)) := by
    intro X0 X1 X2 X3 X4
    first
    | (have i₁ := eq6 X2 (M.op X3 X1) X4 (M.op x X0)
       have i₂ := eq6 X0 X1 x X3
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X1 X2 X3 : G, (M.op X2 X1) = (M.op X2 (M.op X3 X1)) := by
    intro X1 X2 X3
    first
    | (have i₁ := eq12 x X1 X2 X3 x
       have i₂ := eq6 X2 X1 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq15 : ∀ X1 X2 X3 : G, (M.op X1 X3) = (M.op (M.op X2 X1) X3) := by
    intro X1 X2 X3
    first
    | (have i₁ := eq11 x X1 X2 X3 x
       have i₂ := eq6 X1 X3 x x
       grind)
    | exact superpose eq6 eq11
    | exact resolve eq11 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq11
  have eq21 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X2 X1) ∨ (N.op (M.op X2 X1) X0) = X0 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op X2 X1) X0
       have i₂ := eq14 X1 X0 X2
       grind)
    | exact superpose eq14 eq7
    | (have j0 := eq7 (M.op X2 X1) X0
       grind)
    | (have r₁ := eq7 (M.op X2 X1) X2
       have r₂ := eq14 X1 X2 X2
       grind)
    | exact resolve eq7 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq44 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X2 X0) ∨ (M.op X0 X1) = X1 ∨ (N.op X1 (M.op X2 X0)) = X1 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 x (M.op X2 X1)
       have i₂ := eq15 X1 X2 x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X1 X0
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq51 : ∀ X0 X1 : G, (N.op (M.op X0 X1) X0) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq21 X0 X1 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq58 : x ≠ (N.op (M.op x y) (N.op (M.op x y) x)) ∨ x = (M.op x y) ∨ y = (M.op x y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq59 : x ≠ (N.op (N.op y x) (M.op x (N.op y x))) ∨ x = (M.op x (N.op y x)) ∨ (N.op y x) = (M.op x (N.op y x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 (N.op y x) x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 (N.op y x) x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq61 : x ≠ (N.op (M.op x y) x) ∨ x = (M.op x y) ∨ y = (M.op x y) := by
    first
    | (have i₁ := eq58
       have i₂ := eq51 x y
       grind)
    | exact superpose eq51 eq58
    | exact resolve eq58 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq58
  have eq62 : y = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have r₁ := eq61
       have r₂ := eq51 x y
       grind)
    | exact resolve eq61 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq61
  have eq75 : x = (N.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq51 x y
       have i₂ := eq62
       grind)
    | exact superpose eq62 eq51
    | exact resolve eq51 eq62
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq62
  have eq96 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq75
       grind)
    | exact superpose eq75 eq10
    | exact resolve eq10 eq75
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq136 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq59
       have i₂ := eq75
       grind)
    | exact superpose eq75 eq59
    | exact resolve eq59 eq75
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq59 eq75
  have eq139 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x y) := by grind
  clear eq136
  have eq203 : ∀ X0 : G, (N.op X0 (M.op X0 X0)) = X0 ∨ (M.op X0 X0) = X0 := by
    intro X0
    first
    | (have j0 := eq44 X0 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq44
  have eq242 : x ≠ x ∨ x = (M.op x x) ∨ x = (M.op x y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq139
       have i₂ := eq203 x
       grind)
    | exact superpose eq203 eq139
    | (have j1 := eq203 x
       grind)
    | (have r₁ := eq139
       have r₂ := eq203 x
       grind)
    | exact resolve eq139 eq203
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq139 eq203
  have eq244 : x ≠ x ∨ x = (M.op x x) ∨ x = (M.op x y) := by grind
  clear eq242
  have eq245 : x = (M.op x y) ∨ x = (M.op x x) := by grind
  clear eq244
  have eq262 : x = (N.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq51 x y
       have i₂ := eq245
       grind)
    | exact superpose eq245 eq51
    | exact resolve eq51 eq245
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51 eq245
  have eq269 : x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq262
       have r₂ := eq7 x x
       grind)
    | exact resolve eq262 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq262
  have eq273 : x ≠ (N.op x x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq96
       have i₂ := eq269
       grind)
    | exact superpose eq269 eq96
    | exact resolve eq96 eq269
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq96
  have eq274 : x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq9 x x
       have i₂ := eq269
       grind)
    | exact superpose eq269 eq9
    | (have j0 := eq9 x x
       grind)
    | exact resolve eq9 eq269
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq275 : x = (M.op x x) := by grind
  clear eq274
  have eq277 : x = (M.op x y) := by
    first
    | (have r₁ := eq273
       have r₂ := eq269
       grind)
    | exact resolve eq273 eq269
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq273
  have eq320 : x ≠ x ∨ x = y ∨ y = (N.op y x) := by
    first
    | (have i₁ := eq8 y x
       have i₂ := eq277
       grind)
    | exact superpose eq277 eq8
    | (have j0 := eq8 y x
       grind)
    | (have r₁ := eq8 y x
       have r₂ := eq277
       grind)
    | exact resolve eq8 eq277
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq323 : ∀ X0 : G, (M.op X0 y) = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 y X0 x
       have i₂ := eq277
       grind)
    | exact superpose eq277 eq14
    | exact resolve eq14 eq277
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq324 : ∀ X0 : G, (M.op y X0) = (M.op x X0) := by
    intro X0
    first
    | (have i₁ := eq15 y x X0
       have i₂ := eq277
       grind)
    | exact superpose eq277 eq15
    | exact resolve eq15 eq277
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15 eq277
  have eq336 : y = (N.op y x) ∨ x = y := by grind
  clear eq320
  have eq660 : x ≠ (N.op y (N.op y x)) ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq336
       grind)
    | exact superpose eq336 eq10
    | exact resolve eq10 eq336
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq754 : x ≠ (N.op y y) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq660
       have i₂ := eq336
       grind)
    | exact superpose eq336 eq660
    | exact resolve eq660 eq336
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq336 eq660
  have eq760 : x ≠ (N.op y y) ∨ x = y := by grind
  clear eq754
  have eq777 : x ≠ (M.op y y) ∨ x = y ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq760
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq760
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq760 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq760
  have eq778 : x ≠ (M.op y y) ∨ x = y ∨ y = (M.op y y) := by grind
  clear eq777
  have eq779 : x ≠ (M.op y x) ∨ x = y ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq778
       have i₂ := eq323 y
       grind)
    | exact superpose eq323 eq778
    | exact resolve eq778 eq323
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq778
  have eq780 : x ≠ (M.op x x) ∨ x = y ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq779
       have i₂ := eq324 x
       grind)
    | exact superpose eq324 eq779
    | exact resolve eq779 eq324
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq779
  have eq781 : x = y ∨ y = (M.op y y) := by
    first
    | (have r₁ := eq780
       have r₂ := eq275
       grind)
    | exact resolve eq780 eq275
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq780
  have eq782 : y = (M.op y x) ∨ x = y := by
    first
    | (have i₁ := eq781
       have i₂ := eq323 y
       grind)
    | exact superpose eq323 eq781
    | exact resolve eq781 eq323
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq323 eq781
  have eq783 : y = (M.op x x) ∨ x = y := by
    first
    | (have i₁ := eq782
       have i₂ := eq324 x
       grind)
    | exact superpose eq324 eq782
    | exact resolve eq782 eq324
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq324 eq782
  have eq784 : x = y ∨ x = y := by
    first
    | (have i₁ := eq783
       have i₂ := eq275
       grind)
    | exact superpose eq275 eq783
    | exact resolve eq783 eq275
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq275 eq783
  have eq785 : x = y := by grind
  clear eq784
  have eq790 : x ≠ (N.op (N.op x x) (N.op (N.op x x) x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq785
       grind)
    | exact superpose eq785 eq10
    | exact resolve eq10 eq785
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq785
  have eq830 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq790
       have i₂ := eq269
       grind)
    | exact superpose eq269 eq790
    | exact resolve eq790 eq269
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq790
  have eq834 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq830
       have i₂ := eq269
       grind)
    | exact superpose eq269 eq830
    | exact resolve eq830 eq269
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq830
  have eq837 : False := by grind
  exact eq837
