import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation2402` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation2402` is `x = (y ◇ (z ◇ (z ◇ y))) ◇ x`, and the companion is
`x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`.  Its automorphism half is `GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2402`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches none of these on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation307` from `Equation2402`: `x = (y ◇ (z ◇ (z ◇ y))) ◇ x` ⊢ `x ◇ x = x ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation307_StructuralFromFin_Equation2402 : Law307.StructuralFromFin Law2402 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2402 (fun {G} M hM ↦ ?_)
  have hm := (@Law2402.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law307 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law307.models_iff G N]
  intro x
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X2 (M.op X2 X1))) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X2 (M.op (M.op X0 (M.op X1 (M.op X1 X0))) X2)) X3) = X3 := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X3 X2 (M.op X0 (M.op X1 (M.op X1 X0)))
       have i₂ := eq6 (M.op (M.op X0 (M.op X1 (M.op X1 X0))) X2) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq15 : ∀ X2 X3 : G, (M.op (M.op X2 X2) X3) = X3 := by
    intro X2 X3
    first
    | (have i₁ := eq12 x x X2 X3
       have i₂ := eq6 X2 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq21 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op x x)
       have i₂ := eq15 x x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq26 : ∀ X0 X1 : G, (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have j0 := eq21 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq29 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq31 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq29
  have eq32 : x = (M.op x x) := by
    first
    | (have j1 := eq26 x x
       grind)
    | (have r₁ := eq31
       have r₂ := eq26 (N.op x (M.op x x)) x
       grind)
    | (have r₁ := eq31
       have r₂ := eq26 x x
       grind)
    | exact resolve eq31 eq26
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26 eq31
  have eq34 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq32
       grind)
    | exact superpose eq32 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq32
       grind)
    | exact resolve eq7 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq36 : x = (N.op x x) := by grind
  clear eq34
  have eq125 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq36
       grind)
    | exact superpose eq36 eq10
    | exact resolve eq10 eq36
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq129 : False := by grind
  exact eq129

/-- `Equation312` from `Equation2402`: `x = (y ◇ (z ◇ (z ◇ y))) ◇ x` ⊢ `x ◇ x = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation312_StructuralFromFin_Equation2402 : Law312.StructuralFromFin Law2402 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2402 (fun {G} M hM ↦ ?_)
  have hm := (@Law2402.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law312 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law312.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X2 (M.op X2 X1))) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X2 (M.op (M.op X0 (M.op X1 (M.op X1 X0))) X2)) X3) = X3 := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X3 X2 (M.op X0 (M.op X1 (M.op X1 X0)))
       have i₂ := eq6 (M.op (M.op X0 (M.op X1 (M.op X1 X0))) X2) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq15 : ∀ X2 X3 : G, (M.op (M.op X2 X2) X3) = X3 := by
    intro X2 X3
    first
    | (have i₁ := eq12 x x X2 X3
       have i₂ := eq6 X2 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq21 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op x x)
       have i₂ := eq15 x x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq23 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq15 X1 X0
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op x x) (M.op x x)
       have r₂ := eq15 x (M.op x x)
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq26 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq21 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq29 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq31 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq29
  have eq60 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq23 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq23
  have eq155 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq31
       have i₂ := eq26 y x
       grind)
    | exact superpose eq26 eq31
    | (have j1 := eq26 x x
       grind)
    | (have r₁ := eq31
       have r₂ := eq26 y x
       grind)
    | (have r₁ := eq31
       have r₂ := eq26 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq31 eq26
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq156 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq155
  have eq191 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq31
       have i₂ := eq156
       grind)
    | exact superpose eq156 eq31
    | exact resolve eq31 eq156
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq31
  have eq199 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq60 x
       have i₂ := eq156
       grind)
    | exact superpose eq156 eq60
    | exact resolve eq60 eq156
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq203 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq156
  have eq204 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq191
       have r₂ := eq199
       grind)
    | exact resolve eq191 eq199
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq191 eq199
  have eq205 : x = (M.op x x) := by
    first
    | (have r₁ := eq204
       have r₂ := eq203
       grind)
    | exact resolve eq204 eq203
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq203 eq204
  have eq339 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq26 X0 x
       have i₂ := eq205
       grind)
    | exact superpose eq205 eq26
    | exact resolve eq26 eq205
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26
  have eq340 : x = (N.op x x) := by
    first
    | (have i₁ := eq60 x
       have i₂ := eq205
       grind)
    | exact superpose eq205 eq60
    | exact resolve eq60 eq205
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq60 eq205
  have eq458 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq340
       grind)
    | exact superpose eq340 eq10
    | exact resolve eq10 eq340
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1146 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq458
       have i₂ := eq339 y
       grind)
    | exact superpose eq339 eq458
    | (have j1 := eq339 y
       grind)
    | (have r₁ := eq458
       have r₂ := eq339 y
       grind)
    | (have r₁ := eq458
       have r₂ := eq339 (N.op y x)
       grind)
    | exact resolve eq458 eq339
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq339
  have eq1147 : x = y := by grind
  clear eq1146
  have eq1400 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq458
       have i₂ := eq1147
       grind)
    | exact superpose eq1147 eq458
    | exact resolve eq458 eq1147
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq458 eq1147
  have eq1405 : False := by grind
  exact eq1405

/-- `Equation3258` from `Equation2402`: `x = (y ◇ (z ◇ (z ◇ y))) ◇ x` ⊢ `x ◇ x = x ◇ (y ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3258_StructuralFromFin_Equation2402 : Law3258.StructuralFromFin Law2402 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2402 (fun {G} M hM ↦ ?_)
  have hm := (@Law2402.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3258 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3258.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X2 (M.op X2 X1))) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op y (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X2 (M.op (M.op X0 (M.op X1 (M.op X1 X0))) X2)) X3) = X3 := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X3 X2 (M.op X0 (M.op X1 (M.op X1 X0)))
       have i₂ := eq6 (M.op (M.op X0 (M.op X1 (M.op X1 X0))) X2) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq15 : ∀ X2 X3 : G, (M.op (M.op X2 X2) X3) = X3 := by
    intro X2 X3
    first
    | (have i₁ := eq12 x x X2 X3
       have i₂ := eq6 X2 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq20 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq15 X1 X0
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op x x) (M.op x x)
       have r₂ := eq15 x (M.op x x)
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq49 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op x x)
       have i₂ := eq15 x x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq50 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq49 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq49
  have eq56 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq20 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20
  have eq57 : (M.op x x) ≠ (N.op x (N.op y (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq60 : (M.op x x) ≠ (N.op x (N.op y (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq57
  have eq128 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq60
       have i₂ := eq50 y x
       grind)
    | exact superpose eq50 eq60
    | (have j1 := eq50 x x
       grind)
    | (have r₁ := eq60
       have r₂ := eq50 (N.op x (N.op y (M.op x x))) x
       grind)
    | exact resolve eq60 eq50
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq131 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq50 y x
       grind)
    | (have r₁ := eq128
       have r₂ := eq50 x x
       grind)
    | (have r₁ := eq128
       have r₂ := eq50 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq128 eq50
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq128
  have eq132 : y ≠ (N.op x (N.op y y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq60
       have i₂ := eq131
       grind)
    | exact superpose eq131 eq60
    | exact resolve eq60 eq131
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq60
  have eq137 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq50 X0 x
       have i₂ := eq131
       grind)
    | exact superpose eq131 eq50
    | (have j0 := eq50 x x
       grind)
    | exact resolve eq50 eq131
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq138 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq56 x
       have i₂ := eq131
       grind)
    | exact superpose eq131 eq56
    | exact resolve eq56 eq131
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq142 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq131
  have eq143 : y ≠ (N.op x (N.op y y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq132
       have r₂ := eq142
       grind)
    | exact resolve eq132 eq142
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq132
  have eq241 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq143
       have i₂ := eq138
       grind)
    | exact superpose eq138 eq143
    | exact resolve eq143 eq138
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq138 eq143
  have eq245 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq241
  have eq488 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq245
       have i₂ := eq137 x
       grind)
    | exact superpose eq137 eq245
    | (have j1 := eq137 x
       grind)
    | (have r₁ := eq245
       have r₂ := eq137 x
       grind)
    | (have r₁ := eq245
       have r₂ := eq137 (N.op x y)
       grind)
    | exact resolve eq245 eq137
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq137 eq245
  have eq489 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq488
  have eq490 : x = (M.op x x) ∨ x = y := by grind
  clear eq489
  have eq493 : x = (M.op x x) := by
    first
    | (have r₁ := eq490
       have r₂ := eq142
       grind)
    | exact resolve eq490 eq142
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq142 eq490
  have eq578 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq50 X0 x
       have i₂ := eq493
       grind)
    | exact superpose eq493 eq50
    | exact resolve eq50 eq493
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq50
  have eq579 : x = (N.op x x) := by
    first
    | (have i₁ := eq56 x
       have i₂ := eq493
       grind)
    | exact superpose eq493 eq56
    | exact resolve eq56 eq493
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq56 eq493
  have eq608 : x ≠ (N.op x (N.op y x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq579
       grind)
    | exact superpose eq579 eq10
    | exact resolve eq10 eq579
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1014 : x ≠ (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq608
       have i₂ := eq578 y
       grind)
    | exact superpose eq578 eq608
    | (have j1 := eq578 y
       grind)
    | (have r₁ := eq608
       have r₂ := eq578 (N.op x (N.op y x))
       grind)
    | exact resolve eq608 eq578
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq578
  have eq1017 : x = y := by
    first
    | (have r₁ := eq1014
       have r₂ := eq579
       grind)
    | exact resolve eq1014 eq579
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1014
  have eq1260 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq608
       have i₂ := eq1017
       grind)
    | exact superpose eq1017 eq608
    | exact resolve eq608 eq1017
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq608 eq1017
  have eq1262 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq1260
       have i₂ := eq579
       grind)
    | exact superpose eq579 eq1260
    | exact resolve eq1260 eq579
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1260
  have eq1267 : False := by grind
  exact eq1267

/-- `Equation3268` from `Equation2402`: `x = (y ◇ (z ◇ (z ◇ y))) ◇ x` ⊢ `x ◇ x = y ◇ (x ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3268_StructuralFromFin_Equation2402 : Law3268.StructuralFromFin Law2402 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2402 (fun {G} M hM ↦ ?_)
  have hm := (@Law2402.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3268 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3268.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X2 (M.op X2 X1))) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X2 (M.op (M.op X0 (M.op X1 (M.op X1 X0))) X2)) X3) = X3 := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X3 X2 (M.op X0 (M.op X1 (M.op X1 X0)))
       have i₂ := eq6 (M.op (M.op X0 (M.op X1 (M.op X1 X0))) X2) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq15 : ∀ X2 X3 : G, (M.op (M.op X2 X2) X3) = X3 := by
    intro X2 X3
    first
    | (have i₁ := eq12 x x X2 X3
       have i₂ := eq6 X2 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq20 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq15 X1 X0
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op x x) (M.op x x)
       have r₂ := eq15 x (M.op x x)
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq49 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op x x)
       have i₂ := eq15 x x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq50 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq49 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq49
  have eq56 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq20 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20
  have eq57 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq60 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq57
  have eq128 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq60
       have i₂ := eq50 x x
       grind)
    | exact superpose eq50 eq60
    | (have j1 := eq50 x x
       grind)
    | (have r₁ := eq60
       have r₂ := eq50 (N.op y (N.op x (M.op x x))) x
       grind)
    | exact resolve eq60 eq50
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq60
  have eq131 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq128
  have eq132 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq131
       have i₂ := eq50 y x
       grind)
    | exact superpose eq50 eq131
    | (have j1 := eq50 x x
       grind)
    | (have r₁ := eq131
       have r₂ := eq50 y x
       grind)
    | (have r₁ := eq131
       have r₂ := eq50 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq131 eq50
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq134 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq132
  have eq164 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq131
       have i₂ := eq134
       grind)
    | exact superpose eq134 eq131
    | exact resolve eq131 eq134
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq131
  have eq172 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq56 x
       have i₂ := eq134
       grind)
    | exact superpose eq134 eq56
    | exact resolve eq56 eq134
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq176 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq134
  have eq178 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq164
       have r₂ := eq172
       grind)
    | exact resolve eq164 eq172
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq164 eq172
  have eq179 : x = (M.op x x) := by
    first
    | (have r₁ := eq178
       have r₂ := eq176
       grind)
    | exact resolve eq178 eq176
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq176 eq178
  have eq219 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq50 X0 x
       have i₂ := eq179
       grind)
    | exact superpose eq179 eq50
    | exact resolve eq50 eq179
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq50
  have eq220 : x = (N.op x x) := by
    first
    | (have i₁ := eq56 x
       have i₂ := eq179
       grind)
    | exact superpose eq179 eq56
    | exact resolve eq56 eq179
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq56 eq179
  have eq242 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq220
       grind)
    | exact superpose eq220 eq10
    | exact resolve eq10 eq220
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq247 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq242
       have i₂ := eq220
       grind)
    | exact superpose eq220 eq242
    | exact resolve eq242 eq220
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq242
  have eq647 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq247
       have i₂ := eq219 y
       grind)
    | exact superpose eq219 eq247
    | (have j1 := eq219 y
       grind)
    | (have r₁ := eq247
       have r₂ := eq219 y
       grind)
    | (have r₁ := eq247
       have r₂ := eq219 (N.op y x)
       grind)
    | exact resolve eq247 eq219
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq219
  have eq648 : x = y := by grind
  clear eq647
  have eq662 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq247
       have i₂ := eq648
       grind)
    | exact superpose eq648 eq247
    | exact resolve eq247 eq648
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq247 eq648
  have eq664 : False := by grind
  exact eq664

/-- `Equation3288` from `Equation2402`: `x = (y ◇ (z ◇ (z ◇ y))) ◇ x` ⊢ `x ◇ x = y ◇ (z ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3288_StructuralFromFin_Equation2402 : Law3288.StructuralFromFin Law2402 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2402 (fun {G} M hM ↦ ?_)
  have hm := (@Law2402.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3288 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3288.models_iff G N]
  intro x y z
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X2 (M.op X2 X1))) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op z (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X2 (M.op (M.op X0 (M.op X1 (M.op X1 X0))) X2)) X3) = X3 := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X3 X2 (M.op X0 (M.op X1 (M.op X1 X0)))
       have i₂ := eq6 (M.op (M.op X0 (M.op X1 (M.op X1 X0))) X2) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq15 : ∀ X2 X3 : G, (M.op (M.op X2 X2) X3) = X3 := by
    intro X2 X3
    first
    | (have i₁ := eq12 x x X2 X3
       have i₂ := eq6 X2 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq20 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq15 X1 X0
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op x x) (M.op x x)
       have r₂ := eq15 x (M.op x x)
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq49 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op x x)
       have i₂ := eq15 x x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq50 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq49 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq49
  have eq56 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq20 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20
  have eq57 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq60 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq57
  have eq128 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ z = (M.op x x) := by
    first
    | (have i₁ := eq60
       have i₂ := eq50 z x
       grind)
    | exact superpose eq50 eq60
    | (have j1 := eq50 x x
       grind)
    | (have r₁ := eq60
       have r₂ := eq50 (N.op y (N.op z (M.op x x))) x
       grind)
    | exact resolve eq60 eq50
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq560 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ z = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq128
       have i₂ := eq50 y x
       grind)
    | exact superpose eq50 eq128
    | (have j1 := eq50 x x
       grind)
    | (have r₁ := eq128
       have r₂ := eq50 y x
       grind)
    | (have r₁ := eq128
       have r₂ := eq50 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq128 eq50
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq562 : z = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq560
  have eq566 : z ≠ (N.op y (N.op z z)) ∨ x = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq60
       have i₂ := eq562
       grind)
    | exact superpose eq562 eq60
    | exact resolve eq60 eq562
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq578 : ∀ X0 : G, z = (N.op X0 z) ∨ z = X0 ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq50 X0 x
       have i₂ := eq562
       grind)
    | exact superpose eq562 eq50
    | (have j0 := eq50 x x
       grind)
    | exact resolve eq50 eq562
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq579 : z = (N.op z z) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq56 x
       have i₂ := eq562
       grind)
    | exact superpose eq562 eq56
    | exact resolve eq56 eq562
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq581 : x ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  have eq582 : y ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq562
  have eq583 : z ≠ (N.op y (N.op z z)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have r₁ := eq566
       have r₂ := eq581
       grind)
    | exact resolve eq566 eq581
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq566 eq581
  have eq1133 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq583
       have i₂ := eq579
       grind)
    | exact superpose eq579 eq583
    | exact resolve eq583 eq579
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq579 eq583
  have eq1137 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq1133
  have eq1431 : z ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq1137
       have i₂ := eq578 y
       grind)
    | exact superpose eq578 eq1137
    | (have j1 := eq578 y
       grind)
    | (have r₁ := eq1137
       have r₂ := eq578 y
       grind)
    | (have r₁ := eq1137
       have r₂ := eq578 (N.op y z)
       grind)
    | exact resolve eq1137 eq578
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq578 eq1137
  have eq1432 : z ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq1431
  have eq1433 : x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq1432
  have eq1436 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1433
       have r₂ := eq582
       grind)
    | exact resolve eq1433 eq582
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq582 eq1433
  have eq1550 : y ≠ (N.op y y) ∨ x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq128
       have i₂ := eq1436
       grind)
    | exact superpose eq1436 eq128
    | exact resolve eq128 eq1436
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq128
  have eq1551 : y ≠ (N.op y (N.op z y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq60
       have i₂ := eq1436
       grind)
    | exact superpose eq1436 eq60
    | exact resolve eq60 eq1436
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq60
  have eq1571 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq56 x
       have i₂ := eq1436
       grind)
    | exact superpose eq1436 eq56
    | exact resolve eq56 eq1436
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1573 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq1436
  have eq1574 : y ≠ (N.op y (N.op z y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1551
       have r₂ := eq1573
       grind)
    | exact resolve eq1551 eq1573
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1551
  have eq1575 : x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1550
       have r₂ := eq1571
       grind)
    | exact resolve eq1550 eq1571
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1550
  have eq1576 : x = (M.op x x) ∨ y = z := by
    first
    | (have r₁ := eq1575
       have r₂ := eq1573
       grind)
    | exact resolve eq1575 eq1573
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1575
  have eq2376 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ y = z := by
    intro X0
    first
    | (have i₁ := eq50 X0 x
       have i₂ := eq1576
       grind)
    | exact superpose eq1576 eq50
    | exact resolve eq50 eq1576
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2378 : x = (N.op x x) ∨ y = z := by
    first
    | (have i₁ := eq56 x
       have i₂ := eq1576
       grind)
    | exact superpose eq1576 eq56
    | exact resolve eq56 eq1576
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq56 eq1576
  have eq3226 : x ≠ (N.op y (N.op z x)) ∨ y = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq2378
       grind)
    | exact superpose eq2378 eq10
    | exact resolve eq10 eq2378
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq4913 : x ≠ (N.op y x) ∨ y = z ∨ x = z ∨ y = z := by
    first
    | (have i₁ := eq3226
       have i₂ := eq2376 z
       grind)
    | exact superpose eq2376 eq3226
    | (have j1 := eq2376 z
       grind)
    | (have r₁ := eq3226
       have r₂ := eq2376 (N.op y (N.op z x))
       grind)
    | exact resolve eq3226 eq2376
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq4914 : x ≠ (N.op y x) ∨ y = z ∨ x = z := by grind
  clear eq4913
  have eq5599 : x ≠ x ∨ y = z ∨ x = z ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq4914
       have i₂ := eq2376 y
       grind)
    | exact superpose eq2376 eq4914
    | (have j1 := eq2376 z
       grind)
    | (have r₁ := eq4914
       have r₂ := eq2376 y
       grind)
    | (have r₁ := eq4914
       have r₂ := eq2376 (N.op y x)
       grind)
    | exact resolve eq4914 eq2376
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4914
  have eq5601 : x ≠ x ∨ y = z ∨ x = z ∨ x = y := by grind
  clear eq5599
  have eq5602 : y = z ∨ x = z ∨ x = y := by grind
  clear eq5601
  have eq6520 : y ≠ (N.op y (N.op y y)) ∨ x = (M.op x x) ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq1574
       have i₂ := eq5602
       grind)
    | exact superpose eq5602 eq1574
    | exact resolve eq1574 eq5602
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1574 eq5602
  have eq6523 : y ≠ (N.op y (N.op y y)) ∨ x = (M.op x x) ∨ x = z := by
    first
    | (have r₁ := eq6520
       have r₂ := eq1573
       grind)
    | exact resolve eq6520 eq1573
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq6520
  have eq20582 : y ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq6523
       have i₂ := eq1571
       grind)
    | exact superpose eq1571 eq6523
    | exact resolve eq6523 eq1571
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq6523
  have eq20586 : y ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = z := by grind
  clear eq20582
  have eq20587 : x = (M.op x x) ∨ x = z := by
    first
    | (have r₁ := eq20586
       have r₂ := eq1571
       grind)
    | exact resolve eq20586 eq1571
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1571 eq20586
  have eq20945 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq50 X0 x
       have i₂ := eq20587
       grind)
    | exact superpose eq20587 eq50
    | exact resolve eq50 eq20587
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq50
  have eq20965 : x ≠ x ∨ x = (N.op x x) ∨ x = z := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq20587
       grind)
    | exact superpose eq20587 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq20587
       grind)
    | exact resolve eq7 eq20587
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20587
  have eq20988 : x = (N.op x x) ∨ x = z := by grind
  clear eq20965
  have eq23689 : x ≠ (N.op y (N.op z x)) ∨ x = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq20988
       grind)
    | exact superpose eq20988 eq10
    | exact resolve eq10 eq20988
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq28485 : x ≠ (N.op y x) ∨ x = z ∨ x = z ∨ x = z := by
    first
    | (have i₁ := eq23689
       have i₂ := eq20945 z
       grind)
    | exact superpose eq20945 eq23689
    | (have j1 := eq20945 z
       grind)
    | (have r₁ := eq23689
       have r₂ := eq20945 (N.op y (N.op z x))
       grind)
    | exact resolve eq23689 eq20945
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq23689
  have eq28488 : x ≠ (N.op y x) ∨ x = z := by grind
  clear eq28485
  have eq30830 : x ≠ x ∨ x = z ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq28488
       have i₂ := eq20945 y
       grind)
    | exact superpose eq20945 eq28488
    | (have j1 := eq20945 z
       grind)
    | (have r₁ := eq28488
       have r₂ := eq20945 y
       grind)
    | (have r₁ := eq28488
       have r₂ := eq20945 (N.op y x)
       grind)
    | exact resolve eq28488 eq20945
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20945
  have eq30834 : x ≠ x ∨ x = z ∨ x = y := by grind
  clear eq30830
  have eq30835 : x = z ∨ x = y := by grind
  clear eq30834
  have eq31724 : x ≠ (N.op y (N.op x x)) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq3226
       have i₂ := eq30835
       grind)
    | exact superpose eq30835 eq3226
    | exact resolve eq3226 eq30835
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3226
  have eq31725 : x ≠ (N.op y (N.op x x)) ∨ x = y := by grind
  clear eq31724
  have eq34314 : x ≠ (N.op y x) ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq31725
       have i₂ := eq2378
       grind)
    | exact superpose eq2378 eq31725
    | exact resolve eq31725 eq2378
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2378 eq31725
  have eq34319 : y = z ∨ x = y := by
    first
    | (have j1 := eq2376 y
       grind)
    | (have r₁ := eq34314
       have r₂ := eq2376 y
       grind)
    | (have r₁ := eq34314
       have r₂ := eq2376 (N.op y x)
       grind)
    | exact resolve eq34314 eq2376
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2376 eq34314
  have eq34879 : x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq30835
       have i₂ := eq34319
       grind)
    | exact superpose eq34319 eq30835
    | exact resolve eq30835 eq34319
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30835 eq34319
  have eq34880 : x = y := by grind
  clear eq34879
  have eq36174 : x ≠ x ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1573
       have i₂ := eq34880
       grind)
    | exact superpose eq34880 eq1573
    | (have r₁ := eq1573
       have r₂ := eq34880
       grind)
    | exact resolve eq1573 eq34880
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1573
  have eq36182 : x ≠ (N.op x x) ∨ x = z := by
    first
    | (have i₁ := eq28488
       have i₂ := eq34880
       grind)
    | exact superpose eq34880 eq28488
    | exact resolve eq28488 eq34880
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq28488
  have eq36184 : x = (M.op x x) := by grind
  clear eq36174
  have eq36187 : x = z := by
    first
    | (have r₁ := eq36182
       have r₂ := eq20988
       grind)
    | exact resolve eq36182 eq20988
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20988 eq36182
  have eq38233 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq36184
       grind)
    | exact superpose eq36184 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq36184
       grind)
    | exact resolve eq7 eq36184
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36184
  have eq38262 : x = (N.op x x) := by grind
  clear eq38233
  have eq42370 : x ≠ (N.op y (N.op z x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq38262
       grind)
    | exact superpose eq38262 eq10
    | exact resolve eq10 eq38262
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq42379 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq42370
       have i₂ := eq36187
       grind)
    | exact superpose eq36187 eq42370
    | exact resolve eq42370 eq36187
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36187 eq42370
  have eq42381 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq42379
       have i₂ := eq38262
       grind)
    | exact superpose eq38262 eq42379
    | exact resolve eq42379 eq38262
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq42379
  have eq42383 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq42381
       have i₂ := eq34880
       grind)
    | exact superpose eq34880 eq42381
    | exact resolve eq42381 eq34880
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34880 eq42381
  have eq42385 : False := by grind
  exact eq42385

/-- `Equation4272` from `Equation2402`: `x = (y ◇ (z ◇ (z ◇ y))) ◇ x` ⊢ `x ◇ (x ◇ x) = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation4272_StructuralFromFin_Equation2402 : Law4272.StructuralFromFin Law2402 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation2402 (fun {G} M hM ↦ ?_)
  have hm := (@Law2402.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law4272 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law4272.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X2 (M.op X2 X1))) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x (N.op x x)) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 : G, (M.op (M.op X2 (M.op (M.op X0 (M.op X1 (M.op X1 X0))) X2)) X3) = X3 := by
    intro X0 X1 X2 X3
    first
    | (have i₁ := eq6 X3 X2 (M.op X0 (M.op X1 (M.op X1 X0)))
       have i₂ := eq6 (M.op (M.op X0 (M.op X1 (M.op X1 X0))) X2) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq15 : ∀ X2 X3 : G, (M.op (M.op X2 X2) X3) = X3 := by
    intro X2 X3
    first
    | (have i₁ := eq12 x x X2 X3
       have i₂ := eq6 X2 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq20 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq15 X1 X0
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op x x) (M.op x x)
       have r₂ := eq15 x (M.op x x)
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq49 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 x (M.op x x)
       have i₂ := eq15 x x
       grind)
    | exact superpose eq15 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 x (M.op x x)
       have r₂ := eq15 x x
       grind)
    | exact resolve eq8 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq50 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq49 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq49
  have eq56 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq20 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20
  have eq57 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq59 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq57
  have eq127 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq59
       have i₂ := eq50 y x
       grind)
    | exact superpose eq50 eq59
    | (have j1 := eq50 x x
       grind)
    | exact resolve eq59 eq50
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq129 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq50 y x
       grind)
    | (have r₁ := eq127
       have r₂ := eq50 x x
       grind)
    | (have r₁ := eq127
       have r₂ := eq50 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq127 eq50
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq127
  have eq130 : (N.op x y) ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq59
       have i₂ := eq129
       grind)
    | exact superpose eq129 eq59
    | exact resolve eq59 eq129
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq59
  have eq135 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq50 X0 x
       have i₂ := eq129
       grind)
    | exact superpose eq129 eq50
    | (have j0 := eq50 x x
       grind)
    | exact resolve eq50 eq129
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq136 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq56 x
       have i₂ := eq129
       grind)
    | exact superpose eq129 eq56
    | exact resolve eq56 eq129
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq140 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq129
  have eq141 : (N.op x y) ≠ (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq130
       have r₂ := eq140
       grind)
    | exact resolve eq130 eq140
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq130
  have eq371 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq141
       have i₂ := eq136
       grind)
    | exact superpose eq136 eq141
    | exact resolve eq141 eq136
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq136 eq141
  have eq374 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq371
  have eq557 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq374
       have i₂ := eq135 x
       grind)
    | exact superpose eq135 eq374
    | (have j1 := eq135 x
       grind)
    | (have r₁ := eq374
       have r₂ := eq135 x
       grind)
    | (have r₁ := eq374
       have r₂ := eq135 (N.op x y)
       grind)
    | exact resolve eq374 eq135
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq135 eq374
  have eq558 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq557
  have eq559 : x = (M.op x x) ∨ x = y := by grind
  clear eq558
  have eq562 : x = (M.op x x) := by
    first
    | (have r₁ := eq559
       have r₂ := eq140
       grind)
    | exact resolve eq559 eq140
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq140 eq559
  have eq574 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq50 X0 x
       have i₂ := eq562
       grind)
    | exact superpose eq562 eq50
    | exact resolve eq50 eq562
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq50
  have eq575 : x = (N.op x x) := by
    first
    | (have i₁ := eq56 x
       have i₂ := eq562
       grind)
    | exact superpose eq562 eq56
    | exact resolve eq56 eq562
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq56 eq562
  have eq713 : (N.op x x) ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq575
       grind)
    | exact superpose eq575 eq10
    | exact resolve eq10 eq575
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq718 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq713
       have i₂ := eq575
       grind)
    | exact superpose eq575 eq713
    | exact resolve eq713 eq575
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq575 eq713
  have eq1495 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq718
       have i₂ := eq574 y
       grind)
    | exact superpose eq574 eq718
    | (have j1 := eq574 y
       grind)
    | (have r₁ := eq718
       have r₂ := eq574 y
       grind)
    | (have r₁ := eq718
       have r₂ := eq574 (N.op y x)
       grind)
    | exact resolve eq718 eq574
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq574 eq718
  have eq1496 : x = y := by grind
  clear eq1495
  have eq1568 : (N.op x (N.op x x)) ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1496
       grind)
    | exact superpose eq1496 eq10
    | exact resolve eq10 eq1496
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1496
  have eq1583 : False := by grind
  exact eq1583
