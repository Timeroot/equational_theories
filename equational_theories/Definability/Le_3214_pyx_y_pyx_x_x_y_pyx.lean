import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation3214` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3214` is `x = (((y ◇ z) ◇ z) ◇ y) ◇ x`, and the companion is
`x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`.  Its automorphism half is `GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3214`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches none of these on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation307` from `Equation3214`: `x = (((y ◇ z) ◇ z) ◇ y) ◇ x` ⊢ `x ◇ x = x ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation307_StructuralFromFin_Equation3214 : Law307.StructuralFromFin Law3214 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3214 (fun {G} M hM ↦ ?_)
  have hm := (@Law3214.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law307 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law307.models_iff G N]
  intro x
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X2) X2) X1) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X0
       have i₂ := eq6 (M.op X0 X0) X0 X0
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq21 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq13 X0 X1
       grind)
    | exact superpose eq13 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq13 X0 X1
       grind)
    | exact resolve eq8 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq24 : ∀ X0 X1 : G, (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have j0 := eq21 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq26 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq28 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq26
  have eq29 : x = (M.op x x) := by
    first
    | (have j1 := eq24 x x
       grind)
    | (have r₁ := eq28
       have r₂ := eq24 (N.op x (M.op x x)) x
       grind)
    | (have r₁ := eq28
       have r₂ := eq24 x x
       grind)
    | exact resolve eq28 eq24
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq24 eq28
  have eq32 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq29
       grind)
    | exact superpose eq29 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq29
       grind)
    | exact resolve eq7 eq29
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29
  have eq33 : x = (N.op x x) := by grind
  clear eq32
  have eq90 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq33
       grind)
    | exact superpose eq33 eq10
    | exact resolve eq10 eq33
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq94 : False := by grind
  exact eq94

/-- `Equation312` from `Equation3214`: `x = (((y ◇ z) ◇ z) ◇ y) ◇ x` ⊢ `x ◇ x = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation312_StructuralFromFin_Equation3214 : Law312.StructuralFromFin Law3214 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3214 (fun {G} M hM ↦ ?_)
  have hm := (@Law3214.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law312 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law312.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X2) X2) X1) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X0
       have i₂ := eq6 (M.op X0 X0) X0 X0
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq21 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq13 X0 X1
       grind)
    | exact superpose eq13 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq13 X0 X1
       grind)
    | exact resolve eq8 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq23 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq13 X1 X0
       grind)
    | exact superpose eq13 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq13 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq24 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq21 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq26 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq28 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq26
  have eq55 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq23 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq23
  have eq99 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq28
       have i₂ := eq24 y x
       grind)
    | exact superpose eq24 eq28
    | (have j1 := eq24 x x
       grind)
    | (have r₁ := eq28
       have r₂ := eq24 y x
       grind)
    | (have r₁ := eq28
       have r₂ := eq24 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq28 eq24
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq100 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq99
  have eq125 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq28
       have i₂ := eq100
       grind)
    | exact superpose eq100 eq28
    | exact resolve eq28 eq100
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq28
  have eq127 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq55 x
       have i₂ := eq100
       grind)
    | exact superpose eq100 eq55
    | exact resolve eq55 eq100
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq55
  have eq137 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq100
  have eq138 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq125
       have r₂ := eq127
       grind)
    | exact resolve eq125 eq127
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq125 eq127
  have eq139 : x = (M.op x x) := by
    first
    | (have r₁ := eq138
       have r₂ := eq137
       grind)
    | exact resolve eq138 eq137
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq137 eq138
  have eq156 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq24 X0 x
       have i₂ := eq139
       grind)
    | exact superpose eq139 eq24
    | exact resolve eq24 eq139
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq24
  have eq161 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq139
       grind)
    | exact superpose eq139 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq139
       grind)
    | exact resolve eq7 eq139
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq139
  have eq167 : x = (N.op x x) := by grind
  clear eq161
  have eq245 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq167
       grind)
    | exact superpose eq167 eq10
    | exact resolve eq10 eq167
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq273 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq245
       have i₂ := eq156 y
       grind)
    | exact superpose eq156 eq245
    | (have j1 := eq156 y
       grind)
    | (have r₁ := eq245
       have r₂ := eq156 y
       grind)
    | (have r₁ := eq245
       have r₂ := eq156 (N.op y x)
       grind)
    | exact resolve eq245 eq156
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq156
  have eq274 : x = y := by grind
  clear eq273
  have eq287 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq245
       have i₂ := eq274
       grind)
    | exact superpose eq274 eq245
    | exact resolve eq245 eq274
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq245 eq274
  have eq289 : False := by grind
  exact eq289

/-- `Equation3258` from `Equation3214`: `x = (((y ◇ z) ◇ z) ◇ y) ◇ x` ⊢ `x ◇ x = x ◇ (y ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3258_StructuralFromFin_Equation3214 : Law3258.StructuralFromFin Law3214 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3214 (fun {G} M hM ↦ ?_)
  have hm := (@Law3214.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3258 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3258.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X2) X2) X1) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op y (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X0
       have i₂ := eq6 (M.op X0 X0) X0 X0
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq20 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq13 X1 X0
       grind)
    | exact superpose eq13 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq13 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq43 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq13 X0 X1
       grind)
    | exact superpose eq13 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq13 X0 X1
       grind)
    | exact resolve eq8 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq46 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq43 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq43
  have eq51 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq20 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20
  have eq52 : (M.op x x) ≠ (N.op x (N.op y (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq55 : (M.op x x) ≠ (N.op x (N.op y (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq52
  have eq70 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq55
       have i₂ := eq46 y x
       grind)
    | exact superpose eq46 eq55
    | (have j1 := eq46 x x
       grind)
    | (have r₁ := eq55
       have r₂ := eq46 (N.op x (N.op y (M.op x x))) x
       grind)
    | exact resolve eq55 eq46
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq73 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq46 y x
       grind)
    | (have r₁ := eq70
       have r₂ := eq46 x x
       grind)
    | (have r₁ := eq70
       have r₂ := eq46 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq70 eq46
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq70
  have eq88 : y ≠ (N.op x (N.op y y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq55
       have i₂ := eq73
       grind)
    | exact superpose eq73 eq55
    | exact resolve eq55 eq73
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq55
  have eq89 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq46 X0 x
       have i₂ := eq73
       grind)
    | exact superpose eq73 eq46
    | (have j0 := eq46 x x
       grind)
    | exact resolve eq46 eq73
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq90 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq51 x
       have i₂ := eq73
       grind)
    | exact superpose eq73 eq51
    | exact resolve eq51 eq73
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq98 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq73
  have eq99 : y ≠ (N.op x (N.op y y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq88
       have r₂ := eq98
       grind)
    | exact resolve eq88 eq98
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq88
  have eq170 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq99
       have i₂ := eq90
       grind)
    | exact superpose eq90 eq99
    | exact resolve eq99 eq90
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq90 eq99
  have eq174 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq170
  have eq209 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq174
       have i₂ := eq89 x
       grind)
    | exact superpose eq89 eq174
    | (have j1 := eq89 x
       grind)
    | (have r₁ := eq174
       have r₂ := eq89 x
       grind)
    | (have r₁ := eq174
       have r₂ := eq89 (N.op x y)
       grind)
    | exact resolve eq174 eq89
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq89 eq174
  have eq210 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq209
  have eq211 : x = (M.op x x) ∨ x = y := by grind
  clear eq210
  have eq214 : x = (M.op x x) := by
    first
    | (have r₁ := eq211
       have r₂ := eq98
       grind)
    | exact resolve eq211 eq98
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq98 eq211
  have eq218 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq46 X0 x
       have i₂ := eq214
       grind)
    | exact superpose eq214 eq46
    | exact resolve eq46 eq214
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq46
  have eq223 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq214
       grind)
    | exact superpose eq214 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq214
       grind)
    | exact resolve eq7 eq214
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq214
  have eq229 : x = (N.op x x) := by grind
  clear eq223
  have eq306 : x ≠ (N.op x (N.op y x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq229
       grind)
    | exact superpose eq229 eq10
    | exact resolve eq10 eq229
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq344 : x ≠ (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq306
       have i₂ := eq218 y
       grind)
    | exact superpose eq218 eq306
    | (have j1 := eq218 y
       grind)
    | (have r₁ := eq306
       have r₂ := eq218 (N.op x (N.op y x))
       grind)
    | exact resolve eq306 eq218
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq218
  have eq347 : x = y := by
    first
    | (have r₁ := eq344
       have r₂ := eq229
       grind)
    | exact resolve eq344 eq229
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq344
  have eq369 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq306
       have i₂ := eq347
       grind)
    | exact superpose eq347 eq306
    | exact resolve eq306 eq347
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq306 eq347
  have eq371 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq369
       have i₂ := eq229
       grind)
    | exact superpose eq229 eq369
    | exact resolve eq369 eq229
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq369
  have eq376 : False := by grind
  exact eq376

/-- `Equation3268` from `Equation3214`: `x = (((y ◇ z) ◇ z) ◇ y) ◇ x` ⊢ `x ◇ x = y ◇ (x ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3268_StructuralFromFin_Equation3214 : Law3268.StructuralFromFin Law3214 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3214 (fun {G} M hM ↦ ?_)
  have hm := (@Law3214.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3268 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3268.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X2) X2) X1) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X0
       have i₂ := eq6 (M.op X0 X0) X0 X0
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq20 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq13 X1 X0
       grind)
    | exact superpose eq13 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq13 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq43 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq13 X0 X1
       grind)
    | exact superpose eq13 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq13 X0 X1
       grind)
    | exact resolve eq8 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq46 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq43 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq43
  have eq51 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq20 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20
  have eq52 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq55 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq52
  have eq70 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq55
       have i₂ := eq46 x x
       grind)
    | exact superpose eq46 eq55
    | (have j1 := eq46 x x
       grind)
    | (have r₁ := eq55
       have r₂ := eq46 (N.op y (N.op x (M.op x x))) x
       grind)
    | exact resolve eq55 eq46
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq55
  have eq71 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq70
  have eq88 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq71
       have i₂ := eq46 y x
       grind)
    | exact superpose eq46 eq71
    | (have j1 := eq46 x x
       grind)
    | (have r₁ := eq71
       have r₂ := eq46 y x
       grind)
    | (have r₁ := eq71
       have r₂ := eq46 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq71 eq46
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq90 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq88
  have eq108 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq71
       have i₂ := eq90
       grind)
    | exact superpose eq90 eq71
    | exact resolve eq71 eq90
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq71
  have eq111 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq51 x
       have i₂ := eq90
       grind)
    | exact superpose eq90 eq51
    | exact resolve eq51 eq90
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq120 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq90
  have eq122 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq108
       have r₂ := eq111
       grind)
    | exact resolve eq108 eq111
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq108 eq111
  have eq123 : x = (M.op x x) := by
    first
    | (have r₁ := eq122
       have r₂ := eq120
       grind)
    | exact resolve eq122 eq120
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq120 eq122
  have eq128 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq46 X0 x
       have i₂ := eq123
       grind)
    | exact superpose eq123 eq46
    | exact resolve eq46 eq123
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq46
  have eq136 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq123
       grind)
    | exact superpose eq123 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq123
       grind)
    | exact resolve eq7 eq123
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq123
  have eq137 : x = (N.op x x) := by grind
  clear eq136
  have eq204 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq137
       grind)
    | exact superpose eq137 eq10
    | exact resolve eq10 eq137
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq209 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq204
       have i₂ := eq137
       grind)
    | exact superpose eq137 eq204
    | exact resolve eq204 eq137
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq204
  have eq261 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq209
       have i₂ := eq128 y
       grind)
    | exact superpose eq128 eq209
    | (have j1 := eq128 y
       grind)
    | (have r₁ := eq209
       have r₂ := eq128 y
       grind)
    | (have r₁ := eq209
       have r₂ := eq128 (N.op y x)
       grind)
    | exact resolve eq209 eq128
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq128
  have eq262 : x = y := by grind
  clear eq261
  have eq292 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq209
       have i₂ := eq262
       grind)
    | exact superpose eq262 eq209
    | exact resolve eq209 eq262
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq209 eq262
  have eq294 : False := by grind
  exact eq294

/-- `Equation3288` from `Equation3214`: `x = (((y ◇ z) ◇ z) ◇ y) ◇ x` ⊢ `x ◇ x = y ◇ (z ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3288_StructuralFromFin_Equation3214 : Law3288.StructuralFromFin Law3214 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3214 (fun {G} M hM ↦ ?_)
  have hm := (@Law3214.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3288 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3288.models_iff G N]
  intro x y z
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X2) X2) X1) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op z (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X0
       have i₂ := eq6 (M.op X0 X0) X0 X0
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq20 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq13 X1 X0
       grind)
    | exact superpose eq13 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq13 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq43 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq13 X0 X1
       grind)
    | exact superpose eq13 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq13 X0 X1
       grind)
    | exact resolve eq8 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq46 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq43 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq43
  have eq51 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq20 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20
  have eq52 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq55 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq52
  have eq70 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ z = (M.op x x) := by
    first
    | (have i₁ := eq55
       have i₂ := eq46 z x
       grind)
    | exact superpose eq46 eq55
    | (have j1 := eq46 x x
       grind)
    | (have r₁ := eq55
       have r₂ := eq46 (N.op y (N.op z (M.op x x))) x
       grind)
    | exact resolve eq55 eq46
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq87 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ z = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq70
       have i₂ := eq46 y x
       grind)
    | exact superpose eq46 eq70
    | (have j1 := eq46 x x
       grind)
    | (have r₁ := eq70
       have r₂ := eq46 y x
       grind)
    | (have r₁ := eq70
       have r₂ := eq46 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq70 eq46
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq89 : z = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq87
  have eq107 : z ≠ (N.op y (N.op z z)) ∨ x = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq55
       have i₂ := eq89
       grind)
    | exact superpose eq89 eq55
    | exact resolve eq55 eq89
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq108 : ∀ X0 : G, z = (N.op X0 z) ∨ z = X0 ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq46 X0 x
       have i₂ := eq89
       grind)
    | exact superpose eq89 eq46
    | (have j0 := eq46 x x
       grind)
    | exact resolve eq46 eq89
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq109 : z = (N.op z z) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq51 x
       have i₂ := eq89
       grind)
    | exact superpose eq89 eq51
    | exact resolve eq51 eq89
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq118 : x ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  have eq119 : y ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq89
  have eq120 : z ≠ (N.op y (N.op z z)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have r₁ := eq107
       have r₂ := eq118
       grind)
    | exact resolve eq107 eq118
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq107 eq118
  have eq205 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq120
       have i₂ := eq109
       grind)
    | exact superpose eq109 eq120
    | exact resolve eq120 eq109
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq109 eq120
  have eq209 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq205
  have eq232 : z ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq209
       have i₂ := eq108 y
       grind)
    | exact superpose eq108 eq209
    | (have j1 := eq108 y
       grind)
    | (have r₁ := eq209
       have r₂ := eq108 y
       grind)
    | (have r₁ := eq209
       have r₂ := eq108 (N.op y z)
       grind)
    | exact resolve eq209 eq108
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq108 eq209
  have eq233 : z ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq232
  have eq234 : x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq233
  have eq237 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq234
       have r₂ := eq119
       grind)
    | exact resolve eq234 eq119
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq119 eq234
  have eq262 : y ≠ (N.op y y) ∨ x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq70
       have i₂ := eq237
       grind)
    | exact superpose eq237 eq70
    | exact resolve eq70 eq237
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq70
  have eq263 : y ≠ (N.op y (N.op z y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq55
       have i₂ := eq237
       grind)
    | exact superpose eq237 eq55
    | exact resolve eq55 eq237
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq55
  have eq265 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq51 x
       have i₂ := eq237
       grind)
    | exact superpose eq237 eq51
    | exact resolve eq51 eq237
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq278 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq237
  have eq279 : y ≠ (N.op y (N.op z y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq263
       have r₂ := eq278
       grind)
    | exact resolve eq263 eq278
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq263
  have eq280 : x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq262
       have r₂ := eq265
       grind)
    | exact resolve eq262 eq265
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq262
  have eq281 : x = (M.op x x) ∨ y = z := by
    first
    | (have r₁ := eq280
       have r₂ := eq278
       grind)
    | exact resolve eq280 eq278
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq280
  have eq314 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ y = z := by
    intro X0
    first
    | (have i₁ := eq46 X0 x
       have i₂ := eq281
       grind)
    | exact superpose eq281 eq46
    | exact resolve eq46 eq281
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq319 : x ≠ x ∨ x = (N.op x x) ∨ y = z := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq281
       grind)
    | exact superpose eq281 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq281
       grind)
    | exact resolve eq7 eq281
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq281
  have eq327 : x = (N.op x x) ∨ y = z := by grind
  clear eq319
  have eq402 : x ≠ (N.op y (N.op z x)) ∨ y = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq327
       grind)
    | exact superpose eq327 eq10
    | exact resolve eq10 eq327
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq462 : x ≠ (N.op y x) ∨ y = z ∨ x = z ∨ y = z := by
    first
    | (have i₁ := eq402
       have i₂ := eq314 z
       grind)
    | exact superpose eq314 eq402
    | (have j1 := eq314 z
       grind)
    | (have r₁ := eq402
       have r₂ := eq314 (N.op y (N.op z x))
       grind)
    | exact resolve eq402 eq314
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq463 : x ≠ (N.op y x) ∨ y = z ∨ x = z := by grind
  clear eq462
  have eq514 : x ≠ x ∨ y = z ∨ x = z ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq463
       have i₂ := eq314 y
       grind)
    | exact superpose eq314 eq463
    | (have j1 := eq314 z
       grind)
    | (have r₁ := eq463
       have r₂ := eq314 y
       grind)
    | (have r₁ := eq463
       have r₂ := eq314 (N.op y x)
       grind)
    | exact resolve eq463 eq314
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq463
  have eq516 : x ≠ x ∨ y = z ∨ x = z ∨ x = y := by grind
  clear eq514
  have eq517 : y = z ∨ x = z ∨ x = y := by grind
  clear eq516
  have eq605 : y ≠ (N.op y (N.op y y)) ∨ x = (M.op x x) ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq279
       have i₂ := eq517
       grind)
    | exact superpose eq517 eq279
    | exact resolve eq279 eq517
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq279 eq517
  have eq608 : y ≠ (N.op y (N.op y y)) ∨ x = (M.op x x) ∨ x = z := by
    first
    | (have r₁ := eq605
       have r₂ := eq278
       grind)
    | exact resolve eq605 eq278
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq605
  have eq1672 : y ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq608
       have i₂ := eq265
       grind)
    | exact superpose eq265 eq608
    | exact resolve eq608 eq265
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq608
  have eq1676 : y ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = z := by grind
  clear eq1672
  have eq1677 : x = (M.op x x) ∨ x = z := by
    first
    | (have r₁ := eq1676
       have r₂ := eq265
       grind)
    | exact resolve eq1676 eq265
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq265 eq1676
  have eq1757 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq46 X0 x
       have i₂ := eq1677
       grind)
    | exact superpose eq1677 eq46
    | exact resolve eq46 eq1677
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq46
  have eq1763 : x ≠ x ∨ x = (N.op x x) ∨ x = z := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq1677
       grind)
    | exact superpose eq1677 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq1677
       grind)
    | exact resolve eq7 eq1677
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1677
  have eq1771 : x = (N.op x x) ∨ x = z := by grind
  clear eq1763
  have eq2019 : x ≠ (N.op y (N.op z x)) ∨ x = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq1771
       grind)
    | exact superpose eq1771 eq10
    | exact resolve eq10 eq1771
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2107 : x ≠ (N.op y x) ∨ x = z ∨ x = z ∨ x = z := by
    first
    | (have i₁ := eq2019
       have i₂ := eq1757 z
       grind)
    | exact superpose eq1757 eq2019
    | (have j1 := eq1757 z
       grind)
    | (have r₁ := eq2019
       have r₂ := eq1757 (N.op y (N.op z x))
       grind)
    | exact resolve eq2019 eq1757
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2019
  have eq2110 : x ≠ (N.op y x) ∨ x = z := by grind
  clear eq2107
  have eq2125 : x ≠ x ∨ x = z ∨ x = y ∨ x = z := by
    first
    | (have i₁ := eq2110
       have i₂ := eq1757 y
       grind)
    | exact superpose eq1757 eq2110
    | (have j1 := eq1757 z
       grind)
    | (have r₁ := eq2110
       have r₂ := eq1757 y
       grind)
    | (have r₁ := eq2110
       have r₂ := eq1757 (N.op y x)
       grind)
    | exact resolve eq2110 eq1757
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1757
  have eq2129 : x ≠ x ∨ x = z ∨ x = y := by grind
  clear eq2125
  have eq2130 : x = z ∨ x = y := by grind
  clear eq2129
  have eq2210 : x ≠ (N.op y (N.op x x)) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq402
       have i₂ := eq2130
       grind)
    | exact superpose eq2130 eq402
    | exact resolve eq402 eq2130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq402
  have eq2211 : x ≠ (N.op y (N.op x x)) ∨ x = y := by grind
  clear eq2210
  have eq2231 : x ≠ (N.op y x) ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq2211
       have i₂ := eq327
       grind)
    | exact superpose eq327 eq2211
    | exact resolve eq2211 eq327
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq327 eq2211
  have eq2236 : y = z ∨ x = y := by
    first
    | (have j1 := eq314 y
       grind)
    | (have r₁ := eq2231
       have r₂ := eq314 y
       grind)
    | (have r₁ := eq2231
       have r₂ := eq314 (N.op y x)
       grind)
    | exact resolve eq2231 eq314
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq314 eq2231
  have eq2283 : x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq2130
       have i₂ := eq2236
       grind)
    | exact superpose eq2236 eq2130
    | exact resolve eq2130 eq2236
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2130 eq2236
  have eq2284 : x = y := by grind
  clear eq2283
  have eq2389 : x ≠ x ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq278
       have i₂ := eq2284
       grind)
    | exact superpose eq2284 eq278
    | (have r₁ := eq278
       have r₂ := eq2284
       grind)
    | exact resolve eq278 eq2284
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq278
  have eq2407 : x ≠ (N.op x x) ∨ x = z := by
    first
    | (have i₁ := eq2110
       have i₂ := eq2284
       grind)
    | exact superpose eq2284 eq2110
    | exact resolve eq2110 eq2284
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2110
  have eq2409 : x = (M.op x x) := by grind
  clear eq2389
  have eq2413 : x = z := by
    first
    | (have r₁ := eq2407
       have r₂ := eq1771
       grind)
    | exact resolve eq2407 eq1771
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1771 eq2407
  have eq2545 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq2409
       grind)
    | exact superpose eq2409 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq2409
       grind)
    | exact resolve eq7 eq2409
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2409
  have eq2553 : x = (N.op x x) := by grind
  clear eq2545
  have eq2715 : x ≠ (N.op y (N.op z x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq2553
       grind)
    | exact superpose eq2553 eq10
    | exact resolve eq10 eq2553
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2724 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq2715
       have i₂ := eq2413
       grind)
    | exact superpose eq2413 eq2715
    | exact resolve eq2715 eq2413
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2413 eq2715
  have eq2726 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq2724
       have i₂ := eq2553
       grind)
    | exact superpose eq2553 eq2724
    | exact resolve eq2724 eq2553
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2724
  have eq2728 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq2726
       have i₂ := eq2284
       grind)
    | exact superpose eq2284 eq2726
    | exact resolve eq2726 eq2284
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2284 eq2726
  have eq2730 : False := by grind
  exact eq2730

/-- `Equation4272` from `Equation3214`: `x = (((y ◇ z) ◇ z) ◇ y) ◇ x` ⊢ `x ◇ (x ◇ x) = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation4272_StructuralFromFin_Equation3214 : Law4272.StructuralFromFin Law3214 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3214 (fun {G} M hM ↦ ?_)
  have hm := (@Law3214.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law4272 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law4272.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X2) X2) X1) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x (N.op x x)) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq13 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X0
       have i₂ := eq6 (M.op X0 X0) X0 X0
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq20 : ∀ X0 X1 : G, (M.op X1 X1) ≠ X0 ∨ (N.op X0 (M.op X1 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X1 X1)
       have i₂ := eq13 X1 X0
       grind)
    | exact superpose eq13 eq7
    | (have j0 := eq7 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq13 X0 (M.op X0 X0)
       grind)
    | exact resolve eq7 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq43 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq13 X0 X1
       grind)
    | exact superpose eq13 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq13 X0 X1
       grind)
    | exact resolve eq8 eq13
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq46 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq43 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq43
  have eq51 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq20 (M.op X0 X0) X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20
  have eq52 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq54 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq52
  have eq68 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq54
       have i₂ := eq46 y x
       grind)
    | exact superpose eq46 eq54
    | (have j1 := eq46 x x
       grind)
    | exact resolve eq54 eq46
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq71 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq46 y x
       grind)
    | (have r₁ := eq68
       have r₂ := eq46 x x
       grind)
    | (have r₁ := eq68
       have r₂ := eq46 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq68 eq46
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq68
  have eq86 : (N.op x y) ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq54
       have i₂ := eq71
       grind)
    | exact superpose eq71 eq54
    | exact resolve eq54 eq71
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq54
  have eq87 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq46 X0 x
       have i₂ := eq71
       grind)
    | exact superpose eq71 eq46
    | (have j0 := eq46 x x
       grind)
    | exact resolve eq46 eq71
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq88 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq51 x
       have i₂ := eq71
       grind)
    | exact superpose eq71 eq51
    | exact resolve eq51 eq71
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq96 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq71
  have eq97 : (N.op x y) ≠ (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq86
       have r₂ := eq96
       grind)
    | exact resolve eq86 eq96
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq86
  have eq193 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq97
       have i₂ := eq88
       grind)
    | exact superpose eq88 eq97
    | exact resolve eq97 eq88
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq88 eq97
  have eq196 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq193
  have eq202 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq196
       have i₂ := eq87 x
       grind)
    | exact superpose eq87 eq196
    | (have j1 := eq87 x
       grind)
    | (have r₁ := eq196
       have r₂ := eq87 x
       grind)
    | (have r₁ := eq196
       have r₂ := eq87 (N.op x y)
       grind)
    | exact resolve eq196 eq87
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq87 eq196
  have eq203 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq202
  have eq204 : x = (M.op x x) ∨ x = y := by grind
  clear eq203
  have eq207 : x = (M.op x x) := by
    first
    | (have r₁ := eq204
       have r₂ := eq96
       grind)
    | exact resolve eq204 eq96
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq96 eq204
  have eq209 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq46 X0 x
       have i₂ := eq207
       grind)
    | exact superpose eq207 eq46
    | exact resolve eq46 eq207
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq46
  have eq214 : x ≠ x ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq7 x x
       have i₂ := eq207
       grind)
    | exact superpose eq207 eq7
    | (have j0 := eq7 x x
       grind)
    | (have r₁ := eq7 x x
       have r₂ := eq207
       grind)
    | exact resolve eq7 eq207
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq207
  have eq220 : x = (N.op x x) := by grind
  clear eq214
  have eq296 : (N.op x x) ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq220
       grind)
    | exact superpose eq220 eq10
    | exact resolve eq10 eq220
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq301 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq296
       have i₂ := eq220
       grind)
    | exact superpose eq220 eq296
    | exact resolve eq296 eq220
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq220 eq296
  have eq335 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq301
       have i₂ := eq209 y
       grind)
    | exact superpose eq209 eq301
    | (have j1 := eq209 y
       grind)
    | (have r₁ := eq301
       have r₂ := eq209 y
       grind)
    | (have r₁ := eq301
       have r₂ := eq209 (N.op y x)
       grind)
    | exact resolve eq301 eq209
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq209 eq301
  have eq336 : x = y := by grind
  clear eq335
  have eq343 : (N.op x (N.op x x)) ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq336
       grind)
    | exact superpose eq336 eq10
    | exact resolve eq10 eq336
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq336
  have eq363 : False := by grind
  exact eq363
