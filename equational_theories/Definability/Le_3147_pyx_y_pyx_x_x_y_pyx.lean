import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation3147` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3147` is `x = (((y ◇ y) ◇ x) ◇ z) ◇ z`, and the companion is
`x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`.  Its automorphism half is `GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches none of these on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation310` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ x = x ◇ (y ◇ y)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation310_StructuralFromFin_Equation3147 : Law310.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law310 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law310.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op y y)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq21 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq23 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq26 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq23 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq23
  have eq27 : (N.op x x) ≠ (N.op x (M.op y y)) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq29 : (N.op x x) ≠ (N.op x (M.op y y)) ∨ y = (M.op y y) := by grind
  clear eq27
  have eq56 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq21 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq21 X1 X0
       grind)
    | exact superpose eq21 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq21 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq21 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq21
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq62 : ∀ X0 : G, (N.op x x) ≠ (N.op x (M.op X0 X0)) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq29
       have i₂ := eq21 X0 y
       grind)
    | (have i₁ := eq29
       have i₂ := eq21 y x
       grind)
    | exact superpose eq21 eq29
    | exact resolve eq29 eq21
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq71 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq56 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq56
  have eq116 : (N.op x x) ≠ (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq29
       have i₂ := eq26 x y
       grind)
    | exact superpose eq26 eq29
    | (have j1 := eq26 y y
       grind)
    | exact resolve eq29 eq26
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29
  have eq124 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq116
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq116
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq116 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq116
  have eq125 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq124
  have eq126 : y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq125
       have r₂ := eq21 x y
       grind)
    | (have r₁ := eq125
       have r₂ := eq21 y x
       grind)
    | exact resolve eq125 eq21
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq125
  have eq128 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq126
       have i₂ := eq21 X0 y
       grind)
    | (have i₁ := eq126
       have i₂ := eq21 y x
       grind)
    | exact superpose eq21 eq126
    | exact resolve eq126 eq21
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq136 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq21 y X0
       have i₂ := eq126
       grind)
    | exact superpose eq126 eq21
    | exact resolve eq21 eq126
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq126
  have eq187 : x ≠ y ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j0 := eq128 x
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq128
  have eq189 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq187
  have eq293 : ∀ X0 : G, (N.op x x) ≠ (N.op x x) ∨ x = y ∨ y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq62 y
       have i₂ := eq136 X0
       grind)
    | exact superpose eq136 eq62
    | (have j0 := eq62 X0
       have j1 := eq136 X0
       grind)
    | exact resolve eq62 eq136
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq136
  have eq296 : ∀ X0 : G, x = y ∨ y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq293 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq293
  have eq303 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq296 X0
       grind)
    | (have r₁ := eq296 X0
       have r₂ := eq189
       grind)
    | exact resolve eq296 eq189
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq189 eq296
  have eq431 : ∀ X0 X1 : G, x = (M.op X0 X0) ∨ (M.op X1 X1) = y := by
    intro X0 X1
    first
    | (have i₁ := eq21 x X0
       have i₂ := eq303 X1
       grind)
    | exact superpose eq303 eq21
    | (have j1 := eq303 X1
       grind)
    | exact resolve eq21 eq303
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq435 : ∀ X0 : G, (N.op x x) ≠ (N.op x x) ∨ x = y ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq62 x
       have i₂ := eq303 X0
       grind)
    | exact superpose eq303 eq62
    | (have j0 := eq62 X0
       have j1 := eq303 X0
       grind)
    | exact resolve eq62 eq303
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq62
  have eq438 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = y := by
    intro X0
    first
    | (have j0 := eq435 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq435
  have eq474 : ∀ X1 : G, y = (N.op X1 y) ∨ y = X1 ∨ x = y := by
    intro X1
    first
    | (have i₁ := eq26 x x
       have i₂ := eq438 x
       grind)
    | exact superpose eq438 eq26
    | exact resolve eq26 eq438
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26
  have eq476 : y = (N.op y y) ∨ x = y := by
    first
    | (have i₁ := eq71 x
       have i₂ := eq438 x
       grind)
    | exact superpose eq438 eq71
    | exact resolve eq71 eq438
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq571 : (N.op x x) ≠ (N.op x y) ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq476
       grind)
    | exact superpose eq476 eq10
    | exact resolve eq10 eq476
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq476
  have eq1119 : ∀ X0 : G, x ≠ y ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq431 X0 x
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq431
  have eq1882 : (N.op x x) ≠ y ∨ x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq571
       have i₂ := eq474 x
       grind)
    | exact superpose eq474 eq571
    | (have j1 := eq474 x
       grind)
    | exact resolve eq571 eq474
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq474 eq571
  have eq1885 : (N.op x x) ≠ y ∨ x = y := by grind
  clear eq1882
  have eq1891 : y ≠ (M.op x x) ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1885
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq1885
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq1885 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1885
  have eq1892 : y ≠ (M.op x x) ∨ x = y ∨ x = (M.op x x) := by grind
  clear eq1891
  have eq1893 : y ≠ (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq1119 x
       grind)
    | (have r₁ := eq1892
       have r₂ := eq1119 x
       grind)
    | exact resolve eq1892 eq1119
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1119 eq1892
  have eq1894 : x = (M.op x x) := by
    first
    | (have r₁ := eq1893
       have r₂ := eq303 x
       grind)
    | exact resolve eq1893 eq303
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq303 eq1893
  have eq1959 : x = (N.op x x) := by
    first
    | (have i₁ := eq71 x
       have i₂ := eq1894
       grind)
    | exact superpose eq1894 eq71
    | exact resolve eq71 eq1894
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq71
  have eq1962 : x = y ∨ x = y := by
    first
    | (have i₁ := eq438 x
       have i₂ := eq1894
       grind)
    | exact superpose eq1894 eq438
    | exact resolve eq438 eq1894
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq438 eq1894
  have eq1963 : x = y := by grind
  clear eq1962
  have eq1972 : (N.op x x) ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1963
       grind)
    | exact superpose eq1963 eq10
    | exact resolve eq10 eq1963
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1963
  have eq2020 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq1972
       have i₂ := eq1959
       grind)
    | exact superpose eq1959 eq1972
    | exact resolve eq1972 eq1959
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1972
  have eq2021 : False := by grind
  exact eq2021

/-- `Equation312` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ x = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation312_StructuralFromFin_Equation3147 : Law312.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law312 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law312.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq21 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq23 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq26 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq23 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq23
  have eq27 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq29 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq27
  have eq56 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq21 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq21 X1 X0
       grind)
    | exact superpose eq21 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq21 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq21 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq21
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq71 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq56 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq56
  have eq114 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq29
       have i₂ := eq26 y x
       grind)
    | exact superpose eq26 eq29
    | (have j1 := eq26 x x
       grind)
    | (have r₁ := eq29
       have r₂ := eq26 y x
       grind)
    | (have r₁ := eq29
       have r₂ := eq26 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq29 eq26
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq115 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq114
  have eq120 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq29
       have i₂ := eq115
       grind)
    | exact superpose eq115 eq29
    | exact resolve eq29 eq115
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29
  have eq133 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq71 x
       have i₂ := eq115
       grind)
    | exact superpose eq115 eq71
    | exact resolve eq71 eq115
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq134 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq115
  have eq136 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq120
       have r₂ := eq133
       grind)
    | exact resolve eq120 eq133
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq120 eq133
  have eq137 : x = (M.op x x) := by
    first
    | (have r₁ := eq136
       have r₂ := eq134
       grind)
    | exact resolve eq136 eq134
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq134 eq136
  have eq157 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq26 X0 x
       have i₂ := eq137
       grind)
    | exact superpose eq137 eq26
    | exact resolve eq26 eq137
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26
  have eq159 : x = (N.op x x) := by
    first
    | (have i₁ := eq71 x
       have i₂ := eq137
       grind)
    | exact superpose eq137 eq71
    | exact resolve eq71 eq137
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq71 eq137
  have eq257 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq159
       grind)
    | exact superpose eq159 eq10
    | exact resolve eq10 eq159
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq318 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq257
       have i₂ := eq157 y
       grind)
    | exact superpose eq157 eq257
    | (have j1 := eq157 y
       grind)
    | (have r₁ := eq257
       have r₂ := eq157 y
       grind)
    | (have r₁ := eq257
       have r₂ := eq157 (N.op y x)
       grind)
    | exact resolve eq257 eq157
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq157
  have eq319 : x = y := by grind
  clear eq318
  have eq331 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq257
       have i₂ := eq319
       grind)
    | exact superpose eq319 eq257
    | exact resolve eq257 eq319
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq257 eq319
  have eq333 : False := by grind
  exact eq333

/-- `Equation316` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ x = y ◇ (y ◇ y)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation316_StructuralFromFin_Equation3147 : Law316.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law316 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law316.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op y y)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq21 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq23 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq26 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq23 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq23
  have eq27 : (N.op x x) ≠ (N.op y (M.op y y)) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq29 : (N.op x x) ≠ (N.op y (M.op y y)) ∨ y = (M.op y y) := by grind
  clear eq27
  have eq56 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq21 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq21 X1 X0
       grind)
    | exact superpose eq21 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq21 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq21 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq21
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq62 : ∀ X0 : G, (N.op x x) ≠ (N.op y (M.op X0 X0)) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq29
       have i₂ := eq21 X0 y
       grind)
    | (have i₁ := eq29
       have i₂ := eq21 y x
       grind)
    | exact superpose eq21 eq29
    | exact resolve eq29 eq21
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq71 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq56 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq56
  have eq115 : (N.op x x) ≠ (M.op y y) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq29
       have i₂ := eq26 y y
       grind)
    | exact superpose eq26 eq29
    | (have j1 := eq26 y y
       grind)
    | exact resolve eq29 eq26
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29
  have eq118 : (N.op x x) ≠ (M.op y y) ∨ y = (M.op y y) := by grind
  clear eq115
  have eq120 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq118
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq118
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq118 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq118
  have eq121 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq120
  have eq122 : x = (M.op x x) ∨ y = (M.op y y) := by
    first
    | (have r₁ := eq121
       have r₂ := eq21 x y
       grind)
    | (have r₁ := eq121
       have r₂ := eq21 y x
       grind)
    | exact resolve eq121 eq21
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq121
  have eq132 : ∀ X0 : G, x = (M.op X0 X0) ∨ y = (M.op y y) := by
    intro X0
    first
    | (have i₁ := eq21 x X0
       have i₂ := eq122
       grind)
    | exact superpose eq122 eq21
    | exact resolve eq21 eq122
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq136 : x = (N.op x x) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq71 x
       have i₂ := eq122
       grind)
    | exact superpose eq122 eq71
    | exact resolve eq71 eq122
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq122
  have eq144 : ∀ X0 : G, (N.op x x) ≠ (M.op X0 X0) ∨ y = (M.op X0 X0) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq62 X0
       have i₂ := eq26 y X0
       grind)
    | exact superpose eq26 eq62
    | (have j0 := eq62 X0
       have j1 := eq26 y X0
       grind)
    | exact resolve eq62 eq26
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26 eq62
  have eq146 : ∀ X0 : G, (N.op x x) ≠ (M.op X0 X0) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq144 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq144
  have eq191 : x ≠ (N.op x x) ∨ x = y ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq146 x
       have i₂ := eq132 x
       grind)
    | exact superpose eq132 eq146
    | (have j0 := eq146 y
       grind)
    | exact resolve eq146 eq132
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq146
  have eq210 : x ≠ y ∨ y = (M.op y y) := by grind
  clear eq132
  have eq217 : x = y ∨ y = (M.op y y) := by
    first
    | (have r₁ := eq191
       have r₂ := eq136
       grind)
    | exact resolve eq191 eq136
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq136 eq191
  have eq219 : y = (M.op y y) := by
    first
    | (have r₁ := eq217
       have r₂ := eq210
       grind)
    | exact resolve eq217 eq210
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq210 eq217
  have eq237 : ∀ X0 : G, y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq21 y X0
       have i₂ := eq219
       grind)
    | exact superpose eq219 eq21
    | exact resolve eq21 eq219
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq241 : y = (N.op y y) := by
    first
    | (have i₁ := eq71 y
       have i₂ := eq219
       grind)
    | exact superpose eq219 eq71
    | exact resolve eq71 eq219
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq71 eq219
  have eq354 : (N.op x x) ≠ (N.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq241
       grind)
    | exact superpose eq241 eq10
    | exact resolve eq10 eq241
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq359 : (N.op x x) ≠ y := by
    first
    | (have i₁ := eq354
       have i₂ := eq241
       grind)
    | exact superpose eq241 eq354
    | exact resolve eq354 eq241
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq354
  have eq362 : y ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq359
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq359
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq359 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq363 : y ≠ (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq362
  have eq364 : x = (M.op x x) := by
    first
    | (have r₁ := eq363
       have r₂ := eq237 x
       grind)
    | exact resolve eq363 eq237
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq363
  have eq365 : x = y := by
    first
    | (have i₁ := eq364
       have i₂ := eq237 x
       grind)
    | exact superpose eq237 eq364
    | exact resolve eq364 eq237
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq237 eq364
  have eq379 : y ≠ (N.op y y) := by
    first
    | (have i₁ := eq359
       have i₂ := eq365
       grind)
    | exact superpose eq365 eq359
    | exact resolve eq359 eq365
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq359 eq365
  have eq381 : False := by grind
  exact eq381

/-- `Equation3258` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ x = x ◇ (y ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3258_StructuralFromFin_Equation3147 : Law3258.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3258 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3258.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op y (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq61 : (M.op x x) ≠ (N.op x (N.op y (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq64 : (M.op x x) ≠ (N.op x (N.op y (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq61
  have eq68 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq121 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq64
       have i₂ := eq32 y x
       grind)
    | exact superpose eq32 eq64
    | (have j1 := eq32 x x
       grind)
    | (have r₁ := eq64
       have r₂ := eq32 (N.op x (N.op y (M.op x x))) x
       grind)
    | exact resolve eq64 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq124 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq32 y x
       grind)
    | (have r₁ := eq121
       have r₂ := eq32 x x
       grind)
    | (have r₁ := eq121
       have r₂ := eq32 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq121 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq121
  have eq128 : y ≠ (N.op x (N.op y y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq64
       have i₂ := eq124
       grind)
    | exact superpose eq124 eq64
    | exact resolve eq64 eq124
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64
  have eq137 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq124
       grind)
    | exact superpose eq124 eq18
    | exact resolve eq18 eq124
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq139 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq124
       grind)
    | exact superpose eq124 eq32
    | (have j0 := eq32 x x
       grind)
    | exact resolve eq32 eq124
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq141 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq124
       grind)
    | exact superpose eq124 eq68
    | exact resolve eq68 eq124
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq142 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq124
  have eq144 : y ≠ (N.op x (N.op y y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq128
       have r₂ := eq142
       grind)
    | exact resolve eq128 eq142
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq128
  have eq258 : ∀ X0 X1 : G, x = (N.op X0 x) ∨ (M.op X1 X1) = y ∨ x = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq137 X0
       grind)
    | exact superpose eq137 eq32
    | (have j0 := eq32 y X1
       have j1 := eq137 X1
       grind)
    | exact resolve eq32 eq137
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32 eq137
  have eq273 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq144
       have i₂ := eq141
       grind)
    | exact superpose eq141 eq144
    | exact resolve eq144 eq141
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq141 eq144
  have eq277 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq273
  have eq582 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq277
       have i₂ := eq139 x
       grind)
    | exact superpose eq139 eq277
    | (have j1 := eq139 x
       grind)
    | (have r₁ := eq277
       have r₂ := eq139 x
       grind)
    | (have r₁ := eq277
       have r₂ := eq139 (N.op x y)
       grind)
    | exact resolve eq277 eq139
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq139 eq277
  have eq584 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq582
  have eq585 : x = (M.op x x) ∨ x = y := by grind
  clear eq584
  have eq586 : x = (M.op x x) := by
    first
    | (have r₁ := eq585
       have r₂ := eq142
       grind)
    | exact resolve eq585 eq142
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq142 eq585
  have eq606 : ∀ X0 : G, x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq586
       grind)
    | exact superpose eq586 eq18
    | exact resolve eq18 eq586
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq610 : x = (N.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq586
       grind)
    | exact superpose eq586 eq68
    | exact resolve eq68 eq586
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq68 eq586
  have eq735 : x ≠ (N.op x (N.op y x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq610
       grind)
    | exact superpose eq610 eq10
    | exact resolve eq10 eq610
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq752 : ∀ X0 : G, x ≠ (N.op x x) ∨ y = (M.op X0 X0) ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq735
       have i₂ := eq258 y X0
       grind)
    | exact superpose eq258 eq735
    | (have j1 := eq258 y X0
       grind)
    | (have r₁ := eq735
       have r₂ := eq258 (N.op x (N.op y x)) x
       grind)
    | exact resolve eq735 eq258
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq258
  have eq781 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = y := by
    intro X0
    first
    | (have j0 := eq752 X0
       grind)
    | (have r₁ := eq752 X0
       have r₂ := eq610
       grind)
    | exact resolve eq752 eq610
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq752
  have eq782 : x = y ∨ x = y := by
    first
    | (have i₁ := eq781 x
       have i₂ := eq606 x
       grind)
    | exact superpose eq606 eq781
    | exact resolve eq781 eq606
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq606 eq781
  have eq783 : x = y := by grind
  clear eq782
  have eq809 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq735
       have i₂ := eq783
       grind)
    | exact superpose eq783 eq735
    | exact resolve eq735 eq783
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq735 eq783
  have eq812 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq809
       have i₂ := eq610
       grind)
    | exact superpose eq610 eq809
    | exact resolve eq809 eq610
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq809
  have eq817 : False := by grind
  exact eq817

/-- `Equation3262` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ x = x ◇ (y ◇ (y ◇ y))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3262_StructuralFromFin_Equation3147 : Law3262.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3262 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3262.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op x (N.op y (N.op y y))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq61 : (N.op x x) ≠ (N.op x (N.op y (M.op y y))) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq64 : (N.op x x) ≠ (N.op x (N.op y (M.op y y))) ∨ y = (M.op y y) := by grind
  clear eq61
  have eq68 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq121 : (N.op x x) ≠ (N.op x (M.op y y)) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq64
       have i₂ := eq32 y y
       grind)
    | exact superpose eq32 eq64
    | (have j1 := eq32 y y
       grind)
    | exact resolve eq64 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64
  have eq122 : (N.op x x) ≠ (N.op x (M.op y y)) ∨ y = (M.op y y) := by grind
  clear eq121
  have eq127 : ∀ X0 : G, (N.op x x) ≠ (N.op x (M.op X0 X0)) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq122
       have i₂ := eq18 X0 y
       grind)
    | (have i₁ := eq122
       have i₂ := eq18 y x
       grind)
    | exact superpose eq18 eq122
    | exact resolve eq122 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq128 : (N.op x x) ≠ (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq122
       have i₂ := eq32 x y
       grind)
    | exact superpose eq32 eq122
    | (have j1 := eq32 y y
       grind)
    | exact resolve eq122 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq122
  have eq155 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq128
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq128
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq128 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq128
  have eq156 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq155
  have eq157 : y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq156
       have r₂ := eq18 x y
       grind)
    | (have r₁ := eq156
       have r₂ := eq18 y x
       grind)
    | exact resolve eq156 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq156
  have eq159 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq157
       have i₂ := eq18 X0 y
       grind)
    | (have i₁ := eq157
       have i₂ := eq18 y x
       grind)
    | exact superpose eq18 eq157
    | exact resolve eq157 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq167 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 y X0
       have i₂ := eq157
       grind)
    | exact superpose eq157 eq18
    | exact resolve eq18 eq157
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq157
  have eq220 : x ≠ y ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j0 := eq159 x
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq159
  have eq222 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq220
  have eq338 : ∀ X0 : G, (N.op x x) ≠ (N.op x x) ∨ x = y ∨ y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq127 y
       have i₂ := eq167 X0
       grind)
    | exact superpose eq167 eq127
    | (have j0 := eq127 X0
       have j1 := eq167 X0
       grind)
    | exact resolve eq127 eq167
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq167
  have eq341 : ∀ X0 : G, x = y ∨ y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq338 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq338
  have eq349 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq341 X0
       grind)
    | (have r₁ := eq341 X0
       have r₂ := eq222
       grind)
    | exact resolve eq341 eq222
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq222 eq341
  have eq484 : ∀ X0 X1 : G, x = (M.op X0 X0) ∨ (M.op X1 X1) = y := by
    intro X0 X1
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq349 X1
       grind)
    | exact superpose eq349 eq18
    | (have j1 := eq349 X1
       grind)
    | exact resolve eq18 eq349
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq490 : ∀ X0 : G, (N.op x x) ≠ (N.op x x) ∨ x = y ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq127 x
       have i₂ := eq349 X0
       grind)
    | exact superpose eq349 eq127
    | (have j0 := eq127 X0
       have j1 := eq349 X0
       grind)
    | exact resolve eq127 eq349
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq127
  have eq493 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = y := by
    intro X0
    first
    | (have j0 := eq490 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq490
  have eq529 : ∀ X1 : G, y = (N.op X1 y) ∨ y = X1 ∨ x = y := by
    intro X1
    first
    | (have i₁ := eq32 x x
       have i₂ := eq493 x
       grind)
    | exact superpose eq493 eq32
    | exact resolve eq32 eq493
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq531 : y = (N.op y y) ∨ x = y := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq493 x
       grind)
    | exact superpose eq493 eq68
    | exact resolve eq68 eq493
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq628 : (N.op x x) ≠ (N.op x (N.op y y)) ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq531
       grind)
    | exact superpose eq531 eq10
    | exact resolve eq10 eq531
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1212 : ∀ X0 : G, x ≠ y ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq484 X0 x
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq484
  have eq2035 : (N.op x x) ≠ (N.op x y) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq628
       have i₂ := eq531
       grind)
    | exact superpose eq531 eq628
    | exact resolve eq628 eq531
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq531 eq628
  have eq2041 : (N.op x x) ≠ (N.op x y) ∨ x = y := by grind
  clear eq2035
  have eq2044 : (N.op x x) ≠ y ∨ x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq2041
       have i₂ := eq529 x
       grind)
    | exact superpose eq529 eq2041
    | (have j1 := eq529 x
       grind)
    | exact resolve eq2041 eq529
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq529 eq2041
  have eq2050 : (N.op x x) ≠ y ∨ x = y := by grind
  clear eq2044
  have eq2056 : y ≠ (M.op x x) ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq2050
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq2050
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq2050 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2050
  have eq2057 : y ≠ (M.op x x) ∨ x = y ∨ x = (M.op x x) := by grind
  clear eq2056
  have eq2058 : y ≠ (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq1212 x
       grind)
    | (have r₁ := eq2057
       have r₂ := eq1212 x
       grind)
    | exact resolve eq2057 eq1212
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1212 eq2057
  have eq2059 : x = (M.op x x) := by
    first
    | (have r₁ := eq2058
       have r₂ := eq349 x
       grind)
    | exact resolve eq2058 eq349
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq349 eq2058
  have eq2094 : x = (N.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq2059
       grind)
    | exact superpose eq2059 eq68
    | exact resolve eq68 eq2059
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq68
  have eq2100 : x = y ∨ x = y := by
    first
    | (have i₁ := eq493 x
       have i₂ := eq2059
       grind)
    | exact superpose eq2059 eq493
    | exact resolve eq493 eq2059
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq493 eq2059
  have eq2101 : x = y := by grind
  clear eq2100
  have eq2110 : (N.op x x) ≠ (N.op x (N.op x (N.op x x))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq2101
       grind)
    | exact superpose eq2101 eq10
    | exact resolve eq10 eq2101
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2101
  have eq2165 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq2110
       have i₂ := eq2094
       grind)
    | exact superpose eq2094 eq2110
    | exact resolve eq2110 eq2094
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2110
  have eq2169 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq2165
       have i₂ := eq2094
       grind)
    | exact superpose eq2094 eq2165
    | exact resolve eq2165 eq2094
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2165
  have eq2172 : False := by grind
  exact eq2172

/-- `Equation3268` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ x = y ◇ (x ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3268_StructuralFromFin_Equation3147 : Law3268.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3268 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3268.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq61 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq64 : (M.op x x) ≠ (N.op y (N.op x (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq61
  have eq68 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq121 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq64
       have i₂ := eq32 x x
       grind)
    | exact superpose eq32 eq64
    | (have j1 := eq32 x x
       grind)
    | (have r₁ := eq64
       have r₂ := eq32 (N.op y (N.op x (M.op x x))) x
       grind)
    | exact resolve eq64 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64
  have eq122 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq121
  have eq128 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq122
       have i₂ := eq32 y x
       grind)
    | exact superpose eq32 eq122
    | (have j1 := eq32 x x
       grind)
    | (have r₁ := eq122
       have r₂ := eq32 y x
       grind)
    | (have r₁ := eq122
       have r₂ := eq32 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq122 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq130 : y = (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq128
  have eq139 : y ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq122
       have i₂ := eq130
       grind)
    | exact superpose eq130 eq122
    | exact resolve eq122 eq130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq122
  have eq153 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq130
       grind)
    | exact superpose eq130 eq68
    | exact resolve eq68 eq130
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq154 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq130
  have eq157 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq139
       have r₂ := eq153
       grind)
    | exact resolve eq139 eq153
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq139 eq153
  have eq158 : x = (M.op x x) := by
    first
    | (have r₁ := eq157
       have r₂ := eq154
       grind)
    | exact resolve eq157 eq154
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq154 eq157
  have eq173 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq158
       grind)
    | exact superpose eq158 eq32
    | exact resolve eq32 eq158
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq175 : x = (N.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq158
       grind)
    | exact superpose eq158 eq68
    | exact resolve eq68 eq158
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq68 eq158
  have eq252 : x ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq175
       grind)
    | exact superpose eq175 eq10
    | exact resolve eq10 eq175
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq258 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq252
       have i₂ := eq175
       grind)
    | exact superpose eq175 eq252
    | exact resolve eq252 eq175
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq252
  have eq318 : x ≠ x ∨ x = y := by
    first
    | (have i₁ := eq258
       have i₂ := eq173 y
       grind)
    | exact superpose eq173 eq258
    | (have j1 := eq173 y
       grind)
    | (have r₁ := eq258
       have r₂ := eq173 y
       grind)
    | (have r₁ := eq258
       have r₂ := eq173 (N.op y x)
       grind)
    | exact resolve eq258 eq173
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq173
  have eq319 : x = y := by grind
  clear eq318
  have eq386 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq258
       have i₂ := eq319
       grind)
    | exact superpose eq319 eq258
    | exact resolve eq258 eq319
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq258 eq319
  have eq388 : False := by grind
  exact eq388

/-- `Equation3272` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ x = y ◇ (x ◇ (y ◇ y))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3272_StructuralFromFin_Equation3147 : Law3272.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3272 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3272.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op x (N.op y y))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq61 : (N.op x x) ≠ (N.op y (N.op x (M.op y y))) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq64 : (N.op x x) ≠ (N.op y (N.op x (M.op y y))) ∨ y = (M.op y y) := by grind
  clear eq61
  have eq68 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq70 : ∀ X0 : G, (N.op x x) ≠ (N.op y (N.op x (M.op X0 X0))) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq64
       have i₂ := eq18 X0 y
       grind)
    | (have i₁ := eq64
       have i₂ := eq18 y x
       grind)
    | exact superpose eq18 eq64
    | exact resolve eq64 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq121 : (N.op x x) ≠ (N.op y (M.op y y)) ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq64
       have i₂ := eq32 x y
       grind)
    | exact superpose eq32 eq64
    | (have j1 := eq32 y y
       grind)
    | exact resolve eq64 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64
  have eq137 : (N.op x x) ≠ (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq121
       have i₂ := eq32 y y
       grind)
    | exact superpose eq32 eq121
    | (have j1 := eq32 y y
       grind)
    | exact resolve eq121 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq121
  have eq139 : (N.op x x) ≠ (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op y y) := by grind
  clear eq137
  have eq142 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq139
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq139
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq139 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq139
  have eq143 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq142
  have eq144 : y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq143
       have r₂ := eq18 x y
       grind)
    | (have r₁ := eq143
       have r₂ := eq18 y x
       grind)
    | exact resolve eq143 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq143
  have eq152 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq144
       have i₂ := eq18 X0 y
       grind)
    | (have i₁ := eq144
       have i₂ := eq18 y x
       grind)
    | exact superpose eq18 eq144
    | exact resolve eq144 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq144
  have eq214 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 X0) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq68 x
       have i₂ := eq152 X0
       grind)
    | exact superpose eq152 eq68
    | (have j1 := eq152 X0
       grind)
    | exact resolve eq68 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq152
  have eq223 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq214 X0
       have j1 := eq51 X0 x
       grind)
    | (have r₁ := eq214 X0
       have r₂ := eq51 X0 y
       grind)
    | (have r₁ := eq214 X0
       have r₂ := eq51 X0 x
       grind)
    | exact resolve eq214 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51 eq214
  have eq349 : ∀ X0 : G, x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq9 x x
       have i₂ := eq223 X0
       grind)
    | exact superpose eq223 eq9
    | (have j0 := eq9 x x
       have j1 := eq223 X0
       grind)
    | exact resolve eq9 eq223
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq223
  have eq368 : ∀ X0 : G, x = (M.op x x) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq349 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq349
  have eq422 : ∀ X0 X1 : G, x = (M.op X0 X0) ∨ (M.op X1 X1) = y := by
    intro X0 X1
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq368 X1
       grind)
    | exact superpose eq368 eq18
    | (have j1 := eq368 X1
       grind)
    | exact resolve eq18 eq368
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq439 : ∀ X1 : G, y = (N.op X1 y) ∨ y = X1 ∨ x = (M.op x x) := by
    intro X1
    first
    | (have i₁ := eq32 x x
       have i₂ := eq368 x
       grind)
    | exact superpose eq368 eq32
    | (have j0 := eq32 x x
       grind)
    | exact resolve eq32 eq368
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq441 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq368 x
       grind)
    | exact superpose eq368 eq68
    | exact resolve eq68 eq368
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq670 : ∀ X0 : G, x ≠ y ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq422 X0 x
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq422
  have eq960 : (N.op x x) ≠ (N.op y (N.op x y)) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq441
       grind)
    | exact superpose eq441 eq10
    | exact resolve eq10 eq441
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2406 : (N.op x x) ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq960
       have i₂ := eq439 x
       grind)
    | exact superpose eq439 eq960
    | (have j1 := eq439 x
       grind)
    | exact resolve eq960 eq439
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq439 eq960
  have eq2414 : (N.op x x) ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq2406
  have eq2418 : (N.op x x) ≠ (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq670 x
       grind)
    | (have r₁ := eq2414
       have r₂ := eq670 x
       grind)
    | exact resolve eq2414 eq670
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq670 eq2414
  have eq2487 : (N.op x x) ≠ y ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq2418
       have i₂ := eq441
       grind)
    | exact superpose eq441 eq2418
    | exact resolve eq2418 eq441
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq441 eq2418
  have eq2493 : (N.op x x) ≠ y ∨ x = (M.op x x) := by grind
  clear eq2487
  have eq2554 : y ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq2493
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq2493
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq2493 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2493
  have eq2555 : y ≠ (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq2554
  have eq2556 : x = (M.op x x) := by
    first
    | (have r₁ := eq2555
       have r₂ := eq368 x
       grind)
    | exact resolve eq2555 eq368
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq368 eq2555
  have eq2578 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq2556
       grind)
    | exact superpose eq2556 eq32
    | exact resolve eq32 eq2556
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq2580 : x = (N.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq2556
       grind)
    | exact superpose eq2556 eq68
    | exact resolve eq68 eq2556
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq68
  have eq2581 : (N.op x x) ≠ (N.op y (N.op x x)) ∨ x = y := by
    first
    | (have i₁ := eq70 x
       have i₂ := eq2556
       grind)
    | exact superpose eq2556 eq70
    | exact resolve eq70 eq2556
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq70 eq2556
  have eq3282 : x ≠ (N.op y x) ∨ x = y := by
    first
    | (have i₁ := eq2581
       have i₂ := eq2580
       grind)
    | exact superpose eq2580 eq2581
    | exact resolve eq2581 eq2580
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2581
  have eq3292 : x = y := by
    first
    | (have j1 := eq2578 y
       grind)
    | (have r₁ := eq3282
       have r₂ := eq2578 y
       grind)
    | (have r₁ := eq3282
       have r₂ := eq2578 (N.op y x)
       grind)
    | exact resolve eq3282 eq2578
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2578 eq3282
  have eq3302 : (N.op x x) ≠ (N.op x (N.op x (N.op x x))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq3292
       grind)
    | exact superpose eq3292 eq10
    | exact resolve eq10 eq3292
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3292
  have eq3407 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq3302
       have i₂ := eq2580
       grind)
    | exact superpose eq2580 eq3302
    | exact resolve eq3302 eq2580
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3302
  have eq3412 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq3407
       have i₂ := eq2580
       grind)
    | exact superpose eq2580 eq3407
    | exact resolve eq3407 eq2580
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3407
  have eq3415 : False := by grind
  exact eq3415

/-- `Equation3288` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ x = y ◇ (z ◇ (x ◇ x))`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3288_StructuralFromFin_Equation3147 : Law3288.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3288 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3288.models_iff G N]
  intro x y z
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op y (N.op z (N.op x x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq61 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq64 : (M.op x x) ≠ (N.op y (N.op z (M.op x x))) ∨ x = (M.op x x) := by grind
  clear eq61
  have eq68 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq70 : ∀ X0 : G, (M.op X0 X0) ≠ (N.op y (N.op z (M.op X0 X0))) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq64
       have i₂ := eq18 X0 x
       grind)
    | (have i₁ := eq64
       have i₂ := eq18 x x
       grind)
    | exact superpose eq18 eq64
    | exact resolve eq64 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq121 : (M.op x x) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ z = (M.op x x) := by
    first
    | (have i₁ := eq64
       have i₂ := eq32 z x
       grind)
    | exact superpose eq32 eq64
    | (have j1 := eq32 x x
       grind)
    | (have r₁ := eq64
       have r₂ := eq32 (N.op y (N.op z (M.op x x))) x
       grind)
    | exact resolve eq64 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq128 : ∀ X0 : G, (M.op X0 X0) ≠ (N.op y (M.op X0 X0)) ∨ x = (M.op X0 X0) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq70 X0
       have i₂ := eq32 z X0
       grind)
    | exact superpose eq32 eq70
    | (have j0 := eq70 X0
       have j1 := eq32 x X0
       grind)
    | (have r₁ := eq70 x
       have r₂ := eq32 (N.op y (N.op z (M.op x x))) x
       grind)
    | exact resolve eq70 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq137 : (M.op x x) ≠ (M.op x x) ∨ x = (M.op x x) ∨ z = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq121
       have i₂ := eq32 y x
       grind)
    | exact superpose eq32 eq121
    | (have j1 := eq32 x x
       grind)
    | (have r₁ := eq121
       have r₂ := eq32 y x
       grind)
    | (have r₁ := eq121
       have r₂ := eq32 (N.op y (M.op x x)) x
       grind)
    | exact resolve eq121 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq121
  have eq139 : z = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq137
  have eq154 : ∀ X0 : G, z = (M.op X0 X0) ∨ y = (M.op x x) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq139
       grind)
    | exact superpose eq139 eq18
    | exact resolve eq18 eq139
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq156 : ∀ X0 : G, z = (N.op X0 z) ∨ z = X0 ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq139
       grind)
    | exact superpose eq139 eq32
    | (have j0 := eq32 x x
       grind)
    | exact resolve eq32 eq139
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq158 : z = (N.op z z) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq139
       grind)
    | exact superpose eq139 eq68
    | exact resolve eq68 eq139
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq159 : z ≠ (N.op y (N.op z z)) ∨ x = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq70 x
       have i₂ := eq139
       grind)
    | exact superpose eq139 eq70
    | (have j0 := eq70 x
       grind)
    | exact resolve eq70 eq139
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq70
  have eq160 : x ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  have eq161 : y ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq139
  have eq162 : z ≠ (N.op y (N.op z z)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have r₁ := eq159
       have r₂ := eq160
       grind)
    | exact resolve eq159 eq160
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq159 eq160
  have eq275 : ∀ X0 X1 : G, y = (M.op X0 X0) ∨ (M.op X1 X1) = z ∨ x = (M.op x x) := by
    intro X0 X1
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq154 X1
       grind)
    | exact superpose eq154 eq18
    | (have j1 := eq154 X1
       grind)
    | exact resolve eq18 eq154
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq279 : ∀ X0 : G, z = (M.op X0 X0) ∨ y = (N.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq68 x
       have i₂ := eq154 X0
       grind)
    | exact superpose eq154 eq68
    | (have j1 := eq154 X0
       grind)
    | exact resolve eq68 eq154
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq281 : ∀ X0 : G, y ≠ (N.op y y) ∨ x = y ∨ y = z ∨ z = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq128 x
       have i₂ := eq154 X0
       grind)
    | exact superpose eq154 eq128
    | (have j0 := eq128 x
       have j1 := eq154 X0
       grind)
    | exact resolve eq128 eq154
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq128
  have eq285 : ∀ X0 : G, x ≠ y ∨ z = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq154 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq154
  have eq286 : ∀ X0 : G, y ≠ (N.op y y) ∨ y = z ∨ z = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have j0 := eq281 X0
       have j1 := eq285 X0
       grind)
    | (have r₁ := eq281 X0
       have r₂ := eq285 X0
       grind)
    | exact resolve eq281 eq285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq281 eq285
  have eq295 : ∀ X0 : G, z = (M.op X0 X0) ∨ x = (M.op x x) ∨ y = z := by
    intro X0
    first
    | (have j0 := eq286 X0
       have j1 := eq279 X0
       grind)
    | (have r₁ := eq286 X0
       have r₂ := eq279 X0
       grind)
    | exact resolve eq286 eq279
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq279 eq286
  have eq342 : ∀ X0 X1 : G, x = (M.op X0 X0) ∨ (M.op X1 X1) = z ∨ y = z := by
    intro X0 X1
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq295 X1
       grind)
    | exact superpose eq295 eq18
    | (have j1 := eq295 X1
       grind)
    | exact resolve eq18 eq295
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq367 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq162
       have i₂ := eq158
       grind)
    | exact superpose eq158 eq162
    | exact resolve eq162 eq158
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq158 eq162
  have eq371 : z ≠ (N.op y z) ∨ x = (M.op x x) ∨ y = (M.op x x) := by grind
  clear eq367
  have eq557 : ∀ X1 : G, z = (N.op z z) ∨ (M.op X1 X1) = x ∨ y = z := by
    intro X1
    first
    | (have i₁ := eq68 X1
       have i₂ := eq342 x X1
       grind)
    | exact superpose eq342 eq68
    | (have j1 := eq342 X1 X1
       grind)
    | exact resolve eq68 eq342
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq342
  have eq1129 : ∀ X0 X1 X2 : G, z = (M.op X2 X2) ∨ x = (N.op X0 x) ∨ (M.op X1 X1) = y ∨ x = X0 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq275 X0 X1
       grind)
    | exact superpose eq275 eq32
    | (have j0 := eq32 z X2
       have j1 := eq275 X1 X2
       grind)
    | exact resolve eq32 eq275
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq275
  have eq1235 : z ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq371
       have i₂ := eq156 y
       grind)
    | exact superpose eq156 eq371
    | (have j1 := eq156 y
       grind)
    | (have r₁ := eq371
       have r₂ := eq156 y
       grind)
    | (have r₁ := eq371
       have r₂ := eq156 (N.op y z)
       grind)
    | exact resolve eq371 eq156
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq156 eq371
  have eq1237 : z ≠ z ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq1235
  have eq1238 : x = (M.op x x) ∨ y = (M.op x x) ∨ y = z := by grind
  clear eq1237
  have eq1239 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1238
       have r₂ := eq161
       grind)
    | exact resolve eq1238 eq161
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq161 eq1238
  have eq1242 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq1239
       have i₂ := eq18 X0 x
       grind)
    | (have i₁ := eq1239
       have i₂ := eq18 x x
       grind)
    | exact superpose eq18 eq1239
    | exact resolve eq1239 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1264 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq1239
       grind)
    | exact superpose eq1239 eq18
    | exact resolve eq18 eq1239
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1267 : ∀ X0 : G, y ≠ X0 ∨ (N.op X0 X0) = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq51 x X0
       have i₂ := eq1239
       grind)
    | exact superpose eq1239 eq51
    | (have j0 := eq51 X0 X0
       grind)
    | (have r₁ := eq51 x y
       have r₂ := eq1239
       grind)
    | (have r₁ := eq51 x x
       have r₂ := eq1239
       grind)
    | exact resolve eq51 eq1239
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1274 : y = z ∨ x = y ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq295 x
       have i₂ := eq1239
       grind)
    | exact superpose eq1239 eq295
    | exact resolve eq295 eq1239
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq295
  have eq1278 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq1239
  have eq1281 : y = z ∨ x = y ∨ x = (M.op x x) := by grind
  clear eq1274
  have eq1285 : x = (M.op x x) ∨ y = z := by
    first
    | (have r₁ := eq1281
       have r₂ := eq1278
       grind)
    | exact resolve eq1281 eq1278
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1281
  have eq1330 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ y = z := by
    intro X0
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq1285
       grind)
    | exact superpose eq1285 eq32
    | exact resolve eq32 eq1285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1332 : x = (N.op x x) ∨ y = z := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq1285
       grind)
    | exact superpose eq1285 eq68
    | exact resolve eq68 eq1285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1285
  have eq1527 : x ≠ (N.op y (N.op z x)) ∨ y = z := by
    first
    | (have i₁ := eq10
       have i₂ := eq1332
       grind)
    | exact superpose eq1332 eq10
    | exact resolve eq10 eq1332
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1332
  have eq1561 : ∀ X0 : G, y = (N.op y y) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq68 X0
       have i₂ := eq1242 X0
       grind)
    | exact superpose eq1242 eq68
    | (have j1 := eq1242 X0
       grind)
    | exact resolve eq68 eq1242
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1242
  have eq1807 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq68 x
       have i₂ := eq1264 X0
       grind)
    | exact superpose eq1264 eq68
    | (have j1 := eq1264 X0
       grind)
    | exact resolve eq68 eq1264
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2411 : y = (N.op y y) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq1561 x
       grind)
    | exact superpose eq1561 eq68
    | exact resolve eq68 eq1561
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1561
  have eq2655 : ∀ X0 : G, x ≠ (N.op y (N.op z x)) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq10
       have i₂ := eq1807 X0
       grind)
    | exact superpose eq1807 eq10
    | (have j1 := eq1807 X0
       grind)
    | exact resolve eq10 eq1807
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2671 : ∀ X1 : G, y ≠ X1 ∨ (N.op X1 X1) = X1 ∨ x = (N.op x x) := by
    intro X1
    first
    | (have i₁ := eq51 x X1
       have i₂ := eq1807 x
       grind)
    | exact superpose eq1807 eq51
    | (have j0 := eq51 x X1
       grind)
    | (have r₁ := eq51 x y
       have r₂ := eq1807 x
       grind)
    | exact resolve eq51 eq1807
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1807
  have eq3906 : x ≠ (N.op y x) ∨ y = z ∨ x = z ∨ y = z := by
    first
    | (have i₁ := eq1527
       have i₂ := eq1330 z
       grind)
    | exact superpose eq1330 eq1527
    | (have j1 := eq1330 z
       grind)
    | (have r₁ := eq1527
       have r₂ := eq1330 (N.op y (N.op z x))
       grind)
    | exact resolve eq1527 eq1330
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq3907 : x ≠ (N.op y x) ∨ y = z ∨ x = z := by grind
  clear eq3906
  have eq4172 : x ≠ x ∨ y = z ∨ x = z ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq3907
       have i₂ := eq1330 y
       grind)
    | exact superpose eq1330 eq3907
    | (have j1 := eq1330 z
       grind)
    | (have r₁ := eq3907
       have r₂ := eq1330 y
       grind)
    | (have r₁ := eq3907
       have r₂ := eq1330 (N.op y x)
       grind)
    | exact resolve eq3907 eq1330
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1330
  have eq4174 : x ≠ x ∨ y = z ∨ x = z ∨ x = y := by grind
  clear eq4172
  have eq4175 : y = z ∨ x = z ∨ x = y := by grind
  clear eq4174
  have eq5589 : x = (N.op x x) ∨ z = (N.op z z) ∨ y = z := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq557 x
       grind)
    | exact superpose eq557 eq68
    | exact resolve eq68 eq557
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq5614 : x = y ∨ x = (M.op x x) ∨ z = (N.op z z) ∨ y = z := by
    first
    | (have i₁ := eq1264 x
       have i₂ := eq557 x
       grind)
    | exact superpose eq557 eq1264
    | (have j1 := eq557 x
       grind)
    | exact resolve eq1264 eq557
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq557 eq1264
  have eq5647 : x = y ∨ x = (M.op x x) ∨ z = (N.op z z) := by
    first
    | (have j1 := eq1267 z
       grind)
    | (have r₁ := eq5614
       have r₂ := eq1267 x
       grind)
    | (have r₁ := eq5614
       have r₂ := eq1267 z
       grind)
    | exact resolve eq5614 eq1267
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1267 eq5614
  have eq5659 : z = (N.op z z) ∨ x = (N.op x x) := by
    first
    | (have j1 := eq2671 z
       grind)
    | (have r₁ := eq5589
       have r₂ := eq2671 z
       grind)
    | exact resolve eq5589 eq2671
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2671 eq5589
  have eq5675 : z = (N.op z z) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq5647
       have r₂ := eq1278
       grind)
    | exact resolve eq5647 eq1278
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5647
  have eq5696 : z = (M.op z z) ∨ z = (M.op z z) ∨ z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq9 z z
       have i₂ := eq5675
       grind)
    | exact superpose eq5675 eq9
    | (have j0 := eq9 z z
       grind)
    | exact resolve eq9 eq5675
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5675
  have eq5697 : z = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq5696
  have eq5751 : ∀ X0 : G, z = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 z X0
       have i₂ := eq5697
       grind)
    | exact superpose eq5697 eq18
    | exact resolve eq18 eq5697
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5697
  have eq6036 : ∀ X0 X1 : G, x = (M.op X0 X0) ∨ (M.op X1 X1) = z := by
    intro X0 X1
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq5751 X1
       grind)
    | exact superpose eq5751 eq18
    | (have j1 := eq5751 X1
       grind)
    | exact resolve eq18 eq5751
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq6038 : ∀ X0 X1 : G, x = (N.op X0 x) ∨ x = X0 ∨ (M.op X1 X1) = z := by
    intro X0 X1
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq5751 X0
       grind)
    | exact superpose eq5751 eq32
    | (have j0 := eq32 z X1
       have j1 := eq5751 X1
       grind)
    | exact resolve eq32 eq5751
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq6050 : x ≠ z ∨ x = (M.op x x) := by grind
  clear eq5751
  have eq6619 : ∀ X0 : G, z ≠ (N.op y (N.op z z)) ∨ x = z ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq64
       have i₂ := eq6036 X0 x
       grind)
    | exact superpose eq6036 eq64
    | (have j1 := eq6036 X0 x
       grind)
    | exact resolve eq64 eq6036
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64
  have eq6644 : ∀ X0 : G, x ≠ z ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq6036 X0 x
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq6036
  have eq6648 : ∀ X0 : G, z ≠ (N.op y (N.op z z)) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq6619 X0
       have j1 := eq6644 X0
       grind)
    | (have r₁ := eq6619 X0
       have r₂ := eq6644 X0
       grind)
    | exact resolve eq6619 eq6644
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq6619 eq6644
  have eq7571 : ∀ X0 : G, z ≠ (N.op y z) ∨ x = (M.op X0 X0) ∨ x = (N.op x x) := by
    intro X0
    first
    | (have i₁ := eq6648 X0
       have i₂ := eq5659
       grind)
    | exact superpose eq5659 eq6648
    | (have j0 := eq6648 X0
       grind)
    | exact resolve eq6648 eq5659
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5659 eq6648
  have eq7578 : z ≠ (N.op y z) ∨ x = (N.op x x) := by
    first
    | (have j1 := eq51 x x
       grind)
    | (have r₁ := eq7571 x
       have r₂ := eq51 x x
       grind)
    | exact resolve eq7571 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51 eq7571
  have eq7581 : y ≠ (N.op y y) ∨ x = (N.op x x) ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq7578
       have i₂ := eq4175
       grind)
    | exact superpose eq4175 eq7578
    | exact resolve eq7578 eq4175
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4175 eq7578
  have eq7592 : x = (N.op x x) ∨ x = z ∨ x = y := by
    first
    | (have r₁ := eq7581
       have r₂ := eq2411
       grind)
    | exact resolve eq7581 eq2411
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2411 eq7581
  have eq7751 : x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq9 x x
       have i₂ := eq7592
       grind)
    | exact superpose eq7592 eq9
    | (have j0 := eq9 x x
       grind)
    | exact resolve eq9 eq7592
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7592
  have eq7752 : x = (M.op x x) ∨ x = z ∨ x = y := by grind
  clear eq7751
  have eq7754 : x = (M.op x x) ∨ x = y := by
    first
    | (have r₁ := eq7752
       have r₂ := eq6050
       grind)
    | exact resolve eq7752 eq6050
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq6050 eq7752
  have eq7756 : x = (M.op x x) := by
    first
    | (have r₁ := eq7754
       have r₂ := eq1278
       grind)
    | exact resolve eq7754 eq1278
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1278 eq7754
  have eq7792 : ∀ X0 : G, x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq7756
       grind)
    | exact superpose eq7756 eq18
    | exact resolve eq18 eq7756
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq7794 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq7756
       grind)
    | exact superpose eq7756 eq32
    | exact resolve eq32 eq7756
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq7796 : x = (N.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq7756
       grind)
    | exact superpose eq7756 eq68
    | exact resolve eq68 eq7756
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq68 eq7756
  have eq8024 : ∀ X0 X1 X2 : G, x ≠ (N.op y x) ∨ y = (M.op X0 X0) ∨ (M.op X1 X1) = z ∨ y = (M.op X2 X2) ∨ x = z := by
    intro X0 X1 X2
    first
    | (have i₁ := eq2655 X0
       have i₂ := eq1129 z X2 X1
       grind)
    | exact superpose eq1129 eq2655
    | (have j0 := eq2655 X0
       have j1 := eq1129 z X0 X1
       grind)
    | (have r₁ := eq2655 X0
       have r₂ := eq1129 (N.op y (N.op z x)) X1 X2
       grind)
    | exact resolve eq2655 eq1129
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1129 eq2655
  have eq8113 : ∀ X1 X2 : G, x = y ∨ x ≠ (N.op y x) ∨ (M.op X1 X1) = z ∨ y = (M.op X2 X2) ∨ x = z := by
    intro X1 X2
    first
    | (have i₁ := eq8024 x X1 X2
       have i₂ := eq7792 x
       grind)
    | exact superpose eq7792 eq8024
    | (have j0 := eq8024 X2 X1 X2
       grind)
    | exact resolve eq8024 eq7792
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8024
  have eq8122 : ∀ X1 X2 : G, x = y ∨ (M.op X1 X1) = z ∨ y = (M.op X2 X2) ∨ x = z := by
    intro X1 X2
    first
    | (have j0 := eq8113 X1 X2
       have j1 := eq6038 y X1
       grind)
    | (have r₁ := eq8113 X1 X2
       have r₂ := eq6038 y X1
       grind)
    | (have r₁ := eq8113 X1 X2
       have r₂ := eq6038 (N.op y x) X1
       grind)
    | exact resolve eq8113 eq6038
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq6038 eq8113
  have eq8126 : ∀ X2 : G, x = z ∨ x = y ∨ y = (M.op X2 X2) ∨ x = z := by
    intro X2
    first
    | (have i₁ := eq8122 x X2
       have i₂ := eq7792 x
       grind)
    | exact superpose eq7792 eq8122
    | (have j0 := eq8122 x X2
       grind)
    | exact resolve eq8122 eq7792
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8122
  have eq8127 : ∀ X2 : G, x = z ∨ x = y ∨ y = (M.op X2 X2) := by
    intro X2
    first
    | (have j0 := eq8126 X2
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8126
  have eq8130 : x = y ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq8127 x
       have i₂ := eq7792 x
       grind)
    | exact superpose eq7792 eq8127
    | exact resolve eq8127 eq7792
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7792 eq8127
  have eq8131 : x = z ∨ x = y := by grind
  clear eq8130
  have eq8361 : x ≠ (N.op y (N.op x x)) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq1527
       have i₂ := eq8131
       grind)
    | exact superpose eq8131 eq1527
    | exact resolve eq1527 eq8131
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1527 eq8131
  have eq8383 : x ≠ (N.op y (N.op x x)) ∨ x = y := by grind
  clear eq8361
  have eq8404 : x ≠ (N.op y x) ∨ x = y := by
    first
    | (have i₁ := eq8383
       have i₂ := eq7796
       grind)
    | exact superpose eq7796 eq8383
    | exact resolve eq8383 eq7796
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8383
  have eq8411 : x = y := by
    first
    | (have j1 := eq7794 y
       grind)
    | (have r₁ := eq8404
       have r₂ := eq7794 y
       grind)
    | (have r₁ := eq8404
       have r₂ := eq7794 (N.op y x)
       grind)
    | exact resolve eq8404 eq7794
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7794 eq8404
  have eq8433 : (N.op x x) ≠ (N.op x (N.op z (N.op x x))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq8411
       grind)
    | exact superpose eq8411 eq10
    | exact resolve eq10 eq8411
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq8496 : x ≠ (N.op x x) ∨ x = z ∨ x = z := by
    first
    | (have i₁ := eq3907
       have i₂ := eq8411
       grind)
    | exact superpose eq8411 eq3907
    | exact resolve eq3907 eq8411
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3907 eq8411
  have eq8500 : x ≠ (N.op x x) ∨ x = z := by grind
  clear eq8496
  have eq8515 : x = z := by
    first
    | (have r₁ := eq8500
       have r₂ := eq7796
       grind)
    | exact resolve eq8500 eq7796
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8500
  have eq8519 : x ≠ (N.op x (N.op z x)) := by
    first
    | (have i₁ := eq8433
       have i₂ := eq7796
       grind)
    | exact superpose eq7796 eq8433
    | exact resolve eq8433 eq7796
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8433
  have eq8523 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq8519
       have i₂ := eq8515
       grind)
    | exact superpose eq8515 eq8519
    | exact resolve eq8519 eq8515
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8515 eq8519
  have eq8527 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq8523
       have i₂ := eq7796
       grind)
    | exact superpose eq7796 eq8523
    | exact resolve eq8523 eq7796
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq8523
  have eq8531 : False := by grind
  exact eq8531

/-- `Equation3668` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ x = (x ◇ y) ◇ (y ◇ y)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3668_StructuralFromFin_Equation3147 : Law3668.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3668 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3668.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op (N.op x y) (N.op y y)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq62 : (N.op x x) ≠ (N.op (N.op x y) (M.op y y)) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq64 : (N.op x x) ≠ (N.op (N.op x y) (M.op y y)) ∨ y = (M.op y y) := by grind
  clear eq62
  have eq68 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq71 : ∀ X0 : G, (N.op x x) ≠ (N.op (N.op x y) (M.op X0 X0)) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq64
       have i₂ := eq18 X0 y
       grind)
    | (have i₁ := eq64
       have i₂ := eq18 y x
       grind)
    | exact superpose eq18 eq64
    | exact resolve eq64 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq120 : (N.op x x) ≠ (M.op y y) ∨ y = (M.op y y) ∨ (N.op x y) = (M.op y y) := by
    first
    | (have i₁ := eq64
       have i₂ := eq32 (N.op x y) y
       grind)
    | exact superpose eq32 eq64
    | (have j1 := eq32 y y
       grind)
    | exact resolve eq64 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq129 : ∀ X0 : G, (N.op x x) ≠ (M.op X0 X0) ∨ y = (M.op X0 X0) ∨ (N.op x y) = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq71 X0
       have i₂ := eq32 (N.op x y) X0
       grind)
    | exact superpose eq32 eq71
    | (have j0 := eq71 X0
       have j1 := eq32 y X0
       grind)
    | exact resolve eq71 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq135 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ (N.op x y) = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq120
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq120
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq120 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq120
  have eq136 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ (N.op x y) = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq135
  have eq137 : (N.op x y) = (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq136
       have r₂ := eq18 x y
       grind)
    | (have r₁ := eq136
       have r₂ := eq18 y x
       grind)
    | exact resolve eq136 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq136
  have eq143 : (N.op x x) ≠ (N.op (M.op y y) (M.op y y)) ∨ y = (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq64
       have i₂ := eq137
       grind)
    | exact superpose eq137 eq64
    | exact resolve eq64 eq137
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64 eq137
  have eq146 : (N.op x x) ≠ (N.op (M.op y y) (M.op y y)) ∨ y = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq143
  have eq147 : (N.op x x) ≠ (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq146
       have i₂ := eq68 y
       grind)
    | exact superpose eq68 eq146
    | exact resolve eq146 eq68
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq146
  have eq150 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq147
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq147
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq147 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq147
  have eq151 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq150
  have eq152 : y = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq151
       have r₂ := eq18 x y
       grind)
    | (have r₁ := eq151
       have r₂ := eq18 y x
       grind)
    | exact resolve eq151 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq151
  have eq168 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 y X0
       have i₂ := eq152
       grind)
    | exact superpose eq152 eq18
    | exact resolve eq18 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq170 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq32 X0 y
       have i₂ := eq152
       grind)
    | exact superpose eq152 eq32
    | (have j0 := eq32 x x
       grind)
    | exact resolve eq32 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq172 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq68 y
       have i₂ := eq152
       grind)
    | exact superpose eq152 eq68
    | exact resolve eq68 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq152
  have eq272 : ∀ X0 : G, (N.op x x) ≠ (N.op (N.op x y) x) ∨ x = y ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq71 x
       have i₂ := eq168 X0
       grind)
    | exact superpose eq168 eq71
    | (have j0 := eq71 X0
       have j1 := eq168 X0
       grind)
    | exact resolve eq71 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq71
  have eq274 : x ≠ y ∨ x = (M.op x x) := by grind
  have eq304 : (N.op x x) ≠ (N.op (N.op x y) y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq172
       grind)
    | exact superpose eq172 eq10
    | exact resolve eq10 eq172
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq857 : (N.op x x) ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq304
       have i₂ := eq170 x
       grind)
    | exact superpose eq170 eq304
    | (have j1 := eq170 x
       grind)
    | exact resolve eq304 eq170
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq170 eq304
  have eq862 : (N.op x x) ≠ (N.op y y) ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq857
  have eq865 : (N.op x x) ≠ (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq862
       have r₂ := eq274
       grind)
    | exact resolve eq862 eq274
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq274 eq862
  have eq869 : (N.op x x) ≠ y ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq865
       have i₂ := eq172
       grind)
    | exact superpose eq172 eq865
    | exact resolve eq865 eq172
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq172 eq865
  have eq872 : (N.op x x) ≠ y ∨ x = (M.op x x) := by grind
  clear eq869
  have eq883 : y ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq872
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq872
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq872 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq872
  have eq884 : y ≠ (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq883
  have eq885 : x = (M.op x x) := by
    first
    | (have r₁ := eq884
       have r₂ := eq168 x
       grind)
    | exact resolve eq884 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq168 eq884
  have eq906 : ∀ X0 : G, x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq885
       grind)
    | exact superpose eq885 eq18
    | exact resolve eq18 eq885
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq910 : x = (N.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq885
       grind)
    | exact superpose eq885 eq68
    | exact resolve eq68 eq885
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq68 eq885
  have eq1013 : x ≠ (N.op x x) ∨ x = y ∨ x = (N.op x y) := by
    first
    | (have i₁ := eq129 x
       have i₂ := eq906 x
       grind)
    | exact superpose eq906 eq129
    | exact resolve eq129 eq906
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq129
  have eq1050 : x = (N.op x y) ∨ x = y := by
    first
    | (have r₁ := eq1013
       have r₂ := eq910
       grind)
    | exact resolve eq1013 eq910
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1013
  have eq1234 : ∀ X0 : G, (N.op x x) ≠ (N.op x x) ∨ x = y ∨ y = (M.op X0 X0) ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq272 X0
       have i₂ := eq1050
       grind)
    | exact superpose eq1050 eq272
    | (have j0 := eq272 X0
       grind)
    | exact resolve eq272 eq1050
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq272 eq1050
  have eq1242 : ∀ X0 : G, (N.op x x) ≠ (N.op x x) ∨ x = y ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq1234 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1234
  have eq1243 : ∀ X0 : G, x = y ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq1242 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1242
  have eq1247 : x = y ∨ x = y := by
    first
    | (have i₁ := eq1243 x
       have i₂ := eq906 x
       grind)
    | exact superpose eq906 eq1243
    | exact resolve eq1243 eq906
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq906 eq1243
  have eq1248 : x = y := by grind
  clear eq1247
  have eq1256 : (N.op x x) ≠ (N.op (N.op x x) (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1248
       grind)
    | exact superpose eq1248 eq10
    | exact resolve eq10 eq1248
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1248
  have eq1307 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq1256
       have i₂ := eq910
       grind)
    | exact superpose eq910 eq1256
    | exact resolve eq1256 eq910
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1256
  have eq1308 : False := by grind
  exact eq1308

/-- `Equation3672` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ x = (x ◇ y) ◇ (z ◇ z)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3672_StructuralFromFin_Equation3147 : Law3672.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3672 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3672.models_iff G N]
  intro x y z
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op (N.op x y) (N.op z z)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq63 : (N.op x x) ≠ (N.op (N.op x y) (M.op z z)) ∨ z = (M.op z z) ∨ z = (M.op z z) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 z z
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 z z
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq64 : (N.op x x) ≠ (N.op (N.op x y) (M.op z z)) ∨ z = (M.op z z) := by grind
  clear eq63
  have eq68 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq121 : (N.op x x) ≠ (M.op z z) ∨ z = (M.op z z) ∨ (N.op x y) = (M.op z z) := by
    first
    | (have i₁ := eq64
       have i₂ := eq32 (N.op x y) z
       grind)
    | exact superpose eq32 eq64
    | (have j1 := eq32 z z
       grind)
    | exact resolve eq64 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq135 : (M.op z z) ≠ (M.op x x) ∨ z = (M.op z z) ∨ (N.op x y) = (M.op z z) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq121
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq121
    | (have j1 := eq9 z z
       grind)
    | exact resolve eq121 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq136 : (M.op z z) ≠ (M.op x x) ∨ z = (M.op z z) ∨ (N.op x y) = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq135
  have eq137 : (N.op x y) = (M.op z z) ∨ z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq136
       have r₂ := eq18 x z
       grind)
    | (have r₁ := eq136
       have r₂ := eq18 z x
       grind)
    | exact resolve eq136 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq136
  have eq143 : (N.op x x) ≠ (N.op (M.op z z) (M.op z z)) ∨ z = (M.op z z) ∨ z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq64
       have i₂ := eq137
       grind)
    | exact superpose eq137 eq64
    | exact resolve eq64 eq137
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq137
  have eq146 : (N.op x x) ≠ (N.op (M.op z z) (M.op z z)) ∨ z = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq143
  have eq147 : (N.op x x) ≠ (M.op z z) ∨ z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq146
       have i₂ := eq68 z
       grind)
    | exact superpose eq68 eq146
    | exact resolve eq146 eq68
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq146
  have eq150 : (M.op z z) ≠ (M.op x x) ∨ z = (M.op z z) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq147
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq147
    | (have j1 := eq9 z z
       grind)
    | exact resolve eq147 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq147
  have eq151 : (M.op z z) ≠ (M.op x x) ∨ z = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq150
  have eq152 : z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq151
       have r₂ := eq18 x z
       grind)
    | (have r₁ := eq151
       have r₂ := eq18 z x
       grind)
    | exact resolve eq151 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq151
  have eq168 : ∀ X0 : G, z = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 z X0
       have i₂ := eq152
       grind)
    | exact superpose eq152 eq18
    | exact resolve eq18 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq170 : ∀ X0 : G, z = (N.op X0 z) ∨ z = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq32 X0 z
       have i₂ := eq152
       grind)
    | exact superpose eq152 eq32
    | (have j0 := eq32 x x
       grind)
    | exact resolve eq32 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq172 : z = (N.op z z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq68 z
       have i₂ := eq152
       grind)
    | exact superpose eq152 eq68
    | exact resolve eq68 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq152
  have eq266 : ∀ X0 X1 : G, x = (M.op X0 X0) ∨ (M.op X1 X1) = z := by
    intro X0 X1
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq168 X1
       grind)
    | exact superpose eq168 eq18
    | (have j1 := eq168 X1
       grind)
    | exact resolve eq18 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq270 : ∀ X0 : G, x = (N.op x x) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq68 x
       have i₂ := eq168 X0
       grind)
    | exact superpose eq168 eq68
    | (have j1 := eq168 X0
       grind)
    | exact resolve eq68 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq303 : (N.op x x) ≠ (N.op (N.op x y) z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq172
       grind)
    | exact superpose eq172 eq10
    | exact resolve eq10 eq172
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq478 : ∀ X0 : G, x ≠ (M.op z z) ∨ z = (M.op z z) ∨ (N.op x y) = (M.op z z) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq121
       have i₂ := eq270 X0
       grind)
    | exact superpose eq270 eq121
    | (have j1 := eq270 z
       grind)
    | exact resolve eq121 eq270
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq121
  have eq505 : ∀ X0 : G, (N.op x y) = (M.op z z) ∨ z = (M.op X0 X0) ∨ z = (M.op z z) := by
    intro X0
    first
    | (have j0 := eq478 X0
       have j1 := eq266 X0 X0
       grind)
    | (have r₁ := eq478 X0
       have r₂ := eq266 z x
       grind)
    | exact resolve eq478 eq266
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq266 eq478
  have eq792 : (N.op x x) ≠ z ∨ x = (M.op x x) ∨ (N.op x y) = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq303
       have i₂ := eq170 (N.op x y)
       grind)
    | exact superpose eq170 eq303
    | (have j1 := eq170 (N.op x y)
       grind)
    | exact resolve eq303 eq170
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq170
  have eq794 : (N.op x x) ≠ z ∨ x = (M.op x x) ∨ (N.op x y) = z := by grind
  clear eq792
  have eq1151 : z ≠ (M.op x x) ∨ x = (M.op x x) ∨ (N.op x y) = z ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq794
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq794
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq794 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq794
  have eq1152 : z ≠ (M.op x x) ∨ x = (M.op x x) ∨ (N.op x y) = z := by grind
  clear eq1151
  have eq1153 : (N.op x y) = z ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1152
       have r₂ := eq168 x
       grind)
    | exact resolve eq1152 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1152
  have eq1165 : (N.op x x) ≠ (N.op z z) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq303
       have i₂ := eq1153
       grind)
    | exact superpose eq1153 eq303
    | exact resolve eq303 eq1153
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq303 eq1153
  have eq1169 : (N.op x x) ≠ (N.op z z) ∨ x = (M.op x x) := by grind
  clear eq1165
  have eq1179 : (N.op x x) ≠ z ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1169
       have i₂ := eq172
       grind)
    | exact superpose eq172 eq1169
    | exact resolve eq1169 eq172
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq172 eq1169
  have eq1182 : (N.op x x) ≠ z ∨ x = (M.op x x) := by grind
  clear eq1179
  have eq1205 : z ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1182
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq1182
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq1182 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1182
  have eq1206 : z ≠ (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq1205
  have eq1207 : x = (M.op x x) := by
    first
    | (have r₁ := eq1206
       have r₂ := eq168 x
       grind)
    | exact resolve eq1206 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq168 eq1206
  have eq1222 : ∀ X0 : G, x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq1207
       grind)
    | exact superpose eq1207 eq18
    | exact resolve eq18 eq1207
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq1224 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq1207
       grind)
    | exact superpose eq1207 eq32
    | exact resolve eq32 eq1207
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq1226 : x = (N.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq1207
       grind)
    | exact superpose eq1207 eq68
    | exact resolve eq68 eq1207
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1207
  have eq1249 : ∀ X0 : G, (N.op x x) ≠ (N.op (M.op z z) (M.op z z)) ∨ z = (M.op z z) ∨ z = (M.op X0 X0) ∨ z = (M.op z z) := by
    intro X0
    first
    | (have i₁ := eq64
       have i₂ := eq505 X0
       grind)
    | exact superpose eq505 eq64
    | (have j1 := eq505 z
       grind)
    | exact resolve eq64 eq505
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64 eq505
  have eq1285 : ∀ X0 : G, (N.op x x) ≠ (N.op (M.op z z) (M.op z z)) ∨ z = (M.op z z) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq1249 z
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1249
  have eq1300 : ∀ X0 : G, (N.op x x) ≠ (M.op z z) ∨ z = (M.op z z) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq1285 X0
       have i₂ := eq68 z
       grind)
    | exact superpose eq68 eq1285
    | (have j0 := eq1285 z
       grind)
    | exact resolve eq1285 eq68
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq68 eq1285
  have eq1315 : ∀ X0 : G, x ≠ (N.op x x) ∨ z = (M.op z z) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq1300 X0
       have i₂ := eq1222 z
       grind)
    | exact superpose eq1222 eq1300
    | (have j0 := eq1300 z
       grind)
    | exact resolve eq1300 eq1222
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1300
  have eq1323 : ∀ X0 : G, z = (M.op z z) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq1315 z
       have j1 := eq270 z
       grind)
    | (have r₁ := eq1315 X0
       have r₂ := eq270 X0
       grind)
    | exact resolve eq1315 eq270
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq270 eq1315
  have eq1327 : ∀ X0 : G, x = z ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq1323 X0
       have i₂ := eq1222 z
       grind)
    | exact superpose eq1222 eq1323
    | (have j0 := eq1323 X0
       grind)
    | exact resolve eq1323 eq1222
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1323
  have eq1330 : x = z ∨ x = z := by
    first
    | (have i₁ := eq1327 x
       have i₂ := eq1222 x
       grind)
    | exact superpose eq1222 eq1327
    | exact resolve eq1327 eq1222
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1222 eq1327
  have eq1331 : x = z := by grind
  clear eq1330
  have eq1332 : (N.op x x) ≠ (N.op (N.op x y) (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1331
       grind)
    | exact superpose eq1331 eq10
    | exact resolve eq10 eq1331
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1331
  have eq1386 : x ≠ (N.op (N.op x y) x) := by
    first
    | (have i₁ := eq1332
       have i₂ := eq1226
       grind)
    | exact superpose eq1226 eq1332
    | exact resolve eq1332 eq1226
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1332
  have eq1906 : x ≠ x ∨ x = (N.op x y) := by
    first
    | (have i₁ := eq1386
       have i₂ := eq1224 (N.op x y)
       grind)
    | exact superpose eq1224 eq1386
    | (have j1 := eq1224 (N.op x y)
       grind)
    | (have r₁ := eq1386
       have r₂ := eq1224 (N.op x y)
       grind)
    | (have r₁ := eq1386
       have r₂ := eq1224 (N.op (N.op x y) x)
       grind)
    | exact resolve eq1386 eq1224
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1224
  have eq1908 : x = (N.op x y) := by grind
  clear eq1906
  have eq1969 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq1386
       have i₂ := eq1908
       grind)
    | exact superpose eq1908 eq1386
    | exact resolve eq1386 eq1908
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1386 eq1908
  have eq1974 : False := by grind
  exact eq1974

/-- `Equation3678` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ x = (y ◇ x) ◇ (y ◇ y)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3678_StructuralFromFin_Equation3147 : Law3678.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3678 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3678.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op (N.op y x) (N.op y y)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq62 : (N.op x x) ≠ (N.op (N.op y x) (M.op y y)) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq64 : (N.op x x) ≠ (N.op (N.op y x) (M.op y y)) ∨ y = (M.op y y) := by grind
  clear eq62
  have eq68 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq71 : ∀ X0 : G, (N.op x x) ≠ (N.op (N.op y x) (M.op X0 X0)) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq64
       have i₂ := eq18 X0 y
       grind)
    | (have i₁ := eq64
       have i₂ := eq18 y x
       grind)
    | exact superpose eq18 eq64
    | exact resolve eq64 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq121 : (N.op x x) ≠ (M.op y y) ∨ y = (M.op y y) ∨ (N.op y x) = (M.op y y) := by
    first
    | (have i₁ := eq64
       have i₂ := eq32 (N.op y x) y
       grind)
    | exact superpose eq32 eq64
    | (have j1 := eq32 y y
       grind)
    | exact resolve eq64 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq135 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ (N.op y x) = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq121
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq121
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq121 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq121
  have eq136 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ (N.op y x) = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq135
  have eq137 : (N.op y x) = (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq136
       have r₂ := eq18 x y
       grind)
    | (have r₁ := eq136
       have r₂ := eq18 y x
       grind)
    | exact resolve eq136 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq136
  have eq143 : (N.op x x) ≠ (N.op (M.op y y) (M.op y y)) ∨ y = (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq64
       have i₂ := eq137
       grind)
    | exact superpose eq137 eq64
    | exact resolve eq64 eq137
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64 eq137
  have eq146 : (N.op x x) ≠ (N.op (M.op y y) (M.op y y)) ∨ y = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq143
  have eq147 : (N.op x x) ≠ (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq146
       have i₂ := eq68 y
       grind)
    | exact superpose eq68 eq146
    | exact resolve eq146 eq68
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq146
  have eq150 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq147
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq147
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq147 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq147
  have eq151 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq150
  have eq152 : y = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq151
       have r₂ := eq18 x y
       grind)
    | (have r₁ := eq151
       have r₂ := eq18 y x
       grind)
    | exact resolve eq151 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq151
  have eq168 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 y X0
       have i₂ := eq152
       grind)
    | exact superpose eq152 eq18
    | exact resolve eq18 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq170 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq32 X0 y
       have i₂ := eq152
       grind)
    | exact superpose eq152 eq32
    | (have j0 := eq32 x x
       grind)
    | exact resolve eq32 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq172 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq68 y
       have i₂ := eq152
       grind)
    | exact superpose eq152 eq68
    | exact resolve eq68 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq152
  have eq266 : ∀ X0 X1 : G, x = (M.op X0 X0) ∨ (M.op X1 X1) = y := by
    intro X0 X1
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq168 X1
       grind)
    | exact superpose eq168 eq18
    | (have j1 := eq168 X1
       grind)
    | exact resolve eq18 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq268 : ∀ X0 X1 : G, x = (N.op X0 x) ∨ (M.op X1 X1) = y ∨ x = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq168 X0
       grind)
    | exact superpose eq168 eq32
    | (have j0 := eq32 y X1
       have j1 := eq168 X1
       grind)
    | exact resolve eq32 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq269 : ∀ X0 X1 : G, x ≠ X0 ∨ (N.op X0 X0) = X0 ∨ (M.op X1 X1) = y := by
    intro X0 X1
    first
    | (have i₁ := eq51 x X0
       have i₂ := eq168 X1
       grind)
    | exact superpose eq168 eq51
    | (have j0 := eq51 X0 X0
       have j1 := eq168 X1
       grind)
    | (have r₁ := eq51 X0 y
       have r₂ := eq168 X0
       grind)
    | (have r₁ := eq51 x x
       have r₂ := eq168 X0
       grind)
    | exact resolve eq51 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq271 : ∀ X0 : G, (N.op x x) ≠ (N.op (N.op y x) x) ∨ x = y ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq71 x
       have i₂ := eq168 X0
       grind)
    | exact superpose eq168 eq71
    | (have j0 := eq71 X0
       have j1 := eq168 X0
       grind)
    | exact resolve eq71 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq71
  have eq273 : x ≠ y ∨ x = (M.op x x) := by grind
  have eq303 : (N.op x x) ≠ (N.op (N.op y x) y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq172
       grind)
    | exact superpose eq172 eq10
    | exact resolve eq10 eq172
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq172
  have eq469 : ∀ X1 : G, y = (N.op y y) ∨ (M.op X1 X1) = x := by
    intro X1
    first
    | (have i₁ := eq68 X1
       have i₂ := eq266 x X1
       grind)
    | exact superpose eq266 eq68
    | (have j1 := eq266 X1 X1
       grind)
    | exact resolve eq68 eq266
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq68 eq266
  have eq836 : (N.op x x) ≠ y ∨ x = (M.op x x) ∨ y = (N.op y x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq303
       have i₂ := eq170 (N.op y x)
       grind)
    | exact superpose eq170 eq303
    | (have j1 := eq170 (N.op y x)
       grind)
    | exact resolve eq303 eq170
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq170 eq303
  have eq838 : (N.op x x) ≠ y ∨ x = (M.op x x) ∨ y = (N.op y x) := by grind
  clear eq836
  have eq844 : ∀ X0 X1 : G, (N.op x x) ≠ (N.op x x) ∨ x = y ∨ y = (M.op X0 X0) ∨ (M.op X1 X1) = y ∨ x = y := by
    intro X0 X1
    first
    | (have i₁ := eq271 X0
       have i₂ := eq268 y X1
       grind)
    | exact superpose eq268 eq271
    | (have j0 := eq271 X0
       have j1 := eq268 y X0
       grind)
    | exact resolve eq271 eq268
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq268 eq271
  have eq848 : ∀ X0 X1 : G, (N.op x x) ≠ (N.op x x) ∨ x = y ∨ y = (M.op X0 X0) ∨ (M.op X1 X1) = y := by
    intro X0 X1
    first
    | (have j0 := eq844 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq844
  have eq849 : ∀ X0 X1 : G, y = (M.op X0 X0) ∨ (M.op X1 X1) = y ∨ x = y := by
    intro X0 X1
    first
    | (have j0 := eq848 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq848
  have eq1170 : ∀ X1 : G, x = y ∨ y = (N.op y y) ∨ (M.op X1 X1) = y ∨ x = y := by
    intro X1
    first
    | (have i₁ := eq469 x
       have i₂ := eq849 X1 x
       grind)
    | exact superpose eq849 eq469
    | (have j1 := eq849 X1 X1
       grind)
    | exact resolve eq469 eq849
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq469 eq849
  have eq1192 : ∀ X1 : G, x = y ∨ y = (N.op y y) ∨ (M.op X1 X1) = y := by
    intro X1
    first
    | (have j0 := eq1170 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1170
  have eq1216 : ∀ X1 : G, y = (N.op y y) ∨ (M.op X1 X1) = y := by
    intro X1
    first
    | (have j0 := eq1192 X1
       have j1 := eq269 y X1
       grind)
    | (have r₁ := eq1192 X1
       have r₂ := eq269 y X1
       grind)
    | exact resolve eq1192 eq269
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq269 eq1192
  have eq1222 : y = (N.op y y) := by
    first
    | (have j1 := eq51 x y
       grind)
    | (have r₁ := eq1216 x
       have r₂ := eq51 x y
       grind)
    | exact resolve eq1216 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51 eq1216
  have eq1268 : (N.op x x) ≠ (N.op (N.op y x) y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1222
       grind)
    | exact superpose eq1222 eq10
    | exact resolve eq10 eq1222
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1274 : y = (M.op y y) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq9 y y
       have i₂ := eq1222
       grind)
    | exact superpose eq1222 eq9
    | (have j0 := eq9 y y
       grind)
    | exact resolve eq9 eq1222
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1275 : y = (M.op y y) := by grind
  clear eq1274
  have eq1280 : y ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (N.op y x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq838
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq838
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq838 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq838
  have eq1281 : y ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (N.op y x) := by grind
  clear eq1280
  have eq1282 : y = (N.op y x) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1281
       have r₂ := eq168 x
       grind)
    | exact resolve eq1281 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1281
  have eq1299 : ∀ X0 : G, y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq18 y X0
       have i₂ := eq1275
       grind)
    | exact superpose eq1275 eq18
    | exact resolve eq18 eq1275
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18 eq1275
  have eq1854 : (N.op x x) ≠ (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1268
       have i₂ := eq1282
       grind)
    | exact superpose eq1282 eq1268
    | exact resolve eq1268 eq1282
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1268 eq1282
  have eq1874 : (N.op x x) ≠ y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1854
       have i₂ := eq1222
       grind)
    | exact superpose eq1222 eq1854
    | exact resolve eq1854 eq1222
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1854
  have eq1879 : (N.op x x) ≠ y ∨ x = y := by
    first
    | (have i₁ := eq1874
       have i₂ := eq1299 x
       grind)
    | exact superpose eq1299 eq1874
    | exact resolve eq1874 eq1299
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1874
  have eq1942 : y ≠ (M.op x x) ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1879
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq1879
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq1879 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1879
  have eq1943 : y ≠ (M.op x x) ∨ x = y ∨ x = (M.op x x) := by grind
  clear eq1942
  have eq1944 : x = y ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1943
       have r₂ := eq168 x
       grind)
    | exact resolve eq1943 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq168 eq1943
  have eq1945 : x = (M.op x x) := by
    first
    | (have r₁ := eq1944
       have r₂ := eq273
       grind)
    | exact resolve eq1944 eq273
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq273 eq1944
  have eq1946 : x = y := by
    first
    | (have i₁ := eq1945
       have i₂ := eq1299 x
       grind)
    | exact superpose eq1299 eq1945
    | exact resolve eq1945 eq1299
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1299 eq1945
  have eq2011 : (N.op x x) ≠ (N.op (N.op x x) (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1946
       grind)
    | exact superpose eq1946 eq10
    | exact resolve eq10 eq1946
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2064 : x = (N.op x x) := by
    first
    | (have i₁ := eq1222
       have i₂ := eq1946
       grind)
    | exact superpose eq1946 eq1222
    | exact resolve eq1222 eq1946
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1222 eq1946
  have eq2088 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq2011
       have i₂ := eq2064
       grind)
    | exact superpose eq2064 eq2011
    | exact resolve eq2011 eq2064
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2011
  have eq2090 : False := by grind
  exact eq2090

/-- `Equation3682` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ x = (y ◇ x) ◇ (z ◇ z)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation3682_StructuralFromFin_Equation3147 : Law3682.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law3682 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law3682.models_iff G N]
  intro x y z
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x x) ≠ (N.op (N.op y x) (N.op z z)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq63 : (N.op x x) ≠ (N.op (N.op y x) (M.op z z)) ∨ z = (M.op z z) ∨ z = (M.op z z) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 z z
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 z z
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq64 : (N.op x x) ≠ (N.op (N.op y x) (M.op z z)) ∨ z = (M.op z z) := by grind
  clear eq63
  have eq68 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq121 : (N.op x x) ≠ (M.op z z) ∨ z = (M.op z z) ∨ (N.op y x) = (M.op z z) := by
    first
    | (have i₁ := eq64
       have i₂ := eq32 (N.op y x) z
       grind)
    | exact superpose eq32 eq64
    | (have j1 := eq32 z z
       grind)
    | exact resolve eq64 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq135 : (M.op z z) ≠ (M.op x x) ∨ z = (M.op z z) ∨ (N.op y x) = (M.op z z) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq121
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq121
    | (have j1 := eq9 z z
       grind)
    | exact resolve eq121 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq136 : (M.op z z) ≠ (M.op x x) ∨ z = (M.op z z) ∨ (N.op y x) = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq135
  have eq137 : (N.op y x) = (M.op z z) ∨ z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq136
       have r₂ := eq18 x z
       grind)
    | (have r₁ := eq136
       have r₂ := eq18 z x
       grind)
    | exact resolve eq136 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq136
  have eq143 : (N.op x x) ≠ (N.op (M.op z z) (M.op z z)) ∨ z = (M.op z z) ∨ z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq64
       have i₂ := eq137
       grind)
    | exact superpose eq137 eq64
    | exact resolve eq64 eq137
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq137
  have eq146 : (N.op x x) ≠ (N.op (M.op z z) (M.op z z)) ∨ z = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq143
  have eq147 : (N.op x x) ≠ (M.op z z) ∨ z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq146
       have i₂ := eq68 z
       grind)
    | exact superpose eq68 eq146
    | exact resolve eq146 eq68
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq146
  have eq150 : (M.op z z) ≠ (M.op x x) ∨ z = (M.op z z) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq147
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq147
    | (have j1 := eq9 z z
       grind)
    | exact resolve eq147 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq147
  have eq151 : (M.op z z) ≠ (M.op x x) ∨ z = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq150
  have eq152 : z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq151
       have r₂ := eq18 x z
       grind)
    | (have r₁ := eq151
       have r₂ := eq18 z x
       grind)
    | exact resolve eq151 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq151
  have eq168 : ∀ X0 : G, z = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 z X0
       have i₂ := eq152
       grind)
    | exact superpose eq152 eq18
    | exact resolve eq18 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq170 : ∀ X0 : G, z = (N.op X0 z) ∨ z = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq32 X0 z
       have i₂ := eq152
       grind)
    | exact superpose eq152 eq32
    | (have j0 := eq32 x x
       grind)
    | exact resolve eq32 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq172 : z = (N.op z z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq68 z
       have i₂ := eq152
       grind)
    | exact superpose eq152 eq68
    | exact resolve eq68 eq152
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq152
  have eq266 : ∀ X0 X1 : G, x = (M.op X0 X0) ∨ (M.op X1 X1) = z := by
    intro X0 X1
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq168 X1
       grind)
    | exact superpose eq168 eq18
    | (have j1 := eq168 X1
       grind)
    | exact resolve eq18 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq270 : ∀ X0 : G, x = (N.op x x) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq68 x
       have i₂ := eq168 X0
       grind)
    | exact superpose eq168 eq68
    | (have j1 := eq168 X0
       grind)
    | exact resolve eq68 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq303 : (N.op x x) ≠ (N.op (N.op y x) z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq172
       grind)
    | exact superpose eq172 eq10
    | exact resolve eq10 eq172
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq508 : ∀ X0 : G, x ≠ (M.op z z) ∨ z = (M.op z z) ∨ (N.op y x) = (M.op z z) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq121
       have i₂ := eq270 X0
       grind)
    | exact superpose eq270 eq121
    | (have j1 := eq270 z
       grind)
    | exact resolve eq121 eq270
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq121
  have eq535 : ∀ X0 : G, (N.op y x) = (M.op z z) ∨ z = (M.op X0 X0) ∨ z = (M.op z z) := by
    intro X0
    first
    | (have j0 := eq508 X0
       have j1 := eq266 X0 X0
       grind)
    | (have r₁ := eq508 X0
       have r₂ := eq266 z x
       grind)
    | exact resolve eq508 eq266
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq266 eq508
  have eq831 : (N.op x x) ≠ z ∨ x = (M.op x x) ∨ (N.op y x) = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq303
       have i₂ := eq170 (N.op y x)
       grind)
    | exact superpose eq170 eq303
    | (have j1 := eq170 (N.op y x)
       grind)
    | exact resolve eq303 eq170
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq170
  have eq833 : (N.op x x) ≠ z ∨ x = (M.op x x) ∨ (N.op y x) = z := by grind
  clear eq831
  have eq1195 : z ≠ (M.op x x) ∨ x = (M.op x x) ∨ (N.op y x) = z ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq833
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq833
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq833 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq833
  have eq1196 : z ≠ (M.op x x) ∨ x = (M.op x x) ∨ (N.op y x) = z := by grind
  clear eq1195
  have eq1197 : (N.op y x) = z ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1196
       have r₂ := eq168 x
       grind)
    | exact resolve eq1196 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1196
  have eq1210 : (N.op x x) ≠ (N.op z z) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq303
       have i₂ := eq1197
       grind)
    | exact superpose eq1197 eq303
    | exact resolve eq303 eq1197
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq303 eq1197
  have eq1215 : (N.op x x) ≠ (N.op z z) ∨ x = (M.op x x) := by grind
  clear eq1210
  have eq1225 : (N.op x x) ≠ z ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1215
       have i₂ := eq172
       grind)
    | exact superpose eq172 eq1215
    | exact resolve eq1215 eq172
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq172 eq1215
  have eq1228 : (N.op x x) ≠ z ∨ x = (M.op x x) := by grind
  clear eq1225
  have eq1264 : z ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1228
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq1228
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq1228 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1228
  have eq1265 : z ≠ (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq1264
  have eq1266 : x = (M.op x x) := by
    first
    | (have r₁ := eq1265
       have r₂ := eq168 x
       grind)
    | exact resolve eq1265 eq168
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq168 eq1265
  have eq1281 : ∀ X0 : G, x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq1266
       grind)
    | exact superpose eq1266 eq18
    | exact resolve eq18 eq1266
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq1283 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq1266
       grind)
    | exact superpose eq1266 eq32
    | exact resolve eq32 eq1266
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq1285 : x = (N.op x x) := by
    first
    | (have i₁ := eq68 x
       have i₂ := eq1266
       grind)
    | exact superpose eq1266 eq68
    | exact resolve eq68 eq1266
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1266
  have eq1539 : ∀ X0 : G, (N.op x x) ≠ (N.op (M.op z z) (M.op z z)) ∨ z = (M.op z z) ∨ z = (M.op X0 X0) ∨ z = (M.op z z) := by
    intro X0
    first
    | (have i₁ := eq64
       have i₂ := eq535 X0
       grind)
    | exact superpose eq535 eq64
    | (have j1 := eq535 z
       grind)
    | exact resolve eq64 eq535
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64 eq535
  have eq1577 : ∀ X0 : G, (N.op x x) ≠ (N.op (M.op z z) (M.op z z)) ∨ z = (M.op z z) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq1539 z
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1539
  have eq1592 : ∀ X0 : G, (N.op x x) ≠ (M.op z z) ∨ z = (M.op z z) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq1577 X0
       have i₂ := eq68 z
       grind)
    | exact superpose eq68 eq1577
    | (have j0 := eq1577 z
       grind)
    | exact resolve eq1577 eq68
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq68 eq1577
  have eq1607 : ∀ X0 : G, x ≠ (N.op x x) ∨ z = (M.op z z) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq1592 X0
       have i₂ := eq1281 z
       grind)
    | exact superpose eq1281 eq1592
    | (have j0 := eq1592 z
       grind)
    | exact resolve eq1592 eq1281
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1592
  have eq1615 : ∀ X0 : G, z = (M.op z z) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq1607 z
       have j1 := eq270 z
       grind)
    | (have r₁ := eq1607 X0
       have r₂ := eq270 X0
       grind)
    | exact resolve eq1607 eq270
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq270 eq1607
  have eq1619 : ∀ X0 : G, x = z ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq1615 X0
       have i₂ := eq1281 z
       grind)
    | exact superpose eq1281 eq1615
    | (have j0 := eq1615 X0
       grind)
    | exact resolve eq1615 eq1281
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1615
  have eq1622 : x = z ∨ x = z := by
    first
    | (have i₁ := eq1619 x
       have i₂ := eq1281 x
       grind)
    | exact superpose eq1281 eq1619
    | exact resolve eq1619 eq1281
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1281 eq1619
  have eq1623 : x = z := by grind
  clear eq1622
  have eq1624 : (N.op x x) ≠ (N.op (N.op y x) (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1623
       grind)
    | exact superpose eq1623 eq10
    | exact resolve eq10 eq1623
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1623
  have eq1679 : x ≠ (N.op (N.op y x) x) := by
    first
    | (have i₁ := eq1624
       have i₂ := eq1285
       grind)
    | exact superpose eq1285 eq1624
    | exact resolve eq1624 eq1285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1624
  have eq2280 : x ≠ (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq1679
       have i₂ := eq1283 y
       grind)
    | exact superpose eq1283 eq1679
    | (have j1 := eq1283 y
       grind)
    | (have r₁ := eq1679
       have r₂ := eq1283 (N.op y x)
       grind)
    | (have r₁ := eq1679
       have r₂ := eq1283 (N.op (N.op y x) x)
       grind)
    | exact resolve eq1679 eq1283
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1283
  have eq2286 : x = y := by
    first
    | (have r₁ := eq2280
       have r₂ := eq1285
       grind)
    | exact resolve eq2280 eq1285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2280
  have eq2356 : x ≠ (N.op (N.op x x) x) := by
    first
    | (have i₁ := eq1679
       have i₂ := eq2286
       grind)
    | exact superpose eq2286 eq1679
    | exact resolve eq1679 eq2286
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1679 eq2286
  have eq2363 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq2356
       have i₂ := eq1285
       grind)
    | exact superpose eq1285 eq2356
    | exact resolve eq2356 eq1285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2356
  have eq2369 : False := by grind
  exact eq2369

/-- `Equation4272` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ (x ◇ x) = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation4272_StructuralFromFin_Equation3147 : Law4272.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law4272 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law4272.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x (N.op x x)) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq61 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq63 : (N.op x (M.op x x)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq61
  have eq67 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq116 : (M.op x x) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq63
       have i₂ := eq32 y x
       grind)
    | exact superpose eq32 eq63
    | (have j1 := eq32 x x
       grind)
    | exact resolve eq63 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq119 : y = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq32 y x
       grind)
    | (have r₁ := eq116
       have r₂ := eq32 x x
       grind)
    | (have r₁ := eq116
       have r₂ := eq32 (N.op x (M.op x x)) x
       grind)
    | exact resolve eq116 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq116
  have eq122 : (N.op x y) ≠ (N.op y y) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq63
       have i₂ := eq119
       grind)
    | exact superpose eq119 eq63
    | exact resolve eq63 eq119
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq63
  have eq131 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq119
       grind)
    | exact superpose eq119 eq18
    | exact resolve eq18 eq119
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq133 : ∀ X0 : G, y = (N.op X0 y) ∨ y = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq119
       grind)
    | exact superpose eq119 eq32
    | (have j0 := eq32 x x
       grind)
    | exact resolve eq32 eq119
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq135 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq67 x
       have i₂ := eq119
       grind)
    | exact superpose eq119 eq67
    | exact resolve eq67 eq119
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq136 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq119
  have eq138 : (N.op x y) ≠ (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq122
       have r₂ := eq136
       grind)
    | exact resolve eq122 eq136
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq122
  have eq234 : ∀ X0 X1 : G, x = (N.op X0 x) ∨ (M.op X1 X1) = y ∨ x = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq131 X0
       grind)
    | exact superpose eq131 eq32
    | (have j0 := eq32 y X1
       have j1 := eq131 X1
       grind)
    | exact resolve eq32 eq131
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32 eq131
  have eq248 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq138
       have i₂ := eq135
       grind)
    | exact superpose eq135 eq138
    | exact resolve eq138 eq135
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq135 eq138
  have eq251 : y ≠ (N.op x y) ∨ x = (M.op x x) := by grind
  clear eq248
  have eq546 : y ≠ y ∨ x = (M.op x x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq251
       have i₂ := eq133 x
       grind)
    | exact superpose eq133 eq251
    | (have j1 := eq133 x
       grind)
    | (have r₁ := eq251
       have r₂ := eq133 x
       grind)
    | (have r₁ := eq251
       have r₂ := eq133 (N.op x y)
       grind)
    | exact resolve eq251 eq133
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq133 eq251
  have eq548 : y ≠ y ∨ x = (M.op x x) ∨ x = y := by grind
  clear eq546
  have eq549 : x = (M.op x x) ∨ x = y := by grind
  clear eq548
  have eq550 : x = (M.op x x) := by
    first
    | (have r₁ := eq549
       have r₂ := eq136
       grind)
    | exact resolve eq549 eq136
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq136 eq549
  have eq569 : ∀ X0 : G, x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq550
       grind)
    | exact superpose eq550 eq18
    | exact resolve eq18 eq550
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq573 : x = (N.op x x) := by
    first
    | (have i₁ := eq67 x
       have i₂ := eq550
       grind)
    | exact superpose eq550 eq67
    | exact resolve eq67 eq550
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq67 eq550
  have eq697 : (N.op x x) ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq573
       grind)
    | exact superpose eq573 eq10
    | exact resolve eq10 eq573
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq702 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq697
       have i₂ := eq573
       grind)
    | exact superpose eq573 eq697
    | exact resolve eq697 eq573
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq573 eq697
  have eq715 : ∀ X0 : G, x ≠ x ∨ y = (M.op X0 X0) ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq702
       have i₂ := eq234 y X0
       grind)
    | exact superpose eq234 eq702
    | (have j1 := eq234 y X0
       grind)
    | (have r₁ := eq702
       have r₂ := eq234 y x
       grind)
    | (have r₁ := eq702
       have r₂ := eq234 (N.op y x) x
       grind)
    | exact resolve eq702 eq234
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq234 eq702
  have eq741 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = y := by
    intro X0
    first
    | (have j0 := eq715 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq715
  have eq744 : x = y ∨ x = y := by
    first
    | (have i₁ := eq741 x
       have i₂ := eq569 x
       grind)
    | exact superpose eq569 eq741
    | exact resolve eq741 eq569
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq569 eq741
  have eq745 : x = y := by grind
  clear eq744
  have eq746 : (N.op x (N.op x x)) ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq745
       grind)
    | exact superpose eq745 eq10
    | exact resolve eq10 eq745
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq745
  have eq775 : False := by grind
  exact eq775

/-- `Equation4276` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ (x ◇ x) = y ◇ (y ◇ y)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation4276_StructuralFromFin_Equation3147 : Law4276.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law4276 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law4276.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x (N.op x x)) ≠ (N.op y (N.op y y)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq61 : (N.op x (N.op x x)) ≠ (N.op y (M.op y y)) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq63 : (N.op x (N.op x x)) ≠ (N.op y (M.op y y)) ∨ y = (M.op y y) := by grind
  clear eq61
  have eq67 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq69 : ∀ X0 : G, (N.op x (N.op x x)) ≠ (N.op y (M.op X0 X0)) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq63
       have i₂ := eq18 X0 y
       grind)
    | (have i₁ := eq63
       have i₂ := eq18 y x
       grind)
    | exact superpose eq18 eq63
    | exact resolve eq63 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq116 : (N.op x (N.op x x)) ≠ (M.op y y) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq63
       have i₂ := eq32 y y
       grind)
    | exact superpose eq32 eq63
    | (have j1 := eq32 y y
       grind)
    | exact resolve eq63 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq63
  have eq117 : (N.op x (N.op x x)) ≠ (M.op y y) ∨ y = (M.op y y) := by grind
  clear eq116
  have eq120 : (M.op y y) ≠ (N.op x (M.op x x)) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq117
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq117
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq117 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq117
  have eq122 : (M.op y y) ≠ (N.op x (M.op x x)) ∨ y = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq120
  have eq145 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq122
       have i₂ := eq32 x x
       grind)
    | exact superpose eq32 eq122
    | (have j1 := eq32 y y
       grind)
    | (have r₁ := eq122
       have r₂ := eq32 (N.op x (M.op x x)) y
       grind)
    | exact resolve eq122 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq122
  have eq147 : (M.op y y) ≠ (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq145
  have eq148 : y = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq147
       have r₂ := eq18 x y
       grind)
    | (have r₁ := eq147
       have r₂ := eq18 y x
       grind)
    | exact resolve eq147 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq147
  have eq160 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 y X0
       have i₂ := eq148
       grind)
    | exact superpose eq148 eq18
    | exact resolve eq18 eq148
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq164 : y = (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq67 y
       have i₂ := eq148
       grind)
    | exact superpose eq148 eq67
    | exact resolve eq67 eq148
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq148
  have eq233 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq67 x
       have i₂ := eq160 X0
       grind)
    | exact superpose eq160 eq67
    | (have j1 := eq160 X0
       grind)
    | exact resolve eq67 eq160
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq262 : (N.op x (N.op x x)) ≠ (N.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq164
       grind)
    | exact superpose eq164 eq10
    | exact resolve eq10 eq164
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq445 : y = (N.op y y) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq67 x
       have i₂ := eq233 x
       grind)
    | exact superpose eq233 eq67
    | exact resolve eq67 eq233
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq67
  have eq740 : (N.op y y) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq262
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq262
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq262 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq262
  have eq742 : (N.op y y) ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq740
  have eq972 : (N.op y y) ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq742
       have i₂ := eq32 x x
       grind)
    | exact superpose eq32 eq742
    | (have j1 := eq32 x x
       grind)
    | exact resolve eq742 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq742
  have eq974 : (N.op y y) ≠ (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq972
  have eq976 : y ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq974
       have i₂ := eq445
       grind)
    | exact superpose eq445 eq974
    | exact resolve eq974 eq445
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq445
  have eq979 : y ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq974
       have i₂ := eq164
       grind)
    | exact superpose eq164 eq974
    | exact resolve eq974 eq164
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq164 eq974
  have eq982 : y ≠ (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq979
  have eq983 : x = (M.op x x) := by
    first
    | (have r₁ := eq982
       have r₂ := eq160 x
       grind)
    | exact resolve eq982 eq160
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq160 eq982
  have eq986 : y ≠ (M.op x x) ∨ x = (N.op x x) := by
    first
    | (have j1 := eq51 x y
       grind)
    | (have r₁ := eq976
       have r₂ := eq51 x x
       grind)
    | exact resolve eq976 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51 eq976
  have eq987 : x = (N.op x x) := by
    first
    | (have r₁ := eq986
       have r₂ := eq233 x
       grind)
    | exact resolve eq986 eq233
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq233 eq986
  have eq1037 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq983
       grind)
    | exact superpose eq983 eq32
    | exact resolve eq32 eq983
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq1040 : (N.op x (N.op x x)) ≠ (N.op y x) ∨ x = y := by
    first
    | (have i₁ := eq69 x
       have i₂ := eq983
       grind)
    | exact superpose eq983 eq69
    | exact resolve eq69 eq983
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq69 eq983
  have eq1046 : (N.op x x) ≠ (N.op y x) ∨ x = y := by
    first
    | (have i₁ := eq1040
       have i₂ := eq987
       grind)
    | exact superpose eq987 eq1040
    | exact resolve eq1040 eq987
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1040
  have eq1048 : x ≠ (N.op y x) ∨ x = y := by
    first
    | (have i₁ := eq1046
       have i₂ := eq987
       grind)
    | exact superpose eq987 eq1046
    | exact resolve eq1046 eq987
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq987 eq1046
  have eq1049 : x = y := by
    first
    | (have j1 := eq1037 y
       grind)
    | (have r₁ := eq1048
       have r₂ := eq1037 y
       grind)
    | (have r₁ := eq1048
       have r₂ := eq1037 (N.op y x)
       grind)
    | exact resolve eq1048 eq1037
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1037 eq1048
  have eq1056 : (N.op x (N.op x x)) ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1049
       grind)
    | exact superpose eq1049 eq10
    | exact resolve eq10 eq1049
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1049
  have eq1098 : False := by grind
  exact eq1098

/-- `Equation4280` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ (x ◇ x) = y ◇ (z ◇ z)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation4280_StructuralFromFin_Equation3147 : Law4280.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law4280 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law4280.models_iff G N]
  intro x y z
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x (N.op x x)) ≠ (N.op y (N.op z z)) := by grind
  clear hm ht hu hv nh hM
  have eq11 : ∀ X0 X1 X2 : G, (M.op (M.op X1 X1) X0) = (M.op (M.op X0 X2) X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 (M.op (M.op X1 X1) X0) (M.op (M.op X1 X1) X0) X2
       have i₂ := eq6 X0 X1 (M.op (M.op X1 X1) X0)
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X2 : G, (M.op (M.op X0 X2) X2) = X0 := by
    intro X0 X2
    first
    | (have i₁ := eq11 X0 x X2
       have i₂ := eq12 x X0
       grind)
    | exact superpose eq12 eq11
    | exact resolve eq11 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq11
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq28 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (N.op X1 (M.op X0 X1)) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 (M.op X0 X1)
       have i₂ := eq14 X0 X1
       grind)
    | exact superpose eq14 eq7
    | (have j0 := eq7 X1 X0
       grind)
    | exact resolve eq7 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq61 : (N.op x (N.op x x)) ≠ (N.op y (M.op z z)) ∨ z = (M.op z z) ∨ z = (M.op z z) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 z z
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 z z
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq63 : (N.op x (N.op x x)) ≠ (N.op y (M.op z z)) ∨ z = (M.op z z) := by grind
  clear eq61
  have eq67 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq69 : ∀ X0 : G, (N.op x (N.op x x)) ≠ (N.op y (M.op X0 X0)) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq63
       have i₂ := eq18 X0 z
       grind)
    | (have i₁ := eq63
       have i₂ := eq18 z x
       grind)
    | exact superpose eq18 eq63
    | exact resolve eq63 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq116 : (N.op x (N.op x x)) ≠ (M.op z z) ∨ z = (M.op z z) ∨ y = (M.op z z) := by
    first
    | (have i₁ := eq63
       have i₂ := eq32 y z
       grind)
    | exact superpose eq32 eq63
    | (have j1 := eq32 z z
       grind)
    | exact resolve eq63 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq63
  have eq122 : ∀ X0 : G, (N.op x (N.op x x)) ≠ (M.op X0 X0) ∨ z = (M.op X0 X0) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq69 X0
       have i₂ := eq32 y X0
       grind)
    | exact superpose eq32 eq69
    | (have j0 := eq69 X0
       have j1 := eq32 z X0
       grind)
    | exact resolve eq69 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq125 : (M.op z z) ≠ (N.op x (M.op x x)) ∨ z = (M.op z z) ∨ y = (M.op z z) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq116
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq116
    | (have j1 := eq9 z z
       grind)
    | exact resolve eq116 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq116
  have eq127 : (M.op z z) ≠ (N.op x (M.op x x)) ∨ z = (M.op z z) ∨ y = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq125
  have eq143 : (M.op z z) ≠ (M.op x x) ∨ z = (M.op z z) ∨ y = (M.op z z) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq127
       have i₂ := eq32 x x
       grind)
    | exact superpose eq32 eq127
    | (have j1 := eq32 z z
       grind)
    | (have r₁ := eq127
       have r₂ := eq32 (N.op x (M.op x x)) z
       grind)
    | exact resolve eq127 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq127
  have eq145 : (M.op z z) ≠ (M.op x x) ∨ z = (M.op z z) ∨ y = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq143
  have eq146 : y = (M.op z z) ∨ z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq145
       have r₂ := eq18 x z
       grind)
    | (have r₁ := eq145
       have r₂ := eq18 z x
       grind)
    | exact resolve eq145 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq145
  have eq150 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq146
       have i₂ := eq18 X0 z
       grind)
    | (have i₁ := eq146
       have i₂ := eq18 z x
       grind)
    | exact superpose eq18 eq146
    | exact resolve eq146 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq160 : ∀ X0 : G, z = (M.op z z) ∨ y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 z X0
       have i₂ := eq146
       grind)
    | exact superpose eq146 eq18
    | exact resolve eq18 eq146
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq164 : y = (N.op y y) ∨ z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq67 z
       have i₂ := eq146
       grind)
    | exact superpose eq146 eq67
    | exact resolve eq67 eq146
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq165 : (N.op x (N.op x x)) ≠ (N.op y y) ∨ y = z ∨ z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq69 z
       have i₂ := eq146
       grind)
    | exact superpose eq146 eq69
    | (have j0 := eq69 z
       grind)
    | exact resolve eq69 eq146
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq166 : y ≠ z ∨ z = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq146
  have eq167 : (N.op x (N.op x x)) ≠ (N.op y y) ∨ z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq165
       have r₂ := eq166
       grind)
    | exact resolve eq165 eq166
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq165 eq166
  have eq196 : ∀ X0 X1 : G, y = (M.op X0 X0) ∨ x = (M.op x x) ∨ (M.op X1 X1) = z := by
    intro X0 X1
    first
    | (have i₁ := eq18 X1 X0
       have i₂ := eq150 X1
       grind)
    | exact superpose eq150 eq18
    | (have j1 := eq150 X0
       grind)
    | exact resolve eq18 eq150
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq200 : ∀ X0 : G, z = (M.op X0 X0) ∨ y = (N.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq67 X0
       have i₂ := eq150 X0
       grind)
    | exact superpose eq150 eq67
    | (have j1 := eq150 X0
       grind)
    | exact resolve eq67 eq150
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq219 : ∀ X0 X1 : G, x = (M.op X0 X0) ∨ (M.op X1 X1) = y ∨ (M.op X1 X1) = z := by
    intro X0 X1
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq150 X1
       grind)
    | exact superpose eq150 eq18
    | (have j1 := eq150 X1
       grind)
    | exact resolve eq18 eq150
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq223 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (N.op x x) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq67 x
       have i₂ := eq150 X0
       grind)
    | exact superpose eq150 eq67
    | (have j1 := eq150 X0
       grind)
    | exact resolve eq67 eq150
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq150
  have eq301 : ∀ X0 : G, z = (N.op z z) ∨ y = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq67 z
       have i₂ := eq160 X0
       grind)
    | exact superpose eq160 eq67
    | (have j1 := eq160 X0
       grind)
    | exact resolve eq67 eq160
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq465 : ∀ X0 X1 X2 : G, x ≠ X0 ∨ (N.op X0 X0) = X0 ∨ (M.op X1 X1) = y ∨ z = (M.op X2 X2) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq51 x X0
       have i₂ := eq196 X1 X2
       grind)
    | exact superpose eq196 eq51
    | (have j0 := eq51 X0 X0
       have j1 := eq196 X1 X2
       grind)
    | (have r₁ := eq51 X0 y
       have r₂ := eq196 X0 X1
       grind)
    | (have r₁ := eq51 x x
       have r₂ := eq196 X0 X1
       grind)
    | (have r₁ := eq51 X1 z
       have r₂ := eq196 X0 X1
       grind)
    | exact resolve eq51 eq196
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq196
  have eq536 : (N.op x (M.op x x)) ≠ (N.op y y) ∨ z = (M.op z z) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq167
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq167
    | (have j1 := eq9 z z
       grind)
    | exact resolve eq167 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq167
  have eq538 : (N.op x (M.op x x)) ≠ (N.op y y) ∨ z = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq536
  have eq558 : y = (N.op y y) ∨ z = (N.op z z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq67 x
       have i₂ := eq200 x
       grind)
    | exact superpose eq200 eq67
    | exact resolve eq67 eq200
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq200
  have eq762 : ∀ X0 X1 : G, y = (N.op y y) ∨ (M.op X1 X1) = x ∨ z = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq67 X1
       have i₂ := eq219 X0 X1
       grind)
    | exact superpose eq219 eq67
    | (have j1 := eq219 X1 X0
       grind)
    | exact resolve eq67 eq219
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq219
  have eq843 : ∀ X0 X1 : G, (N.op x x) ≠ (M.op X0 X0) ∨ z = (M.op X0 X0) ∨ y = (M.op X0 X0) ∨ (M.op X1 X1) = y ∨ (M.op X1 X1) = z := by
    intro X0 X1
    first
    | (have i₁ := eq122 X0
       have i₂ := eq223 X1
       grind)
    | exact superpose eq223 eq122
    | (have j0 := eq122 X0
       have j1 := eq223 X0
       grind)
    | exact resolve eq122 eq223
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq122 eq223
  have eq1950 : (M.op x x) ≠ (N.op y y) ∨ z = (M.op z z) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq538
       have i₂ := eq32 x x
       grind)
    | exact superpose eq32 eq538
    | (have j1 := eq32 z z
       grind)
    | exact resolve eq538 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq538
  have eq1952 : (M.op x x) ≠ (N.op y y) ∨ z = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq1950
  have eq2308 : ∀ X0 : G, x ≠ z ∨ y = (N.op y y) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq762 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq3899 : y ≠ (M.op x x) ∨ z = (M.op z z) ∨ x = (M.op x x) ∨ z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1952
       have i₂ := eq164
       grind)
    | exact superpose eq164 eq1952
    | exact resolve eq1952 eq164
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq164
  have eq3902 : y ≠ (M.op x x) ∨ z = (M.op z z) ∨ x = (M.op x x) ∨ z = (N.op z z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq1952
       have i₂ := eq558
       grind)
    | exact superpose eq558 eq1952
    | exact resolve eq1952 eq558
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq558 eq1952
  have eq3908 : y ≠ (M.op x x) ∨ z = (M.op z z) ∨ x = (M.op x x) ∨ z = (N.op z z) := by grind
  clear eq3902
  have eq3911 : y ≠ (M.op x x) ∨ z = (M.op z z) ∨ x = (M.op x x) := by grind
  clear eq3899
  have eq3915 : y ≠ (M.op x x) ∨ x = (M.op x x) ∨ z = (N.op z z) := by
    first
    | (have j1 := eq51 x y
       grind)
    | (have r₁ := eq3908
       have r₂ := eq51 z z
       grind)
    | (have r₁ := eq3908
       have r₂ := eq51 x x
       grind)
    | exact resolve eq3908 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3908
  have eq3918 : z = (M.op z z) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq3911
       have r₂ := eq160 x
       grind)
    | exact resolve eq3911 eq160
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq160 eq3911
  have eq3920 : z = (N.op z z) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq3915
       have r₂ := eq301 x
       grind)
    | exact resolve eq3915 eq301
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq301 eq3915
  have eq3945 : ∀ X0 : G, z = (M.op X0 X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq18 z X0
       have i₂ := eq3918
       grind)
    | exact superpose eq3918 eq18
    | exact resolve eq18 eq3918
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq3947 : ∀ X0 : G, z = (N.op X0 z) ∨ z = X0 ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq32 X0 z
       have i₂ := eq3918
       grind)
    | exact superpose eq3918 eq32
    | (have j0 := eq32 x x
       grind)
    | exact resolve eq32 eq3918
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3918
  have eq4036 : (N.op x (N.op x x)) ≠ (N.op y z) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq3920
       grind)
    | exact superpose eq3920 eq10
    | exact resolve eq10 eq3920
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq4405 : ∀ X0 X1 : G, (M.op x X0) = X0 ∨ (M.op X1 X1) = z := by
    intro X0 X1
    first
    | (have i₁ := eq12 x X0
       have i₂ := eq3945 X1
       grind)
    | exact superpose eq3945 eq12
    | (have j1 := eq3945 X1
       grind)
    | exact resolve eq12 eq3945
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq4408 : ∀ X0 X1 : G, x = (M.op X0 X0) ∨ (M.op X1 X1) = z := by
    intro X0 X1
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq3945 X1
       grind)
    | exact superpose eq3945 eq18
    | (have j1 := eq3945 X1
       grind)
    | exact resolve eq18 eq3945
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq4412 : ∀ X0 : G, x = (N.op x x) ∨ z = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq67 x
       have i₂ := eq3945 X0
       grind)
    | exact superpose eq3945 eq67
    | (have j1 := eq3945 X0
       grind)
    | exact resolve eq67 eq3945
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq4420 : x ≠ z ∨ x = (M.op x x) := by grind
  have eq4580 : ∀ X1 : G, z = (N.op z z) ∨ (M.op x X1) = X1 := by
    intro X1
    first
    | (have i₁ := eq67 X1
       have i₂ := eq4405 x X1
       grind)
    | exact superpose eq4405 eq67
    | (have j1 := eq4405 X1 X1
       grind)
    | exact resolve eq67 eq4405
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4405
  have eq4844 : ∀ X1 X2 : G, z ≠ X1 ∨ (N.op X1 X1) = X1 ∨ x = (M.op X2 X2) := by
    intro X1 X2
    first
    | (have i₁ := eq51 x X1
       have i₂ := eq4408 X2 x
       grind)
    | exact superpose eq4408 eq51
    | (have j0 := eq51 x X1
       have j1 := eq4408 X2 X1
       grind)
    | (have r₁ := eq51 x x
       have r₂ := eq4408 x X1
       grind)
    | (have r₁ := eq51 X1 z
       have r₂ := eq4408 x X1
       grind)
    | exact resolve eq51 eq4408
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq4845 : ∀ X1 : G, z = (N.op z z) ∨ (M.op X1 X1) = x := by
    intro X1
    first
    | (have i₁ := eq67 X1
       have i₂ := eq4408 x X1
       grind)
    | exact superpose eq4408 eq67
    | (have j1 := eq4408 X1 X1
       grind)
    | exact resolve eq67 eq4408
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4408
  have eq5201 : z = (N.op z z) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq67 x
       have i₂ := eq4412 x
       grind)
    | exact superpose eq4412 eq67
    | exact resolve eq67 eq4412
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq67
  have eq5332 : ∀ X0 : G, x ≠ X0 ∨ (N.op X0 X0) = X0 ∨ z = (N.op z z) := by
    intro X0
    first
    | (have i₁ := eq28 x x
       have i₂ := eq4580 x
       grind)
    | exact superpose eq4580 eq28
    | (have r₁ := eq28 x x
       have r₂ := eq4580 x
       grind)
    | exact resolve eq28 eq4580
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq28 eq4580
  have eq7120 : (N.op x (M.op x x)) ≠ (N.op y z) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq4036
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq4036
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq4036 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4036
  have eq7122 : (N.op x (M.op x x)) ≠ (N.op y z) ∨ x = (M.op x x) := by grind
  clear eq7120
  have eq7138 : (M.op x x) ≠ (N.op y z) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq7122
       have i₂ := eq32 x x
       grind)
    | exact superpose eq32 eq7122
    | (have j1 := eq32 x x
       grind)
    | exact resolve eq7122 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7122
  have eq7140 : (M.op x x) ≠ (N.op y z) ∨ x = (M.op x x) := by grind
  clear eq7138
  have eq7147 : z ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = z ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq7140
       have i₂ := eq3947 y
       grind)
    | exact superpose eq3947 eq7140
    | (have j1 := eq3947 y
       grind)
    | exact resolve eq7140 eq3947
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3947
  have eq7152 : z ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = z := by grind
  clear eq7147
  have eq7159 : x = (M.op x x) ∨ y = z := by
    first
    | (have r₁ := eq7152
       have r₂ := eq3945 x
       grind)
    | exact resolve eq7152 eq3945
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7152
  have eq7292 : ∀ X0 : G, x = (M.op X0 X0) ∨ y = z := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq7159
       grind)
    | exact superpose eq7159 eq18
    | exact resolve eq18 eq7159
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq7294 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 ∨ y = z := by
    intro X0
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq7159
       grind)
    | exact superpose eq7159 eq32
    | exact resolve eq32 eq7159
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32 eq7159
  have eq7493 : ∀ X1 : G, x = z ∨ y = (N.op y y) ∨ (M.op X1 X1) = x ∨ y = z := by
    intro X1
    first
    | (have i₁ := eq762 x X1
       have i₂ := eq7292 x
       grind)
    | exact superpose eq7292 eq762
    | (have j0 := eq762 x X1
       have j1 := eq7292 X1
       grind)
    | exact resolve eq762 eq7292
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq762
  have eq7553 : ∀ X1 : G, x = z ∨ y = (N.op y y) ∨ (M.op X1 X1) = x := by
    intro X1
    first
    | (have j0 := eq7493 X1
       have j1 := eq4844 y X1
       grind)
    | (have r₁ := eq7493 X1
       have r₂ := eq4844 x x
       grind)
    | (have r₁ := eq7493 X1
       have r₂ := eq4844 y x
       grind)
    | exact resolve eq7493 eq4844
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4844 eq7493
  have eq7562 : ∀ X1 : G, y = (N.op y y) ∨ (M.op X1 X1) = x := by
    intro X1
    first
    | (have j0 := eq7553 X1
       have j1 := eq2308 X1
       grind)
    | (have r₁ := eq7553 X1
       have r₂ := eq2308 x
       grind)
    | exact resolve eq7553 eq2308
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2308 eq7553
  have eq7631 : ∀ X0 : G, y = (M.op y y) ∨ y = (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq9 y y
       have i₂ := eq7562 X0
       grind)
    | exact superpose eq7562 eq9
    | (have j0 := eq9 y y
       have j1 := eq7562 X0
       grind)
    | exact resolve eq9 eq7562
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq7713 : ∀ X0 : G, y = (M.op y y) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq7631 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7631
  have eq7878 : ∀ X0 X1 : G, y = (M.op X0 X0) ∨ (M.op X1 X1) = x := by
    intro X0 X1
    first
    | (have i₁ := eq18 y X0
       have i₂ := eq7713 X1
       grind)
    | exact superpose eq7713 eq18
    | (have j1 := eq7713 X1
       grind)
    | exact resolve eq18 eq7713
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7713
  have eq9025 : ∀ X0 : G, x ≠ y ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq7878 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7878
  have eq12982 : ∀ X1 : G, x ≠ (N.op x x) ∨ x = z ∨ x = y ∨ (M.op X1 X1) = y ∨ (M.op X1 X1) = z ∨ z = (N.op z z) := by
    intro X1
    first
    | (have i₁ := eq843 X1 X1
       have i₂ := eq4845 X1
       grind)
    | exact superpose eq4845 eq843
    | (have j0 := eq843 X1 X1
       grind)
    | exact resolve eq843 eq4845
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq843
  have eq12998 : ∀ X1 : G, x = z ∨ x = y ∨ (M.op X1 X1) = y ∨ (M.op X1 X1) = z ∨ z = (N.op z z) := by
    intro X1
    first
    | (have j0 := eq12982 X1
       grind)
    | (have r₁ := eq12982 X1
       have r₂ := eq5201
       grind)
    | exact resolve eq12982 eq5201
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12982
  have eq13023 : ∀ X1 : G, x = y ∨ (M.op X1 X1) = y ∨ (M.op X1 X1) = z ∨ z = (N.op z z) := by
    intro X1
    first
    | (have j0 := eq12998 X1
       have j1 := eq465 z X1 X1
       grind)
    | (have r₁ := eq12998 X1
       have r₂ := eq465 z X1 x
       grind)
    | (have r₁ := eq12998 X1
       have r₂ := eq465 y X1 x
       grind)
    | exact resolve eq12998 eq465
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq465 eq12998
  have eq13027 : ∀ X1 : G, z = (N.op z z) ∨ (M.op X1 X1) = y ∨ x = y := by
    intro X1
    first
    | (have j0 := eq13023 X1
       have j1 := eq51 x z
       grind)
    | (have r₁ := eq13023 x
       have r₂ := eq51 x y
       grind)
    | (have r₁ := eq13023 x
       have r₂ := eq51 x z
       grind)
    | exact resolve eq13023 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13023
  have eq13159 : x = y ∨ z = (N.op z z) ∨ z = (N.op z z) ∨ x = y := by
    first
    | (have i₁ := eq4845 x
       have i₂ := eq13027 x
       grind)
    | exact superpose eq13027 eq4845
    | exact resolve eq4845 eq13027
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4845
  have eq13162 : x = y ∨ y = (N.op y y) ∨ z = (N.op z z) ∨ x = y := by
    first
    | (have i₁ := eq7562 x
       have i₂ := eq13027 x
       grind)
    | exact superpose eq13027 eq7562
    | exact resolve eq7562 eq13027
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13027
  have eq13189 : x = y ∨ y = (N.op y y) ∨ z = (N.op z z) := by grind
  clear eq13162
  have eq13192 : z = (N.op z z) ∨ x = y := by grind
  clear eq13159
  have eq13233 : y = (N.op y y) ∨ z = (N.op z z) := by
    first
    | (have j1 := eq5332 y
       grind)
    | (have r₁ := eq13189
       have r₂ := eq5332 y
       grind)
    | exact resolve eq13189 eq5332
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5332 eq13189
  have eq13420 : z = (M.op z z) ∨ z = (M.op z z) ∨ z = (M.op z z) ∨ x = y := by
    first
    | (have i₁ := eq9 z z
       have i₂ := eq13192
       grind)
    | exact superpose eq13192 eq9
    | (have j0 := eq9 z z
       grind)
    | exact resolve eq9 eq13192
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13192
  have eq13421 : z = (M.op z z) ∨ x = y := by grind
  clear eq13420
  have eq13592 : y = z ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq7292 z
       have i₂ := eq13421
       grind)
    | exact superpose eq13421 eq7292
    | exact resolve eq7292 eq13421
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7292 eq13421
  have eq14177 : (N.op z z) ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = z ∨ x = y := by
    first
    | (have i₁ := eq7140
       have i₂ := eq13592
       grind)
    | exact superpose eq13592 eq7140
    | exact resolve eq7140 eq13592
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7140 eq13592
  have eq14212 : (N.op z z) ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = y := by
    first
    | (have r₁ := eq14177
       have r₂ := eq4420
       grind)
    | exact resolve eq14177 eq4420
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4420 eq14177
  have eq14235 : (N.op z z) ≠ (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq9025 x
       grind)
    | (have r₁ := eq14212
       have r₂ := eq9025 x
       grind)
    | exact resolve eq14212 eq9025
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq9025 eq14212
  have eq14255 : y = (M.op y y) ∨ y = (M.op y y) ∨ y = (M.op y y) ∨ z = (N.op z z) := by
    first
    | (have i₁ := eq9 y y
       have i₂ := eq13233
       grind)
    | exact superpose eq13233 eq9
    | (have j0 := eq9 y y
       grind)
    | exact resolve eq9 eq13233
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13233
  have eq14256 : z = (N.op z z) ∨ y = (M.op y y) := by grind
  clear eq14255
  have eq14270 : z = (M.op z z) ∨ z = (M.op z z) ∨ z = (M.op z z) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq9 z z
       have i₂ := eq14256
       grind)
    | exact superpose eq14256 eq9
    | (have j0 := eq9 z z
       grind)
    | exact resolve eq9 eq14256
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14256
  have eq14271 : y = (M.op y y) ∨ z = (M.op z z) := by grind
  clear eq14270
  have eq14476 : ∀ X0 : G, y = (M.op X0 X0) ∨ z = (M.op z z) := by
    intro X0
    first
    | (have i₁ := eq18 y X0
       have i₂ := eq14271
       grind)
    | exact superpose eq14271 eq18
    | exact resolve eq18 eq14271
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14271
  have eq15173 : ∀ X0 X1 : G, z = (M.op X0 X0) ∨ (M.op X1 X1) = y := by
    intro X0 X1
    first
    | (have i₁ := eq18 z X0
       have i₂ := eq14476 X1
       grind)
    | exact superpose eq14476 eq18
    | (have j1 := eq14476 X1
       grind)
    | exact resolve eq18 eq14476
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14476
  have eq15778 : ∀ X1 : G, x = z ∨ y = (N.op y y) ∨ (M.op X1 X1) = y := by
    intro X1
    first
    | (have i₁ := eq7562 x
       have i₂ := eq15173 x X1
       grind)
    | exact superpose eq15173 eq7562
    | (have j1 := eq15173 x X1
       grind)
    | exact resolve eq7562 eq15173
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7562 eq15173
  have eq16057 : y = (N.op y y) ∨ x = z := by
    first
    | (have j1 := eq51 x y
       grind)
    | (have r₁ := eq15778 x
       have r₂ := eq51 x y
       grind)
    | exact resolve eq15778 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15778
  have eq16122 : y = (M.op y y) ∨ y = (M.op y y) ∨ y = (M.op y y) ∨ x = z := by
    first
    | (have i₁ := eq9 y y
       have i₂ := eq16057
       grind)
    | exact superpose eq16057 eq9
    | (have j0 := eq9 y y
       grind)
    | exact resolve eq9 eq16057
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq16057
  have eq16123 : y = (M.op y y) ∨ x = z := by grind
  clear eq16122
  have eq16201 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq18 y X0
       have i₂ := eq16123
       grind)
    | exact superpose eq16123 eq18
    | exact resolve eq18 eq16123
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq16123
  have eq20463 : z ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq14235
       have i₂ := eq3920
       grind)
    | exact superpose eq3920 eq14235
    | exact resolve eq14235 eq3920
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3920
  have eq20467 : z ≠ (M.op x x) ∨ x = (M.op x x) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq14235
       have i₂ := eq5201
       grind)
    | exact superpose eq5201 eq14235
    | exact resolve eq14235 eq5201
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq5201 eq14235
  have eq20477 : z ≠ (M.op x x) ∨ x = (M.op x x) := by grind
  clear eq20463
  have eq20484 : z ≠ (M.op x x) ∨ x = (N.op x x) := by
    first
    | (have j1 := eq51 x z
       grind)
    | (have r₁ := eq20467
       have r₂ := eq51 x x
       grind)
    | exact resolve eq20467 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51 eq20467
  have eq20488 : x = (M.op x x) := by
    first
    | (have r₁ := eq20477
       have r₂ := eq3945 x
       grind)
    | exact resolve eq20477 eq3945
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3945 eq20477
  have eq20491 : x = (N.op x x) := by
    first
    | (have r₁ := eq20484
       have r₂ := eq4412 x
       grind)
    | exact resolve eq20484 eq4412
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20484
  have eq20755 : ∀ X0 : G, x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq20488
       grind)
    | exact superpose eq20488 eq18
    | exact resolve eq18 eq20488
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq20779 : x = y ∨ x = z := by
    first
    | (have i₁ := eq16201 x
       have i₂ := eq20488
       grind)
    | exact superpose eq20488 eq16201
    | exact resolve eq16201 eq20488
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq16201 eq20488
  have eq21374 : ∀ X0 : G, (N.op x (N.op x x)) ≠ (N.op x (M.op X0 X0)) ∨ z = (M.op X0 X0) ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq69 X0
       have i₂ := eq20779
       grind)
    | exact superpose eq20779 eq69
    | (have j0 := eq69 X0
       grind)
    | exact resolve eq69 eq20779
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq69 eq20779
  have eq21558 : ∀ X0 : G, (N.op x x) ≠ (N.op x (N.op x x)) ∨ z = (M.op X0 X0) ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq21374 X0
       have i₂ := eq20755 X0
       grind)
    | exact superpose eq20755 eq21374
    | (have j0 := eq21374 X0
       grind)
    | exact resolve eq21374 eq20755
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21374
  have eq21563 : ∀ X0 : G, x ≠ (N.op x x) ∨ z = (M.op X0 X0) ∨ x = z := by
    intro X0
    first
    | (have i₁ := eq21558 X0
       have i₂ := eq20491
       grind)
    | exact superpose eq20491 eq21558
    | (have j0 := eq21558 X0
       grind)
    | exact resolve eq21558 eq20491
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21558
  have eq21566 : ∀ X0 : G, z = (M.op X0 X0) ∨ x = z := by
    intro X0
    first
    | (have j0 := eq21563 X0
       have j1 := eq4412 X0
       grind)
    | (have r₁ := eq21563 X0
       have r₂ := eq4412 X0
       grind)
    | exact resolve eq21563 eq4412
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4412 eq21563
  have eq21568 : x = z ∨ x = z := by
    first
    | (have i₁ := eq21566 x
       have i₂ := eq20755 x
       grind)
    | exact superpose eq20755 eq21566
    | exact resolve eq21566 eq20755
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq20755 eq21566
  have eq21569 : x = z := by grind
  clear eq21568
  have eq21767 : (N.op x (N.op x x)) ≠ (N.op y (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq21569
       grind)
    | exact superpose eq21569 eq10
    | exact resolve eq10 eq21569
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq21906 : (N.op x x) ≠ (N.op y x) := by
    first
    | (have i₁ := eq21767
       have i₂ := eq20491
       grind)
    | exact superpose eq20491 eq21767
    | exact resolve eq21767 eq20491
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21767
  have eq21908 : x ≠ (N.op y x) := by
    first
    | (have i₁ := eq21906
       have i₂ := eq20491
       grind)
    | exact superpose eq20491 eq21906
    | exact resolve eq21906 eq20491
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21906
  have eq22317 : x ≠ x ∨ x = y ∨ y = z := by
    first
    | (have i₁ := eq21908
       have i₂ := eq7294 y
       grind)
    | exact superpose eq7294 eq21908
    | (have j1 := eq7294 y
       grind)
    | (have r₁ := eq21908
       have r₂ := eq7294 y
       grind)
    | (have r₁ := eq21908
       have r₂ := eq7294 (N.op y x)
       grind)
    | exact resolve eq21908 eq7294
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq7294
  have eq22319 : x = y ∨ y = z := by grind
  clear eq22317
  have eq22327 : x = y ∨ x = y := by
    first
    | (have i₁ := eq22319
       have i₂ := eq21569
       grind)
    | exact superpose eq21569 eq22319
    | exact resolve eq22319 eq21569
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21569 eq22319
  have eq22328 : x = y := by grind
  clear eq22327
  have eq22703 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq21908
       have i₂ := eq22328
       grind)
    | exact superpose eq22328 eq21908
    | exact resolve eq21908 eq22328
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21908 eq22328
  have eq22720 : False := by grind
  exact eq22720

/-- `Equation4343` from `Equation3147`: `x = (((y ◇ y) ◇ x) ◇ z) ◇ z` ⊢ `x ◇ (y ◇ y) = y ◇ (x ◇ x)`, through the
companion `x □ y = if (M.op b a) = b then a else if (M.op b a) = a then b else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation4343_StructuralFromFin_Equation3147 : Law4343.StructuralFromFin Law3147 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_y_pyx_x_x_y_pyx_Equation3147 (fun {G} M hM ↦ ?_)
  have hm := (@Law3147.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = b ∨ (M.op b a) = a ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law4343 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 1) ((Lf 1) ⋆ (Lf 0)) (Lf 0) (Lf 0) (Lf 1) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law4343.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op (M.op (M.op X1 X1) X0) X2) X2) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (M.op X1 X0) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X0 ∨ (M.op X1 X0) = X1 := by
    intro X0 X1
    grind
  have eq10 : (N.op x (N.op y y)) ≠ (N.op y (N.op x x)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X0) X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X1 (M.op X0 X0) X1
       have i₂ := eq6 (M.op X0 X0) X0 X1
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 : G, (M.op X1 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq6 (M.op X0 X0) X0 X1
       have i₂ := eq12 (M.op X0 X0) X1
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq30 : ∀ X0 X1 : G, X0 ≠ X0 ∨ (M.op X1 X1) = X0 ∨ (M.op X1 X1) = (N.op X0 (M.op X1 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq8 X1 (M.op X0 X0)
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq8
    | (have j0 := eq8 X0 (M.op X1 X1)
       grind)
    | (have r₁ := eq8 X1 (M.op X0 X0)
       have r₂ := eq12 X0 X1
       grind)
    | exact resolve eq8 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq32 : ∀ X0 X1 : G, (M.op X1 X1) = (N.op X0 (M.op X1 X1)) ∨ (M.op X1 X1) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq30 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq30
  have eq51 : ∀ X0 X1 : G, (M.op X0 X0) ≠ X1 ∨ (N.op X1 X1) = X1 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X0 X1
       grind)
    | (have i₁ := eq7 X1 X1
       have i₂ := eq18 X1 X0
       grind)
    | exact superpose eq18 eq7
    | (have j0 := eq7 X1 X1
       grind)
    | (have r₁ := eq7 (M.op X0 X0) (M.op X0 X0)
       have r₂ := eq18 X0 (M.op X0 X0)
       grind)
    | (have r₁ := eq7 (M.op X1 X1) (M.op X1 X1)
       have r₂ := eq18 (M.op X1 X1) X1
       grind)
    | exact resolve eq7 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq61 : (N.op x (N.op y y)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq63 : (N.op x (N.op y y)) ≠ (N.op y (M.op x x)) ∨ x = (M.op x x) := by grind
  clear eq61
  have eq67 : ∀ X0 : G, (M.op X0 X0) = (N.op (M.op X0 X0) (M.op X0 X0)) := by
    intro X0
    first
    | (have j0 := eq51 X0 (M.op X0 X0)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq116 : (N.op x (N.op y y)) ≠ (M.op x x) ∨ x = (M.op x x) ∨ y = (M.op x x) := by
    first
    | (have i₁ := eq63
       have i₂ := eq32 y x
       grind)
    | exact superpose eq32 eq63
    | (have j1 := eq32 x x
       grind)
    | exact resolve eq63 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq125 : (M.op x x) ≠ (N.op x (M.op y y)) ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq116
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq116
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq116 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq116
  have eq127 : (M.op x x) ≠ (N.op x (M.op y y)) ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = (M.op y y) := by grind
  clear eq125
  have eq142 : (M.op x x) ≠ (M.op y y) ∨ x = (M.op x x) ∨ y = (M.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq127
       have i₂ := eq32 x y
       grind)
    | exact superpose eq32 eq127
    | (have j1 := eq32 x x
       grind)
    | (have r₁ := eq127
       have r₂ := eq32 (N.op x (M.op y y)) x
       grind)
    | exact resolve eq127 eq32
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq127
  have eq144 : y = (M.op y y) ∨ y = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    first
    | (have r₁ := eq142
       have r₂ := eq18 y x
       grind)
    | (have r₁ := eq142
       have r₂ := eq18 x y
       grind)
    | exact resolve eq142 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq142
  have eq163 : y = (N.op y y) ∨ y = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq67 y
       have i₂ := eq144
       grind)
    | exact superpose eq144 eq67
    | exact resolve eq67 eq144
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq144
  have eq169 : y = (N.op y y) ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    first
    | (have j1 := eq51 x y
       grind)
    | (have r₁ := eq163
       have r₂ := eq51 x y
       grind)
    | (have r₁ := eq163
       have r₂ := eq51 x x
       grind)
    | (have r₁ := eq163
       have r₂ := eq51 y x
       grind)
    | exact resolve eq163 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq163
  have eq180 : y = (M.op y y) ∨ y = (M.op y y) ∨ y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq9 y y
       have i₂ := eq169
       grind)
    | exact superpose eq169 eq9
    | (have j0 := eq9 y y
       grind)
    | exact resolve eq9 eq169
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq169
  have eq181 : y = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op y y) := by grind
  clear eq180
  have eq189 : ∀ X0 : G, y = (M.op X0 X0) ∨ x = (M.op x x) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq181
       have i₂ := eq18 X0 y
       grind)
    | (have i₁ := eq181
       have i₂ := eq18 y x
       grind)
    | exact superpose eq18 eq181
    | exact resolve eq181 eq18
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq181
  have eq246 : (N.op y y) ≠ (N.op x (N.op y y)) ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq63
       have i₂ := eq189 x
       grind)
    | exact superpose eq189 eq63
    | (have j1 := eq189 x
       grind)
    | exact resolve eq63 eq189
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq63
  have eq258 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 X0) ∨ x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq67 x
       have i₂ := eq189 X0
       grind)
    | exact superpose eq189 eq67
    | (have j1 := eq189 X0
       grind)
    | exact resolve eq67 eq189
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq260 : x ≠ y ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have j0 := eq189 x
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq189
  have eq262 : x ≠ y ∨ x = (M.op x x) := by grind
  clear eq260
  have eq265 : (N.op y y) ≠ (N.op x (N.op y y)) ∨ x = y ∨ x = (M.op x x) := by grind
  clear eq246
  have eq267 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op X0 X0) := by
    intro X0
    first
    | (have j0 := eq258 X0
       have j1 := eq51 X0 x
       grind)
    | (have r₁ := eq258 X0
       have r₂ := eq51 X0 y
       grind)
    | (have r₁ := eq258 X0
       have r₂ := eq51 X0 x
       grind)
    | exact resolve eq258 eq51
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51 eq258
  have eq271 : (N.op y y) ≠ (N.op x (N.op y y)) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq265
       have r₂ := eq262
       grind)
    | exact resolve eq265 eq262
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq265
  have eq309 : ∀ X1 : G, y = (N.op X1 y) ∨ x = (N.op x x) ∨ y = X1 := by
    intro X1
    first
    | (have i₁ := eq32 x x
       have i₂ := eq267 x
       grind)
    | exact superpose eq267 eq32
    | exact resolve eq32 eq267
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq311 : y = (N.op y y) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq67 x
       have i₂ := eq267 x
       grind)
    | exact superpose eq267 eq67
    | exact resolve eq67 eq267
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq267
  have eq1456 : y ≠ (N.op x y) ∨ x = (M.op x x) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq271
       have i₂ := eq311
       grind)
    | exact superpose eq311 eq271
    | exact resolve eq271 eq311
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq271 eq311
  have eq1463 : y ≠ (N.op x y) ∨ x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq1456
       have r₂ := eq7 x x
       grind)
    | exact resolve eq1456 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1456
  have eq1466 : y ≠ y ∨ x = (N.op x x) ∨ x = (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq1463
       have i₂ := eq309 x
       grind)
    | exact superpose eq309 eq1463
    | (have j1 := eq309 x
       grind)
    | (have r₁ := eq1463
       have r₂ := eq309 x
       grind)
    | (have r₁ := eq1463
       have r₂ := eq309 (N.op x y)
       grind)
    | exact resolve eq1463 eq309
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq309 eq1463
  have eq1470 : y ≠ y ∨ x = (N.op x x) ∨ x = y := by grind
  clear eq1466
  have eq1471 : x = (N.op x x) ∨ x = y := by grind
  clear eq1470
  have eq1610 : x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = y := by
    first
    | (have i₁ := eq9 x x
       have i₂ := eq1471
       grind)
    | exact superpose eq1471 eq9
    | (have j0 := eq9 x x
       grind)
    | exact resolve eq9 eq1471
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1471
  have eq1611 : x = (M.op x x) ∨ x = y := by grind
  clear eq1610
  have eq1613 : x = (M.op x x) := by
    first
    | (have r₁ := eq1611
       have r₂ := eq262
       grind)
    | exact resolve eq1611 eq262
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq262 eq1611
  have eq1810 : ∀ X0 : G, x = (M.op X0 X0) := by
    intro X0
    first
    | (have i₁ := eq18 x X0
       have i₂ := eq1613
       grind)
    | exact superpose eq1613 eq18
    | exact resolve eq18 eq1613
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq1812 : ∀ X0 : G, x = (N.op X0 x) ∨ x = X0 := by
    intro X0
    first
    | (have i₁ := eq32 X0 x
       have i₂ := eq1613
       grind)
    | exact superpose eq1613 eq32
    | exact resolve eq32 eq1613
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32
  have eq1814 : x = (N.op x x) := by
    first
    | (have i₁ := eq67 x
       have i₂ := eq1613
       grind)
    | exact superpose eq1613 eq67
    | exact resolve eq67 eq1613
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq67 eq1613
  have eq2189 : (N.op x (N.op y y)) ≠ (N.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1814
       grind)
    | exact superpose eq1814 eq10
    | exact resolve eq10 eq1814
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq2343 : (N.op x (M.op y y)) ≠ (N.op y x) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq2189
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq2189
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq2189 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2189
  have eq2345 : (N.op x (M.op y y)) ≠ (N.op y x) ∨ y = (M.op y y) := by grind
  clear eq2343
  have eq2346 : (N.op x x) ≠ (N.op y x) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq2345
       have i₂ := eq1810 y
       grind)
    | exact superpose eq1810 eq2345
    | exact resolve eq2345 eq1810
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2345
  have eq2347 : x ≠ (N.op y x) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq2346
       have i₂ := eq1814
       grind)
    | exact superpose eq1814 eq2346
    | exact resolve eq2346 eq1814
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1814 eq2346
  have eq2348 : x = y ∨ x ≠ (N.op y x) := by
    first
    | (have i₁ := eq2347
       have i₂ := eq1810 y
       grind)
    | exact superpose eq1810 eq2347
    | exact resolve eq2347 eq1810
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1810 eq2347
  have eq2349 : x = y := by
    first
    | (have j1 := eq1812 y
       grind)
    | (have r₁ := eq2348
       have r₂ := eq1812 y
       grind)
    | (have r₁ := eq2348
       have r₂ := eq1812 (N.op y x)
       grind)
    | exact resolve eq2348 eq1812
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1812 eq2348
  have eq2388 : (N.op x (N.op x x)) ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq2349
       grind)
    | exact superpose eq2349 eq10
    | exact resolve eq10 eq2349
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2349
  have eq2497 : False := by grind
  exact eq2497
