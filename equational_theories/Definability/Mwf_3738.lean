import equational_theories.Definability.AutBox2
import equational_theories.Definability.Mwc_00

/-!
# Structurality from `Equation3738` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3738` is `x ◇ y = (x ◇ z) ◇ (y ◇ w)`, and the companion is
`x □ y = if a = b then (M.op a a) else if (M.op a b) = b then a else (M.op b a)`.  Its automorphism half is `GuardAut2_x_y_pxy_y_pxx_x_pyx_Equation3738`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches it on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation835` from `Equation3738`: `x ◇ y = (x ◇ z) ◇ (y ◇ w)` ⊢ `x = x ◇ ((y ◇ x) ◇ (y ◇ x))`, through the
companion `x □ y = if a = b then (M.op a a) else if (M.op a b) = b then a else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation835_StructuralFromFin_Equation3738 : Law835.StructuralFromFin Law3738 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_x_y_pxy_y_pxx_x_pyx_Equation3738 (fun {G} M hM ↦ ?_)
  have hm := (@Law3738.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, a ≠ b ∨ N.op a b = (M.op a a)) →
      (∀ a b : G, a = b ∨ (M.op a b) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, a = b ∨ (M.op a b) = b ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law835 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (b)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne (a) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (b)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law835.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) = (M.op (M.op X0 X2) (M.op X1 X3)) := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, X0 ≠ X1 ∨ (N.op X0 X1) = (M.op X0 X0) := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X1 ∨ X0 = X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (N.op X0 X1) = (M.op X1 X0) ∨ (M.op X0 X1) = X1 ∨ X0 = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op x (N.op (N.op y x) (N.op y x))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 X5 : G, (M.op (M.op X0 X2) X3) = (M.op (M.op X0 X1) (M.op X3 X5)) := by
    intro X0 X1 X2 X3 X5
    first
    | (have i₁ := eq6 (M.op X0 X2) X3 (M.op X1 x) X5
       have i₂ := eq6 X0 X1 X2 x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq13 : ∀ X0 X1 X2 X3 X4 : G, (M.op X2 (M.op X0 X3)) = (M.op (M.op X2 X4) (M.op X0 X1)) := by
    intro X0 X1 X2 X3 X4
    first
    | (have i₁ := eq6 X2 (M.op X0 X3) X4 (M.op X1 x)
       have i₂ := eq6 X0 X1 X3 x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X2 X3 : G, (M.op X2 (M.op X0 X3)) = (M.op X2 X0) := by
    intro X0 X2 X3
    first
    | (have i₁ := eq13 X0 x X2 X3 x
       have i₂ := eq6 X2 X0 x x
       grind)
    | exact superpose eq6 eq13
    | exact resolve eq13 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq15 : ∀ X0 X2 X3 : G, (M.op (M.op X0 X2) X3) = (M.op X0 X3) := by
    intro X0 X2 X3
    first
    | (have i₁ := eq12 X0 x X2 X3 x
       have i₂ := eq6 X0 X3 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq40 : x ≠ (N.op x (M.op (N.op y x) (N.op y x))) := by grind
  have eq42 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X1 X2) ∨ (M.op X1 X2) = X0 ∨ (N.op X0 (M.op X1 X2)) = X0 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 X0 (M.op X1 X2)
       have i₂ := eq14 X1 X0 X2
       grind)
    | exact superpose eq14 eq8
    | (have j0 := eq8 (M.op X1 X2) X0
       grind)
    | (have r₁ := eq8 X2 (M.op X2 X2)
       have r₂ := eq14 X2 X2 X2
       grind)
    | exact resolve eq8 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq46 : x ≠ (N.op x (M.op (M.op x y) (M.op x y))) ∨ x = (M.op y x) ∨ x = y := by
    first
    | (have i₁ := eq40
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq40
    | (have j1 := eq9 y x
       grind)
    | (have r₁ := eq40
       have r₂ := eq9 x (N.op x (M.op (N.op y x) (N.op y x)))
       grind)
    | (have r₁ := eq40
       have r₂ := eq9 (N.op x (M.op (N.op y x) (N.op y x))) x
       grind)
    | exact resolve eq40 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq49 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op y x) ∨ x = y := by
    first
    | (have i₁ := eq46
       have i₂ := eq6 x x y y
       grind)
    | exact superpose eq6 eq46
    | exact resolve eq46 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq46
  have eq65 : ∀ X0 : G, (N.op X0 (M.op X0 X0)) = X0 ∨ (M.op X0 X0) = X0 := by
    intro X0
    first
    | (have j0 := eq42 X0 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq42
  have eq74 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op (M.op X0 X1) (M.op X0 X0)) ∨ (M.op X0 X1) = (M.op X0 X0) := by
    intro X0 X1
    first
    | (have i₁ := eq65 (M.op X1 x)
       have i₂ := eq6 X1 X1 x x
       grind)
    | exact superpose eq6 eq65
    | exact resolve eq65 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq79 : x ≠ x ∨ x = (M.op y x) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq49
       have i₂ := eq65 x
       grind)
    | exact superpose eq65 eq49
    | (have j1 := eq65 x
       grind)
    | (have r₁ := eq49
       have r₂ := eq65 x
       grind)
    | exact resolve eq49 eq65
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq80 : x = y ∨ x = (M.op y x) ∨ x = (M.op x x) := by grind
  clear eq79
  have eq87 : x ≠ (N.op x (M.op (N.op x x) (N.op x x))) ∨ x = (M.op y x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq40
       have i₂ := eq80
       grind)
    | exact superpose eq80 eq40
    | exact resolve eq40 eq80
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq80
  have eq90 : x ≠ (N.op x (M.op (M.op x x) (M.op x x))) ∨ x = (M.op y x) ∨ x = (M.op x x) := by grind
  clear eq87
  have eq92 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op y x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq90
       have i₂ := eq6 x x x x
       grind)
    | exact superpose eq6 eq90
    | exact resolve eq90 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq90
  have eq94 : x = (M.op y x) ∨ x = (M.op x x) := by
    first
    | (have j1 := eq65 x
       grind)
    | (have r₁ := eq92
       have r₂ := eq65 x
       grind)
    | exact resolve eq92 eq65
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq92
  have eq99 : x ≠ x ∨ x = y ∨ y = (N.op y x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq8 y x
       have i₂ := eq94
       grind)
    | exact superpose eq94 eq8
    | (have j0 := eq8 x y
       grind)
    | (have r₁ := eq8 y x
       have r₂ := eq94
       grind)
    | (have r₁ := eq8 x x
       have r₂ := eq94
       grind)
    | exact resolve eq8 eq94
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq104 : y = (N.op y x) ∨ x = y ∨ x = (M.op x x) := by grind
  clear eq99
  have eq123 : x ≠ (N.op x (M.op y y)) ∨ x = y ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq40
       have i₂ := eq104
       grind)
    | exact superpose eq104 eq40
    | exact resolve eq40 eq104
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq104
  have eq154 : x = (N.op x (M.op y y)) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq74 y x
       have i₂ := eq94
       grind)
    | exact superpose eq94 eq74
    | exact resolve eq74 eq94
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq74
  have eq182 : x ≠ x ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq123
       have i₂ := eq154
       grind)
    | exact superpose eq154 eq123
    | (have r₁ := eq123
       have r₂ := eq154
       grind)
    | exact resolve eq123 eq154
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq123 eq154
  have eq185 : x ≠ x ∨ x = y ∨ x = (M.op x x) ∨ x = (M.op y y) := by grind
  clear eq182
  have eq186 : x = y ∨ x = (M.op x x) ∨ x = (M.op y y) := by grind
  clear eq185
  have eq195 : x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq94
       have i₂ := eq186
       grind)
    | exact superpose eq186 eq94
    | exact resolve eq94 eq186
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq186
  have eq198 : x = (M.op x x) ∨ x = (M.op y y) := by grind
  clear eq195
  have eq199 : x ≠ (N.op x x) ∨ x = (M.op y x) ∨ x = y ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq49
       have i₂ := eq198
       grind)
    | exact superpose eq198 eq49
    | exact resolve eq49 eq198
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq49
  have eq205 : x ≠ (M.op x x) ∨ x = (M.op y x) ∨ x = y ∨ x = (M.op y y) := by grind
  clear eq199
  have eq206 : x = y ∨ x = (M.op y x) ∨ x = (M.op y y) := by
    first
    | (have r₁ := eq205
       have r₂ := eq94
       grind)
    | exact resolve eq205 eq94
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq205
  have eq208 : x ≠ (N.op x (M.op (N.op x x) (N.op x x))) ∨ x = (M.op y x) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq40
       have i₂ := eq206
       grind)
    | exact superpose eq206 eq40
    | exact resolve eq40 eq206
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq206
  have eq213 : x ≠ (N.op x (M.op (M.op x x) (M.op x x))) ∨ x = (M.op y x) ∨ x = (M.op y y) := by grind
  clear eq208
  have eq215 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op y x) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq213
       have i₂ := eq6 x x x x
       grind)
    | exact superpose eq6 eq213
    | exact resolve eq213 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq213
  have eq218 : x ≠ (N.op x x) ∨ x = (M.op y x) ∨ x = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq215
       have i₂ := eq198
       grind)
    | exact superpose eq198 eq215
    | exact resolve eq215 eq198
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq198 eq215
  have eq224 : x ≠ (N.op x x) ∨ x = (M.op y x) ∨ x = (M.op y y) := by grind
  clear eq218
  have eq227 : x ≠ (M.op x x) ∨ x = (M.op y x) ∨ x = (M.op y y) := by grind
  clear eq224
  have eq230 : x = (M.op y x) ∨ x = (M.op y y) := by
    first
    | (have r₁ := eq227
       have r₂ := eq94
       grind)
    | exact resolve eq227 eq94
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq94 eq227
  have eq239 : ∀ X0 : G, x = (M.op y y) ∨ (M.op X0 y) = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 y X0 x
       have i₂ := eq230
       grind)
    | exact superpose eq230 eq14
    | exact resolve eq14 eq230
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq262 : x ≠ (M.op y x) ∨ (M.op y x) = (M.op y y) := by
    first
    | (have j0 := eq239 y
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq239
  have eq292 : x ≠ x ∨ x = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq262
       have i₂ := eq230
       grind)
    | exact superpose eq230 eq262
    | (have r₁ := eq262
       have r₂ := eq230
       grind)
    | exact resolve eq262 eq230
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq230 eq262
  have eq295 : x ≠ x ∨ x = (M.op y y) := by grind
  clear eq292
  have eq296 : x = (M.op y y) := by grind
  clear eq295
  have eq305 : ∀ X0 : G, (M.op X0 y) = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 y X0 y
       have i₂ := eq296
       grind)
    | exact superpose eq296 eq14
    | exact resolve eq14 eq296
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq306 : ∀ X0 : G, (M.op y X0) = (M.op x X0) := by
    intro X0
    first
    | (have i₁ := eq15 y y X0
       have i₂ := eq296
       grind)
    | exact superpose eq296 eq15
    | exact resolve eq15 eq296
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq310 : y = (N.op y x) ∨ x = y := by
    first
    | (have i₁ := eq65 y
       have i₂ := eq296
       grind)
    | exact superpose eq296 eq65
    | exact resolve eq65 eq296
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq65
  have eq331 : x = (M.op y x) := by
    first
    | (have i₁ := eq296
       have i₂ := eq305 y
       grind)
    | exact superpose eq305 eq296
    | exact resolve eq296 eq305
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq296
  have eq339 : x = (M.op x x) := by
    first
    | (have i₁ := eq331
       have i₂ := eq306 x
       grind)
    | exact superpose eq306 eq331
    | exact resolve eq331 eq306
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq331
  have eq484 : x ≠ (N.op x (M.op y y)) ∨ x = y := by
    first
    | (have i₁ := eq40
       have i₂ := eq310
       grind)
    | exact superpose eq310 eq40
    | exact resolve eq40 eq310
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq310
  have eq491 : x ≠ (N.op x (M.op y x)) ∨ x = y := by
    first
    | (have i₁ := eq484
       have i₂ := eq305 y
       grind)
    | exact superpose eq305 eq484
    | exact resolve eq484 eq305
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq305 eq484
  have eq496 : x ≠ (N.op x (M.op x x)) ∨ x = y := by
    first
    | (have i₁ := eq491
       have i₂ := eq306 x
       grind)
    | exact superpose eq306 eq491
    | exact resolve eq491 eq306
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq306 eq491
  have eq501 : x ≠ (N.op x x) ∨ x = y := by
    first
    | (have i₁ := eq496
       have i₂ := eq339
       grind)
    | exact superpose eq339 eq496
    | exact resolve eq496 eq339
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq496
  have eq504 : x ≠ (M.op x x) ∨ x = y := by grind
  clear eq501
  have eq506 : x = y := by
    first
    | (have r₁ := eq504
       have r₂ := eq339
       grind)
    | exact resolve eq504 eq339
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq504
  have eq509 : x ≠ (N.op x (M.op (N.op x x) (N.op x x))) := by
    first
    | (have i₁ := eq40
       have i₂ := eq506
       grind)
    | exact superpose eq506 eq40
    | exact resolve eq40 eq506
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq40 eq506
  have eq518 : x ≠ (N.op x (M.op (M.op x x) (M.op x x))) := by grind
  clear eq509
  have eq520 : x ≠ (N.op x (M.op x x)) := by
    first
    | (have i₁ := eq518
       have i₂ := eq6 x x x x
       grind)
    | exact superpose eq6 eq518
    | exact resolve eq518 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq518
  have eq522 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq520
       have i₂ := eq339
       grind)
    | exact superpose eq339 eq520
    | exact resolve eq520 eq339
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq520
  have eq524 : x ≠ (M.op x x) := by grind
  clear eq522
  have eq526 : False := by grind
  exact eq526
