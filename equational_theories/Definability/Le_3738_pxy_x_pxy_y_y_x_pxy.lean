import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation3738` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3738` is `x ◇ y = (x ◇ z) ◇ (y ◇ w)`, and the companion is
`x □ y = if (M.op a b) = a then b else if (M.op a b) = b then a else (M.op a b)`.  Its automorphism half is `GuardAut2_pxy_x_pxy_y_y_x_pxy_Equation3738`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches it on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation1691` from `Equation3738`: `x ◇ y = (x ◇ z) ◇ (y ◇ w)` ⊢ `x = (y ◇ x) ◇ ((y ◇ x) ◇ x)`, through the
companion `x □ y = if (M.op a b) = a then b else if (M.op a b) = b then a else (M.op a b)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation1691_StructuralFromFin_Equation3738 : Law1691.StructuralFromFin Law3738 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 1)) (Lf 1) (Lf 1) (Lf 0) ((Lf 0) ⋆ (Lf 1))
    GuardAut2_pxy_x_pxy_y_y_x_pxy_Equation3738 (fun {G} M hM ↦ ?_)
  have hm := (@Law3738.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op a b) = a ∨ (M.op a b) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op a b) = a ∨ (M.op a b) = b ∨ N.op a b = (M.op a b)) → @satisfies _ G N Law1691 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 1)) (Lf 1) (Lf 1) (Lf 0) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 1)) (Lf 1) (Lf 1) (Lf 0) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (a)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (b)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 0) ((Lf 0) ⋆ (Lf 1)) (Lf 1) (Lf 1) (Lf 0) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (a)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (b)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law1691.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) = (M.op (M.op X0 X2) (M.op X1 X3)) := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X1 ∨ (M.op X0 X1) = X0 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op X0 X1) ∨ (M.op X0 X1) = X1 ∨ (M.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op (N.op y x) (N.op (N.op y x) x)) := by grind
  clear hm ht hu hv nh hM
  have eq11 : ∀ X0 X1 X2 X3 X5 : G, (M.op (M.op X0 X2) X3) = (M.op (M.op X0 X1) (M.op X3 X5)) := by
    intro X0 X1 X2 X3 X5
    first
    | (have i₁ := eq6 (M.op X0 X2) X3 (M.op X1 x) X5
       have i₂ := eq6 X0 X1 X2 x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq12 : ∀ X0 X1 X2 X3 X4 : G, (M.op X2 (M.op X0 X3)) = (M.op (M.op X2 X4) (M.op X0 X1)) := by
    intro X0 X1 X2 X3 X4
    first
    | (have i₁ := eq6 X2 (M.op X0 X3) X4 (M.op X1 x)
       have i₂ := eq6 X0 X1 X3 x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X2 X3 : G, (M.op X2 (M.op X0 X3)) = (M.op X2 X0) := by
    intro X0 X2 X3
    first
    | (have i₁ := eq12 X0 x X2 X3 x
       have i₂ := eq6 X2 X0 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq15 : ∀ X0 X2 X3 : G, (M.op (M.op X0 X2) X3) = (M.op X0 X3) := by
    intro X0 X2 X3
    first
    | (have i₁ := eq11 X0 x X2 X3 x
       have i₂ := eq6 X0 X3 x x
       grind)
    | exact superpose eq6 eq11
    | exact resolve eq11 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq11
  have eq33 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X0 X2) ∨ (N.op (M.op X0 X2) X1) = X1 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op X0 X2) X1
       have i₂ := eq15 X0 X2 X1
       grind)
    | exact superpose eq15 eq7
    | (have j0 := eq7 (M.op X0 X2) X1
       grind)
    | (have r₁ := eq7 (M.op X0 x) x
       have r₂ := eq15 X0 x x
       grind)
    | exact resolve eq7 eq15
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq15
  have eq43 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X1 X2) ∨ (M.op X0 X1) = X0 ∨ (N.op X0 (M.op X1 X2)) = X0 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 X2 (M.op X0 x)
       have i₂ := eq14 X0 X2 x
       grind)
    | exact superpose eq14 eq8
    | (have j0 := eq8 X0 X1
       grind)
    | (have r₁ := eq8 X2 (M.op X2 X2)
       have r₂ := eq14 X2 X2 X2
       grind)
    | exact resolve eq8 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq50 : x ≠ (N.op (M.op y x) (N.op (M.op y x) x)) ∨ x = (M.op y x) ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq51 : x ≠ (N.op (N.op y x) (M.op (N.op y x) x)) ∨ x = (M.op (N.op y x) x) ∨ (N.op y x) = (M.op (N.op y x) x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 (N.op y x) x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 (N.op y x) x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq59 : ∀ X0 X1 : G, (N.op (M.op X0 X1) X1) = X1 := by
    intro X0 X1
    first
    | (have j0 := eq33 X0 X1 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33
  have eq88 : x ≠ (N.op (M.op y x) x) ∨ x = (M.op y x) ∨ y = (M.op y x) := by
    first
    | (have i₁ := eq50
       have i₂ := eq59 y x
       grind)
    | exact superpose eq59 eq50
    | exact resolve eq50 eq59
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq50
  have eq92 : y = (M.op y x) ∨ x = (M.op y x) := by
    first
    | (have r₁ := eq88
       have r₂ := eq59 y x
       grind)
    | exact resolve eq88 eq59
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq88
  have eq102 : x = (N.op y x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq59 y x
       have i₂ := eq92
       grind)
    | exact superpose eq92 eq59
    | exact resolve eq59 eq92
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq92
  have eq133 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq51
       have i₂ := eq102
       grind)
    | exact superpose eq102 eq51
    | exact resolve eq51 eq102
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq51
  have eq134 : x ≠ (N.op x (N.op x x)) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq102
       grind)
    | exact superpose eq102 eq10
    | exact resolve eq10 eq102
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq102
  have eq137 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op y x) := by grind
  clear eq133
  have eq200 : ∀ X0 : G, (N.op X0 (M.op X0 X0)) = X0 ∨ (M.op X0 X0) = X0 := by
    intro X0
    first
    | (have j0 := eq43 X0 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq43
  have eq245 : x ≠ x ∨ x = (M.op x x) ∨ x = (M.op y x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq137
       have i₂ := eq200 x
       grind)
    | exact superpose eq200 eq137
    | (have j1 := eq200 x
       grind)
    | (have r₁ := eq137
       have r₂ := eq200 x
       grind)
    | exact resolve eq137 eq200
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq137 eq200
  have eq247 : x ≠ x ∨ x = (M.op x x) ∨ x = (M.op y x) := by grind
  clear eq245
  have eq248 : x = (M.op y x) ∨ x = (M.op x x) := by grind
  clear eq247
  have eq269 : x = (N.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq59 y x
       have i₂ := eq248
       grind)
    | exact superpose eq248 eq59
    | exact resolve eq59 eq248
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq59 eq248
  have eq276 : x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq269
       have r₂ := eq7 x x
       grind)
    | exact resolve eq269 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq269
  have eq281 : x ≠ (N.op x x) ∨ x = (M.op y x) := by
    first
    | (have i₁ := eq134
       have i₂ := eq276
       grind)
    | exact superpose eq276 eq134
    | exact resolve eq134 eq276
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq134
  have eq285 : x = (M.op y x) := by
    first
    | (have r₁ := eq281
       have r₂ := eq276
       grind)
    | exact resolve eq281 eq276
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq281
  have eq380 : x ≠ x ∨ x = y ∨ y = (N.op y x) := by
    first
    | (have i₁ := eq8 y x
       have i₂ := eq285
       grind)
    | exact superpose eq285 eq8
    | (have j0 := eq8 y x
       grind)
    | (have r₁ := eq8 y x
       have r₂ := eq285
       grind)
    | exact resolve eq8 eq285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq383 : ∀ X0 : G, (M.op X0 y) = (M.op X0 x) := by
    intro X0
    first
    | (have i₁ := eq14 y X0 x
       have i₂ := eq285
       grind)
    | exact superpose eq285 eq14
    | exact resolve eq14 eq285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14
  have eq398 : y = (N.op y x) ∨ x = y := by grind
  clear eq380
  have eq664 : x ≠ (N.op y (N.op y x)) ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq398
       grind)
    | exact superpose eq398 eq10
    | exact resolve eq10 eq398
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq766 : x ≠ (N.op y y) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq664
       have i₂ := eq398
       grind)
    | exact superpose eq398 eq664
    | exact resolve eq664 eq398
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq398 eq664
  have eq772 : x ≠ (N.op y y) ∨ x = y := by grind
  clear eq766
  have eq789 : x ≠ (M.op y y) ∨ x = y ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq772
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq772
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq772 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq772
  have eq790 : x ≠ (M.op y y) ∨ x = y ∨ y = (M.op y y) := by grind
  clear eq789
  have eq791 : x ≠ (M.op y x) ∨ x = y ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq790
       have i₂ := eq383 y
       grind)
    | exact superpose eq383 eq790
    | exact resolve eq790 eq383
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq790
  have eq792 : x = y ∨ y = (M.op y y) := by
    first
    | (have r₁ := eq791
       have r₂ := eq285
       grind)
    | exact resolve eq791 eq285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq791
  have eq793 : y = (M.op y x) ∨ x = y := by
    first
    | (have i₁ := eq792
       have i₂ := eq383 y
       grind)
    | exact superpose eq383 eq792
    | exact resolve eq792 eq383
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq383 eq792
  have eq794 : x = y ∨ x = y := by
    first
    | (have i₁ := eq793
       have i₂ := eq285
       grind)
    | exact superpose eq285 eq793
    | exact resolve eq793 eq285
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq285 eq793
  have eq795 : x = y := by grind
  clear eq794
  have eq800 : x ≠ (N.op (N.op x x) (N.op (N.op x x) x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq795
       grind)
    | exact superpose eq795 eq10
    | exact resolve eq10 eq795
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq795
  have eq843 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq800
       have i₂ := eq276
       grind)
    | exact superpose eq276 eq800
    | exact resolve eq800 eq276
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq800
  have eq847 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq843
       have i₂ := eq276
       grind)
    | exact superpose eq276 eq843
    | exact resolve eq843 eq276
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq843
  have eq850 : False := by grind
  exact eq850
