import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation224` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation224` is `x = (y ◇ (x ◇ z)) ◇ x`, and the companion is
`x □ y = if (M.op a b) = b then a else if (M.op a b) = a then b else (M.op a b)`.  Its automorphism half is `GuardAut2_pxy_y_pxy_x_x_y_pxy_Equation224`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches it on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation1023` from `Equation224`: `x = (y ◇ (x ◇ z)) ◇ x` ⊢ `x = x ◇ ((x ◇ (x ◇ y)) ◇ y)`, through the
companion `x □ y = if (M.op a b) = b then a else if (M.op a b) = a then b else (M.op a b)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation1023_StructuralFromFin_Equation224 : Law1023.StructuralFromFin Law224 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1))
    GuardAut2_pxy_y_pxy_x_x_y_pxy_Equation224 (fun {G} M hM ↦ ?_)
  have hm := (@Law224.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op a b) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op a b) = b ∨ (M.op a b) = a ∨ N.op a b = (M.op a b)) → @satisfies _ G N Law1023 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 0) ⋆ (Lf 1)) (Lf 1) ((Lf 0) ⋆ (Lf 1)) (Lf 0) (Lf 0) (Lf 1) ((Lf 0) ⋆ (Lf 1)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op a b)) (b)).imp id (fun hg ↦
        (eq_or_ne ((M.op a b)) (a)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law1023.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 : G, (M.op (M.op X1 (M.op X0 X2)) X0) = X0 := by
    intro X0 X1 X2
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X1 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X0 X1) ≠ X0 ∨ (M.op X0 X1) = X1 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op X0 X1) ∨ (M.op X0 X1) = X0 ∨ (M.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op x (N.op (N.op x (N.op x y)) y)) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 : G, (M.op (M.op X0 X1) X0) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq6 X0 (M.op x (M.op (M.op X0 X1) x)) X1
       have i₂ := eq6 (M.op X0 X1) x x
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq13 : ∀ X0 X1 X2 : G, X0 ≠ X0 ∨ (M.op X1 (M.op X0 X2)) = (N.op (M.op X1 (M.op X0 X2)) X0) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op X1 (M.op X0 X2)) X0
       have i₂ := eq6 X0 X1 X2
       grind)
    | exact superpose eq6 eq7
    | (have j0 := eq7 (M.op X1 (M.op X0 X2)) X0
       grind)
    | (have r₁ := eq7 (M.op X1 (M.op X0 X2)) X0
       have r₂ := eq6 X0 X1 X2
       grind)
    | exact resolve eq7 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X2 : G, (M.op X1 (M.op X0 X2)) = (N.op (M.op X1 (M.op X0 X2)) X0) := by
    intro X0 X1 X2
    first
    | (have j0 := eq13 X0 X1 X2
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13
  have eq16 : ∀ X0 X1 : G, (M.op X0 X1) = (M.op X0 (M.op X0 X1)) := by
    intro X0 X1
    first
    | (have i₁ := eq12 (M.op X0 X1) X0
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq12
    | exact resolve eq12 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq18 : ∀ X0 X1 X2 : G, (M.op X0 X2) = (M.op (M.op X1 X0) (M.op X0 X2)) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 (M.op X0 X2) X1 X0
       have i₂ := eq12 X0 X2
       grind)
    | exact superpose eq12 eq6
    | exact resolve eq6 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq26 : ∀ X0 X1 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (N.op X0 (M.op X0 X1)) = X0 := by
    intro X0 X1
    first
    | (have i₁ := eq7 X0 (M.op X0 X1)
       have i₂ := eq16 X0 X1
       grind)
    | exact superpose eq16 eq7
    | (have j0 := eq7 X0 (M.op X0 X1)
       grind)
    | (have r₁ := eq7 X0 (M.op X0 X1)
       have r₂ := eq16 X0 X1
       grind)
    | exact resolve eq7 eq16
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq16
  have eq28 : ∀ X0 X1 : G, (N.op X0 (M.op X0 X1)) = X0 := by
    intro X0 X1
    first
    | (have j0 := eq26 X0 X1
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq26
  have eq33 : x ≠ (N.op x (N.op (N.op x (M.op x y)) y)) ∨ x = (M.op x y) ∨ y = (M.op x y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq34 : x ≠ (N.op x (N.op (M.op x (N.op x y)) y)) ∨ x = (M.op x (N.op x y)) ∨ (N.op x y) = (M.op x (N.op x y)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x (N.op x y)
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x (N.op x y)
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq40 : x ≠ (N.op x (N.op x y)) ∨ x = (M.op x y) ∨ y = (M.op x y) := by
    first
    | (have i₁ := eq33
       have i₂ := eq28 x y
       grind)
    | exact superpose eq28 eq33
    | exact resolve eq33 eq28
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq33
  have eq49 : ∀ X0 X1 X2 : G, (M.op X1 X0) = (N.op (M.op X1 X0) (M.op X0 X2)) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq14 (M.op X0 X1) X1 X0
       have i₂ := eq12 X0 X1
       grind)
    | exact superpose eq12 eq14
    | exact resolve eq14 eq12
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq117 : x ≠ (N.op x (M.op x y)) ∨ x = (M.op x y) ∨ y = (M.op x y) ∨ x = (M.op x y) ∨ y = (M.op x y) := by
    first
    | (have i₁ := eq40
       have i₂ := eq9 x y
       grind)
    | exact superpose eq9 eq40
    | (have j1 := eq9 x y
       grind)
    | exact resolve eq40 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq40
  have eq119 : x ≠ (N.op x (M.op x y)) ∨ x = (M.op x y) ∨ y = (M.op x y) := by grind
  clear eq117
  have eq120 : y = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have r₁ := eq119
       have r₂ := eq28 x y
       grind)
    | exact resolve eq119 eq28
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq119
  have eq125 : x = (M.op y x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq12 x y
       have i₂ := eq120
       grind)
    | exact superpose eq120 eq12
    | exact resolve eq12 eq120
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq131 : x = (N.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq28 x y
       have i₂ := eq120
       grind)
    | exact superpose eq120 eq28
    | exact resolve eq28 eq120
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq120
  have eq146 : ∀ X0 : G, (M.op X0 x) = (N.op (M.op X0 x) y) ∨ x = (M.op x y) := by
    intro X0
    first
    | (have i₁ := eq14 y x x
       have i₂ := eq125
       grind)
    | exact superpose eq125 eq14
    | exact resolve eq14 eq125
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14 eq125
  have eq211 : x ≠ (N.op x (N.op (M.op x x) y)) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq34
       have i₂ := eq131
       grind)
    | exact superpose eq131 eq34
    | exact resolve eq34 eq131
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq34
  have eq213 : x ≠ (N.op x (N.op (N.op x x) y)) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq131
       grind)
    | exact superpose eq131 eq10
    | exact resolve eq10 eq131
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq217 : x ≠ (N.op x (N.op (M.op x x) y)) ∨ x = (M.op x x) ∨ x = (M.op x y) := by grind
  clear eq211
  have eq13431 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq217
       have i₂ := eq146 x
       grind)
    | exact superpose eq146 eq217
    | exact resolve eq217 eq146
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq146 eq217
  have eq13434 : x ≠ (N.op x (M.op x x)) ∨ x = (M.op x x) ∨ x = (M.op x y) := by grind
  clear eq13431
  have eq13435 : x = (M.op x y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq13434
       have r₂ := eq28 x x
       grind)
    | exact resolve eq13434 eq28
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13434
  have eq13736 : x = (N.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq28 x y
       have i₂ := eq13435
       grind)
    | exact superpose eq13435 eq28
    | exact resolve eq28 eq13435
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq28 eq13435
  have eq13835 : x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq13736
       have r₂ := eq7 x x
       grind)
    | exact resolve eq13736 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq13736
  have eq14439 : x ≠ (N.op x (N.op x y)) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq213
       have i₂ := eq13835
       grind)
    | exact superpose eq13835 eq213
    | exact resolve eq213 eq13835
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq213
  have eq21243 : x ≠ (N.op x x) ∨ x = (M.op x y) ∨ x = (M.op x y) := by
    first
    | (have i₁ := eq14439
       have i₂ := eq131
       grind)
    | exact superpose eq131 eq14439
    | exact resolve eq14439 eq131
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq131 eq14439
  have eq21247 : x ≠ (N.op x x) ∨ x = (M.op x y) := by grind
  clear eq21243
  have eq21248 : x = (M.op x y) := by
    first
    | (have r₁ := eq21247
       have r₂ := eq13835
       grind)
    | exact resolve eq21247 eq13835
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21247
  have eq21656 : x ≠ x ∨ x = y ∨ y = (N.op x y) := by
    first
    | (have i₁ := eq8 x y
       have i₂ := eq21248
       grind)
    | exact superpose eq21248 eq8
    | (have j0 := eq8 x y
       grind)
    | (have r₁ := eq8 x y
       have r₂ := eq21248
       grind)
    | exact resolve eq8 eq21248
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq21664 : ∀ X0 : G, (M.op y X0) = (M.op x (M.op y X0)) := by
    intro X0
    first
    | (have i₁ := eq18 y x X0
       have i₂ := eq21248
       grind)
    | exact superpose eq21248 eq18
    | exact resolve eq18 eq21248
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq18
  have eq21671 : ∀ X0 : G, x = (N.op x (M.op y X0)) := by
    intro X0
    first
    | (have i₁ := eq49 y x x
       have i₂ := eq21248
       grind)
    | exact superpose eq21248 eq49
    | exact resolve eq49 eq21248
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq49
  have eq21782 : y = (N.op x y) ∨ x = y := by grind
  clear eq21656
  have eq23305 : x ≠ (N.op x (N.op (N.op x y) y)) ∨ x = y := by
    first
    | (have i₁ := eq10
       have i₂ := eq21782
       grind)
    | exact superpose eq21782 eq10
    | exact resolve eq10 eq21782
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq29952 : x ≠ (N.op x (N.op y y)) ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq23305
       have i₂ := eq21782
       grind)
    | exact superpose eq21782 eq23305
    | exact resolve eq23305 eq21782
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21782 eq23305
  have eq29957 : x ≠ (N.op x (N.op y y)) ∨ x = y := by grind
  clear eq29952
  have eq30834 : x ≠ (N.op x (M.op y y)) ∨ x = y ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq29957
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq29957
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq29957 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq29957
  have eq30836 : x ≠ (N.op x (M.op y y)) ∨ x = y ∨ y = (M.op y y) := by grind
  clear eq30834
  have eq30837 : y = (M.op y y) ∨ x = y := by
    first
    | (have r₁ := eq30836
       have r₂ := eq21671 y
       grind)
    | exact resolve eq30836 eq21671
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21671 eq30836
  have eq31299 : y = (M.op x y) ∨ x = y := by
    first
    | (have i₁ := eq21664 y
       have i₂ := eq30837
       grind)
    | exact superpose eq30837 eq21664
    | exact resolve eq21664 eq30837
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21664 eq30837
  have eq31497 : x = y ∨ x = y := by
    first
    | (have i₁ := eq31299
       have i₂ := eq21248
       grind)
    | exact superpose eq21248 eq31299
    | exact resolve eq31299 eq21248
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21248 eq31299
  have eq31498 : x = y := by grind
  clear eq31497
  have eq32021 : x ≠ (N.op x (N.op (N.op x (N.op x x)) x)) := by
    first
    | (have i₁ := eq10
       have i₂ := eq31498
       grind)
    | exact superpose eq31498 eq10
    | exact resolve eq10 eq31498
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq31498
  have eq32113 : x ≠ (N.op x (N.op (N.op x x) x)) := by
    first
    | (have i₁ := eq32021
       have i₂ := eq13835
       grind)
    | exact superpose eq13835 eq32021
    | exact resolve eq32021 eq13835
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32021
  have eq32116 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq32113
       have i₂ := eq13835
       grind)
    | exact superpose eq13835 eq32113
    | exact resolve eq32113 eq13835
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32113
  have eq32119 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq32116
       have i₂ := eq13835
       grind)
    | exact superpose eq13835 eq32116
    | exact resolve eq32116 eq13835
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq32116
  have eq32120 : False := by grind
  exact eq32120
