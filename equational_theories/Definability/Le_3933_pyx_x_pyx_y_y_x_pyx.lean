import equational_theories.Definability.AutBox2
import equational_theories.Definability.Lw_00

/-!
# Structurality from `Equation3933` through a two-guard companion, by replay

Generated by `defwork/itautcreplay2.py`.  `Equation3933` is `x ◇ y = (x ◇ (y ◇ z)) ◇ w`, and the companion is
`x □ y = if (M.op b a) = a then b else if (M.op b a) = b then a else (M.op b a)`.  Its automorphism half is `GuardAut2_pyx_x_pyx_y_y_x_pyx_Equation3933`, proved
elsewhere; each cell below is one more law the companion satisfies, written out one superposition
step at a time because `grind` reaches it on its own.
-/

open FirstOrder FirstOrder.Language Law Law.MagmaLaw FreeMagma

set_option maxHeartbeats 1000000
set_option linter.unusedSimpArgs false
set_option linter.unusedVariables false
set_option linter.unusedTactic false
set_option linter.unreachableTactic false

/-- `Equation1429` from `Equation3933`: `x ◇ y = (x ◇ (y ◇ z)) ◇ w` ⊢ `x = (x ◇ x) ◇ (x ◇ (y ◇ y))`, through the
companion `x □ y = if (M.op b a) = a then b else if (M.op b a) = b then a else (M.op b a)`, writing `a`, `b` for `x`, `y`.
Vampire refutation replayed step by step. -/
theorem Equation1429_StructuralFromFin_Equation3933 : Law1429.StructuralFromFin Law3933 := by
  classical
  refine AutBox.structuralFromFin_iteOf2 ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0))
    GuardAut2_pyx_x_pyx_y_y_x_pyx_Equation3933 (fun {G} M hM ↦ ?_)
  have hm := (@Law3933.models_iff G M).mp hM
  suffices h : ∀ N : Magma G,
      (∀ a b : G, (M.op b a) ≠ a ∨ N.op a b = b) →
      (∀ a b : G, (M.op b a) = a ∨ (M.op b a) ≠ b ∨ N.op a b = a) →
      (∀ a b : G, (M.op b a) = a ∨ (M.op b a) = b ∨ N.op a b = (M.op b a)) → @satisfies _ G N Law1429 by
    refine h _ (fun a b ↦ ?_) (fun a b ↦ ?_) (fun a b ↦ ?_)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (a)).symm.imp id (fun hg ↦ if_pos hg)
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (a)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (b)).symm.imp id (fun hg2 ↦ (if_neg hg).trans (if_pos hg2)))
    · rw [AutBox.iteOf2_apply ((Lf 1) ⋆ (Lf 0)) (Lf 0) ((Lf 1) ⋆ (Lf 0)) (Lf 1) (Lf 1) (Lf 0) ((Lf 1) ⋆ (Lf 0)) M a b]
      simp only [evalInMagma, Matrix.cons_val_zero, Matrix.cons_val_one, Matrix.head_cons, Matrix.cons_val_fin_one]
      exact (eq_or_ne ((M.op b a)) (a)).imp id (fun hg ↦
        (eq_or_ne ((M.op b a)) (b)).imp id (fun hg2 ↦ (if_neg hg).trans (if_neg hg2)))
  intro N ht hu hv
  rw [@Law1429.models_iff G N]
  intro x y
  by_contra nh
  have eq6 : ∀ X0 X1 X2 X3 : G, (M.op X0 X1) = (M.op (M.op X0 (M.op X1 X2)) X3) := by
    intro X0 X1 X2 X3
    grind
  have eq7 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X0 ∨ (N.op X0 X1) = X1 := by
    intro X0 X1
    grind
  have eq8 : ∀ X0 X1 : G, (M.op X1 X0) ≠ X1 ∨ (M.op X1 X0) = X0 ∨ (N.op X0 X1) = X0 := by
    intro X0 X1
    grind
  have eq9 : ∀ X0 X1 : G, (M.op X1 X0) = (N.op X0 X1) ∨ (M.op X1 X0) = X1 ∨ (M.op X1 X0) = X0 := by
    intro X0 X1
    grind
  have eq10 : x ≠ (N.op (N.op x x) (N.op x (N.op y y))) := by grind
  clear hm ht hu hv nh hM
  have eq12 : ∀ X0 X1 X2 X3 X5 : G, (M.op (M.op X0 (M.op X1 X2)) X3) = (M.op (M.op X0 X1) X5) := by
    intro X0 X1 X2 X3 X5
    first
    | (have i₁ := eq6 (M.op X0 (M.op X1 X2)) X3 x X5
       have i₂ := eq6 X0 X1 X2 (M.op X3 x)
       grind)
    | exact superpose eq6 eq6
    | exact resolve eq6 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq14 : ∀ X0 X1 X5 : G, (M.op X0 X1) = (M.op (M.op X0 X1) X5) := by
    intro X0 X1 X5
    first
    | (have i₁ := eq12 X0 X1 x x X5
       have i₂ := eq6 X0 X1 x x
       grind)
    | exact superpose eq6 eq12
    | exact resolve eq12 eq6
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq12
  have eq19 : ∀ X0 X1 X2 : G, (M.op X0 X1) = (M.op X0 (M.op X1 X2)) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq6 X0 X1 X2 x
       have i₂ := eq14 X0 (M.op X1 X2) x
       grind)
    | exact superpose eq14 eq6
    | exact resolve eq6 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq21 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ X2 ∨ (M.op X0 X1) = (N.op X2 (M.op X0 X1)) := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 X2 (M.op X0 X1)
       have i₂ := eq14 X0 X1 X2
       grind)
    | exact superpose eq14 eq7
    | (have j0 := eq7 X2 (M.op X0 X1)
       grind)
    | (have r₁ := eq7 (M.op X0 X1) (M.op X0 X1)
       have r₂ := eq14 X0 X1 (M.op X0 X1)
       grind)
    | exact resolve eq7 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq31 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X1 X2) ∨ (N.op (M.op X1 X2) X0) = X0 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq7 (M.op X1 X2) X0
       have i₂ := eq19 X0 X1 X2
       grind)
    | exact superpose eq19 eq7
    | (have j0 := eq7 (M.op X1 X2) X0
       grind)
    | (have r₁ := eq7 (M.op X0 X0) X0
       have r₂ := eq19 X0 X0 X0
       grind)
    | exact resolve eq7 eq19
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq19
  have eq36 : ∀ X0 X1 X2 : G, (M.op X0 X1) ≠ (M.op X0 X1) ∨ (M.op X0 X1) = X2 ∨ (N.op X2 (M.op X0 X1)) = X2 := by
    intro X0 X1 X2
    first
    | (have i₁ := eq8 x (M.op X0 X1)
       have i₂ := eq14 X0 X1 x
       grind)
    | exact superpose eq14 eq8
    | (have j0 := eq8 X2 (M.op X0 X1)
       grind)
    | (have r₁ := eq8 x (M.op X0 X1)
       have r₂ := eq14 X0 X1 x
       grind)
    | exact resolve eq8 eq14
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq38 : ∀ X0 X1 X2 : G, (N.op X2 (M.op X0 X1)) = X2 ∨ (M.op X0 X1) = X2 := by
    intro X0 X1 X2
    first
    | (have j0 := eq36 X0 X1 X2
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq36
  have eq60 : x ≠ (N.op (M.op x x) (N.op x (N.op y y))) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 x x
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq62 : x ≠ (N.op (N.op x x) (N.op x (M.op y y))) ∨ y = (M.op y y) ∨ y = (M.op y y) := by
    first
    | (have i₁ := eq10
       have i₂ := eq9 y y
       grind)
    | exact superpose eq9 eq10
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq10 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq63 : x ≠ (N.op (N.op x x) (N.op x (M.op y y))) ∨ y = (M.op y y) := by grind
  clear eq62
  have eq64 : x ≠ (N.op (M.op x x) (N.op x (N.op y y))) ∨ x = (M.op x x) := by grind
  clear eq60
  have eq74 : x ≠ (N.op (N.op x x) x) ∨ y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq63
       have i₂ := eq38 y y x
       grind)
    | exact superpose eq38 eq63
    | (have j1 := eq38 y y y
       grind)
    | exact resolve eq63 eq38
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq82 : ∀ X0 X1 : G, (M.op X0 X1) = (N.op (M.op X0 X1) (M.op X0 X1)) := by
    intro X0 X1
    first
    | (have j0 := eq21 X0 X1 (M.op X0 X1)
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq21
  have eq116 : ∀ X0 : G, (N.op (M.op X0 X0) X0) = X0 := by
    intro X0
    first
    | (have j0 := eq31 X0 X0 X0
       grind)
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq31
  have eq152 : x ≠ (N.op (M.op x x) x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq74
       have i₂ := eq9 x x
       grind)
    | exact superpose eq9 eq74
    | (have j1 := eq9 y y
       grind)
    | exact resolve eq74 eq9
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq154 : x ≠ (N.op (M.op x x) x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq152
  have eq155 : y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq154
       have r₂ := eq116 x
       grind)
    | exact resolve eq154 eq116
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq154
  have eq165 : ∀ X0 : G, y = (M.op y X0) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq155
       grind)
    | exact superpose eq155 eq14
    | exact resolve eq14 eq155
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq170 : ∀ X0 : G, (N.op X0 y) = X0 ∨ y = X0 ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq38 y y x
       have i₂ := eq155
       grind)
    | exact superpose eq155 eq38
    | (have j0 := eq38 y y x
       grind)
    | exact resolve eq38 eq155
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq173 : x ≠ y ∨ x = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq155
  have eq215 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op y X0) ∨ x = (M.op x x) := by
    intro X0
    first
    | (have i₁ := eq82 y y
       have i₂ := eq165 X0
       grind)
    | exact superpose eq165 eq82
    | (have j1 := eq165 X0
       grind)
    | exact resolve eq82 eq165
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq165
  have eq222 : ∀ X0 : G, x = (N.op x x) ∨ y = (M.op y X0) := by
    intro X0
    first
    | (have j0 := eq215 X0
       have j1 := eq7 x x
       grind)
    | (have r₁ := eq215 y
       have r₂ := eq7 y y
       grind)
    | (have r₁ := eq215 X0
       have r₂ := eq7 x x
       grind)
    | exact resolve eq215 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq215
  have eq255 : y = (N.op y y) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq82 y x
       have i₂ := eq222 x
       grind)
    | exact superpose eq222 eq82
    | exact resolve eq82 eq222
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq222
  have eq287 : x ≠ (N.op (M.op x x) (N.op x y)) ∨ x = (M.op x x) ∨ x = (N.op x x) := by
    first
    | (have i₁ := eq64
       have i₂ := eq255
       grind)
    | exact superpose eq255 eq64
    | exact resolve eq64 eq255
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq64 eq255
  have eq294 : x ≠ (N.op (M.op x x) (N.op x y)) ∨ x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq287
       have r₂ := eq7 x x
       grind)
    | exact resolve eq287 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq287
  have eq1167 : x ≠ (N.op (M.op x x) x) ∨ x = (N.op x x) ∨ x = y ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq294
       have i₂ := eq170 x
       grind)
    | exact superpose eq170 eq294
    | (have j1 := eq170 x
       grind)
    | exact resolve eq294 eq170
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq170 eq294
  have eq1172 : x ≠ (N.op (M.op x x) x) ∨ x = (N.op x x) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1167
       have r₂ := eq173
       grind)
    | exact resolve eq1167 eq173
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq173 eq1167
  have eq1176 : x = (N.op x x) ∨ x = (M.op y y) ∨ x = (M.op x x) := by
    first
    | (have r₁ := eq1172
       have r₂ := eq116 x
       grind)
    | exact resolve eq1172 eq116
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq116 eq1172
  have eq1178 : x = (N.op x x) ∨ x = (M.op y y) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq1176
       have r₂ := eq7 x x
       grind)
    | exact resolve eq1176 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1176
  have eq1365 : x ≠ (N.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq74
       have i₂ := eq1178
       grind)
    | exact superpose eq1178 eq74
    | exact resolve eq74 eq1178
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq74
  have eq1369 : x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op x x) ∨ x = (M.op y y) := by
    first
    | (have i₁ := eq9 x x
       have i₂ := eq1178
       grind)
    | exact superpose eq1178 eq9
    | (have j0 := eq9 x x
       grind)
    | exact resolve eq9 eq1178
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1370 : x = (M.op y y) ∨ x = (M.op x x) := by grind
  clear eq1369
  have eq1373 : x ≠ (N.op x x) ∨ y = (M.op y y) ∨ x = (M.op y y) := by grind
  clear eq1365
  have eq1375 : y = (M.op y y) ∨ x = (M.op y y) := by
    first
    | (have r₁ := eq1373
       have r₂ := eq1178
       grind)
    | exact resolve eq1373 eq1178
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1178 eq1373
  have eq1453 : x = (N.op x x) ∨ x = (M.op x x) := by
    first
    | (have i₁ := eq82 y y
       have i₂ := eq1370
       grind)
    | exact superpose eq1370 eq82
    | exact resolve eq82 eq1370
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1370
  have eq1478 : x = (N.op x x) := by
    first
    | (have j1 := eq7 x x
       grind)
    | (have r₁ := eq1453
       have r₂ := eq7 x x
       grind)
    | exact resolve eq1453 eq7
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1453
  have eq1492 : x ≠ (N.op x (N.op x (N.op y y))) := by
    first
    | (have i₁ := eq10
       have i₂ := eq1478
       grind)
    | exact superpose eq1478 eq10
    | exact resolve eq10 eq1478
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  have eq1774 : ∀ X0 : G, y = (M.op y X0) ∨ x = (M.op y y) := by
    intro X0
    first
    | (have i₁ := eq14 y y x
       have i₂ := eq1375
       grind)
    | exact superpose eq1375 eq14
    | exact resolve eq14 eq1375
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq14 eq1375
  have eq2136 : ∀ X0 : G, x ≠ (N.op (N.op x x) (N.op x x)) ∨ x = y ∨ y = (M.op y X0) := by
    intro X0
    first
    | (have i₁ := eq63
       have i₂ := eq1774 X0
       grind)
    | exact superpose eq1774 eq63
    | (have j1 := eq1774 X0
       grind)
    | exact resolve eq63 eq1774
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq63 eq1774
  have eq2187 : ∀ X0 : G, x ≠ (N.op x x) ∨ x = y ∨ y = (M.op y X0) := by
    intro X0
    first
    | (have i₁ := eq2136 X0
       have i₂ := eq1478
       grind)
    | exact superpose eq1478 eq2136
    | (have j0 := eq2136 X0
       grind)
    | exact resolve eq2136 eq1478
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2136
  have eq2192 : ∀ X0 : G, y = (M.op y X0) ∨ x = y := by
    intro X0
    first
    | (have j0 := eq2187 X0
       grind)
    | (have r₁ := eq2187 X0
       have r₂ := eq1478
       grind)
    | exact resolve eq2187 eq1478
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2187
  have eq2269 : ∀ X0 : G, (N.op X0 y) = X0 ∨ y = X0 ∨ x = y := by
    intro X0
    first
    | (have i₁ := eq38 y X0 x
       have i₂ := eq2192 X0
       grind)
    | exact superpose eq2192 eq38
    | exact resolve eq38 eq2192
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq38
  have eq2270 : y = (N.op y y) ∨ x = y := by
    first
    | (have i₁ := eq82 y x
       have i₂ := eq2192 x
       grind)
    | exact superpose eq2192 eq82
    | exact resolve eq82 eq2192
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq82 eq2192
  have eq2425 : x ≠ (N.op x (N.op x y)) ∨ x = y := by
    first
    | (have i₁ := eq1492
       have i₂ := eq2270
       grind)
    | exact superpose eq2270 eq1492
    | exact resolve eq1492 eq2270
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2270
  have eq3801 : x ≠ (N.op x x) ∨ x = y ∨ x = y ∨ x = y := by
    first
    | (have i₁ := eq2425
       have i₂ := eq2269 x
       grind)
    | exact superpose eq2269 eq2425
    | (have j1 := eq2269 x
       grind)
    | exact resolve eq2425 eq2269
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq2269 eq2425
  have eq3812 : x ≠ (N.op x x) ∨ x = y := by grind
  clear eq3801
  have eq3826 : x = y := by
    first
    | (have r₁ := eq3812
       have r₂ := eq1478
       grind)
    | exact resolve eq3812 eq1478
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3812
  have eq3993 : x ≠ (N.op x (N.op x (N.op x x))) := by
    first
    | (have i₁ := eq1492
       have i₂ := eq3826
       grind)
    | exact superpose eq3826 eq1492
    | exact resolve eq1492 eq3826
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq1492 eq3826
  have eq4041 : x ≠ (N.op x (N.op x x)) := by
    first
    | (have i₁ := eq3993
       have i₂ := eq1478
       grind)
    | exact superpose eq1478 eq3993
    | exact resolve eq3993 eq1478
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq3993
  have eq4049 : x ≠ (N.op x x) := by
    first
    | (have i₁ := eq4041
       have i₂ := eq1478
       grind)
    | exact superpose eq1478 eq4041
    | exact resolve eq4041 eq1478
    | grind
    | grind (splits := 40)
    | grind (splits := 40) (ematch := 20)
  clear eq4041
  have eq4054 : False := by grind
  exact eq4054
